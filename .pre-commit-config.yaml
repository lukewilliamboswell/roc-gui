repos:
  - repo: local
    hooks:
      - id: no-tracked-machine-code
        name: Reject tracked machine code
        entry: python3 scripts/check_no_tracked_machine_code.py
        language: system
        pass_filenames: false
      - id: cargo-fmt
        name: Check Rust formatting
        entry: cargo fmt --all -- --check
        language: system
        files: \.rs$
        pass_filenames: false
      # Clippy runs through run_cargo.py so the host links against its verified
      # build-time inputs, the same way CI builds it.
      - id: cargo-clippy
        name: Lint Rust with Clippy
        entry: python3 scripts/run_cargo.py clippy --locked --workspace --all-targets -- -D warnings
        language: system
        files: (\.rs|Cargo\.toml|Cargo\.lock)$
        pass_filenames: false
