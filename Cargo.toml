[workspace]
members = ["crates/host", "vendor/gpui"]
default-members = ["crates/host"]
resolver = "3"

[patch.crates-io]
gpui = { path = "vendor/gpui" }

[profile.dev]
debug = 0
panic = "abort"

[profile.release]
panic = "abort"
lto = "thin"
strip = "debuginfo"
