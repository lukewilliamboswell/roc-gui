//! Roc Platform ABI
//!
//! This file defines the Rust interface for hosted functions in a Roc platform.
//! It is automatically generated by the Roc glue generator.
//!
//! Hosted argument ownership:
//! - Roc transfers ownership of refcounted arguments to the hosted function.
//! - The hosted function must release owned refcounted arguments when done, using
//!   the release call named in each hosted symbol's doc comment below.
//! - Releasing a container releases its elements only when the container's own
//!   count reaches zero, which is what compiled Roc code does when it drops one
//!   it owns. Releasing the elements unconditionally double-frees them whenever
//!   the Roc caller still holds the container.
//! - If the host stores or returns an argument, it must retain or transfer ownership explicitly.
//!
//! Import this module from the platform host and implement the listed hosted symbols
//! with the exact natural C ABI signatures shown below.

#![cfg_attr(rustfmt, rustfmt_skip)]
#![allow(dead_code)]
#![allow(improper_ctypes)]
#![allow(improper_ctypes_definitions)]
// `no_roc_std_helpers` is supplied by host build tooling and cannot be registered from source.
#![allow(unexpected_cfgs)]

use core::ffi::c_void;
use core::sync::atomic::{fence, AtomicIsize, Ordering};
#[cfg(not(no_roc_std_helpers))]
use std::alloc::Layout;

/// Runtime representation of Roc's fixed-point `Dec` value.
///
/// `num` stores the decimal value scaled by 10^18.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct RocDec {
    pub num: i128,
}

const _: [(); 16] = [(); core::mem::size_of::<RocDec>()];
const _: [(); 16] = [(); core::mem::align_of::<RocDec>()];

#[cfg(target_arch = "x86_64")]
type RocU8x16Native = core::arch::x86_64::__m128i;
#[cfg(target_arch = "x86_64")]
type RocI8x16Native = core::arch::x86_64::__m128i;
#[cfg(target_arch = "x86_64")]
type RocU16x8Native = core::arch::x86_64::__m128i;
#[cfg(target_arch = "x86_64")]
type RocI16x8Native = core::arch::x86_64::__m128i;
#[cfg(target_arch = "x86_64")]
type RocU32x4Native = core::arch::x86_64::__m128i;
#[cfg(target_arch = "x86_64")]
type RocI32x4Native = core::arch::x86_64::__m128i;
#[cfg(target_arch = "x86_64")]
type RocU64x2Native = core::arch::x86_64::__m128i;
#[cfg(target_arch = "x86_64")]
type RocI64x2Native = core::arch::x86_64::__m128i;
#[cfg(target_arch = "aarch64")]
type RocU8x16Native = core::arch::aarch64::uint8x16_t;
#[cfg(target_arch = "aarch64")]
type RocI8x16Native = core::arch::aarch64::int8x16_t;
#[cfg(target_arch = "aarch64")]
type RocU16x8Native = core::arch::aarch64::uint16x8_t;
#[cfg(target_arch = "aarch64")]
type RocI16x8Native = core::arch::aarch64::int16x8_t;
#[cfg(target_arch = "aarch64")]
type RocU32x4Native = core::arch::aarch64::uint32x4_t;
#[cfg(target_arch = "aarch64")]
type RocI32x4Native = core::arch::aarch64::int32x4_t;
#[cfg(target_arch = "aarch64")]
type RocU64x2Native = core::arch::aarch64::uint64x2_t;
#[cfg(target_arch = "aarch64")]
type RocI64x2Native = core::arch::aarch64::int64x2_t;
#[cfg(target_arch = "wasm32")]
type RocU8x16Native = core::arch::wasm32::v128;
#[cfg(target_arch = "wasm32")]
type RocI8x16Native = core::arch::wasm32::v128;
#[cfg(target_arch = "wasm32")]
type RocU16x8Native = core::arch::wasm32::v128;
#[cfg(target_arch = "wasm32")]
type RocI16x8Native = core::arch::wasm32::v128;
#[cfg(target_arch = "wasm32")]
type RocU32x4Native = core::arch::wasm32::v128;
#[cfg(target_arch = "wasm32")]
type RocI32x4Native = core::arch::wasm32::v128;
#[cfg(target_arch = "wasm32")]
type RocU64x2Native = core::arch::wasm32::v128;
#[cfg(target_arch = "wasm32")]
type RocI64x2Native = core::arch::wasm32::v128;

macro_rules! roc_simd_type {
    ($name:ident, $native:ident) => {
        #[repr(transparent)]
        #[derive(Clone, Copy)]
        pub struct $name(pub $native);
        const _: [(); 16] = [(); core::mem::size_of::<$name>()];
        const _: [(); 16] = [(); core::mem::align_of::<$name>()];
    };
}
roc_simd_type!(RocU8x16, RocU8x16Native);
roc_simd_type!(RocI8x16, RocI8x16Native);
roc_simd_type!(RocU16x8, RocU16x8Native);
roc_simd_type!(RocI16x8, RocI16x8Native);
roc_simd_type!(RocU32x4, RocU32x4Native);
roc_simd_type!(RocI32x4, RocI32x4Native);
roc_simd_type!(RocU64x2, RocU64x2Native);
roc_simd_type!(RocI64x2, RocI64x2Native);


/// Runtime representation of an opaque `Box(T)` value.
pub type RocBox = *mut c_void;

/// Host-internal allocator and diagnostic context used by helper functions in this file.
///
/// Compiled Roc code does not receive this value. The real host ABI is the set of direct
/// linker symbols declared below (`roc_alloc`, hosted symbols, and provided entrypoints).
#[repr(C)]
pub struct RocHost {
    pub env: *mut c_void,
    pub roc_alloc: extern "C" fn(*mut RocHost, usize, usize) -> *mut c_void,
    pub roc_dealloc: extern "C" fn(*mut RocHost, *mut c_void, usize),
    pub roc_realloc: extern "C" fn(*mut RocHost, *mut c_void, usize, usize) -> *mut c_void,
    pub roc_dbg: extern "C" fn(*mut RocHost, *const u8, usize),
    pub roc_expect_failed: extern "C" fn(*mut RocHost, *const u8, usize),
    pub roc_crashed: extern "C" fn(*mut RocHost, *const u8, usize),
}

impl RocHost {
    /// Allocate memory with the given alignment and length.
    ///
    /// # Safety
    /// The returned pointer must be used only according to Roc allocation layout
    /// rules and later released through the matching host deallocator.
    #[inline]
    pub unsafe fn alloc(&self, alignment: usize, length: usize) -> *mut c_void {
        let host = self as *const RocHost as *mut RocHost;
        (self.roc_alloc)(host, length, alignment)
    }

    /// Deallocate memory previously allocated with `alloc`.
    ///
    /// # Safety
    /// `ptr` must have been allocated by this host with the same alignment and must
    /// not be used after this call.
    #[inline]
    pub unsafe fn dealloc(&self, ptr: *mut c_void, alignment: usize) {
        let host = self as *const RocHost as *mut RocHost;
        (self.roc_dealloc)(host, ptr, alignment);
    }

    /// Reallocate memory to a new size.
    ///
    /// # Safety
    /// `old_ptr` must have been allocated by this host with the same alignment.
    /// The returned pointer replaces `old_ptr`; the old pointer must not be used.
    #[inline]
    pub unsafe fn realloc(
        &self,
        old_ptr: *mut c_void,
        alignment: usize,
        new_length: usize,
    ) -> *mut c_void {
        let host = self as *const RocHost as *mut RocHost;
        (self.roc_realloc)(host, old_ptr, new_length, alignment)
    }
}

#[inline]
fn checked_add_usize(left: usize, right: usize, context: &str) -> usize {
    left.checked_add(right).expect(context)
}

#[inline]
fn checked_mul_usize(left: usize, right: usize, context: &str) -> usize {
    left.checked_mul(right).expect(context)
}

#[inline]
fn shifted_capacity(capacity: usize) -> usize {
    capacity.checked_shl(1).expect("Roc capacity does not fit shifted representation")
}

/// Uniform ABI function pointer stored in `RocErasedCallablePayload`.
///
/// The final `reuse` pointer is nullable. Non-null must be the callable data
/// pointer whose inline capture begins at `capture`; it transfers one owned
/// reference to the callee. The caller must not use or decref that ownership
/// unit after the call. The callee consumes it exactly once, whether or not the
/// result can reuse the allocation.
pub type RocErasedCallableFn = extern "C" fn(*mut RocHost, *mut u8, *const u8, *mut u8, *mut u8, *mut *const c_void);

/// Final-drop callback for inline erased-callable captures.
pub type RocErasedCallableOnDrop = extern "C" fn(*mut u8, *mut RocHost);

/// Payload header for `Box(function)`.
#[repr(C)]
#[derive(Debug, Clone, Copy)]
pub struct RocErasedCallablePayload {
    pub callable_fn_ptr: RocErasedCallableFn,
    pub on_drop: Option<RocErasedCallableOnDrop>,
}

const _: () = assert!(core::mem::size_of::<RocErasedCallablePayload>() == 2 * core::mem::size_of::<usize>(), "RocErasedCallablePayload size mismatch");
const _: () = assert!(core::mem::offset_of!(RocErasedCallablePayload, callable_fn_ptr) == 0, "RocErasedCallablePayload.callable_fn_ptr offset mismatch");
const _: () = assert!(core::mem::offset_of!(RocErasedCallablePayload, on_drop) == core::mem::size_of::<usize>(), "RocErasedCallablePayload.on_drop offset mismatch");

/// Runtime representation of `Box(function)`.
pub type RocErasedCallable = *mut u8;

pub const ROC_ERASED_CALLABLE_CAPTURE_ALIGNMENT: usize = 16;
pub const ROC_ERASED_CALLABLE_PAYLOAD_ALIGNMENT: usize = 16;
pub const ROC_ERASED_CALLABLE_CAPTURE_OFFSET: usize =
    (core::mem::size_of::<RocErasedCallablePayload>() + 15) & !15;

#[inline]
pub fn roc_erased_callable_payload_size(capture_size: usize) -> usize {
    checked_add_usize(
        ROC_ERASED_CALLABLE_CAPTURE_OFFSET,
        capture_size,
        "Roc erased-callable payload size overflow",
    )
}

#[inline]
/// # Safety
/// `callable` must be a non-null Roc erased-callable data pointer.
pub unsafe fn roc_erased_callable_payload_ptr(callable: RocErasedCallable) -> *mut RocErasedCallablePayload {
    callable as *mut RocErasedCallablePayload
}

#[inline]
/// # Safety
/// `callable` must be a non-null Roc erased-callable data pointer.
pub unsafe fn roc_erased_callable_capture_ptr(callable: RocErasedCallable) -> *mut u8 {
    callable.add(ROC_ERASED_CALLABLE_CAPTURE_OFFSET)
}

/// Allocate a Roc erased callable payload.
///
/// # Safety
/// The caller must initialize and use the returned callable according to Roc's
/// erased-callable ABI. `callable_fn_ptr` and `on_drop` must have matching ABI
/// signatures for the captured payload.
pub unsafe fn roc_erased_callable_allocate(
    roc_host: &RocHost,
    callable_fn_ptr: RocErasedCallableFn,
    on_drop: Option<RocErasedCallableOnDrop>,
    capture_size: usize,
) -> RocErasedCallable {
    let ptr_width = core::mem::size_of::<usize>();
    let alignment = core::cmp::max(ptr_width, ROC_ERASED_CALLABLE_PAYLOAD_ALIGNMENT);
    let extra_bytes = core::cmp::max(ptr_width, ROC_ERASED_CALLABLE_PAYLOAD_ALIGNMENT);
    let total = checked_add_usize(
        extra_bytes,
        roc_erased_callable_payload_size(capture_size),
        "Roc erased-callable allocation size overflow",
    );
    let base = roc_host.alloc(alignment, total) as *mut u8;
    let data = base.add(extra_bytes);
    let rc = data.sub(core::mem::size_of::<isize>()) as *mut isize;
    *rc = 1;
    let payload = roc_erased_callable_payload_ptr(data);
    *payload = RocErasedCallablePayload { callable_fn_ptr, on_drop };
    data
}

/// Payload drop callback for a boxed value.
///
/// The callback receives the boxed payload data pointer and must recursively
/// decref any Roc refcounted values inside the payload. It must not free the
/// box allocation; `decref_box_with` and `free_box_with` free it after the callback.
pub type RocBoxPayloadDecref = extern "C" fn(*mut c_void, *mut RocHost);

/// Increment the refcount of a boxed payload data pointer.
///
/// # Safety
/// `data_ptr` must be a valid Roc box payload pointer. The caller must ensure
/// that the extra retained references are balanced by later decrefs.
pub unsafe fn incref_box(data_ptr: RocBox, amount: isize) {
    let data = match box_data_ptr(data_ptr) {
        Some(ptr) => ptr,
        None => return,
    };
    let rc = box_refcount_ptr(data);
    if unsafe { (*rc).load(Ordering::Relaxed) } == 0 {
        return; // REFCOUNT_STATIC_DATA
    }
    unsafe {
        (*rc).fetch_add(amount, Ordering::Relaxed);
    }
}

/// Allocate a Roc box and return a pointer to its payload data.
///
/// # Safety
/// The returned payload memory is uninitialized. The caller must initialize it
/// according to the Roc type before exposing it to safe APIs or Roc code.
pub unsafe fn allocate_box(
    payload_size: usize,
    payload_alignment: usize,
    payload_contains_refcounted: bool,
    roc_host: &RocHost,
) -> RocBox {
    let ptr_width = core::mem::size_of::<usize>();
    let required_space = if payload_contains_refcounted { checked_mul_usize(2, ptr_width, "Roc box header size overflow") } else { ptr_width };
    let header_bytes = required_space.max(payload_alignment);
    let alloc_alignment = ptr_width.max(payload_alignment);
    let total = checked_add_usize(header_bytes, payload_size, "Roc box allocation size overflow");
    let base = roc_host.alloc(alloc_alignment, total) as *mut u8;
    let data = base.add(header_bytes);
    unsafe {
        let rc = data.sub(core::mem::size_of::<isize>()) as *mut isize;
        *rc = 1;
    }
    data as RocBox
}

/// Decrement a pointer-aligned boxed payload with no Roc refcounted values.
///
/// # Safety
/// `data_ptr` must be a valid Roc box payload pointer owned by this reference.
pub unsafe fn decref_box(data_ptr: RocBox, roc_host: &RocHost) {
    unsafe {
        decref_box_with(data_ptr, core::mem::align_of::<usize>(), false, None, roc_host);
    }
}

/// Increment a boxed function closure.
///
/// # Safety
/// `callable` must be a valid Roc erased-callable payload pointer.
pub unsafe fn incref_erased_callable(callable: RocErasedCallable, amount: isize) {
    unsafe {
        incref_box(callable as RocBox, amount);
    }
}

/// Decrement a boxed function closure and run its capture drop callback on final release.
///
/// # Safety
/// `callable` must be a valid Roc erased-callable payload pointer owned by this reference.
pub unsafe fn decref_erased_callable(callable: RocErasedCallable, roc_host: &RocHost) {
    unsafe {
        decref_box_with(
            callable as RocBox,
            ROC_ERASED_CALLABLE_PAYLOAD_ALIGNMENT,
            false,
            Some(drop_erased_callable_payload),
            roc_host,
        );
    }
}

extern "C" fn drop_erased_callable_payload(data_ptr: *mut c_void, roc_host: *mut RocHost) {
    if data_ptr.is_null() || roc_host.is_null() {
        return;
    }
    unsafe {
        let callable = data_ptr as RocErasedCallable;
        let payload = roc_erased_callable_payload_ptr(callable);
        if let Some(on_drop) = (*payload).on_drop {
            on_drop(roc_erased_callable_capture_ptr(callable), roc_host);
        }
    }
}

/// Decrement a boxed payload and run payload teardown when this is the final ref.
///
/// `payload_contains_refcounted` must match the value passed to `allocate_box`:
/// it determines the box header size, and is independent of whether a
/// `payload_decref` teardown callback is supplied. A host resource handle such
/// as `Box(U64)` holding a raw pointer has `payload_contains_refcounted: false`
/// even when it provides a teardown callback to free the underlying resource.
///
/// # Safety
/// `data_ptr` must be a valid Roc box payload pointer owned by this reference,
/// and `payload_alignment`/`payload_contains_refcounted` must match allocation.
pub unsafe fn decref_box_with(
    data_ptr: RocBox,
    payload_alignment: usize,
    payload_contains_refcounted: bool,
    payload_decref: Option<RocBoxPayloadDecref>,
    roc_host: &RocHost,
) {
    let data = match box_data_ptr(data_ptr) {
        Some(ptr) => ptr,
        None => return,
    };
    let rc = box_refcount_ptr(data);
    if unsafe { (*rc).load(Ordering::Relaxed) } == 0 {
        return; // REFCOUNT_STATIC_DATA
    }
    let prev = unsafe { (*rc).fetch_sub(1, Ordering::Release) };
    if prev == 1 {
        fence(Ordering::Acquire);
        if let Some(callback) = payload_decref {
            callback(data_ptr, roc_host as *const RocHost as *mut RocHost);
        }
        free_box_allocation(data, payload_alignment, payload_contains_refcounted, roc_host);
    }
}

/// Free a boxed payload allocation immediately after running payload teardown.
///
/// See `decref_box_with` for the meaning of `payload_contains_refcounted`.
///
/// # Safety
/// `data_ptr` must be a valid Roc box payload pointer that will not be used after this call.
pub unsafe fn free_box_with(
    data_ptr: RocBox,
    payload_alignment: usize,
    payload_contains_refcounted: bool,
    payload_decref: Option<RocBoxPayloadDecref>,
    roc_host: &RocHost,
) {
    let data = match box_data_ptr(data_ptr) {
        Some(ptr) => ptr,
        None => return,
    };
    if let Some(callback) = payload_decref {
        callback(data_ptr, roc_host as *const RocHost as *mut RocHost);
    }
    free_box_allocation(data, payload_alignment, payload_contains_refcounted, roc_host);
}

/// Return true when a boxed payload data pointer has exactly one live ref.
pub fn is_unique_box(data_ptr: RocBox) -> bool {
    let data = match box_data_ptr(data_ptr) {
        Some(ptr) => ptr,
        None => return true,
    };
    let rc = box_refcount_ptr(data);
    unsafe { (*rc).load(Ordering::Acquire) == 1 }
}

fn box_data_ptr(data_ptr: RocBox) -> Option<*mut u8> {
    if data_ptr.is_null() {
        None
    } else {
        Some(data_ptr as *mut u8)
    }
}

fn box_refcount_ptr(data: *mut u8) -> *mut AtomicIsize {
    unsafe { data.sub(core::mem::size_of::<isize>()) as *mut AtomicIsize }
}

fn free_box_allocation(
    data: *mut u8,
    payload_alignment: usize,
    payload_contains_refcounted: bool,
    roc_host: &RocHost,
) {
    let ptr_width = core::mem::size_of::<usize>();
    let required_space = if payload_contains_refcounted { checked_mul_usize(2, ptr_width, "Roc box header size overflow") } else { ptr_width };
    let header_bytes = required_space.max(payload_alignment);
    let alloc_alignment = ptr_width.max(payload_alignment);
    let base = unsafe { data.sub(header_bytes) } as *mut c_void;
    unsafe {
        roc_host.dealloc(base, alloc_alignment);
    }
}

/// A Roc string value. Small strings (up to 23 bytes on 64-bit) are stored inline;
/// larger strings are heap-allocated with a reference count.
///
/// `bytes` is never tagged. Operations, host code, glue code, and object-file
/// relocations can use it directly as the UTF-8 byte pointer for non-small
/// strings. Seamless-slice tagging lives in `capacity_or_alloc_ptr` instead.
/// Big-string capacity is stored shifted left by one bit, so max capacity is
/// essentially `isize::MAX` bytes: about 2 GiB on 32-bit targets and 8 EiB on
/// 64-bit targets.
///
/// This type is ABI-compatible with the Zig RocStr (24 bytes, `#[repr(C)]`).
#[repr(C)]
#[derive(Clone, Copy)]
pub struct RocStr {
    pub bytes: *mut u8,
    pub capacity_or_alloc_ptr: usize,
    pub length: usize,
}

const _: () = assert!(core::mem::size_of::<RocStr>() == 3 * core::mem::size_of::<usize>(), "RocStr size mismatch");
const _: () = assert!(core::mem::align_of::<RocStr>() == core::mem::align_of::<usize>(), "RocStr alignment mismatch");
const _: () = assert!(core::mem::offset_of!(RocStr, bytes) == 0, "RocStr.bytes offset mismatch");
const _: () = assert!(core::mem::offset_of!(RocStr, capacity_or_alloc_ptr) == core::mem::size_of::<usize>(), "RocStr.capacity_or_alloc_ptr offset mismatch");
const _: () = assert!(core::mem::offset_of!(RocStr, length) == 2 * core::mem::size_of::<usize>(), "RocStr.length offset mismatch");

const ROC_STR_SIZE: usize = core::mem::size_of::<RocStr>();
const ROC_SMALL_STR_MAX_LEN: usize = ROC_STR_SIZE - 1;
const ROC_SMALL_STR_BIT: usize = isize::MIN as usize;
const ROC_SEAMLESS_SLICE_TAG: usize = 1;

impl RocStr {
    /// Return an empty RocStr (small string with zero length).
    pub fn empty() -> Self {
        Self {
            bytes: core::ptr::null_mut(),
            capacity_or_alloc_ptr: 0,
            length: ROC_SMALL_STR_BIT,
        }
    }

    /// Return true if this string is stored inline (small string optimization).
    #[inline]
    pub fn is_small_str(&self) -> bool {
        (self.length as isize) < 0
    }

    /// Return true if this string is a seamless slice into another allocation.
    #[inline]
    pub fn is_seamless_slice(&self) -> bool {
        !self.is_small_str() && (self.capacity_or_alloc_ptr & ROC_SEAMLESS_SLICE_TAG) != 0
    }

    /// Return the length of the string in bytes.
    #[inline]
    pub fn len(&self) -> usize {
        if self.is_small_str() {
            let bytes_ptr = self as *const Self as *const u8;
            let last_byte = unsafe { *bytes_ptr.add(ROC_STR_SIZE - 1) };
            (last_byte ^ 0b1000_0000) as usize
        } else {
            self.length
        }
    }

    /// Return true if the string has zero length.
    #[inline]
    pub fn is_empty(&self) -> bool {
        self.len() == 0
    }

    /// Return the string contents as a byte slice.
    pub fn as_slice(&self) -> &[u8] {
        let ptr = self.as_u8_ptr();
        unsafe { core::slice::from_raw_parts(ptr, self.len()) }
    }

    /// Return a pointer to the raw UTF-8 bytes.
    #[inline]
    pub fn as_u8_ptr(&self) -> *const u8 {
        if self.is_small_str() {
            self as *const Self as *const u8
        } else {
            self.bytes as *const u8
        }
    }

    /// Return the string contents as a `&str`.
    pub fn as_str(&self) -> &str {
        core::str::from_utf8(self.as_slice()).expect("RocStr contained invalid UTF-8")
    }

    /// Create a RocStr from a UTF-8 byte slice, using `roc_host` for heap allocation if needed.
    pub fn from_slice(slice: &[u8], roc_host: &RocHost) -> Self {
        core::str::from_utf8(slice).expect("RocStr::from_slice requires valid UTF-8");
        if slice.len() < ROC_STR_SIZE {
            let mut result = Self::empty();
            let ptr = &mut result as *mut Self as *mut u8;
            unsafe {
                core::ptr::copy_nonoverlapping(slice.as_ptr(), ptr, slice.len());
                *ptr.add(ROC_STR_SIZE - 1) = (slice.len() as u8) | 0b1000_0000;
            }
            result
        } else {
            let ptr_width = core::mem::size_of::<usize>();
            let total = checked_add_usize(ptr_width, slice.len(), "RocStr allocation size overflow");
            let base = unsafe { roc_host.alloc(core::mem::align_of::<usize>(), total) };
            let data_ptr = unsafe { (base as *mut u8).add(ptr_width) };
            // Write refcount = 1
            unsafe {
                let rc = (data_ptr as *mut isize).sub(1);
                *rc = 1;
                core::ptr::copy_nonoverlapping(slice.as_ptr(), data_ptr, slice.len());
            }
            Self {
                bytes: data_ptr,
                capacity_or_alloc_ptr: shifted_capacity(slice.len()),
                length: slice.len(),
            }
        }
    }

    /// Create a RocStr from a `&str`.
    pub fn from_str(s: &str, roc_host: &RocHost) -> Self {
        Self::from_slice(s.as_bytes(), roc_host)
    }

    /// Decrement the reference count; frees the allocation when it reaches zero.
    ///
    /// # Safety
    /// `self` must own one live Roc reference. Calling this more than once for the
    /// same ownership reference may double-free.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        if self.is_small_str() {
            return;
        }
        let alloc_ptr = self.get_allocation_ptr();
        if alloc_ptr.is_null() {
            return;
        }
        let rc = unsafe { (alloc_ptr as *mut AtomicIsize).sub(1) };
        if unsafe { (*rc).load(Ordering::Relaxed) } == 0 {
            return; // REFCOUNT_STATIC_DATA—bytes are in read-only memory
        }
        let prev = unsafe { (*rc).fetch_sub(1, Ordering::Release) };
        if prev == 1 {
            fence(Ordering::Acquire);
            let ptr_width = core::mem::size_of::<usize>();
            let base = unsafe { alloc_ptr.sub(ptr_width) } as *mut c_void;
            unsafe {
                roc_host.dealloc(base, core::mem::align_of::<usize>());
            }
        }
    }

    /// Increment the reference count by `amount`.
    ///
    /// # Safety
    /// `self` must point at a live Roc allocation. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        if self.is_small_str() {
            return;
        }
        let alloc_ptr = self.get_allocation_ptr();
        if alloc_ptr.is_null() {
            return;
        }
        let rc = unsafe { (alloc_ptr as *mut AtomicIsize).sub(1) };
        if unsafe { (*rc).load(Ordering::Relaxed) } == 0 {
            return; // REFCOUNT_STATIC_DATA
        }
        unsafe {
            (*rc).fetch_add(amount, Ordering::Relaxed);
        }
    }

    /// Return true if this string has a reference count of exactly one.
    pub fn is_unique(&self) -> bool {
        if self.is_small_str() {
            return true;
        }
        let alloc_ptr = self.get_allocation_ptr();
        if alloc_ptr.is_null() {
            return true;
        }
        unsafe {
            let rc = (alloc_ptr as *const AtomicIsize).sub(1);
            let count = (*rc).load(Ordering::Acquire);
            count == 0 || count == 1
        }
    }

    fn get_allocation_ptr(&self) -> *mut u8 {
        if self.is_seamless_slice() {
            (self.capacity_or_alloc_ptr & !ROC_SEAMLESS_SLICE_TAG) as *mut u8
        } else {
            self.bytes
        }
    }
}

impl core::fmt::Debug for RocStr {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.debug_struct("RocStr")
            .field("len", &self.len())
            .field("is_small", &self.is_small_str())
            .finish()
    }
}

/// A generic Roc list. Elements are reference-counted and heap-allocated.
///
/// When `ELEMENTS_REFCOUNTED` is true (the default via `RocList<T>`), an extra
/// `ptr_width` bytes are reserved in the allocation header for the element count,
/// matching the Roc runtime's `allocateWithRefcount` layout.
pub type RocList<T> = RocListWith<T, true>;

/// Parameterized list constructor; use `RocList<T>` for refcounted elements.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct RocListWith<T, const ELEMENTS_REFCOUNTED: bool> {
    pub elements: *mut T,
    pub length: usize,
    pub capacity_or_alloc_ptr: usize,
}

const _: () = assert!(core::mem::size_of::<RocList<u8>>() == 3 * core::mem::size_of::<usize>(), "RocList size mismatch");
const _: () = assert!(core::mem::align_of::<RocList<u8>>() == core::mem::align_of::<usize>(), "RocList alignment mismatch");
const _: () = assert!(core::mem::offset_of!(RocList<u8>, elements) == 0, "RocList.elements offset mismatch");
const _: () = assert!(core::mem::offset_of!(RocList<u8>, length) == core::mem::size_of::<usize>(), "RocList.length offset mismatch");
const _: () = assert!(core::mem::offset_of!(RocList<u8>, capacity_or_alloc_ptr) == 2 * core::mem::size_of::<usize>(), "RocList.capacity_or_alloc_ptr offset mismatch");

impl<T, const ELEMENTS_REFCOUNTED: bool> RocListWith<T, ELEMENTS_REFCOUNTED> {
    #[inline]
    fn header_bytes() -> usize {
        let ptr_width = core::mem::size_of::<usize>();
        let required_space = if ELEMENTS_REFCOUNTED { checked_mul_usize(2, ptr_width, "RocList header size overflow") } else { ptr_width };
        required_space.max(core::mem::align_of::<T>())
    }

    /// Return an empty RocList.
    pub fn empty() -> Self {
        Self {
            elements: core::ptr::null_mut(),
            length: 0,
            capacity_or_alloc_ptr: 0,
        }
    }

    /// Return the number of elements in the list.
    #[inline]
    pub fn len(&self) -> usize {
        self.length
    }

    /// Return true if the list has zero elements.
    #[inline]
    pub fn is_empty(&self) -> bool {
        self.length == 0
    }

    /// Return true if this list is a seamless slice into another allocation.
    /// Slices share the rc slot with their backing allocation; the alloc ptr is
    /// encoded in `capacity_or_alloc_ptr` with the low bit set.
    #[inline]
    pub fn is_seamless_slice(&self) -> bool {
        (self.capacity_or_alloc_ptr & 1) != 0
    }

    /// Resolve `self` to the start of its backing allocation (the element block
    /// just after the rc slot). Returns `null` for empty lists. Handles both
    /// whole-backing and seamless-slice forms.
    fn get_allocation_ptr(&self) -> *mut u8 {
        if self.is_seamless_slice() {
            (self.capacity_or_alloc_ptr & !1) as *mut u8
        } else {
            self.elements as *mut u8
        }
    }

    fn allocation_element_count(&self) -> usize {
        if self.is_seamless_slice() && ELEMENTS_REFCOUNTED {
            let alloc_ptr = self.get_allocation_ptr();
            if alloc_ptr.is_null() {
                return 0;
            }
            unsafe {
                let ptr = alloc_ptr as *const usize;
                *ptr.sub(2)
            }
        } else {
            self.length
        }
    }

    /// Return the list elements as a slice.
    pub fn as_slice(&self) -> &[T] {
        if self.elements.is_null() {
            &[]
        } else {
            unsafe { core::slice::from_raw_parts(self.elements, self.length) }
        }
    }

    /// Return all items in the backing allocation, not just this slice.
    pub fn allocation_items(&self) -> &[T] {
        if self.elements.is_null() {
            &[]
        } else {
            unsafe { core::slice::from_raw_parts(self.get_allocation_ptr() as *const T, self.allocation_element_count()) }
        }
    }

    /// Allocate a new list with space for `length` elements.
    ///
    /// # Safety
    /// The returned element memory is uninitialized while `length` is already
    /// set. The caller must initialize every element before exposing the list
    /// to safe APIs, Roc code, or generated refcount helpers.
    pub unsafe fn allocate(length: usize, roc_host: &RocHost) -> Self {
        if length == 0 {
            return Self::empty();
        }
        let align = core::mem::align_of::<T>().max(core::mem::align_of::<usize>());
        let header_bytes = Self::header_bytes();
        let data_bytes = checked_mul_usize(length, core::mem::size_of::<T>(), "RocList allocation element bytes overflow");
        let total = checked_add_usize(data_bytes, header_bytes, "RocList allocation size overflow");
        let base = roc_host.alloc(align, total);
        let data_ptr = (base as *mut u8).add(header_bytes);
        unsafe {
            let rc = (data_ptr as *mut isize).sub(1);
            *rc = 1;
            if ELEMENTS_REFCOUNTED {
                let count = (data_ptr as *mut usize).sub(2);
                *count = length;
            }
        }
        Self {
            elements: data_ptr as *mut T,
            length,
            capacity_or_alloc_ptr: shifted_capacity(length),
        }
    }

    /// Create a RocList from a slice, copying elements into a new allocation.
    ///
    /// # Safety
    /// This is a shallow copy. For element types that own Roc references, the
    /// caller must ensure each copied element is already retained for the new
    /// list or will not be decref'd through another owner.
    pub unsafe fn from_slice(slice: &[T], roc_host: &RocHost) -> Self where T: Copy {
        if slice.is_empty() {
            return Self::empty();
        }
        let list = unsafe { Self::allocate(slice.len(), roc_host) };
        unsafe {
            core::ptr::copy_nonoverlapping(
                slice.as_ptr(),
                list.elements,
                slice.len(),
            );
        }
        list
    }

    /// Drop this reference to the list allocation, freeing it when this was the
    /// last one.
    ///
    /// Shallow: it never touches the elements. To release an owned list whose
    /// elements are refcounted, call that list's generated `decref_list_of_...`
    /// helper instead, which drops the elements when this reference is the last
    /// one and then calls this.
    ///
    /// # Safety
    /// `self` must own one live Roc list reference. Calling this more than once
    /// for the same ownership reference may double-free.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        if self.elements.is_null() {
            return;
        }
        let alloc_ptr = self.get_allocation_ptr();
        if alloc_ptr.is_null() {
            return;
        }
        let align = core::mem::align_of::<T>().max(core::mem::align_of::<usize>());
        let header_bytes = Self::header_bytes();
        let rc = unsafe { (alloc_ptr as *mut AtomicIsize).sub(1) };
        if unsafe { (*rc).load(Ordering::Relaxed) } == 0 {
            return; // REFCOUNT_STATIC_DATA—elements are in read-only memory
        }
        let prev = unsafe { (*rc).fetch_sub(1, Ordering::Release) };
        if prev == 1 {
            fence(Ordering::Acquire);
            let base = unsafe { alloc_ptr.sub(header_bytes) } as *mut c_void;
            unsafe {
                roc_host.dealloc(base, align);
            }
        }
    }

    /// Recursively release this list and its elements.
    ///
    /// The reference-count decrement claims the final reference atomically
    /// before reading any elements, so concurrent releases cannot skip or
    /// duplicate element teardown.
    ///
    /// # Safety
    /// `self` must own one live Roc list reference, and `P` must exactly
    /// describe how Roc releases one initialized `T` element.
    pub unsafe fn release_with<P>(self, roc_host: &RocHost)
    where
        P: RocRelease<T>,
    {
        if self.elements.is_null() {
            return;
        }
        let alloc_ptr = self.get_allocation_ptr();
        if alloc_ptr.is_null() {
            return;
        }
        let align = core::mem::align_of::<T>().max(core::mem::align_of::<usize>());
        let header_bytes = Self::header_bytes();
        let rc = unsafe { (alloc_ptr as *mut AtomicIsize).sub(1) };
        if unsafe { (*rc).load(Ordering::Relaxed) } == 0 {
            return; // REFCOUNT_STATIC_DATA—elements are in read-only memory
        }
        let prev = unsafe { (*rc).fetch_sub(1, Ordering::Release) };
        if prev == 1 {
            fence(Ordering::Acquire);
            if ELEMENTS_REFCOUNTED {
                for item_ref in self.allocation_items() {
                    let item = unsafe { core::ptr::read(item_ref) };
                    unsafe { P::release(item, roc_host); }
                }
            }
            let base = unsafe { alloc_ptr.sub(header_bytes) } as *mut c_void;
            unsafe { roc_host.dealloc(base, align); }
        }
    }

    /// Increment the reference count by `amount`.
    ///
    /// # Safety
    /// `self` must point at a live Roc list allocation. The retained references
    /// must be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        if self.elements.is_null() {
            return;
        }
        let alloc_ptr = self.get_allocation_ptr();
        if alloc_ptr.is_null() {
            return;
        }
        let rc = unsafe { (alloc_ptr as *mut AtomicIsize).sub(1) };
        if unsafe { (*rc).load(Ordering::Relaxed) } == 0 {
            return; // REFCOUNT_STATIC_DATA
        }
        unsafe {
            (*rc).fetch_add(amount, Ordering::Relaxed);
        }
    }

    /// Return true if this list has a reference count of exactly one.
    pub fn is_unique(&self) -> bool {
        let alloc_ptr = self.get_allocation_ptr();
        if alloc_ptr.is_null() {
            return true;
        }
        unsafe {
            let rc = (alloc_ptr as *const AtomicIsize).sub(1);
            let count = (*rc).load(Ordering::Acquire);
            count == 0 || count == 1
        }
    }

    /// Return true if this list's allocation has exactly one counted ref.
    pub fn has_one_ref(&self) -> bool {
        let alloc_ptr = self.get_allocation_ptr();
        if alloc_ptr.is_null() {
            return false;
        }
        unsafe {
            let rc = (alloc_ptr as *const AtomicIsize).sub(1);
            (*rc).load(Ordering::Acquire) == 1
        }
    }
}

impl<T: core::fmt::Debug, const ELEMENTS_REFCOUNTED: bool> core::fmt::Debug for RocListWith<T, ELEMENTS_REFCOUNTED> {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.debug_list().entries(self.as_slice().iter()).finish()
    }
}

/// Statically describes how to release one owned Roc ABI value of type `T`.
///
/// # Safety
/// Implementations must match Roc's exact allocation and nested ownership layout.
pub unsafe trait RocRelease<T> {
    unsafe fn release(value: T, roc_host: &RocHost);
}

/// An owning host-language wrapper around a value returned by Roc.
///
/// The release policy is zero-sized, so this wrapper adds no runtime metadata.
/// Dropping it recursively releases the value through the host's direct Roc
/// runtime symbols. Use `into_raw` to transfer ownership elsewhere.
#[repr(transparent)]
pub struct RocOwned<T, P: RocRelease<T>> {
    value: core::mem::ManuallyDrop<T>,
    policy: core::marker::PhantomData<P>,
}

impl<T, P: RocRelease<T>> RocOwned<T, P> {
    /// Take ownership of a raw value returned by a Roc-provided entrypoint.
    ///
    /// # Safety
    /// `value` must be one live owned result whose concrete release plan is `P`.
    pub unsafe fn from_raw(value: T) -> Self {
        Self { value: core::mem::ManuallyDrop::new(value), policy: core::marker::PhantomData }
    }

    pub fn as_ref(&self) -> &T {
        &self.value
    }

    /// Transfer ownership back to raw host code without releasing the value.
    pub fn into_raw(self) -> T {
        let mut this = core::mem::ManuallyDrop::new(self);
        unsafe { core::mem::ManuallyDrop::take(&mut this.value) }
    }
}

impl<T, P: RocRelease<T>> core::ops::Deref for RocOwned<T, P> {
    type Target = T;

    fn deref(&self) -> &T {
        self.as_ref()
    }
}

impl<T, P: RocRelease<T>> Drop for RocOwned<T, P> {
    fn drop(&mut self) {
        let value = unsafe { core::mem::ManuallyDrop::take(&mut self.value) };
        let roc_host = direct_roc_host();
        unsafe { P::release(value, &roc_host); }
    }
}

pub struct RocNoopRelease;

unsafe impl<T> RocRelease<T> for RocNoopRelease {
    unsafe fn release(_value: T, _roc_host: &RocHost) {}
}

pub struct RocStrRelease;

unsafe impl RocRelease<RocStr> for RocStrRelease {
    unsafe fn release(value: RocStr, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

pub struct RocListRelease<P>(core::marker::PhantomData<P>);

unsafe impl<T, P> RocRelease<RocListWith<T, true>> for RocListRelease<P>
where
    P: RocRelease<T>,
{
    unsafe fn release(value: RocListWith<T, true>, roc_host: &RocHost) {
        unsafe { value.release_with::<P>(roc_host); }
    }
}

pub struct RocListSpineRelease;

unsafe impl<T> RocRelease<RocListWith<T, false>> for RocListSpineRelease {
    unsafe fn release(value: RocListWith<T, false>, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

pub struct RocErasedCallableRelease;

unsafe impl RocRelease<RocErasedCallable> for RocErasedCallableRelease {
    unsafe fn release(value: RocErasedCallable, roc_host: &RocHost) {
        unsafe { decref_erased_callable(value, roc_host); }
    }
}

pub struct RocBoxRelease<T, P>(core::marker::PhantomData<(T, P)>);

extern "C" fn release_box_payload<T, P>(data_ptr: *mut c_void, roc_host: *mut RocHost)
where
    P: RocRelease<T>,
{
    if data_ptr.is_null() || roc_host.is_null() {
        return;
    }
    let value = unsafe { core::ptr::read(data_ptr as *const T) };
    unsafe { P::release(value, &*roc_host); }
}

unsafe impl<T, P> RocRelease<*mut T> for RocBoxRelease<T, P>
where
    P: RocRelease<T>,
{
    unsafe fn release(value: *mut T, roc_host: &RocHost) {
        unsafe {
            decref_box_with(
                value as RocBox,
                core::mem::align_of::<T>(),
                true,
                Some(release_box_payload::<T, P>),
                roc_host,
            );
        }
    }
}

pub struct RocBoxSpineRelease<T>(core::marker::PhantomData<T>);

unsafe impl<T> RocRelease<*mut T> for RocBoxSpineRelease<T> {
    unsafe fn release(value: *mut T, roc_host: &RocHost) {
        unsafe {
            decref_box_with(
                value as RocBox,
                core::mem::align_of::<T>(),
                false,
                None,
                roc_host,
            );
        }
    }
}

/// Element type for __AnonStruct_a47a6e0f4ab34c8a
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructA47a6e0f4ab34c8a {
    pub fg: u64,
    pub runs: RocListWith<AnonStructFdc3cce9386720ed, false>,
    pub value: RocStr,
    pub font_size: u32,
    pub font_weight: u32,
    pub font_face: u8,
}

/// Element type for __AnonStruct_a47a6e0f4ab34c8a
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructA47a6e0f4ab34c8a {
    pub fg: u64,
    pub runs: RocListWith<AnonStructFdc3cce9386720ed, false>,
    pub value: RocStr,
    pub font_size: u32,
    pub font_weight: u32,
    pub font_face: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStructA47a6e0f4ab34c8a>() == 72, "AnonStructA47a6e0f4ab34c8a size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStructA47a6e0f4ab34c8a>() == 8, "AnonStructA47a6e0f4ab34c8a alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStructA47a6e0f4ab34c8a>() == 48, "AnonStructA47a6e0f4ab34c8a size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStructA47a6e0f4ab34c8a>() == 8, "AnonStructA47a6e0f4ab34c8a alignment mismatch");

/// Element type for __AnonStruct_fdc3cce9386720ed
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructFdc3cce9386720ed {
    pub bg: u64,
    pub fg: u64,
    pub len: u64,
    pub font_weight: u32,
    pub monospace: bool,
    pub underline: bool,
}

/// Element type for __AnonStruct_fdc3cce9386720ed
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructFdc3cce9386720ed {
    pub bg: u64,
    pub fg: u64,
    pub len: u64,
    pub font_weight: u32,
    pub monospace: bool,
    pub underline: bool,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStructFdc3cce9386720ed>() == 32, "AnonStructFdc3cce9386720ed size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStructFdc3cce9386720ed>() == 8, "AnonStructFdc3cce9386720ed alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStructFdc3cce9386720ed>() == 32, "AnonStructFdc3cce9386720ed size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStructFdc3cce9386720ed>() == 8, "AnonStructFdc3cce9386720ed alignment mismatch");

/// Element type for __AnonStruct_ec521db91a8efd1f
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructEc521db91a8efd1f {
    pub container: u64,
    pub revision: u64,
    pub keys: RocList<RocListWith<u8, false>>,
}

/// Element type for __AnonStruct_ec521db91a8efd1f
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructEc521db91a8efd1f {
    pub container: u64,
    pub revision: u64,
    pub keys: RocList<RocListWith<u8, false>>,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStructEc521db91a8efd1f>() == 40, "AnonStructEc521db91a8efd1f size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStructEc521db91a8efd1f>() == 8, "AnonStructEc521db91a8efd1f alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStructEc521db91a8efd1f>() == 32, "AnonStructEc521db91a8efd1f size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStructEc521db91a8efd1f>() == 8, "AnonStructEc521db91a8efd1f alignment mismatch");

/// Element type for __AnonStruct_805ea43d90e5fb6e
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct805ea43d90e5fb6e {
    pub base_revision: u64,
    pub container: u64,
    pub new_revision: u64,
}

/// Element type for __AnonStruct_805ea43d90e5fb6e
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct805ea43d90e5fb6e {
    pub base_revision: u64,
    pub container: u64,
    pub new_revision: u64,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct805ea43d90e5fb6e>() == 24, "AnonStruct805ea43d90e5fb6e size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct805ea43d90e5fb6e>() == 8, "AnonStruct805ea43d90e5fb6e alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct805ea43d90e5fb6e>() == 24, "AnonStruct805ea43d90e5fb6e size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct805ea43d90e5fb6e>() == 8, "AnonStruct805ea43d90e5fb6e alignment mismatch");

/// Element type for __AnonStruct_1f2ff78011a2f7cd
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct1f2ff78011a2f7cd {
    pub root: u64,
    pub before: RocListWith<u8, false>,
    pub key: RocListWith<u8, false>,
}

/// Element type for __AnonStruct_1f2ff78011a2f7cd
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct1f2ff78011a2f7cd {
    pub root: u64,
    pub before: RocListWith<u8, false>,
    pub key: RocListWith<u8, false>,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct1f2ff78011a2f7cd>() == 56, "AnonStruct1f2ff78011a2f7cd size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct1f2ff78011a2f7cd>() == 8, "AnonStruct1f2ff78011a2f7cd alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct1f2ff78011a2f7cd>() == 32, "AnonStruct1f2ff78011a2f7cd size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct1f2ff78011a2f7cd>() == 8, "AnonStruct1f2ff78011a2f7cd alignment mismatch");

/// Element type for __AnonStruct_8e6ae4c4e7aeda0d
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct8e6ae4c4e7aeda0d {
    pub before: RocListWith<u8, false>,
    pub key: RocListWith<u8, false>,
}

/// Element type for __AnonStruct_8e6ae4c4e7aeda0d
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct8e6ae4c4e7aeda0d {
    pub before: RocListWith<u8, false>,
    pub key: RocListWith<u8, false>,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct8e6ae4c4e7aeda0d>() == 48, "AnonStruct8e6ae4c4e7aeda0d size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct8e6ae4c4e7aeda0d>() == 8, "AnonStruct8e6ae4c4e7aeda0d alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct8e6ae4c4e7aeda0d>() == 24, "AnonStruct8e6ae4c4e7aeda0d size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct8e6ae4c4e7aeda0d>() == 4, "AnonStruct8e6ae4c4e7aeda0d alignment mismatch");

/// Element type for __AnonStruct_73b63742301b46aa
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct73b63742301b46aa {
    pub root: u64,
    pub key: RocListWith<u8, false>,
}

/// Element type for __AnonStruct_73b63742301b46aa
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct73b63742301b46aa {
    pub root: u64,
    pub key: RocListWith<u8, false>,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct73b63742301b46aa>() == 32, "AnonStruct73b63742301b46aa size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct73b63742301b46aa>() == 8, "AnonStruct73b63742301b46aa alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct73b63742301b46aa>() == 24, "AnonStruct73b63742301b46aa size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct73b63742301b46aa>() == 8, "AnonStruct73b63742301b46aa alignment mismatch");

/// Element type for __AnonStruct_2c3712e02fef0cd
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct2c3712e02fef0cd {
    pub instance: u64,
    pub root: u64,
}

/// Element type for __AnonStruct_2c3712e02fef0cd
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct2c3712e02fef0cd {
    pub instance: u64,
    pub root: u64,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct2c3712e02fef0cd>() == 16, "AnonStruct2c3712e02fef0cd size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct2c3712e02fef0cd>() == 8, "AnonStruct2c3712e02fef0cd alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct2c3712e02fef0cd>() == 16, "AnonStruct2c3712e02fef0cd size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct2c3712e02fef0cd>() == 8, "AnonStruct2c3712e02fef0cd alignment mismatch");

/// Element type for __AnonStruct_b61da4ff1a34a156
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructB61da4ff1a34a156 {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub builder: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub shadow_color: u64,
    pub label: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

/// Element type for __AnonStruct_b61da4ff1a34a156
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructB61da4ff1a34a156 {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub builder: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub shadow_color: u64,
    pub label: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStructB61da4ff1a34a156>() == 208, "AnonStructB61da4ff1a34a156 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStructB61da4ff1a34a156>() == 8, "AnonStructB61da4ff1a34a156 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStructB61da4ff1a34a156>() == 192, "AnonStructB61da4ff1a34a156 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStructB61da4ff1a34a156>() == 8, "AnonStructB61da4ff1a34a156 alignment mismatch");

/// Element type for __AnonStruct_10af2f3a422106e8
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct10af2f3a422106e8 {
    pub hover_enter: u64,
    pub hover_exit: u64,
    pub id: u64,
    pub shortcut: u64,
}

/// Element type for __AnonStruct_10af2f3a422106e8
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct10af2f3a422106e8 {
    pub hover_enter: u64,
    pub hover_exit: u64,
    pub id: u64,
    pub shortcut: u64,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct10af2f3a422106e8>() == 32, "AnonStruct10af2f3a422106e8 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct10af2f3a422106e8>() == 8, "AnonStruct10af2f3a422106e8 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct10af2f3a422106e8>() == 32, "AnonStruct10af2f3a422106e8 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct10af2f3a422106e8>() == 8, "AnonStruct10af2f3a422106e8 alignment mismatch");

/// Element type for __AnonStruct_91244b23e2207007
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct91244b23e2207007 {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub builder: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub focus_serial: u64,
    pub hover_bg: u64,
    pub shadow_color: u64,
    pub label: RocStr,
    pub shortcuts: RocList<AnonStruct1d0ac13b7d648fa3>,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub delay_ms: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub hover_enter: bool,
    pub hover_exit: bool,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub placement: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

/// Element type for __AnonStruct_91244b23e2207007
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct91244b23e2207007 {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub builder: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub focus_serial: u64,
    pub hover_bg: u64,
    pub shadow_color: u64,
    pub label: RocStr,
    pub shortcuts: RocList<AnonStruct1d0ac13b7d648fa3>,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub delay_ms: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub hover_enter: bool,
    pub hover_exit: bool,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub placement: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct91244b23e2207007>() == 240, "AnonStruct91244b23e2207007 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct91244b23e2207007>() == 8, "AnonStruct91244b23e2207007 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct91244b23e2207007>() == 216, "AnonStruct91244b23e2207007 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct91244b23e2207007>() == 8, "AnonStruct91244b23e2207007 alignment mismatch");

/// Element type for __AnonStruct_1d0ac13b7d648fa3
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct1d0ac13b7d648fa3 {
    pub keys: RocStr,
    pub focus: bool,
}

/// Element type for __AnonStruct_1d0ac13b7d648fa3
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct1d0ac13b7d648fa3 {
    pub keys: RocStr,
    pub focus: bool,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct1d0ac13b7d648fa3>() == 32, "AnonStruct1d0ac13b7d648fa3 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct1d0ac13b7d648fa3>() == 8, "AnonStruct1d0ac13b7d648fa3 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct1d0ac13b7d648fa3>() == 16, "AnonStruct1d0ac13b7d648fa3 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct1d0ac13b7d648fa3>() == 4, "AnonStruct1d0ac13b7d648fa3 alignment mismatch");

/// Element type for __AnonStruct_df60cd23530654b2
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructDf60cd23530654b2 {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub child: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub shadow_color: u64,
    pub name: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub axis: u8,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

/// Element type for __AnonStruct_df60cd23530654b2
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructDf60cd23530654b2 {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub child: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub shadow_color: u64,
    pub name: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub axis: u8,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStructDf60cd23530654b2>() == 208, "AnonStructDf60cd23530654b2 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStructDf60cd23530654b2>() == 8, "AnonStructDf60cd23530654b2 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStructDf60cd23530654b2>() == 192, "AnonStructDf60cd23530654b2 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStructDf60cd23530654b2>() == 8, "AnonStructDf60cd23530654b2 alignment mismatch");

/// Element type for __AnonStruct_becf162f14687660
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructBecf162f14687660 {
    pub hover_enter: u64,
    pub hover_exit: u64,
    pub id: u64,
}

/// Element type for __AnonStruct_becf162f14687660
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructBecf162f14687660 {
    pub hover_enter: u64,
    pub hover_exit: u64,
    pub id: u64,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStructBecf162f14687660>() == 24, "AnonStructBecf162f14687660 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStructBecf162f14687660>() == 8, "AnonStructBecf162f14687660 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStructBecf162f14687660>() == 24, "AnonStructBecf162f14687660 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStructBecf162f14687660>() == 8, "AnonStructBecf162f14687660 alignment mismatch");

/// Element type for __AnonStruct_3188ec42c5b5e0d3
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct3188ec42c5b5e0d3 {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub shadow_color: u64,
    pub caption: RocStr,
    pub label: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub enabled: bool,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub hover_enter: bool,
    pub hover_exit: bool,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub role: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

/// Element type for __AnonStruct_3188ec42c5b5e0d3
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct3188ec42c5b5e0d3 {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub shadow_color: u64,
    pub caption: RocStr,
    pub label: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub enabled: bool,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub hover_enter: bool,
    pub hover_exit: bool,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub role: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct3188ec42c5b5e0d3>() == 224, "AnonStruct3188ec42c5b5e0d3 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct3188ec42c5b5e0d3>() == 8, "AnonStruct3188ec42c5b5e0d3 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct3188ec42c5b5e0d3>() == 200, "AnonStruct3188ec42c5b5e0d3 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct3188ec42c5b5e0d3>() == 8, "AnonStruct3188ec42c5b5e0d3 alignment mismatch");

/// Element type for __AnonStruct_fad101fcce1ec1a3
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructFad101fcce1ec1a3 {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub builder: u64,
    pub count: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub first: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub instance: u64,
    pub shadow_color: u64,
    pub name: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub row_gap: u32,
    pub row_height: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub notify: bool,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

/// Element type for __AnonStruct_fad101fcce1ec1a3
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructFad101fcce1ec1a3 {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub builder: u64,
    pub count: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub first: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub instance: u64,
    pub shadow_color: u64,
    pub name: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub row_gap: u32,
    pub row_height: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub notify: bool,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStructFad101fcce1ec1a3>() == 240, "AnonStructFad101fcce1ec1a3 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStructFad101fcce1ec1a3>() == 8, "AnonStructFad101fcce1ec1a3 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStructFad101fcce1ec1a3>() == 224, "AnonStructFad101fcce1ec1a3 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStructFad101fcce1ec1a3>() == 8, "AnonStructFad101fcce1ec1a3 alignment mismatch");

/// Element type for __AnonStruct_751f7db6342b42f6
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct751f7db6342b42f6 {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub box_bg: u64,
    pub box_border: u64,
    pub box_checked_bg: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub mark_color: u64,
    pub shadow_color: u64,
    pub label: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub checked: bool,
    pub enabled: bool,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

/// Element type for __AnonStruct_751f7db6342b42f6
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct751f7db6342b42f6 {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub box_bg: u64,
    pub box_border: u64,
    pub box_checked_bg: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub mark_color: u64,
    pub shadow_color: u64,
    pub label: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub checked: bool,
    pub enabled: bool,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct751f7db6342b42f6>() == 232, "AnonStruct751f7db6342b42f6 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct751f7db6342b42f6>() == 8, "AnonStruct751f7db6342b42f6 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct751f7db6342b42f6>() == 216, "AnonStruct751f7db6342b42f6 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct751f7db6342b42f6>() == 8, "AnonStruct751f7db6342b42f6 alignment mismatch");

/// Element type for __AnonStruct_a0ad260b0f5e9c52
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructA0ad260b0f5e9c52 {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub shadow_color: u64,
    pub label: RocStr,
    pub placeholder: RocStr,
    pub value: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub enabled: bool,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub read_only: bool,
    pub text_overflow: u8,
    pub width_kind: u8,
}

/// Element type for __AnonStruct_a0ad260b0f5e9c52
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructA0ad260b0f5e9c52 {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub shadow_color: u64,
    pub label: RocStr,
    pub placeholder: RocStr,
    pub value: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub enabled: bool,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub read_only: bool,
    pub text_overflow: u8,
    pub width_kind: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStructA0ad260b0f5e9c52>() == 248, "AnonStructA0ad260b0f5e9c52 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStructA0ad260b0f5e9c52>() == 8, "AnonStructA0ad260b0f5e9c52 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStructA0ad260b0f5e9c52>() == 208, "AnonStructA0ad260b0f5e9c52 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStructA0ad260b0f5e9c52>() == 8, "AnonStructA0ad260b0f5e9c52 alignment mismatch");

/// Element type for __AnonStruct_71144019e2d669fc
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct71144019e2d669fc {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub shadow_color: u64,
    pub bytes: RocListWith<u8, false>,
    pub label: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub fit: u8,
    pub font_face: u8,
    pub format: u8,
    pub grayscale: bool,
    pub grow: bool,
    pub height_kind: u8,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

/// Element type for __AnonStruct_71144019e2d669fc
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct71144019e2d669fc {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub shadow_color: u64,
    pub bytes: RocListWith<u8, false>,
    pub label: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub fit: u8,
    pub font_face: u8,
    pub format: u8,
    pub grayscale: bool,
    pub grow: bool,
    pub height_kind: u8,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct71144019e2d669fc>() == 224, "AnonStruct71144019e2d669fc size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct71144019e2d669fc>() == 8, "AnonStruct71144019e2d669fc alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct71144019e2d669fc>() == 200, "AnonStruct71144019e2d669fc size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct71144019e2d669fc>() == 8, "AnonStruct71144019e2d669fc alignment mismatch");

/// Element type for __AnonStruct_97820d55023100c7
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct97820d55023100c7 {
    pub bg: u64,
    pub border_color: u64,
    pub label: RocStr,
    pub primitives: RocList<AnonStructF74678f34f8b3cbf>,
    pub border_width: u32,
    pub height: u32,
    pub radius: u32,
    pub width: u32,
    pub grow: bool,
    pub height_kind: u8,
    pub hover: bool,
    pub wheel: bool,
    pub width_kind: u8,
}

/// Element type for __AnonStruct_97820d55023100c7
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct97820d55023100c7 {
    pub bg: u64,
    pub border_color: u64,
    pub label: RocStr,
    pub primitives: RocList<AnonStructF74678f34f8b3cbf>,
    pub border_width: u32,
    pub height: u32,
    pub radius: u32,
    pub width: u32,
    pub grow: bool,
    pub height_kind: u8,
    pub hover: bool,
    pub wheel: bool,
    pub width_kind: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct97820d55023100c7>() == 88, "AnonStruct97820d55023100c7 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct97820d55023100c7>() == 8, "AnonStruct97820d55023100c7 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct97820d55023100c7>() == 64, "AnonStruct97820d55023100c7 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct97820d55023100c7>() == 8, "AnonStruct97820d55023100c7 alignment mismatch");

/// Element type for __AnonStruct_f74678f34f8b3cbf
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructF74678f34f8b3cbf {
    pub fill: u64,
    pub key: u64,
    pub stroke: u64,
    pub label: RocStr,
    pub text: RocStr,
    pub height: u32,
    pub radius: u32,
    pub stroke_width: u32,
    pub text_size: u32,
    pub width: u32,
    pub x: i32,
    pub x2: i32,
    pub y: i32,
    pub y2: i32,
    pub align: u8,
    pub kind: u8,
}

/// Element type for __AnonStruct_f74678f34f8b3cbf
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructF74678f34f8b3cbf {
    pub fill: u64,
    pub key: u64,
    pub stroke: u64,
    pub label: RocStr,
    pub text: RocStr,
    pub height: u32,
    pub radius: u32,
    pub stroke_width: u32,
    pub text_size: u32,
    pub width: u32,
    pub x: i32,
    pub x2: i32,
    pub y: i32,
    pub y2: i32,
    pub align: u8,
    pub kind: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStructF74678f34f8b3cbf>() == 112, "AnonStructF74678f34f8b3cbf size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStructF74678f34f8b3cbf>() == 8, "AnonStructF74678f34f8b3cbf alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStructF74678f34f8b3cbf>() == 88, "AnonStructF74678f34f8b3cbf size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStructF74678f34f8b3cbf>() == 8, "AnonStructF74678f34f8b3cbf alignment mismatch");

/// Element type for __AnonStruct_be98516b6662a57b
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructBe98516b6662a57b {
    pub end: u64,
    pub first: u64,
}

/// Element type for __AnonStruct_be98516b6662a57b
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructBe98516b6662a57b {
    pub end: u64,
    pub first: u64,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStructBe98516b6662a57b>() == 16, "AnonStructBe98516b6662a57b size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStructBe98516b6662a57b>() == 8, "AnonStructBe98516b6662a57b alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStructBe98516b6662a57b>() == 16, "AnonStructBe98516b6662a57b size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStructBe98516b6662a57b>() == 8, "AnonStructBe98516b6662a57b alignment mismatch");

/// Element type for __AnonStruct_521b63caff3553f9
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct521b63caff3553f9 {
    pub count: u64,
    pub instance: u64,
    pub scroll_row: u64,
    pub scroll_serial: u64,
    pub row_height: u32,
    pub scroll_align: u8,
}

/// Element type for __AnonStruct_521b63caff3553f9
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct521b63caff3553f9 {
    pub count: u64,
    pub instance: u64,
    pub scroll_row: u64,
    pub scroll_serial: u64,
    pub row_height: u32,
    pub scroll_align: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct521b63caff3553f9>() == 40, "AnonStruct521b63caff3553f9 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct521b63caff3553f9>() == 8, "AnonStruct521b63caff3553f9 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct521b63caff3553f9>() == 40, "AnonStruct521b63caff3553f9 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct521b63caff3553f9>() == 8, "AnonStruct521b63caff3553f9 alignment mismatch");

/// Element type for __AnonStruct_d56b0eabf62517b9
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructD56b0eabf62517b9 {
    pub end: u64,
    pub start: u64,
    pub refresh: bool,
    pub report: bool,
}

/// Element type for __AnonStruct_d56b0eabf62517b9
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructD56b0eabf62517b9 {
    pub end: u64,
    pub start: u64,
    pub refresh: bool,
    pub report: bool,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStructD56b0eabf62517b9>() == 24, "AnonStructD56b0eabf62517b9 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStructD56b0eabf62517b9>() == 8, "AnonStructD56b0eabf62517b9 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStructD56b0eabf62517b9>() == 24, "AnonStructD56b0eabf62517b9 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStructD56b0eabf62517b9>() == 8, "AnonStructD56b0eabf62517b9 alignment mismatch");

/// Element type for __AnonStruct_e7c8b7462a78f14b
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructE7c8b7462a78f14b {
    pub target: u64,
    pub dx: i32,
    pub dy: i32,
    pub x: i32,
    pub y: i32,
    pub phase: u8,
}

/// Element type for __AnonStruct_e7c8b7462a78f14b
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructE7c8b7462a78f14b {
    pub target: u64,
    pub dx: i32,
    pub dy: i32,
    pub x: i32,
    pub y: i32,
    pub phase: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStructE7c8b7462a78f14b>() == 32, "AnonStructE7c8b7462a78f14b size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStructE7c8b7462a78f14b>() == 8, "AnonStructE7c8b7462a78f14b alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStructE7c8b7462a78f14b>() == 32, "AnonStructE7c8b7462a78f14b size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStructE7c8b7462a78f14b>() == 8, "AnonStructE7c8b7462a78f14b alignment mismatch");

/// Element type for __AnonStruct_bdcbe7a50e42954d
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructBdcbe7a50e42954d {
    pub index: u64,
    pub keys: RocStr,
}

/// Element type for __AnonStruct_bdcbe7a50e42954d
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructBdcbe7a50e42954d {
    pub index: u64,
    pub keys: RocStr,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStructBdcbe7a50e42954d>() == 32, "AnonStructBdcbe7a50e42954d size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStructBdcbe7a50e42954d>() == 8, "AnonStructBdcbe7a50e42954d alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStructBdcbe7a50e42954d>() == 24, "AnonStructBdcbe7a50e42954d size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStructBdcbe7a50e42954d>() == 8, "AnonStructBdcbe7a50e42954d alignment mismatch");

/// Element type for __AnonStruct_1da781635f2f9fce
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct1da781635f2f9fce {
    pub id: u64,
    pub shortcut: u64,
}

/// Element type for __AnonStruct_1da781635f2f9fce
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct1da781635f2f9fce {
    pub id: u64,
    pub shortcut: u64,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct1da781635f2f9fce>() == 16, "AnonStruct1da781635f2f9fce size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct1da781635f2f9fce>() == 8, "AnonStruct1da781635f2f9fce alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct1da781635f2f9fce>() == 16, "AnonStruct1da781635f2f9fce size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct1da781635f2f9fce>() == 8, "AnonStruct1da781635f2f9fce alignment mismatch");

/// Element type for __AnonStruct_e5ac0e79f166cfe4
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructE5ac0e79f166cfe4 {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub builder: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub shadow_color: u64,
    pub keys: RocList<AnonStruct1d0ac13b7d648fa3>,
    pub label: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub size: u32,
    pub thickness: u32,
    pub width: u32,
    pub align: u8,
    pub axis: u8,
    pub collapsed: bool,
    pub collapsible: bool,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub side: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

/// Element type for __AnonStruct_e5ac0e79f166cfe4
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructE5ac0e79f166cfe4 {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub builder: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub shadow_color: u64,
    pub keys: RocList<AnonStruct1d0ac13b7d648fa3>,
    pub label: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub size: u32,
    pub thickness: u32,
    pub width: u32,
    pub align: u8,
    pub axis: u8,
    pub collapsed: bool,
    pub collapsible: bool,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub side: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStructE5ac0e79f166cfe4>() == 248, "AnonStructE5ac0e79f166cfe4 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStructE5ac0e79f166cfe4>() == 8, "AnonStructE5ac0e79f166cfe4 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStructE5ac0e79f166cfe4>() == 224, "AnonStructE5ac0e79f166cfe4 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStructE5ac0e79f166cfe4>() == 8, "AnonStructE5ac0e79f166cfe4 alignment mismatch");

/// Element type for __AnonStruct_7dea1af4373e83a
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct7dea1af4373e83a {
    pub size: u32,
    pub collapsed: bool,
}

/// Element type for __AnonStruct_7dea1af4373e83a
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct7dea1af4373e83a {
    pub size: u32,
    pub collapsed: bool,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct7dea1af4373e83a>() == 8, "AnonStruct7dea1af4373e83a size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct7dea1af4373e83a>() == 4, "AnonStruct7dea1af4373e83a alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct7dea1af4373e83a>() == 8, "AnonStruct7dea1af4373e83a size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct7dea1af4373e83a>() == 4, "AnonStruct7dea1af4373e83a alignment mismatch");

/// Element type for __AnonStruct_dc4c26f806dfc5bf
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructDc4c26f806dfc5bf {
    pub change: u64,
    pub id: u64,
    pub submit: u64,
}

/// Element type for __AnonStruct_dc4c26f806dfc5bf
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructDc4c26f806dfc5bf {
    pub change: u64,
    pub id: u64,
    pub submit: u64,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStructDc4c26f806dfc5bf>() == 24, "AnonStructDc4c26f806dfc5bf size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStructDc4c26f806dfc5bf>() == 8, "AnonStructDc4c26f806dfc5bf alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStructDc4c26f806dfc5bf>() == 24, "AnonStructDc4c26f806dfc5bf size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStructDc4c26f806dfc5bf>() == 8, "AnonStructDc4c26f806dfc5bf alignment mismatch");

/// Element type for __AnonStruct_2ffca3ac98d0c52c
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct2ffca3ac98d0c52c {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub shadow_color: u64,
    pub label: RocStr,
    pub placeholder: RocStr,
    pub value: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub enabled: bool,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

/// Element type for __AnonStruct_2ffca3ac98d0c52c
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct2ffca3ac98d0c52c {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub shadow_color: u64,
    pub label: RocStr,
    pub placeholder: RocStr,
    pub value: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub enabled: bool,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct2ffca3ac98d0c52c>() == 248, "AnonStruct2ffca3ac98d0c52c size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct2ffca3ac98d0c52c>() == 8, "AnonStruct2ffca3ac98d0c52c alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct2ffca3ac98d0c52c>() == 208, "AnonStruct2ffca3ac98d0c52c size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct2ffca3ac98d0c52c>() == 8, "AnonStruct2ffca3ac98d0c52c alignment mismatch");

/// Element type for __AnonStruct_4c605145331877e7
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct4c605145331877e7 {
    pub message: RocStr,
    pub code: u8,
}

/// Element type for __AnonStruct_4c605145331877e7
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct4c605145331877e7 {
    pub message: RocStr,
    pub code: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct4c605145331877e7>() == 32, "AnonStruct4c605145331877e7 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct4c605145331877e7>() == 8, "AnonStruct4c605145331877e7 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct4c605145331877e7>() == 16, "AnonStruct4c605145331877e7 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct4c605145331877e7>() == 4, "AnonStruct4c605145331877e7 alignment mismatch");

/// Element type for __AnonStruct_89852815e920c968
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct89852815e920c968 {
    pub columns: RocList<RocStr>,
    pub rows: RocList<RocList<AnonStructBff7bc5a4faa26ae>>,
    pub more: bool,
}

/// Element type for __AnonStruct_89852815e920c968
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct89852815e920c968 {
    pub columns: RocList<RocStr>,
    pub rows: RocList<RocList<AnonStructBff7bc5a4faa26ae>>,
    pub more: bool,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct89852815e920c968>() == 56, "AnonStruct89852815e920c968 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct89852815e920c968>() == 8, "AnonStruct89852815e920c968 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct89852815e920c968>() == 28, "AnonStruct89852815e920c968 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct89852815e920c968>() == 4, "AnonStruct89852815e920c968 alignment mismatch");

/// Element type for __AnonStruct_bff7bc5a4faa26ae
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructBff7bc5a4faa26ae {
    pub integer: i64,
    pub real: f64,
    pub bytes: RocListWith<u8, false>,
    pub text: RocStr,
    pub kind: u8,
}

/// Element type for __AnonStruct_bff7bc5a4faa26ae
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructBff7bc5a4faa26ae {
    pub integer: i64,
    pub real: f64,
    pub bytes: RocListWith<u8, false>,
    pub text: RocStr,
    pub kind: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStructBff7bc5a4faa26ae>() == 72, "AnonStructBff7bc5a4faa26ae size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStructBff7bc5a4faa26ae>() == 8, "AnonStructBff7bc5a4faa26ae alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStructBff7bc5a4faa26ae>() == 48, "AnonStructBff7bc5a4faa26ae size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStructBff7bc5a4faa26ae>() == 8, "AnonStructBff7bc5a4faa26ae alignment mismatch");

/// Element type for __AnonStruct_ad07206fc55b9842
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructAd07206fc55b9842 {
    pub page_rows: u64,
    pub params: RocList<AnonStructBff7bc5a4faa26ae>,
    pub sql: RocStr,
}

/// Element type for __AnonStruct_ad07206fc55b9842
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructAd07206fc55b9842 {
    pub page_rows: u64,
    pub params: RocList<AnonStructBff7bc5a4faa26ae>,
    pub sql: RocStr,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStructAd07206fc55b9842>() == 56, "AnonStructAd07206fc55b9842 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStructAd07206fc55b9842>() == 8, "AnonStructAd07206fc55b9842 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStructAd07206fc55b9842>() == 32, "AnonStructAd07206fc55b9842 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStructAd07206fc55b9842>() == 8, "AnonStructAd07206fc55b9842 alignment mismatch");

/// Element type for __AnonStruct_2a44a28a63f85620
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct2a44a28a63f85620 {
    pub sequence: u64,
    pub text: RocStr,
}

/// Element type for __AnonStruct_2a44a28a63f85620
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct2a44a28a63f85620 {
    pub sequence: u64,
    pub text: RocStr,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct2a44a28a63f85620>() == 32, "AnonStruct2a44a28a63f85620 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct2a44a28a63f85620>() == 8, "AnonStruct2a44a28a63f85620 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct2a44a28a63f85620>() == 24, "AnonStruct2a44a28a63f85620 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct2a44a28a63f85620>() == 8, "AnonStruct2a44a28a63f85620 alignment mismatch");

/// Element type for __AnonStruct_8e993eac1f1cd0c
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct8e993eac1f1cd0c {
    pub duration_ms: u64,
    pub track: *mut u64,
}

/// Element type for __AnonStruct_8e993eac1f1cd0c
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct8e993eac1f1cd0c {
    pub duration_ms: u64,
    pub track: *mut u64,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct8e993eac1f1cd0c>() == 16, "AnonStruct8e993eac1f1cd0c size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct8e993eac1f1cd0c>() == 8, "AnonStruct8e993eac1f1cd0c alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct8e993eac1f1cd0c>() == 16, "AnonStruct8e993eac1f1cd0c size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct8e993eac1f1cd0c>() == 8, "AnonStruct8e993eac1f1cd0c alignment mismatch");

/// Element type for __AnonStruct_18a809e7fe3ae87a
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct18a809e7fe3ae87a {
    pub duration_ms: u64,
    pub position_ms: u64,
    pub state: u8,
}

/// Element type for __AnonStruct_18a809e7fe3ae87a
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct18a809e7fe3ae87a {
    pub duration_ms: u64,
    pub position_ms: u64,
    pub state: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct18a809e7fe3ae87a>() == 24, "AnonStruct18a809e7fe3ae87a size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct18a809e7fe3ae87a>() == 8, "AnonStruct18a809e7fe3ae87a alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct18a809e7fe3ae87a>() == 24, "AnonStruct18a809e7fe3ae87a size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct18a809e7fe3ae87a>() == 8, "AnonStruct18a809e7fe3ae87a alignment mismatch");

/// Element type for __AnonStruct_93136bf334c2a2fc
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct93136bf334c2a2fc {
    pub columns: u16,
    pub rows: u16,
}

/// Element type for __AnonStruct_93136bf334c2a2fc
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct93136bf334c2a2fc {
    pub columns: u16,
    pub rows: u16,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct93136bf334c2a2fc>() == 4, "AnonStruct93136bf334c2a2fc size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct93136bf334c2a2fc>() == 2, "AnonStruct93136bf334c2a2fc alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct93136bf334c2a2fc>() == 4, "AnonStruct93136bf334c2a2fc size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct93136bf334c2a2fc>() == 2, "AnonStruct93136bf334c2a2fc alignment mismatch");

/// Element type for __AnonStruct_27556b2f7cb4f65f
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct27556b2f7cb4f65f {
    pub manufacturer: RocStr,
    pub product: RocStr,
    pub product_id: u16,
    pub vendor_id: u16,
}

/// Element type for __AnonStruct_27556b2f7cb4f65f
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct27556b2f7cb4f65f {
    pub manufacturer: RocStr,
    pub product: RocStr,
    pub product_id: u16,
    pub vendor_id: u16,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct27556b2f7cb4f65f>() == 56, "AnonStruct27556b2f7cb4f65f size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct27556b2f7cb4f65f>() == 8, "AnonStruct27556b2f7cb4f65f alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct27556b2f7cb4f65f>() == 28, "AnonStruct27556b2f7cb4f65f size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct27556b2f7cb4f65f>() == 4, "AnonStruct27556b2f7cb4f65f alignment mismatch");

/// Element type for __AnonStruct_98efc21233c140ff
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct98efc21233c140ff {
    pub disk_read_bytes: u64,
    pub disk_written_bytes: u64,
    pub memory_total_bytes: u64,
    pub memory_used_bytes: u64,
    pub network_received_bytes: u64,
    pub network_transmitted_bytes: u64,
    pub sequence: u64,
    pub processes: RocList<AnonStruct916a0c1ad2ed4712>,
    pub cpu_tenths: u16,
    pub cpu_available: bool,
    pub disk_available: bool,
    pub memory_available: bool,
    pub network_available: bool,
    pub processes_available: bool,
}

/// Element type for __AnonStruct_98efc21233c140ff
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct98efc21233c140ff {
    pub disk_read_bytes: u64,
    pub disk_written_bytes: u64,
    pub memory_total_bytes: u64,
    pub memory_used_bytes: u64,
    pub network_received_bytes: u64,
    pub network_transmitted_bytes: u64,
    pub sequence: u64,
    pub processes: RocList<AnonStruct916a0c1ad2ed4712>,
    pub cpu_tenths: u16,
    pub cpu_available: bool,
    pub disk_available: bool,
    pub memory_available: bool,
    pub network_available: bool,
    pub processes_available: bool,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct98efc21233c140ff>() == 88, "AnonStruct98efc21233c140ff size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct98efc21233c140ff>() == 8, "AnonStruct98efc21233c140ff alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct98efc21233c140ff>() == 80, "AnonStruct98efc21233c140ff size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct98efc21233c140ff>() == 8, "AnonStruct98efc21233c140ff alignment mismatch");

/// Element type for __AnonStruct_916a0c1ad2ed4712
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct916a0c1ad2ed4712 {
    pub memory_bytes: u64,
    pub pid: u64,
    pub name: RocStr,
    pub cpu_tenths: u16,
}

/// Element type for __AnonStruct_916a0c1ad2ed4712
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct916a0c1ad2ed4712 {
    pub memory_bytes: u64,
    pub pid: u64,
    pub name: RocStr,
    pub cpu_tenths: u16,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct916a0c1ad2ed4712>() == 48, "AnonStruct916a0c1ad2ed4712 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct916a0c1ad2ed4712>() == 8, "AnonStruct916a0c1ad2ed4712 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct916a0c1ad2ed4712>() == 32, "AnonStruct916a0c1ad2ed4712 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct916a0c1ad2ed4712>() == 8, "AnonStruct916a0c1ad2ed4712 alignment mismatch");

/// Element type for __AnonStruct_b3b29ac2cb34a461
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructB3b29ac2cb34a461 {
    pub height: u32,
    pub width: u32,
}

/// Element type for __AnonStruct_b3b29ac2cb34a461
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructB3b29ac2cb34a461 {
    pub height: u32,
    pub width: u32,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStructB3b29ac2cb34a461>() == 8, "AnonStructB3b29ac2cb34a461 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStructB3b29ac2cb34a461>() == 4, "AnonStructB3b29ac2cb34a461 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStructB3b29ac2cb34a461>() == 8, "AnonStructB3b29ac2cb34a461 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStructB3b29ac2cb34a461>() == 4, "AnonStructB3b29ac2cb34a461 alignment mismatch");

/// Element type for __AnonStruct_513f8b80afa01b67
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct513f8b80afa01b67 {
    pub asset_set: RocStr,
    pub content_hash: RocStr,
    pub root: RocStr,
    pub content_version: u32,
    pub schema: u32,
    pub content_mode: u8,
    pub manifest_required: bool,
    pub root_kind: u8,
}

/// Element type for __AnonStruct_513f8b80afa01b67
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct513f8b80afa01b67 {
    pub asset_set: RocStr,
    pub content_hash: RocStr,
    pub root: RocStr,
    pub content_version: u32,
    pub schema: u32,
    pub content_mode: u8,
    pub manifest_required: bool,
    pub root_kind: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct513f8b80afa01b67>() == 88, "AnonStruct513f8b80afa01b67 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct513f8b80afa01b67>() == 8, "AnonStruct513f8b80afa01b67 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct513f8b80afa01b67>() == 48, "AnonStruct513f8b80afa01b67 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct513f8b80afa01b67>() == 4, "AnonStruct513f8b80afa01b67 alignment mismatch");

/// Element type for __AnonStruct_8ae3ad5a5c0e22dc
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct8ae3ad5a5c0e22dc {
    pub root: u64,
}

/// Element type for __AnonStruct_8ae3ad5a5c0e22dc
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct8ae3ad5a5c0e22dc {
    pub root: u64,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct8ae3ad5a5c0e22dc>() == 8, "AnonStruct8ae3ad5a5c0e22dc size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct8ae3ad5a5c0e22dc>() == 8, "AnonStruct8ae3ad5a5c0e22dc alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct8ae3ad5a5c0e22dc>() == 8, "AnonStruct8ae3ad5a5c0e22dc size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct8ae3ad5a5c0e22dc>() == 8, "AnonStruct8ae3ad5a5c0e22dc alignment mismatch");

/// Element type for __AnonStruct_8d3c9b3ac95ef074
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct8d3c9b3ac95ef074 {
    pub old_root: u64,
    pub root: u64,
}

/// Element type for __AnonStruct_8d3c9b3ac95ef074
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct8d3c9b3ac95ef074 {
    pub old_root: u64,
    pub root: u64,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct8d3c9b3ac95ef074>() == 16, "AnonStruct8d3c9b3ac95ef074 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct8d3c9b3ac95ef074>() == 8, "AnonStruct8d3c9b3ac95ef074 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct8d3c9b3ac95ef074>() == 16, "AnonStruct8d3c9b3ac95ef074 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct8d3c9b3ac95ef074>() == 8, "AnonStruct8d3c9b3ac95ef074 alignment mismatch");

/// Element type for __AnonStruct_be6bcbc15f8a1360
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructBe6bcbc15f8a1360 {
    pub body: RocListWith<u8, false>,
    pub headers: RocList<AnonStruct77eaba63dfee299d>,
    pub status: u16,
}

/// Element type for __AnonStruct_be6bcbc15f8a1360
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructBe6bcbc15f8a1360 {
    pub body: RocListWith<u8, false>,
    pub headers: RocList<AnonStruct77eaba63dfee299d>,
    pub status: u16,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStructBe6bcbc15f8a1360>() == 56, "AnonStructBe6bcbc15f8a1360 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStructBe6bcbc15f8a1360>() == 8, "AnonStructBe6bcbc15f8a1360 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStructBe6bcbc15f8a1360>() == 28, "AnonStructBe6bcbc15f8a1360 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStructBe6bcbc15f8a1360>() == 4, "AnonStructBe6bcbc15f8a1360 alignment mismatch");

/// Element type for __AnonStruct_77eaba63dfee299d
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct77eaba63dfee299d {
    pub _0: RocStr,
    pub _1: RocStr,
}

/// Element type for __AnonStruct_77eaba63dfee299d
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct77eaba63dfee299d {
    pub _0: RocStr,
    pub _1: RocStr,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct77eaba63dfee299d>() == 48, "AnonStruct77eaba63dfee299d size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct77eaba63dfee299d>() == 8, "AnonStruct77eaba63dfee299d alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct77eaba63dfee299d>() == 24, "AnonStruct77eaba63dfee299d size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct77eaba63dfee299d>() == 4, "AnonStruct77eaba63dfee299d alignment mismatch");

/// Element type for __AnonStruct_e6f574a7975f5cda
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructE6f574a7975f5cda {
    pub max_response_bytes: u64,
    pub timeout_ms: u64,
    pub body: RocListWith<u8, false>,
    pub client: *mut u64,
    pub headers: RocList<AnonStruct77eaba63dfee299d>,
    pub method_ext: RocStr,
    pub url: RocStr,
    pub max_redirects: u8,
    pub method: u8,
}

/// Element type for __AnonStruct_e6f574a7975f5cda
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructE6f574a7975f5cda {
    pub max_response_bytes: u64,
    pub timeout_ms: u64,
    pub body: RocListWith<u8, false>,
    pub client: *mut u64,
    pub headers: RocList<AnonStruct77eaba63dfee299d>,
    pub method_ext: RocStr,
    pub url: RocStr,
    pub max_redirects: u8,
    pub method: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStructE6f574a7975f5cda>() == 128, "AnonStructE6f574a7975f5cda size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStructE6f574a7975f5cda>() == 8, "AnonStructE6f574a7975f5cda alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStructE6f574a7975f5cda>() == 72, "AnonStructE6f574a7975f5cda size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStructE6f574a7975f5cda>() == 8, "AnonStructE6f574a7975f5cda alignment mismatch");

/// Element type for __AnonStruct_4869dafad3498788
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct4869dafad3498788 {
    pub directory: *mut u64,
    pub name: RocStr,
}

/// Element type for __AnonStruct_4869dafad3498788
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct4869dafad3498788 {
    pub directory: *mut u64,
    pub name: RocStr,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct4869dafad3498788>() == 32, "AnonStruct4869dafad3498788 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct4869dafad3498788>() == 8, "AnonStruct4869dafad3498788 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct4869dafad3498788>() == 16, "AnonStruct4869dafad3498788 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct4869dafad3498788>() == 4, "AnonStruct4869dafad3498788 alignment mismatch");

/// Element type for __AnonStruct_770b9d9b3d3d255
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct770b9d9b3d3d255 {
    pub bytes: NoneOrSome,
    pub name: RocStr,
    pub kind: DirectoryOrFileOrOtherOrSymbolicLink,
}

/// Element type for __AnonStruct_770b9d9b3d3d255
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct770b9d9b3d3d255 {
    pub bytes: NoneOrSome,
    pub name: RocStr,
    pub kind: DirectoryOrFileOrOtherOrSymbolicLink,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct770b9d9b3d3d255>() == 48, "AnonStruct770b9d9b3d3d255 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct770b9d9b3d3d255>() == 8, "AnonStruct770b9d9b3d3d255 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct770b9d9b3d3d255>() == 32, "AnonStruct770b9d9b3d3d255 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct770b9d9b3d3d255>() == 8, "AnonStruct770b9d9b3d3d255 alignment mismatch");

/// Element type for __AnonStruct_5d4fced1f96c13bb
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct5d4fced1f96c13bb {
    pub names: RocList<RocStr>,
    pub code: u8,
    pub overflowed: bool,
    pub replaced: bool,
}

/// Element type for __AnonStruct_5d4fced1f96c13bb
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct5d4fced1f96c13bb {
    pub names: RocList<RocStr>,
    pub code: u8,
    pub overflowed: bool,
    pub replaced: bool,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct5d4fced1f96c13bb>() == 32, "AnonStruct5d4fced1f96c13bb size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct5d4fced1f96c13bb>() == 8, "AnonStruct5d4fced1f96c13bb alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct5d4fced1f96c13bb>() == 16, "AnonStruct5d4fced1f96c13bb size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct5d4fced1f96c13bb>() == 4, "AnonStruct5d4fced1f96c13bb alignment mismatch");

/// Element type for __AnonStruct_a295f39559baa24d
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructA295f39559baa24d {
    pub file: *mut u64,
    pub name: RocStr,
}

/// Element type for __AnonStruct_a295f39559baa24d
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructA295f39559baa24d {
    pub file: *mut u64,
    pub name: RocStr,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStructA295f39559baa24d>() == 32, "AnonStructA295f39559baa24d size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStructA295f39559baa24d>() == 8, "AnonStructA295f39559baa24d alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStructA295f39559baa24d>() == 16, "AnonStructA295f39559baa24d size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStructA295f39559baa24d>() == 4, "AnonStructA295f39559baa24d alignment mismatch");

/// Element type for __AnonStruct_da3665c9cce9a3c
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructDa3665c9cce9a3c {
    pub extensions: RocList<RocStr>,
    pub label: RocStr,
    pub mime_types: RocList<RocStr>,
}

/// Element type for __AnonStruct_da3665c9cce9a3c
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructDa3665c9cce9a3c {
    pub extensions: RocList<RocStr>,
    pub label: RocStr,
    pub mime_types: RocList<RocStr>,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStructDa3665c9cce9a3c>() == 72, "AnonStructDa3665c9cce9a3c size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStructDa3665c9cce9a3c>() == 8, "AnonStructDa3665c9cce9a3c alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStructDa3665c9cce9a3c>() == 36, "AnonStructDa3665c9cce9a3c size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStructDa3665c9cce9a3c>() == 4, "AnonStructDa3665c9cce9a3c alignment mismatch");

/// Element type for __AnonStruct_a2f37e1d5ed0e4cd
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructA2f37e1d5ed0e4cd {
    pub value: RocStr,
    pub found: bool,
}

/// Element type for __AnonStruct_a2f37e1d5ed0e4cd
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructA2f37e1d5ed0e4cd {
    pub value: RocStr,
    pub found: bool,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStructA2f37e1d5ed0e4cd>() == 32, "AnonStructA2f37e1d5ed0e4cd size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStructA2f37e1d5ed0e4cd>() == 8, "AnonStructA2f37e1d5ed0e4cd alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStructA2f37e1d5ed0e4cd>() == 16, "AnonStructA2f37e1d5ed0e4cd size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStructA2f37e1d5ed0e4cd>() == 4, "AnonStructA2f37e1d5ed0e4cd alignment mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum HostGlueSqliteOpenReadResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union HostGlueSqliteOpenReadResultPayload {
    pub err: core::mem::ManuallyDrop<AnonStruct4c605145331877e7>,
    pub ok: core::mem::ManuallyDrop<*mut u64>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct HostGlueSqliteOpenReadResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueSqliteOpenReadResult {
    pub _payload_alignment: [HostGlueSqliteOpenReadResultPayloadAlignment; 0],
    pub payload: [u8; 16],
    pub tag: HostGlueSqliteOpenReadResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueSqliteOpenReadResult {
    pub payload: HostGlueSqliteOpenReadResultPayload,
    pub tag: HostGlueSqliteOpenReadResultTag,
}

impl HostGlueSqliteOpenReadResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueSqliteOpenReadResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AnonStruct4c605145331877e7 {
        unsafe { &*(self.payload.as_ptr() as *const AnonStruct4c605145331877e7) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueSqliteOpenReadResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AnonStruct4c605145331877e7 {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<AnonStruct4c605145331877e7> as *const AnonStruct4c605145331877e7) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueSqliteOpenReadResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AnonStruct4c605145331877e7 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AnonStruct4c605145331877e7) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueSqliteOpenReadResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AnonStruct4c605145331877e7 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueSqliteOpenReadResultTag::Ok` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &*mut u64 {
        unsafe { &*(self.payload.as_ptr() as *const *mut u64) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueSqliteOpenReadResultTag::Ok` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &*mut u64 {
        unsafe { &*(&self.payload.ok as *const core::mem::ManuallyDrop<*mut u64> as *const *mut u64) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueSqliteOpenReadResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> *mut u64 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const *mut u64) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueSqliteOpenReadResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> *mut u64 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.ok) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueSqliteOpenReadResult>() == 40, "HostGlueSqliteOpenReadResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueSqliteOpenReadResult>() == 8, "HostGlueSqliteOpenReadResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(HostGlueSqliteOpenReadResult, tag) == 32, "HostGlueSqliteOpenReadResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueSqliteOpenReadResult>() == 20, "HostGlueSqliteOpenReadResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueSqliteOpenReadResult>() == 4, "HostGlueSqliteOpenReadResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(HostGlueSqliteOpenReadResult, tag) == 16, "HostGlueSqliteOpenReadResult tag offset mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum HostGlueSqliteWatchResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union HostGlueSqliteWatchResultPayload {
    pub err: core::mem::ManuallyDrop<AnonStruct4c605145331877e7>,
    pub ok: core::mem::ManuallyDrop<*mut u64>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct HostGlueSqliteWatchResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueSqliteWatchResult {
    pub _payload_alignment: [HostGlueSqliteWatchResultPayloadAlignment; 0],
    pub payload: [u8; 16],
    pub tag: HostGlueSqliteWatchResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueSqliteWatchResult {
    pub payload: HostGlueSqliteWatchResultPayload,
    pub tag: HostGlueSqliteWatchResultTag,
}

impl HostGlueSqliteWatchResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueSqliteWatchResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AnonStruct4c605145331877e7 {
        unsafe { &*(self.payload.as_ptr() as *const AnonStruct4c605145331877e7) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueSqliteWatchResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AnonStruct4c605145331877e7 {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<AnonStruct4c605145331877e7> as *const AnonStruct4c605145331877e7) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueSqliteWatchResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AnonStruct4c605145331877e7 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AnonStruct4c605145331877e7) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueSqliteWatchResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AnonStruct4c605145331877e7 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueSqliteWatchResultTag::Ok` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &*mut u64 {
        unsafe { &*(self.payload.as_ptr() as *const *mut u64) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueSqliteWatchResultTag::Ok` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &*mut u64 {
        unsafe { &*(&self.payload.ok as *const core::mem::ManuallyDrop<*mut u64> as *const *mut u64) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueSqliteWatchResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> *mut u64 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const *mut u64) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueSqliteWatchResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> *mut u64 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.ok) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueSqliteWatchResult>() == 40, "HostGlueSqliteWatchResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueSqliteWatchResult>() == 8, "HostGlueSqliteWatchResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(HostGlueSqliteWatchResult, tag) == 32, "HostGlueSqliteWatchResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueSqliteWatchResult>() == 20, "HostGlueSqliteWatchResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueSqliteWatchResult>() == 4, "HostGlueSqliteWatchResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(HostGlueSqliteWatchResult, tag) == 16, "HostGlueSqliteWatchResult tag offset mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum HostGlueSqliteQueryResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union HostGlueSqliteQueryResultPayload {
    pub err: core::mem::ManuallyDrop<AnonStruct4c605145331877e7>,
    pub ok: core::mem::ManuallyDrop<AnonStruct89852815e920c968>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct HostGlueSqliteQueryResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueSqliteQueryResult {
    pub _payload_alignment: [HostGlueSqliteQueryResultPayloadAlignment; 0],
    pub payload: [u8; 28],
    pub tag: HostGlueSqliteQueryResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueSqliteQueryResult {
    pub payload: HostGlueSqliteQueryResultPayload,
    pub tag: HostGlueSqliteQueryResultTag,
}

impl HostGlueSqliteQueryResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueSqliteQueryResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AnonStruct4c605145331877e7 {
        unsafe { &*(self.payload.as_ptr() as *const AnonStruct4c605145331877e7) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueSqliteQueryResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AnonStruct4c605145331877e7 {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<AnonStruct4c605145331877e7> as *const AnonStruct4c605145331877e7) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueSqliteQueryResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AnonStruct4c605145331877e7 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AnonStruct4c605145331877e7) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueSqliteQueryResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AnonStruct4c605145331877e7 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueSqliteQueryResultTag::Ok` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &AnonStruct89852815e920c968 {
        unsafe { &*(self.payload.as_ptr() as *const AnonStruct89852815e920c968) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueSqliteQueryResultTag::Ok` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &AnonStruct89852815e920c968 {
        unsafe { &*(&self.payload.ok as *const core::mem::ManuallyDrop<AnonStruct89852815e920c968> as *const AnonStruct89852815e920c968) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueSqliteQueryResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> AnonStruct89852815e920c968 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AnonStruct89852815e920c968) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueSqliteQueryResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> AnonStruct89852815e920c968 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.ok) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueSqliteQueryResult>() == 64, "HostGlueSqliteQueryResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueSqliteQueryResult>() == 8, "HostGlueSqliteQueryResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(HostGlueSqliteQueryResult, tag) == 56, "HostGlueSqliteQueryResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueSqliteQueryResult>() == 32, "HostGlueSqliteQueryResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueSqliteQueryResult>() == 4, "HostGlueSqliteQueryResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(HostGlueSqliteQueryResult, tag) == 28, "HostGlueSqliteQueryResult tag offset mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum HostGlueClipboardAcquireResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union HostGlueClipboardAcquireResultPayload {
    pub err: core::mem::ManuallyDrop<AnonStruct4c605145331877e7>,
    pub ok: core::mem::ManuallyDrop<*mut u64>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct HostGlueClipboardAcquireResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueClipboardAcquireResult {
    pub _payload_alignment: [HostGlueClipboardAcquireResultPayloadAlignment; 0],
    pub payload: [u8; 16],
    pub tag: HostGlueClipboardAcquireResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueClipboardAcquireResult {
    pub payload: HostGlueClipboardAcquireResultPayload,
    pub tag: HostGlueClipboardAcquireResultTag,
}

impl HostGlueClipboardAcquireResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueClipboardAcquireResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AnonStruct4c605145331877e7 {
        unsafe { &*(self.payload.as_ptr() as *const AnonStruct4c605145331877e7) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueClipboardAcquireResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AnonStruct4c605145331877e7 {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<AnonStruct4c605145331877e7> as *const AnonStruct4c605145331877e7) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueClipboardAcquireResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AnonStruct4c605145331877e7 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AnonStruct4c605145331877e7) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueClipboardAcquireResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AnonStruct4c605145331877e7 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueClipboardAcquireResultTag::Ok` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &*mut u64 {
        unsafe { &*(self.payload.as_ptr() as *const *mut u64) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueClipboardAcquireResultTag::Ok` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &*mut u64 {
        unsafe { &*(&self.payload.ok as *const core::mem::ManuallyDrop<*mut u64> as *const *mut u64) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueClipboardAcquireResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> *mut u64 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const *mut u64) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueClipboardAcquireResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> *mut u64 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.ok) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueClipboardAcquireResult>() == 40, "HostGlueClipboardAcquireResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueClipboardAcquireResult>() == 8, "HostGlueClipboardAcquireResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(HostGlueClipboardAcquireResult, tag) == 32, "HostGlueClipboardAcquireResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueClipboardAcquireResult>() == 20, "HostGlueClipboardAcquireResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueClipboardAcquireResult>() == 4, "HostGlueClipboardAcquireResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(HostGlueClipboardAcquireResult, tag) == 16, "HostGlueClipboardAcquireResult tag offset mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum HostGlueClipboardReadTextResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union HostGlueClipboardReadTextResultPayload {
    pub err: core::mem::ManuallyDrop<AnonStruct4c605145331877e7>,
    pub ok: core::mem::ManuallyDrop<AnonStruct2a44a28a63f85620>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(8))]
#[derive(Clone, Copy)]
pub struct HostGlueClipboardReadTextResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueClipboardReadTextResult {
    pub _payload_alignment: [HostGlueClipboardReadTextResultPayloadAlignment; 0],
    pub payload: [u8; 24],
    pub tag: HostGlueClipboardReadTextResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueClipboardReadTextResult {
    pub payload: HostGlueClipboardReadTextResultPayload,
    pub tag: HostGlueClipboardReadTextResultTag,
}

impl HostGlueClipboardReadTextResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueClipboardReadTextResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AnonStruct4c605145331877e7 {
        unsafe { &*(self.payload.as_ptr() as *const AnonStruct4c605145331877e7) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueClipboardReadTextResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AnonStruct4c605145331877e7 {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<AnonStruct4c605145331877e7> as *const AnonStruct4c605145331877e7) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueClipboardReadTextResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AnonStruct4c605145331877e7 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AnonStruct4c605145331877e7) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueClipboardReadTextResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AnonStruct4c605145331877e7 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueClipboardReadTextResultTag::Ok` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &AnonStruct2a44a28a63f85620 {
        unsafe { &*(self.payload.as_ptr() as *const AnonStruct2a44a28a63f85620) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueClipboardReadTextResultTag::Ok` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &AnonStruct2a44a28a63f85620 {
        unsafe { &*(&self.payload.ok as *const core::mem::ManuallyDrop<AnonStruct2a44a28a63f85620> as *const AnonStruct2a44a28a63f85620) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueClipboardReadTextResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> AnonStruct2a44a28a63f85620 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AnonStruct2a44a28a63f85620) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueClipboardReadTextResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> AnonStruct2a44a28a63f85620 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.ok) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueClipboardReadTextResult>() == 40, "HostGlueClipboardReadTextResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueClipboardReadTextResult>() == 8, "HostGlueClipboardReadTextResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(HostGlueClipboardReadTextResult, tag) == 32, "HostGlueClipboardReadTextResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueClipboardReadTextResult>() == 32, "HostGlueClipboardReadTextResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueClipboardReadTextResult>() == 8, "HostGlueClipboardReadTextResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(HostGlueClipboardReadTextResult, tag) == 24, "HostGlueClipboardReadTextResult tag offset mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum HostGlueClipboardWriteTextResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union HostGlueClipboardWriteTextResultPayload {
    pub err: core::mem::ManuallyDrop<AnonStruct4c605145331877e7>,
    pub ok: [u8; 0],
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct HostGlueClipboardWriteTextResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueClipboardWriteTextResult {
    pub _payload_alignment: [HostGlueClipboardWriteTextResultPayloadAlignment; 0],
    pub payload: [u8; 16],
    pub tag: HostGlueClipboardWriteTextResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueClipboardWriteTextResult {
    pub payload: HostGlueClipboardWriteTextResultPayload,
    pub tag: HostGlueClipboardWriteTextResultTag,
}

impl HostGlueClipboardWriteTextResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueClipboardWriteTextResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AnonStruct4c605145331877e7 {
        unsafe { &*(self.payload.as_ptr() as *const AnonStruct4c605145331877e7) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueClipboardWriteTextResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AnonStruct4c605145331877e7 {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<AnonStruct4c605145331877e7> as *const AnonStruct4c605145331877e7) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueClipboardWriteTextResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AnonStruct4c605145331877e7 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AnonStruct4c605145331877e7) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueClipboardWriteTextResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AnonStruct4c605145331877e7 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueClipboardWriteTextResult>() == 40, "HostGlueClipboardWriteTextResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueClipboardWriteTextResult>() == 8, "HostGlueClipboardWriteTextResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(HostGlueClipboardWriteTextResult, tag) == 32, "HostGlueClipboardWriteTextResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueClipboardWriteTextResult>() == 20, "HostGlueClipboardWriteTextResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueClipboardWriteTextResult>() == 4, "HostGlueClipboardWriteTextResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(HostGlueClipboardWriteTextResult, tag) == 16, "HostGlueClipboardWriteTextResult tag offset mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum HostGlueAudioAcquireResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union HostGlueAudioAcquireResultPayload {
    pub err: core::mem::ManuallyDrop<AnonStruct4c605145331877e7>,
    pub ok: core::mem::ManuallyDrop<*mut u64>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct HostGlueAudioAcquireResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueAudioAcquireResult {
    pub _payload_alignment: [HostGlueAudioAcquireResultPayloadAlignment; 0],
    pub payload: [u8; 16],
    pub tag: HostGlueAudioAcquireResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueAudioAcquireResult {
    pub payload: HostGlueAudioAcquireResultPayload,
    pub tag: HostGlueAudioAcquireResultTag,
}

impl HostGlueAudioAcquireResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueAudioAcquireResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AnonStruct4c605145331877e7 {
        unsafe { &*(self.payload.as_ptr() as *const AnonStruct4c605145331877e7) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueAudioAcquireResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AnonStruct4c605145331877e7 {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<AnonStruct4c605145331877e7> as *const AnonStruct4c605145331877e7) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueAudioAcquireResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AnonStruct4c605145331877e7 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AnonStruct4c605145331877e7) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueAudioAcquireResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AnonStruct4c605145331877e7 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueAudioAcquireResultTag::Ok` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &*mut u64 {
        unsafe { &*(self.payload.as_ptr() as *const *mut u64) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueAudioAcquireResultTag::Ok` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &*mut u64 {
        unsafe { &*(&self.payload.ok as *const core::mem::ManuallyDrop<*mut u64> as *const *mut u64) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueAudioAcquireResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> *mut u64 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const *mut u64) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueAudioAcquireResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> *mut u64 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.ok) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueAudioAcquireResult>() == 40, "HostGlueAudioAcquireResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueAudioAcquireResult>() == 8, "HostGlueAudioAcquireResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(HostGlueAudioAcquireResult, tag) == 32, "HostGlueAudioAcquireResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueAudioAcquireResult>() == 20, "HostGlueAudioAcquireResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueAudioAcquireResult>() == 4, "HostGlueAudioAcquireResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(HostGlueAudioAcquireResult, tag) == 16, "HostGlueAudioAcquireResult tag offset mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum HostGlueAudioLoadResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union HostGlueAudioLoadResultPayload {
    pub err: core::mem::ManuallyDrop<AnonStruct4c605145331877e7>,
    pub ok: core::mem::ManuallyDrop<AnonStruct8e993eac1f1cd0c>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(8))]
#[derive(Clone, Copy)]
pub struct HostGlueAudioLoadResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueAudioLoadResult {
    pub _payload_alignment: [HostGlueAudioLoadResultPayloadAlignment; 0],
    pub payload: [u8; 16],
    pub tag: HostGlueAudioLoadResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueAudioLoadResult {
    pub payload: HostGlueAudioLoadResultPayload,
    pub tag: HostGlueAudioLoadResultTag,
}

impl HostGlueAudioLoadResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueAudioLoadResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AnonStruct4c605145331877e7 {
        unsafe { &*(self.payload.as_ptr() as *const AnonStruct4c605145331877e7) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueAudioLoadResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AnonStruct4c605145331877e7 {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<AnonStruct4c605145331877e7> as *const AnonStruct4c605145331877e7) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueAudioLoadResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AnonStruct4c605145331877e7 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AnonStruct4c605145331877e7) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueAudioLoadResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AnonStruct4c605145331877e7 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueAudioLoadResultTag::Ok` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &AnonStruct8e993eac1f1cd0c {
        unsafe { &*(self.payload.as_ptr() as *const AnonStruct8e993eac1f1cd0c) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueAudioLoadResultTag::Ok` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &AnonStruct8e993eac1f1cd0c {
        unsafe { &*(&self.payload.ok as *const core::mem::ManuallyDrop<AnonStruct8e993eac1f1cd0c> as *const AnonStruct8e993eac1f1cd0c) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueAudioLoadResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> AnonStruct8e993eac1f1cd0c {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AnonStruct8e993eac1f1cd0c) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueAudioLoadResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> AnonStruct8e993eac1f1cd0c {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.ok) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueAudioLoadResult>() == 40, "HostGlueAudioLoadResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueAudioLoadResult>() == 8, "HostGlueAudioLoadResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(HostGlueAudioLoadResult, tag) == 32, "HostGlueAudioLoadResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueAudioLoadResult>() == 24, "HostGlueAudioLoadResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueAudioLoadResult>() == 8, "HostGlueAudioLoadResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(HostGlueAudioLoadResult, tag) == 16, "HostGlueAudioLoadResult tag offset mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum HostGlueAudioStatusResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union HostGlueAudioStatusResultPayload {
    pub err: core::mem::ManuallyDrop<AnonStruct4c605145331877e7>,
    pub ok: core::mem::ManuallyDrop<AnonStruct18a809e7fe3ae87a>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(8))]
#[derive(Clone, Copy)]
pub struct HostGlueAudioStatusResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueAudioStatusResult {
    pub _payload_alignment: [HostGlueAudioStatusResultPayloadAlignment; 0],
    pub payload: [u8; 24],
    pub tag: HostGlueAudioStatusResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueAudioStatusResult {
    pub payload: HostGlueAudioStatusResultPayload,
    pub tag: HostGlueAudioStatusResultTag,
}

impl HostGlueAudioStatusResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueAudioStatusResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AnonStruct4c605145331877e7 {
        unsafe { &*(self.payload.as_ptr() as *const AnonStruct4c605145331877e7) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueAudioStatusResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AnonStruct4c605145331877e7 {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<AnonStruct4c605145331877e7> as *const AnonStruct4c605145331877e7) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueAudioStatusResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AnonStruct4c605145331877e7 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AnonStruct4c605145331877e7) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueAudioStatusResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AnonStruct4c605145331877e7 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueAudioStatusResultTag::Ok` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &AnonStruct18a809e7fe3ae87a {
        unsafe { &*(self.payload.as_ptr() as *const AnonStruct18a809e7fe3ae87a) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueAudioStatusResultTag::Ok` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &AnonStruct18a809e7fe3ae87a {
        unsafe { &*(&self.payload.ok as *const core::mem::ManuallyDrop<AnonStruct18a809e7fe3ae87a> as *const AnonStruct18a809e7fe3ae87a) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueAudioStatusResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> AnonStruct18a809e7fe3ae87a {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AnonStruct18a809e7fe3ae87a) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueAudioStatusResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> AnonStruct18a809e7fe3ae87a {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.ok) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueAudioStatusResult>() == 40, "HostGlueAudioStatusResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueAudioStatusResult>() == 8, "HostGlueAudioStatusResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(HostGlueAudioStatusResult, tag) == 32, "HostGlueAudioStatusResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueAudioStatusResult>() == 32, "HostGlueAudioStatusResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueAudioStatusResult>() == 8, "HostGlueAudioStatusResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(HostGlueAudioStatusResult, tag) == 24, "HostGlueAudioStatusResult tag offset mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum HostGlueTcpConnectResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union HostGlueTcpConnectResultPayload {
    pub err: core::mem::ManuallyDrop<u8>,
    pub ok: core::mem::ManuallyDrop<*mut u64>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct HostGlueTcpConnectResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueTcpConnectResult {
    pub _payload_alignment: [HostGlueTcpConnectResultPayloadAlignment; 0],
    pub payload: [u8; 4],
    pub tag: HostGlueTcpConnectResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueTcpConnectResult {
    pub payload: HostGlueTcpConnectResultPayload,
    pub tag: HostGlueTcpConnectResultTag,
}

impl HostGlueTcpConnectResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueTcpConnectResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &u8 {
        unsafe { &*(self.payload.as_ptr() as *const u8) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueTcpConnectResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &u8 {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<u8> as *const u8) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueTcpConnectResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> u8 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const u8) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueTcpConnectResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> u8 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueTcpConnectResultTag::Ok` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &*mut u64 {
        unsafe { &*(self.payload.as_ptr() as *const *mut u64) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueTcpConnectResultTag::Ok` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &*mut u64 {
        unsafe { &*(&self.payload.ok as *const core::mem::ManuallyDrop<*mut u64> as *const *mut u64) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueTcpConnectResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> *mut u64 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const *mut u64) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueTcpConnectResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> *mut u64 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.ok) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueTcpConnectResult>() == 16, "HostGlueTcpConnectResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueTcpConnectResult>() == 8, "HostGlueTcpConnectResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(HostGlueTcpConnectResult, tag) == 8, "HostGlueTcpConnectResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueTcpConnectResult>() == 8, "HostGlueTcpConnectResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueTcpConnectResult>() == 4, "HostGlueTcpConnectResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(HostGlueTcpConnectResult, tag) == 4, "HostGlueTcpConnectResult tag offset mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum HostGlueTcpReadUpToResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union HostGlueTcpReadUpToResultPayload {
    pub err: core::mem::ManuallyDrop<u8>,
    pub ok: core::mem::ManuallyDrop<RocListWith<u8, false>>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct HostGlueTcpReadUpToResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueTcpReadUpToResult {
    pub _payload_alignment: [HostGlueTcpReadUpToResultPayloadAlignment; 0],
    pub payload: [u8; 12],
    pub tag: HostGlueTcpReadUpToResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueTcpReadUpToResult {
    pub payload: HostGlueTcpReadUpToResultPayload,
    pub tag: HostGlueTcpReadUpToResultTag,
}

impl HostGlueTcpReadUpToResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueTcpReadUpToResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &u8 {
        unsafe { &*(self.payload.as_ptr() as *const u8) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueTcpReadUpToResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &u8 {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<u8> as *const u8) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueTcpReadUpToResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> u8 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const u8) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueTcpReadUpToResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> u8 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueTcpReadUpToResultTag::Ok` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &RocListWith<u8, false> {
        unsafe { &*(self.payload.as_ptr() as *const RocListWith<u8, false>) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueTcpReadUpToResultTag::Ok` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &RocListWith<u8, false> {
        unsafe { &*(&self.payload.ok as *const core::mem::ManuallyDrop<RocListWith<u8, false>> as *const RocListWith<u8, false>) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueTcpReadUpToResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> RocListWith<u8, false> {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const RocListWith<u8, false>) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueTcpReadUpToResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> RocListWith<u8, false> {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.ok) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueTcpReadUpToResult>() == 32, "HostGlueTcpReadUpToResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueTcpReadUpToResult>() == 8, "HostGlueTcpReadUpToResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(HostGlueTcpReadUpToResult, tag) == 24, "HostGlueTcpReadUpToResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueTcpReadUpToResult>() == 16, "HostGlueTcpReadUpToResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueTcpReadUpToResult>() == 4, "HostGlueTcpReadUpToResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(HostGlueTcpReadUpToResult, tag) == 12, "HostGlueTcpReadUpToResult tag offset mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum HostGlueTcpWriteAllResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union HostGlueTcpWriteAllResultPayload {
    pub err: core::mem::ManuallyDrop<u8>,
    pub ok: [u8; 0],
}

#[cfg(target_pointer_width = "32")]
#[repr(align(1))]
#[derive(Clone, Copy)]
pub struct HostGlueTcpWriteAllResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueTcpWriteAllResult {
    pub _payload_alignment: [HostGlueTcpWriteAllResultPayloadAlignment; 0],
    pub payload: [u8; 1],
    pub tag: HostGlueTcpWriteAllResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueTcpWriteAllResult {
    pub payload: HostGlueTcpWriteAllResultPayload,
    pub tag: HostGlueTcpWriteAllResultTag,
}

impl HostGlueTcpWriteAllResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueTcpWriteAllResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &u8 {
        unsafe { &*(self.payload.as_ptr() as *const u8) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueTcpWriteAllResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &u8 {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<u8> as *const u8) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueTcpWriteAllResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> u8 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const u8) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueTcpWriteAllResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> u8 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueTcpWriteAllResult>() == 2, "HostGlueTcpWriteAllResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueTcpWriteAllResult>() == 1, "HostGlueTcpWriteAllResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(HostGlueTcpWriteAllResult, tag) == 1, "HostGlueTcpWriteAllResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueTcpWriteAllResult>() == 2, "HostGlueTcpWriteAllResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueTcpWriteAllResult>() == 1, "HostGlueTcpWriteAllResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(HostGlueTcpWriteAllResult, tag) == 1, "HostGlueTcpWriteAllResult tag offset mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum HostGlueProcessAcquireResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union HostGlueProcessAcquireResultPayload {
    pub err: core::mem::ManuallyDrop<AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr>,
    pub ok: core::mem::ManuallyDrop<*mut u64>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct HostGlueProcessAcquireResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueProcessAcquireResult {
    pub _payload_alignment: [HostGlueProcessAcquireResultPayloadAlignment; 0],
    pub payload: [u8; 4],
    pub tag: HostGlueProcessAcquireResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueProcessAcquireResult {
    pub payload: HostGlueProcessAcquireResultPayload,
    pub tag: HostGlueProcessAcquireResultTag,
}

impl HostGlueProcessAcquireResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessAcquireResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr {
        unsafe { &*(self.payload.as_ptr() as *const AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessAcquireResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr> as *const AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessAcquireResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessAcquireResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessAcquireResultTag::Ok` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &*mut u64 {
        unsafe { &*(self.payload.as_ptr() as *const *mut u64) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessAcquireResultTag::Ok` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &*mut u64 {
        unsafe { &*(&self.payload.ok as *const core::mem::ManuallyDrop<*mut u64> as *const *mut u64) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessAcquireResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> *mut u64 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const *mut u64) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessAcquireResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> *mut u64 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.ok) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueProcessAcquireResult>() == 16, "HostGlueProcessAcquireResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueProcessAcquireResult>() == 8, "HostGlueProcessAcquireResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(HostGlueProcessAcquireResult, tag) == 8, "HostGlueProcessAcquireResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueProcessAcquireResult>() == 8, "HostGlueProcessAcquireResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueProcessAcquireResult>() == 4, "HostGlueProcessAcquireResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(HostGlueProcessAcquireResult, tag) == 4, "HostGlueProcessAcquireResult tag offset mismatch");

/// Tag discriminant for AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag {
    AcquireProcessErr = 0,
    CancelProcessErr = 1,
    ReadProcessErr = 2,
    ResizeProcessErr = 3,
    SpawnProcessErr = 4,
    WriteProcessErr = 5,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrPayload {
    pub acquire_process_err: core::mem::ManuallyDrop<AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported>,
    pub cancel_process_err: core::mem::ManuallyDrop<AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported>,
    pub read_process_err: core::mem::ManuallyDrop<AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported>,
    pub resize_process_err: core::mem::ManuallyDrop<AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported>,
    pub spawn_process_err: core::mem::ManuallyDrop<AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported>,
    pub write_process_err: core::mem::ManuallyDrop<AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(1))]
#[derive(Clone, Copy)]
pub struct AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrPayloadAlignment;

/// Tag union: AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr {
    pub _payload_alignment: [AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrPayloadAlignment; 0],
    pub payload: [u8; 1],
    pub tag: AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag,
}

/// Tag union: AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr {
    pub payload: AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrPayload,
    pub tag: AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag,
}

impl AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr {
    /// Borrow the `AcquireProcessErr` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag::AcquireProcessErr` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_acquire_process_err_unchecked(&self) -> &AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported {
        unsafe { &*(self.payload.as_ptr() as *const AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported) }
    }

    /// Borrow the `AcquireProcessErr` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag::AcquireProcessErr` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_acquire_process_err_unchecked(&self) -> &AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported {
        unsafe { &*(&self.payload.acquire_process_err as *const core::mem::ManuallyDrop<AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported> as *const AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported) }
    }

    /// Move the `AcquireProcessErr` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag::AcquireProcessErr`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_acquire_process_err_unchecked(&mut self) -> AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported) }
    }

    /// Move the `AcquireProcessErr` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag::AcquireProcessErr`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_acquire_process_err_unchecked(&mut self) -> AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.acquire_process_err) }
    }

    /// Borrow the `CancelProcessErr` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag::CancelProcessErr` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_cancel_process_err_unchecked(&self) -> &AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported {
        unsafe { &*(self.payload.as_ptr() as *const AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported) }
    }

    /// Borrow the `CancelProcessErr` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag::CancelProcessErr` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_cancel_process_err_unchecked(&self) -> &AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported {
        unsafe { &*(&self.payload.cancel_process_err as *const core::mem::ManuallyDrop<AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported> as *const AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported) }
    }

    /// Move the `CancelProcessErr` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag::CancelProcessErr`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_cancel_process_err_unchecked(&mut self) -> AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported) }
    }

    /// Move the `CancelProcessErr` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag::CancelProcessErr`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_cancel_process_err_unchecked(&mut self) -> AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.cancel_process_err) }
    }

    /// Borrow the `ReadProcessErr` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag::ReadProcessErr` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_read_process_err_unchecked(&self) -> &AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported {
        unsafe { &*(self.payload.as_ptr() as *const AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported) }
    }

    /// Borrow the `ReadProcessErr` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag::ReadProcessErr` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_read_process_err_unchecked(&self) -> &AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported {
        unsafe { &*(&self.payload.read_process_err as *const core::mem::ManuallyDrop<AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported> as *const AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported) }
    }

    /// Move the `ReadProcessErr` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag::ReadProcessErr`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_read_process_err_unchecked(&mut self) -> AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported) }
    }

    /// Move the `ReadProcessErr` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag::ReadProcessErr`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_read_process_err_unchecked(&mut self) -> AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.read_process_err) }
    }

    /// Borrow the `ResizeProcessErr` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag::ResizeProcessErr` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_resize_process_err_unchecked(&self) -> &AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported {
        unsafe { &*(self.payload.as_ptr() as *const AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported) }
    }

    /// Borrow the `ResizeProcessErr` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag::ResizeProcessErr` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_resize_process_err_unchecked(&self) -> &AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported {
        unsafe { &*(&self.payload.resize_process_err as *const core::mem::ManuallyDrop<AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported> as *const AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported) }
    }

    /// Move the `ResizeProcessErr` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag::ResizeProcessErr`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_resize_process_err_unchecked(&mut self) -> AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported) }
    }

    /// Move the `ResizeProcessErr` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag::ResizeProcessErr`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_resize_process_err_unchecked(&mut self) -> AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.resize_process_err) }
    }

    /// Borrow the `SpawnProcessErr` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag::SpawnProcessErr` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_spawn_process_err_unchecked(&self) -> &AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported {
        unsafe { &*(self.payload.as_ptr() as *const AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported) }
    }

    /// Borrow the `SpawnProcessErr` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag::SpawnProcessErr` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_spawn_process_err_unchecked(&self) -> &AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported {
        unsafe { &*(&self.payload.spawn_process_err as *const core::mem::ManuallyDrop<AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported> as *const AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported) }
    }

    /// Move the `SpawnProcessErr` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag::SpawnProcessErr`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_spawn_process_err_unchecked(&mut self) -> AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported) }
    }

    /// Move the `SpawnProcessErr` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag::SpawnProcessErr`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_spawn_process_err_unchecked(&mut self) -> AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.spawn_process_err) }
    }

    /// Borrow the `WriteProcessErr` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag::WriteProcessErr` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_write_process_err_unchecked(&self) -> &AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported {
        unsafe { &*(self.payload.as_ptr() as *const AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported) }
    }

    /// Borrow the `WriteProcessErr` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag::WriteProcessErr` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_write_process_err_unchecked(&self) -> &AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported {
        unsafe { &*(&self.payload.write_process_err as *const core::mem::ManuallyDrop<AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported> as *const AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported) }
    }

    /// Move the `WriteProcessErr` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag::WriteProcessErr`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_write_process_err_unchecked(&mut self) -> AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported) }
    }

    /// Move the `WriteProcessErr` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag::WriteProcessErr`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_write_process_err_unchecked(&mut self) -> AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.write_process_err) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr>() == 2, "AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr>() == 1, "AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr, tag) == 1, "AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr>() == 2, "AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr>() == 1, "AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr, tag) == 1, "AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr tag offset mismatch");

/// Tag union: AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported {
    AccessDenied = 0,
    Busy = 1,
    Exited = 2,
    InvalidCapability = 3,
    InvalidSize = 4,
    Io = 5,
    ResourceLimit = 6,
    Revoked = 7,
    Unsupported = 8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported>() == 1, "AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported>() == 1, "AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported>() == 1, "AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported>() == 1, "AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported alignment mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum HostGlueProcessSpawnResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union HostGlueProcessSpawnResultPayload {
    pub err: core::mem::ManuallyDrop<AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr>,
    pub ok: core::mem::ManuallyDrop<*mut u64>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct HostGlueProcessSpawnResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueProcessSpawnResult {
    pub _payload_alignment: [HostGlueProcessSpawnResultPayloadAlignment; 0],
    pub payload: [u8; 4],
    pub tag: HostGlueProcessSpawnResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueProcessSpawnResult {
    pub payload: HostGlueProcessSpawnResultPayload,
    pub tag: HostGlueProcessSpawnResultTag,
}

impl HostGlueProcessSpawnResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessSpawnResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr {
        unsafe { &*(self.payload.as_ptr() as *const AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessSpawnResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr> as *const AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessSpawnResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessSpawnResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessSpawnResultTag::Ok` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &*mut u64 {
        unsafe { &*(self.payload.as_ptr() as *const *mut u64) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessSpawnResultTag::Ok` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &*mut u64 {
        unsafe { &*(&self.payload.ok as *const core::mem::ManuallyDrop<*mut u64> as *const *mut u64) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessSpawnResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> *mut u64 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const *mut u64) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessSpawnResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> *mut u64 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.ok) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueProcessSpawnResult>() == 16, "HostGlueProcessSpawnResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueProcessSpawnResult>() == 8, "HostGlueProcessSpawnResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(HostGlueProcessSpawnResult, tag) == 8, "HostGlueProcessSpawnResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueProcessSpawnResult>() == 8, "HostGlueProcessSpawnResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueProcessSpawnResult>() == 4, "HostGlueProcessSpawnResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(HostGlueProcessSpawnResult, tag) == 4, "HostGlueProcessSpawnResult tag offset mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum HostGlueProcessReadResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union HostGlueProcessReadResultPayload {
    pub err: core::mem::ManuallyDrop<AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr>,
    pub ok: core::mem::ManuallyDrop<CanceledOrDataOrEndOfFile>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct HostGlueProcessReadResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueProcessReadResult {
    pub _payload_alignment: [HostGlueProcessReadResultPayloadAlignment; 0],
    pub payload: [u8; 16],
    pub tag: HostGlueProcessReadResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueProcessReadResult {
    pub payload: HostGlueProcessReadResultPayload,
    pub tag: HostGlueProcessReadResultTag,
}

impl HostGlueProcessReadResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessReadResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr {
        unsafe { &*(self.payload.as_ptr() as *const AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessReadResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr> as *const AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessReadResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessReadResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessReadResultTag::Ok` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &CanceledOrDataOrEndOfFile {
        unsafe { &*(self.payload.as_ptr() as *const CanceledOrDataOrEndOfFile) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessReadResultTag::Ok` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &CanceledOrDataOrEndOfFile {
        unsafe { &*(&self.payload.ok as *const core::mem::ManuallyDrop<CanceledOrDataOrEndOfFile> as *const CanceledOrDataOrEndOfFile) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessReadResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> CanceledOrDataOrEndOfFile {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const CanceledOrDataOrEndOfFile) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessReadResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> CanceledOrDataOrEndOfFile {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.ok) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueProcessReadResult>() == 40, "HostGlueProcessReadResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueProcessReadResult>() == 8, "HostGlueProcessReadResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(HostGlueProcessReadResult, tag) == 32, "HostGlueProcessReadResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueProcessReadResult>() == 20, "HostGlueProcessReadResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueProcessReadResult>() == 4, "HostGlueProcessReadResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(HostGlueProcessReadResult, tag) == 16, "HostGlueProcessReadResult tag offset mismatch");

/// Tag discriminant for CanceledOrDataOrEndOfFile.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum CanceledOrDataOrEndOfFileTag {
    Canceled = 0,
    Data = 1,
    EndOfFile = 2,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union CanceledOrDataOrEndOfFilePayload {
    pub canceled: [u8; 0],
    pub data: core::mem::ManuallyDrop<RocListWith<u8, false>>,
    pub end_of_file: [u8; 0],
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct CanceledOrDataOrEndOfFilePayloadAlignment;

/// Tag union: CanceledOrDataOrEndOfFile
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct CanceledOrDataOrEndOfFile {
    pub _payload_alignment: [CanceledOrDataOrEndOfFilePayloadAlignment; 0],
    pub payload: [u8; 12],
    pub tag: CanceledOrDataOrEndOfFileTag,
}

/// Tag union: CanceledOrDataOrEndOfFile
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct CanceledOrDataOrEndOfFile {
    pub payload: CanceledOrDataOrEndOfFilePayload,
    pub tag: CanceledOrDataOrEndOfFileTag,
}

impl CanceledOrDataOrEndOfFile {
    /// Borrow the `Data` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `CanceledOrDataOrEndOfFileTag::Data` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_data_unchecked(&self) -> &RocListWith<u8, false> {
        unsafe { &*(self.payload.as_ptr() as *const RocListWith<u8, false>) }
    }

    /// Borrow the `Data` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `CanceledOrDataOrEndOfFileTag::Data` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_data_unchecked(&self) -> &RocListWith<u8, false> {
        unsafe { &*(&self.payload.data as *const core::mem::ManuallyDrop<RocListWith<u8, false>> as *const RocListWith<u8, false>) }
    }

    /// Move the `Data` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `CanceledOrDataOrEndOfFileTag::Data`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_data_unchecked(&mut self) -> RocListWith<u8, false> {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const RocListWith<u8, false>) }
    }

    /// Move the `Data` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `CanceledOrDataOrEndOfFileTag::Data`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_data_unchecked(&mut self) -> RocListWith<u8, false> {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.data) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<CanceledOrDataOrEndOfFile>() == 32, "CanceledOrDataOrEndOfFile size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<CanceledOrDataOrEndOfFile>() == 8, "CanceledOrDataOrEndOfFile alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(CanceledOrDataOrEndOfFile, tag) == 24, "CanceledOrDataOrEndOfFile tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<CanceledOrDataOrEndOfFile>() == 16, "CanceledOrDataOrEndOfFile size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<CanceledOrDataOrEndOfFile>() == 4, "CanceledOrDataOrEndOfFile alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(CanceledOrDataOrEndOfFile, tag) == 12, "CanceledOrDataOrEndOfFile tag offset mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum HostGlueProcessWriteResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union HostGlueProcessWriteResultPayload {
    pub err: core::mem::ManuallyDrop<AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr>,
    pub ok: core::mem::ManuallyDrop<u32>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct HostGlueProcessWriteResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueProcessWriteResult {
    pub _payload_alignment: [HostGlueProcessWriteResultPayloadAlignment; 0],
    pub payload: [u8; 4],
    pub tag: HostGlueProcessWriteResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueProcessWriteResult {
    pub payload: HostGlueProcessWriteResultPayload,
    pub tag: HostGlueProcessWriteResultTag,
}

impl HostGlueProcessWriteResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessWriteResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr {
        unsafe { &*(self.payload.as_ptr() as *const AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessWriteResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr> as *const AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessWriteResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessWriteResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessWriteResultTag::Ok` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &u32 {
        unsafe { &*(self.payload.as_ptr() as *const u32) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessWriteResultTag::Ok` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &u32 {
        unsafe { &*(&self.payload.ok as *const core::mem::ManuallyDrop<u32> as *const u32) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessWriteResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> u32 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const u32) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessWriteResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> u32 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.ok) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueProcessWriteResult>() == 8, "HostGlueProcessWriteResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueProcessWriteResult>() == 4, "HostGlueProcessWriteResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(HostGlueProcessWriteResult, tag) == 4, "HostGlueProcessWriteResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueProcessWriteResult>() == 8, "HostGlueProcessWriteResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueProcessWriteResult>() == 4, "HostGlueProcessWriteResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(HostGlueProcessWriteResult, tag) == 4, "HostGlueProcessWriteResult tag offset mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum HostGlueProcessResizeResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union HostGlueProcessResizeResultPayload {
    pub err: core::mem::ManuallyDrop<AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr>,
    pub ok: [u8; 0],
}

#[cfg(target_pointer_width = "32")]
#[repr(align(1))]
#[derive(Clone, Copy)]
pub struct HostGlueProcessResizeResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueProcessResizeResult {
    pub _payload_alignment: [HostGlueProcessResizeResultPayloadAlignment; 0],
    pub payload: [u8; 2],
    pub tag: HostGlueProcessResizeResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueProcessResizeResult {
    pub payload: HostGlueProcessResizeResultPayload,
    pub tag: HostGlueProcessResizeResultTag,
}

impl HostGlueProcessResizeResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessResizeResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr {
        unsafe { &*(self.payload.as_ptr() as *const AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessResizeResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr> as *const AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessResizeResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessResizeResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueProcessResizeResult>() == 3, "HostGlueProcessResizeResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueProcessResizeResult>() == 1, "HostGlueProcessResizeResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(HostGlueProcessResizeResult, tag) == 2, "HostGlueProcessResizeResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueProcessResizeResult>() == 3, "HostGlueProcessResizeResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueProcessResizeResult>() == 1, "HostGlueProcessResizeResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(HostGlueProcessResizeResult, tag) == 2, "HostGlueProcessResizeResult tag offset mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum HostGlueProcessCancelResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union HostGlueProcessCancelResultPayload {
    pub err: core::mem::ManuallyDrop<AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr>,
    pub ok: core::mem::ManuallyDrop<AlreadyStoppedOrCanceled>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(1))]
#[derive(Clone, Copy)]
pub struct HostGlueProcessCancelResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueProcessCancelResult {
    pub _payload_alignment: [HostGlueProcessCancelResultPayloadAlignment; 0],
    pub payload: [u8; 2],
    pub tag: HostGlueProcessCancelResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueProcessCancelResult {
    pub payload: HostGlueProcessCancelResultPayload,
    pub tag: HostGlueProcessCancelResultTag,
}

impl HostGlueProcessCancelResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessCancelResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr {
        unsafe { &*(self.payload.as_ptr() as *const AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessCancelResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr> as *const AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessCancelResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessCancelResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessCancelResultTag::Ok` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &AlreadyStoppedOrCanceled {
        unsafe { &*(self.payload.as_ptr() as *const AlreadyStoppedOrCanceled) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessCancelResultTag::Ok` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &AlreadyStoppedOrCanceled {
        unsafe { &*(&self.payload.ok as *const core::mem::ManuallyDrop<AlreadyStoppedOrCanceled> as *const AlreadyStoppedOrCanceled) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessCancelResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> AlreadyStoppedOrCanceled {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AlreadyStoppedOrCanceled) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueProcessCancelResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> AlreadyStoppedOrCanceled {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.ok) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueProcessCancelResult>() == 3, "HostGlueProcessCancelResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueProcessCancelResult>() == 1, "HostGlueProcessCancelResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(HostGlueProcessCancelResult, tag) == 2, "HostGlueProcessCancelResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueProcessCancelResult>() == 3, "HostGlueProcessCancelResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueProcessCancelResult>() == 1, "HostGlueProcessCancelResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(HostGlueProcessCancelResult, tag) == 2, "HostGlueProcessCancelResult tag offset mismatch");

/// Tag union: AlreadyStoppedOrCanceled
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum AlreadyStoppedOrCanceled {
    AlreadyStopped = 0,
    Canceled = 1,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AlreadyStoppedOrCanceled>() == 1, "AlreadyStoppedOrCanceled size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AlreadyStoppedOrCanceled>() == 1, "AlreadyStoppedOrCanceled alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AlreadyStoppedOrCanceled>() == 1, "AlreadyStoppedOrCanceled size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AlreadyStoppedOrCanceled>() == 1, "AlreadyStoppedOrCanceled alignment mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum HostGlueDeviceAcquireResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union HostGlueDeviceAcquireResultPayload {
    pub err: core::mem::ManuallyDrop<AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr>,
    pub ok: core::mem::ManuallyDrop<*mut u64>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct HostGlueDeviceAcquireResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueDeviceAcquireResult {
    pub _payload_alignment: [HostGlueDeviceAcquireResultPayloadAlignment; 0],
    pub payload: [u8; 4],
    pub tag: HostGlueDeviceAcquireResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueDeviceAcquireResult {
    pub payload: HostGlueDeviceAcquireResultPayload,
    pub tag: HostGlueDeviceAcquireResultTag,
}

impl HostGlueDeviceAcquireResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueDeviceAcquireResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr {
        unsafe { &*(self.payload.as_ptr() as *const AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueDeviceAcquireResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr> as *const AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueDeviceAcquireResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueDeviceAcquireResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueDeviceAcquireResultTag::Ok` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &*mut u64 {
        unsafe { &*(self.payload.as_ptr() as *const *mut u64) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueDeviceAcquireResultTag::Ok` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &*mut u64 {
        unsafe { &*(&self.payload.ok as *const core::mem::ManuallyDrop<*mut u64> as *const *mut u64) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueDeviceAcquireResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> *mut u64 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const *mut u64) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueDeviceAcquireResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> *mut u64 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.ok) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueDeviceAcquireResult>() == 16, "HostGlueDeviceAcquireResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueDeviceAcquireResult>() == 8, "HostGlueDeviceAcquireResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(HostGlueDeviceAcquireResult, tag) == 8, "HostGlueDeviceAcquireResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueDeviceAcquireResult>() == 8, "HostGlueDeviceAcquireResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueDeviceAcquireResult>() == 4, "HostGlueDeviceAcquireResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(HostGlueDeviceAcquireResult, tag) == 4, "HostGlueDeviceAcquireResult tag offset mismatch");

/// Tag discriminant for AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrTag {
    AcquireDeviceErr = 0,
    CloseDeviceErr = 1,
    ConnectDeviceErr = 2,
    DiscoverDeviceErr = 3,
    TransactDeviceErr = 4,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrPayload {
    pub acquire_device_err: core::mem::ManuallyDrop<AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported>,
    pub close_device_err: core::mem::ManuallyDrop<AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported>,
    pub connect_device_err: core::mem::ManuallyDrop<AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported>,
    pub discover_device_err: core::mem::ManuallyDrop<AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported>,
    pub transact_device_err: core::mem::ManuallyDrop<AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(1))]
#[derive(Clone, Copy)]
pub struct AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrPayloadAlignment;

/// Tag union: AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr {
    pub _payload_alignment: [AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrPayloadAlignment; 0],
    pub payload: [u8; 1],
    pub tag: AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrTag,
}

/// Tag union: AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr {
    pub payload: AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrPayload,
    pub tag: AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrTag,
}

impl AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr {
    /// Borrow the `AcquireDeviceErr` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrTag::AcquireDeviceErr` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_acquire_device_err_unchecked(&self) -> &AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported {
        unsafe { &*(self.payload.as_ptr() as *const AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported) }
    }

    /// Borrow the `AcquireDeviceErr` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrTag::AcquireDeviceErr` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_acquire_device_err_unchecked(&self) -> &AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported {
        unsafe { &*(&self.payload.acquire_device_err as *const core::mem::ManuallyDrop<AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported> as *const AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported) }
    }

    /// Move the `AcquireDeviceErr` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrTag::AcquireDeviceErr`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_acquire_device_err_unchecked(&mut self) -> AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported) }
    }

    /// Move the `AcquireDeviceErr` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrTag::AcquireDeviceErr`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_acquire_device_err_unchecked(&mut self) -> AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.acquire_device_err) }
    }

    /// Borrow the `CloseDeviceErr` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrTag::CloseDeviceErr` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_close_device_err_unchecked(&self) -> &AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported {
        unsafe { &*(self.payload.as_ptr() as *const AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported) }
    }

    /// Borrow the `CloseDeviceErr` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrTag::CloseDeviceErr` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_close_device_err_unchecked(&self) -> &AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported {
        unsafe { &*(&self.payload.close_device_err as *const core::mem::ManuallyDrop<AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported> as *const AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported) }
    }

    /// Move the `CloseDeviceErr` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrTag::CloseDeviceErr`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_close_device_err_unchecked(&mut self) -> AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported) }
    }

    /// Move the `CloseDeviceErr` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrTag::CloseDeviceErr`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_close_device_err_unchecked(&mut self) -> AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.close_device_err) }
    }

    /// Borrow the `ConnectDeviceErr` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrTag::ConnectDeviceErr` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_connect_device_err_unchecked(&self) -> &AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported {
        unsafe { &*(self.payload.as_ptr() as *const AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported) }
    }

    /// Borrow the `ConnectDeviceErr` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrTag::ConnectDeviceErr` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_connect_device_err_unchecked(&self) -> &AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported {
        unsafe { &*(&self.payload.connect_device_err as *const core::mem::ManuallyDrop<AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported> as *const AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported) }
    }

    /// Move the `ConnectDeviceErr` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrTag::ConnectDeviceErr`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_connect_device_err_unchecked(&mut self) -> AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported) }
    }

    /// Move the `ConnectDeviceErr` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrTag::ConnectDeviceErr`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_connect_device_err_unchecked(&mut self) -> AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.connect_device_err) }
    }

    /// Borrow the `DiscoverDeviceErr` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrTag::DiscoverDeviceErr` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_discover_device_err_unchecked(&self) -> &AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported {
        unsafe { &*(self.payload.as_ptr() as *const AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported) }
    }

    /// Borrow the `DiscoverDeviceErr` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrTag::DiscoverDeviceErr` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_discover_device_err_unchecked(&self) -> &AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported {
        unsafe { &*(&self.payload.discover_device_err as *const core::mem::ManuallyDrop<AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported> as *const AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported) }
    }

    /// Move the `DiscoverDeviceErr` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrTag::DiscoverDeviceErr`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_discover_device_err_unchecked(&mut self) -> AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported) }
    }

    /// Move the `DiscoverDeviceErr` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrTag::DiscoverDeviceErr`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_discover_device_err_unchecked(&mut self) -> AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.discover_device_err) }
    }

    /// Borrow the `TransactDeviceErr` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrTag::TransactDeviceErr` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_transact_device_err_unchecked(&self) -> &AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported {
        unsafe { &*(self.payload.as_ptr() as *const AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported) }
    }

    /// Borrow the `TransactDeviceErr` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrTag::TransactDeviceErr` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_transact_device_err_unchecked(&self) -> &AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported {
        unsafe { &*(&self.payload.transact_device_err as *const core::mem::ManuallyDrop<AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported> as *const AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported) }
    }

    /// Move the `TransactDeviceErr` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrTag::TransactDeviceErr`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_transact_device_err_unchecked(&mut self) -> AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported) }
    }

    /// Move the `TransactDeviceErr` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrTag::TransactDeviceErr`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_transact_device_err_unchecked(&mut self) -> AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.transact_device_err) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr>() == 2, "AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr>() == 1, "AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr, tag) == 1, "AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr>() == 2, "AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr>() == 1, "AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr, tag) == 1, "AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr tag offset mismatch");

/// Tag union: AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported {
    AccessDenied = 0,
    Busy = 1,
    Closed = 2,
    Disconnected = 3,
    InvalidCapability = 4,
    InvalidRequest = 5,
    Io = 6,
    NotFound = 7,
    Protocol = 8,
    ResourceLimit = 9,
    Revoked = 10,
    Timeout = 11,
    Unsupported = 12,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported>() == 1, "AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported>() == 1, "AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported>() == 1, "AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported>() == 1, "AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported alignment mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum HostGlueDeviceDiscoverResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union HostGlueDeviceDiscoverResultPayload {
    pub err: core::mem::ManuallyDrop<AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr>,
    pub ok: core::mem::ManuallyDrop<RocList<AnonStruct27556b2f7cb4f65f>>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct HostGlueDeviceDiscoverResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueDeviceDiscoverResult {
    pub _payload_alignment: [HostGlueDeviceDiscoverResultPayloadAlignment; 0],
    pub payload: [u8; 12],
    pub tag: HostGlueDeviceDiscoverResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueDeviceDiscoverResult {
    pub payload: HostGlueDeviceDiscoverResultPayload,
    pub tag: HostGlueDeviceDiscoverResultTag,
}

impl HostGlueDeviceDiscoverResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueDeviceDiscoverResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr {
        unsafe { &*(self.payload.as_ptr() as *const AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueDeviceDiscoverResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr> as *const AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueDeviceDiscoverResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueDeviceDiscoverResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueDeviceDiscoverResultTag::Ok` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &RocList<AnonStruct27556b2f7cb4f65f> {
        unsafe { &*(self.payload.as_ptr() as *const RocList<AnonStruct27556b2f7cb4f65f>) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueDeviceDiscoverResultTag::Ok` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &RocList<AnonStruct27556b2f7cb4f65f> {
        unsafe { &*(&self.payload.ok as *const core::mem::ManuallyDrop<RocList<AnonStruct27556b2f7cb4f65f>> as *const RocList<AnonStruct27556b2f7cb4f65f>) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueDeviceDiscoverResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> RocList<AnonStruct27556b2f7cb4f65f> {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const RocList<AnonStruct27556b2f7cb4f65f>) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueDeviceDiscoverResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> RocList<AnonStruct27556b2f7cb4f65f> {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.ok) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueDeviceDiscoverResult>() == 32, "HostGlueDeviceDiscoverResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueDeviceDiscoverResult>() == 8, "HostGlueDeviceDiscoverResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(HostGlueDeviceDiscoverResult, tag) == 24, "HostGlueDeviceDiscoverResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueDeviceDiscoverResult>() == 16, "HostGlueDeviceDiscoverResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueDeviceDiscoverResult>() == 4, "HostGlueDeviceDiscoverResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(HostGlueDeviceDiscoverResult, tag) == 12, "HostGlueDeviceDiscoverResult tag offset mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum HostGlueDeviceConnectResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union HostGlueDeviceConnectResultPayload {
    pub err: core::mem::ManuallyDrop<AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr>,
    pub ok: core::mem::ManuallyDrop<*mut u64>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct HostGlueDeviceConnectResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueDeviceConnectResult {
    pub _payload_alignment: [HostGlueDeviceConnectResultPayloadAlignment; 0],
    pub payload: [u8; 4],
    pub tag: HostGlueDeviceConnectResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueDeviceConnectResult {
    pub payload: HostGlueDeviceConnectResultPayload,
    pub tag: HostGlueDeviceConnectResultTag,
}

impl HostGlueDeviceConnectResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueDeviceConnectResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr {
        unsafe { &*(self.payload.as_ptr() as *const AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueDeviceConnectResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr> as *const AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueDeviceConnectResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueDeviceConnectResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueDeviceConnectResultTag::Ok` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &*mut u64 {
        unsafe { &*(self.payload.as_ptr() as *const *mut u64) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueDeviceConnectResultTag::Ok` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &*mut u64 {
        unsafe { &*(&self.payload.ok as *const core::mem::ManuallyDrop<*mut u64> as *const *mut u64) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueDeviceConnectResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> *mut u64 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const *mut u64) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueDeviceConnectResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> *mut u64 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.ok) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueDeviceConnectResult>() == 16, "HostGlueDeviceConnectResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueDeviceConnectResult>() == 8, "HostGlueDeviceConnectResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(HostGlueDeviceConnectResult, tag) == 8, "HostGlueDeviceConnectResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueDeviceConnectResult>() == 8, "HostGlueDeviceConnectResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueDeviceConnectResult>() == 4, "HostGlueDeviceConnectResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(HostGlueDeviceConnectResult, tag) == 4, "HostGlueDeviceConnectResult tag offset mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum HostGlueDeviceTransactResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union HostGlueDeviceTransactResultPayload {
    pub err: core::mem::ManuallyDrop<AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr>,
    pub ok: core::mem::ManuallyDrop<RocListWith<u8, false>>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct HostGlueDeviceTransactResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueDeviceTransactResult {
    pub _payload_alignment: [HostGlueDeviceTransactResultPayloadAlignment; 0],
    pub payload: [u8; 12],
    pub tag: HostGlueDeviceTransactResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueDeviceTransactResult {
    pub payload: HostGlueDeviceTransactResultPayload,
    pub tag: HostGlueDeviceTransactResultTag,
}

impl HostGlueDeviceTransactResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueDeviceTransactResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr {
        unsafe { &*(self.payload.as_ptr() as *const AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueDeviceTransactResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr> as *const AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueDeviceTransactResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueDeviceTransactResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueDeviceTransactResultTag::Ok` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &RocListWith<u8, false> {
        unsafe { &*(self.payload.as_ptr() as *const RocListWith<u8, false>) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueDeviceTransactResultTag::Ok` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &RocListWith<u8, false> {
        unsafe { &*(&self.payload.ok as *const core::mem::ManuallyDrop<RocListWith<u8, false>> as *const RocListWith<u8, false>) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueDeviceTransactResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> RocListWith<u8, false> {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const RocListWith<u8, false>) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueDeviceTransactResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> RocListWith<u8, false> {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.ok) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueDeviceTransactResult>() == 32, "HostGlueDeviceTransactResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueDeviceTransactResult>() == 8, "HostGlueDeviceTransactResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(HostGlueDeviceTransactResult, tag) == 24, "HostGlueDeviceTransactResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueDeviceTransactResult>() == 16, "HostGlueDeviceTransactResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueDeviceTransactResult>() == 4, "HostGlueDeviceTransactResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(HostGlueDeviceTransactResult, tag) == 12, "HostGlueDeviceTransactResult tag offset mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum HostGlueDeviceCloseResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union HostGlueDeviceCloseResultPayload {
    pub err: core::mem::ManuallyDrop<AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr>,
    pub ok: [u8; 0],
}

#[cfg(target_pointer_width = "32")]
#[repr(align(1))]
#[derive(Clone, Copy)]
pub struct HostGlueDeviceCloseResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueDeviceCloseResult {
    pub _payload_alignment: [HostGlueDeviceCloseResultPayloadAlignment; 0],
    pub payload: [u8; 2],
    pub tag: HostGlueDeviceCloseResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueDeviceCloseResult {
    pub payload: HostGlueDeviceCloseResultPayload,
    pub tag: HostGlueDeviceCloseResultTag,
}

impl HostGlueDeviceCloseResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueDeviceCloseResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr {
        unsafe { &*(self.payload.as_ptr() as *const AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueDeviceCloseResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr> as *const AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueDeviceCloseResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueDeviceCloseResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueDeviceCloseResult>() == 3, "HostGlueDeviceCloseResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueDeviceCloseResult>() == 1, "HostGlueDeviceCloseResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(HostGlueDeviceCloseResult, tag) == 2, "HostGlueDeviceCloseResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueDeviceCloseResult>() == 3, "HostGlueDeviceCloseResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueDeviceCloseResult>() == 1, "HostGlueDeviceCloseResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(HostGlueDeviceCloseResult, tag) == 2, "HostGlueDeviceCloseResult tag offset mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum HostGlueSystemAcquireResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union HostGlueSystemAcquireResultPayload {
    pub err: core::mem::ManuallyDrop<u8>,
    pub ok: core::mem::ManuallyDrop<*mut u64>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct HostGlueSystemAcquireResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueSystemAcquireResult {
    pub _payload_alignment: [HostGlueSystemAcquireResultPayloadAlignment; 0],
    pub payload: [u8; 4],
    pub tag: HostGlueSystemAcquireResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueSystemAcquireResult {
    pub payload: HostGlueSystemAcquireResultPayload,
    pub tag: HostGlueSystemAcquireResultTag,
}

impl HostGlueSystemAcquireResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueSystemAcquireResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &u8 {
        unsafe { &*(self.payload.as_ptr() as *const u8) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueSystemAcquireResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &u8 {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<u8> as *const u8) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueSystemAcquireResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> u8 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const u8) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueSystemAcquireResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> u8 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueSystemAcquireResultTag::Ok` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &*mut u64 {
        unsafe { &*(self.payload.as_ptr() as *const *mut u64) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueSystemAcquireResultTag::Ok` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &*mut u64 {
        unsafe { &*(&self.payload.ok as *const core::mem::ManuallyDrop<*mut u64> as *const *mut u64) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueSystemAcquireResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> *mut u64 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const *mut u64) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueSystemAcquireResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> *mut u64 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.ok) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueSystemAcquireResult>() == 16, "HostGlueSystemAcquireResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueSystemAcquireResult>() == 8, "HostGlueSystemAcquireResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(HostGlueSystemAcquireResult, tag) == 8, "HostGlueSystemAcquireResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueSystemAcquireResult>() == 8, "HostGlueSystemAcquireResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueSystemAcquireResult>() == 4, "HostGlueSystemAcquireResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(HostGlueSystemAcquireResult, tag) == 4, "HostGlueSystemAcquireResult tag offset mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum HostGlueSystemSampleResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union HostGlueSystemSampleResultPayload {
    pub err: core::mem::ManuallyDrop<u8>,
    pub ok: core::mem::ManuallyDrop<AnonStruct98efc21233c140ff>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(8))]
#[derive(Clone, Copy)]
pub struct HostGlueSystemSampleResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueSystemSampleResult {
    pub _payload_alignment: [HostGlueSystemSampleResultPayloadAlignment; 0],
    pub payload: [u8; 80],
    pub tag: HostGlueSystemSampleResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueSystemSampleResult {
    pub payload: HostGlueSystemSampleResultPayload,
    pub tag: HostGlueSystemSampleResultTag,
}

impl HostGlueSystemSampleResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueSystemSampleResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &u8 {
        unsafe { &*(self.payload.as_ptr() as *const u8) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueSystemSampleResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &u8 {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<u8> as *const u8) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueSystemSampleResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> u8 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const u8) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueSystemSampleResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> u8 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueSystemSampleResultTag::Ok` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &AnonStruct98efc21233c140ff {
        unsafe { &*(self.payload.as_ptr() as *const AnonStruct98efc21233c140ff) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueSystemSampleResultTag::Ok` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &AnonStruct98efc21233c140ff {
        unsafe { &*(&self.payload.ok as *const core::mem::ManuallyDrop<AnonStruct98efc21233c140ff> as *const AnonStruct98efc21233c140ff) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueSystemSampleResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> AnonStruct98efc21233c140ff {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AnonStruct98efc21233c140ff) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueSystemSampleResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> AnonStruct98efc21233c140ff {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.ok) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueSystemSampleResult>() == 96, "HostGlueSystemSampleResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueSystemSampleResult>() == 8, "HostGlueSystemSampleResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(HostGlueSystemSampleResult, tag) == 88, "HostGlueSystemSampleResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueSystemSampleResult>() == 88, "HostGlueSystemSampleResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueSystemSampleResult>() == 8, "HostGlueSystemSampleResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(HostGlueSystemSampleResult, tag) == 80, "HostGlueSystemSampleResult tag offset mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum HostGlueImageInspectResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union HostGlueImageInspectResultPayload {
    pub err: core::mem::ManuallyDrop<u8>,
    pub ok: core::mem::ManuallyDrop<AnonStructB3b29ac2cb34a461>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct HostGlueImageInspectResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueImageInspectResult {
    pub _payload_alignment: [HostGlueImageInspectResultPayloadAlignment; 0],
    pub payload: [u8; 8],
    pub tag: HostGlueImageInspectResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueImageInspectResult {
    pub payload: HostGlueImageInspectResultPayload,
    pub tag: HostGlueImageInspectResultTag,
}

impl HostGlueImageInspectResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueImageInspectResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &u8 {
        unsafe { &*(self.payload.as_ptr() as *const u8) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueImageInspectResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &u8 {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<u8> as *const u8) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueImageInspectResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> u8 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const u8) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueImageInspectResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> u8 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueImageInspectResultTag::Ok` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &AnonStructB3b29ac2cb34a461 {
        unsafe { &*(self.payload.as_ptr() as *const AnonStructB3b29ac2cb34a461) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueImageInspectResultTag::Ok` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &AnonStructB3b29ac2cb34a461 {
        unsafe { &*(&self.payload.ok as *const core::mem::ManuallyDrop<AnonStructB3b29ac2cb34a461> as *const AnonStructB3b29ac2cb34a461) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueImageInspectResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> AnonStructB3b29ac2cb34a461 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AnonStructB3b29ac2cb34a461) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueImageInspectResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> AnonStructB3b29ac2cb34a461 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.ok) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueImageInspectResult>() == 12, "HostGlueImageInspectResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueImageInspectResult>() == 4, "HostGlueImageInspectResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(HostGlueImageInspectResult, tag) == 8, "HostGlueImageInspectResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueImageInspectResult>() == 12, "HostGlueImageInspectResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueImageInspectResult>() == 4, "HostGlueImageInspectResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(HostGlueImageInspectResult, tag) == 8, "HostGlueImageInspectResult tag offset mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum HostGlueAssetsOpenResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union HostGlueAssetsOpenResultPayload {
    pub err: core::mem::ManuallyDrop<u8>,
    pub ok: core::mem::ManuallyDrop<*mut u64>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct HostGlueAssetsOpenResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueAssetsOpenResult {
    pub _payload_alignment: [HostGlueAssetsOpenResultPayloadAlignment; 0],
    pub payload: [u8; 4],
    pub tag: HostGlueAssetsOpenResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueAssetsOpenResult {
    pub payload: HostGlueAssetsOpenResultPayload,
    pub tag: HostGlueAssetsOpenResultTag,
}

impl HostGlueAssetsOpenResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueAssetsOpenResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &u8 {
        unsafe { &*(self.payload.as_ptr() as *const u8) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueAssetsOpenResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &u8 {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<u8> as *const u8) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueAssetsOpenResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> u8 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const u8) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueAssetsOpenResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> u8 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueAssetsOpenResultTag::Ok` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &*mut u64 {
        unsafe { &*(self.payload.as_ptr() as *const *mut u64) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueAssetsOpenResultTag::Ok` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &*mut u64 {
        unsafe { &*(&self.payload.ok as *const core::mem::ManuallyDrop<*mut u64> as *const *mut u64) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueAssetsOpenResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> *mut u64 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const *mut u64) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueAssetsOpenResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> *mut u64 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.ok) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueAssetsOpenResult>() == 16, "HostGlueAssetsOpenResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueAssetsOpenResult>() == 8, "HostGlueAssetsOpenResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(HostGlueAssetsOpenResult, tag) == 8, "HostGlueAssetsOpenResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueAssetsOpenResult>() == 8, "HostGlueAssetsOpenResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueAssetsOpenResult>() == 4, "HostGlueAssetsOpenResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(HostGlueAssetsOpenResult, tag) == 4, "HostGlueAssetsOpenResult tag offset mismatch");

/// Tag discriminant for MountOrNoChangeOrReplace.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum MountOrNoChangeOrReplaceTag {
    Mount = 0,
    NoChange = 1,
    Replace = 2,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union MountOrNoChangeOrReplacePayload {
    pub mount: core::mem::ManuallyDrop<AnonStruct8ae3ad5a5c0e22dc>,
    pub no_change: [u8; 0],
    pub replace: core::mem::ManuallyDrop<AnonStruct8d3c9b3ac95ef074>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(8))]
#[derive(Clone, Copy)]
pub struct MountOrNoChangeOrReplacePayloadAlignment;

/// Tag union: MountOrNoChangeOrReplace
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct MountOrNoChangeOrReplace {
    pub _payload_alignment: [MountOrNoChangeOrReplacePayloadAlignment; 0],
    pub payload: [u8; 16],
    pub tag: MountOrNoChangeOrReplaceTag,
}

/// Tag union: MountOrNoChangeOrReplace
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct MountOrNoChangeOrReplace {
    pub payload: MountOrNoChangeOrReplacePayload,
    pub tag: MountOrNoChangeOrReplaceTag,
}

impl MountOrNoChangeOrReplace {
    /// Borrow the `Mount` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `MountOrNoChangeOrReplaceTag::Mount` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_mount_unchecked(&self) -> &AnonStruct8ae3ad5a5c0e22dc {
        unsafe { &*(self.payload.as_ptr() as *const AnonStruct8ae3ad5a5c0e22dc) }
    }

    /// Borrow the `Mount` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `MountOrNoChangeOrReplaceTag::Mount` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_mount_unchecked(&self) -> &AnonStruct8ae3ad5a5c0e22dc {
        unsafe { &*(&self.payload.mount as *const core::mem::ManuallyDrop<AnonStruct8ae3ad5a5c0e22dc> as *const AnonStruct8ae3ad5a5c0e22dc) }
    }

    /// Move the `Mount` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `MountOrNoChangeOrReplaceTag::Mount`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_mount_unchecked(&mut self) -> AnonStruct8ae3ad5a5c0e22dc {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AnonStruct8ae3ad5a5c0e22dc) }
    }

    /// Move the `Mount` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `MountOrNoChangeOrReplaceTag::Mount`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_mount_unchecked(&mut self) -> AnonStruct8ae3ad5a5c0e22dc {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.mount) }
    }

    /// Borrow the `Replace` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `MountOrNoChangeOrReplaceTag::Replace` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_replace_unchecked(&self) -> &AnonStruct8d3c9b3ac95ef074 {
        unsafe { &*(self.payload.as_ptr() as *const AnonStruct8d3c9b3ac95ef074) }
    }

    /// Borrow the `Replace` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `MountOrNoChangeOrReplaceTag::Replace` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_replace_unchecked(&self) -> &AnonStruct8d3c9b3ac95ef074 {
        unsafe { &*(&self.payload.replace as *const core::mem::ManuallyDrop<AnonStruct8d3c9b3ac95ef074> as *const AnonStruct8d3c9b3ac95ef074) }
    }

    /// Move the `Replace` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `MountOrNoChangeOrReplaceTag::Replace`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_replace_unchecked(&mut self) -> AnonStruct8d3c9b3ac95ef074 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AnonStruct8d3c9b3ac95ef074) }
    }

    /// Move the `Replace` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `MountOrNoChangeOrReplaceTag::Replace`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_replace_unchecked(&mut self) -> AnonStruct8d3c9b3ac95ef074 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.replace) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<MountOrNoChangeOrReplace>() == 24, "MountOrNoChangeOrReplace size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<MountOrNoChangeOrReplace>() == 8, "MountOrNoChangeOrReplace alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(MountOrNoChangeOrReplace, tag) == 16, "MountOrNoChangeOrReplace tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<MountOrNoChangeOrReplace>() == 24, "MountOrNoChangeOrReplace size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<MountOrNoChangeOrReplace>() == 8, "MountOrNoChangeOrReplace alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(MountOrNoChangeOrReplace, tag) == 16, "MountOrNoChangeOrReplace tag offset mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum HostGlueHttpAcquireResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union HostGlueHttpAcquireResultPayload {
    pub err: core::mem::ManuallyDrop<AccessDeniedOrBodyTooLargeOrConnectFailedOrInvalidCapabilityOrInvalidHeaderOrInvalidRequestOrInvalidUrlOrRedirectLimitOrRevokedOrTimeoutOrUnsupportedScheme>,
    pub ok: core::mem::ManuallyDrop<*mut u64>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct HostGlueHttpAcquireResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueHttpAcquireResult {
    pub _payload_alignment: [HostGlueHttpAcquireResultPayloadAlignment; 0],
    pub payload: [u8; 4],
    pub tag: HostGlueHttpAcquireResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueHttpAcquireResult {
    pub payload: HostGlueHttpAcquireResultPayload,
    pub tag: HostGlueHttpAcquireResultTag,
}

impl HostGlueHttpAcquireResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueHttpAcquireResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AccessDeniedOrBodyTooLargeOrConnectFailedOrInvalidCapabilityOrInvalidHeaderOrInvalidRequestOrInvalidUrlOrRedirectLimitOrRevokedOrTimeoutOrUnsupportedScheme {
        unsafe { &*(self.payload.as_ptr() as *const AccessDeniedOrBodyTooLargeOrConnectFailedOrInvalidCapabilityOrInvalidHeaderOrInvalidRequestOrInvalidUrlOrRedirectLimitOrRevokedOrTimeoutOrUnsupportedScheme) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueHttpAcquireResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AccessDeniedOrBodyTooLargeOrConnectFailedOrInvalidCapabilityOrInvalidHeaderOrInvalidRequestOrInvalidUrlOrRedirectLimitOrRevokedOrTimeoutOrUnsupportedScheme {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<AccessDeniedOrBodyTooLargeOrConnectFailedOrInvalidCapabilityOrInvalidHeaderOrInvalidRequestOrInvalidUrlOrRedirectLimitOrRevokedOrTimeoutOrUnsupportedScheme> as *const AccessDeniedOrBodyTooLargeOrConnectFailedOrInvalidCapabilityOrInvalidHeaderOrInvalidRequestOrInvalidUrlOrRedirectLimitOrRevokedOrTimeoutOrUnsupportedScheme) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueHttpAcquireResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AccessDeniedOrBodyTooLargeOrConnectFailedOrInvalidCapabilityOrInvalidHeaderOrInvalidRequestOrInvalidUrlOrRedirectLimitOrRevokedOrTimeoutOrUnsupportedScheme {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AccessDeniedOrBodyTooLargeOrConnectFailedOrInvalidCapabilityOrInvalidHeaderOrInvalidRequestOrInvalidUrlOrRedirectLimitOrRevokedOrTimeoutOrUnsupportedScheme) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueHttpAcquireResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AccessDeniedOrBodyTooLargeOrConnectFailedOrInvalidCapabilityOrInvalidHeaderOrInvalidRequestOrInvalidUrlOrRedirectLimitOrRevokedOrTimeoutOrUnsupportedScheme {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueHttpAcquireResultTag::Ok` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &*mut u64 {
        unsafe { &*(self.payload.as_ptr() as *const *mut u64) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueHttpAcquireResultTag::Ok` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &*mut u64 {
        unsafe { &*(&self.payload.ok as *const core::mem::ManuallyDrop<*mut u64> as *const *mut u64) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueHttpAcquireResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> *mut u64 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const *mut u64) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueHttpAcquireResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> *mut u64 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.ok) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueHttpAcquireResult>() == 16, "HostGlueHttpAcquireResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueHttpAcquireResult>() == 8, "HostGlueHttpAcquireResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(HostGlueHttpAcquireResult, tag) == 8, "HostGlueHttpAcquireResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueHttpAcquireResult>() == 8, "HostGlueHttpAcquireResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueHttpAcquireResult>() == 4, "HostGlueHttpAcquireResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(HostGlueHttpAcquireResult, tag) == 4, "HostGlueHttpAcquireResult tag offset mismatch");

/// Tag union: AccessDeniedOrBodyTooLargeOrConnectFailedOrInvalidCapabilityOrInvalidHeaderOrInvalidRequestOrInvalidUrlOrRedirectLimitOrRevokedOrTimeoutOrUnsupportedScheme
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum AccessDeniedOrBodyTooLargeOrConnectFailedOrInvalidCapabilityOrInvalidHeaderOrInvalidRequestOrInvalidUrlOrRedirectLimitOrRevokedOrTimeoutOrUnsupportedScheme {
    AccessDenied = 0,
    BodyTooLarge = 1,
    ConnectFailed = 2,
    InvalidCapability = 3,
    InvalidHeader = 4,
    InvalidRequest = 5,
    InvalidUrl = 6,
    RedirectLimit = 7,
    Revoked = 8,
    Timeout = 9,
    UnsupportedScheme = 10,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AccessDeniedOrBodyTooLargeOrConnectFailedOrInvalidCapabilityOrInvalidHeaderOrInvalidRequestOrInvalidUrlOrRedirectLimitOrRevokedOrTimeoutOrUnsupportedScheme>() == 1, "AccessDeniedOrBodyTooLargeOrConnectFailedOrInvalidCapabilityOrInvalidHeaderOrInvalidRequestOrInvalidUrlOrRedirectLimitOrRevokedOrTimeoutOrUnsupportedScheme size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AccessDeniedOrBodyTooLargeOrConnectFailedOrInvalidCapabilityOrInvalidHeaderOrInvalidRequestOrInvalidUrlOrRedirectLimitOrRevokedOrTimeoutOrUnsupportedScheme>() == 1, "AccessDeniedOrBodyTooLargeOrConnectFailedOrInvalidCapabilityOrInvalidHeaderOrInvalidRequestOrInvalidUrlOrRedirectLimitOrRevokedOrTimeoutOrUnsupportedScheme alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AccessDeniedOrBodyTooLargeOrConnectFailedOrInvalidCapabilityOrInvalidHeaderOrInvalidRequestOrInvalidUrlOrRedirectLimitOrRevokedOrTimeoutOrUnsupportedScheme>() == 1, "AccessDeniedOrBodyTooLargeOrConnectFailedOrInvalidCapabilityOrInvalidHeaderOrInvalidRequestOrInvalidUrlOrRedirectLimitOrRevokedOrTimeoutOrUnsupportedScheme size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AccessDeniedOrBodyTooLargeOrConnectFailedOrInvalidCapabilityOrInvalidHeaderOrInvalidRequestOrInvalidUrlOrRedirectLimitOrRevokedOrTimeoutOrUnsupportedScheme>() == 1, "AccessDeniedOrBodyTooLargeOrConnectFailedOrInvalidCapabilityOrInvalidHeaderOrInvalidRequestOrInvalidUrlOrRedirectLimitOrRevokedOrTimeoutOrUnsupportedScheme alignment mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum HostGlueHttpSendResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union HostGlueHttpSendResultPayload {
    pub err: core::mem::ManuallyDrop<AccessDeniedOrBodyTooLargeOrConnectFailedOrInvalidCapabilityOrInvalidHeaderOrInvalidRequestOrInvalidUrlOrRedirectLimitOrRevokedOrTimeoutOrUnsupportedScheme>,
    pub ok: core::mem::ManuallyDrop<AnonStructBe6bcbc15f8a1360>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct HostGlueHttpSendResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueHttpSendResult {
    pub _payload_alignment: [HostGlueHttpSendResultPayloadAlignment; 0],
    pub payload: [u8; 28],
    pub tag: HostGlueHttpSendResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueHttpSendResult {
    pub payload: HostGlueHttpSendResultPayload,
    pub tag: HostGlueHttpSendResultTag,
}

impl HostGlueHttpSendResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueHttpSendResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AccessDeniedOrBodyTooLargeOrConnectFailedOrInvalidCapabilityOrInvalidHeaderOrInvalidRequestOrInvalidUrlOrRedirectLimitOrRevokedOrTimeoutOrUnsupportedScheme {
        unsafe { &*(self.payload.as_ptr() as *const AccessDeniedOrBodyTooLargeOrConnectFailedOrInvalidCapabilityOrInvalidHeaderOrInvalidRequestOrInvalidUrlOrRedirectLimitOrRevokedOrTimeoutOrUnsupportedScheme) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueHttpSendResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AccessDeniedOrBodyTooLargeOrConnectFailedOrInvalidCapabilityOrInvalidHeaderOrInvalidRequestOrInvalidUrlOrRedirectLimitOrRevokedOrTimeoutOrUnsupportedScheme {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<AccessDeniedOrBodyTooLargeOrConnectFailedOrInvalidCapabilityOrInvalidHeaderOrInvalidRequestOrInvalidUrlOrRedirectLimitOrRevokedOrTimeoutOrUnsupportedScheme> as *const AccessDeniedOrBodyTooLargeOrConnectFailedOrInvalidCapabilityOrInvalidHeaderOrInvalidRequestOrInvalidUrlOrRedirectLimitOrRevokedOrTimeoutOrUnsupportedScheme) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueHttpSendResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AccessDeniedOrBodyTooLargeOrConnectFailedOrInvalidCapabilityOrInvalidHeaderOrInvalidRequestOrInvalidUrlOrRedirectLimitOrRevokedOrTimeoutOrUnsupportedScheme {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AccessDeniedOrBodyTooLargeOrConnectFailedOrInvalidCapabilityOrInvalidHeaderOrInvalidRequestOrInvalidUrlOrRedirectLimitOrRevokedOrTimeoutOrUnsupportedScheme) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueHttpSendResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AccessDeniedOrBodyTooLargeOrConnectFailedOrInvalidCapabilityOrInvalidHeaderOrInvalidRequestOrInvalidUrlOrRedirectLimitOrRevokedOrTimeoutOrUnsupportedScheme {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueHttpSendResultTag::Ok` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &AnonStructBe6bcbc15f8a1360 {
        unsafe { &*(self.payload.as_ptr() as *const AnonStructBe6bcbc15f8a1360) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueHttpSendResultTag::Ok` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &AnonStructBe6bcbc15f8a1360 {
        unsafe { &*(&self.payload.ok as *const core::mem::ManuallyDrop<AnonStructBe6bcbc15f8a1360> as *const AnonStructBe6bcbc15f8a1360) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueHttpSendResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> AnonStructBe6bcbc15f8a1360 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AnonStructBe6bcbc15f8a1360) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostGlueHttpSendResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> AnonStructBe6bcbc15f8a1360 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.ok) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueHttpSendResult>() == 64, "HostGlueHttpSendResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueHttpSendResult>() == 8, "HostGlueHttpSendResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(HostGlueHttpSendResult, tag) == 56, "HostGlueHttpSendResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueHttpSendResult>() == 32, "HostGlueHttpSendResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueHttpSendResult>() == 4, "HostGlueHttpSendResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(HostGlueHttpSendResult, tag) == 28, "HostGlueHttpSendResult tag offset mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum InternalFilesPickDirectoryResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union InternalFilesPickDirectoryResultPayload {
    pub err: core::mem::ManuallyDrop<ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr>,
    pub ok: core::mem::ManuallyDrop<CanceledOrChosenType240>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct InternalFilesPickDirectoryResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct InternalFilesPickDirectoryResult {
    pub _payload_alignment: [InternalFilesPickDirectoryResultPayloadAlignment; 0],
    pub payload: [u8; 20],
    pub tag: InternalFilesPickDirectoryResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct InternalFilesPickDirectoryResult {
    pub payload: InternalFilesPickDirectoryResultPayload,
    pub tag: InternalFilesPickDirectoryResultTag,
}

impl InternalFilesPickDirectoryResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesPickDirectoryResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr {
        unsafe { &*(self.payload.as_ptr() as *const ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesPickDirectoryResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr> as *const ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesPickDirectoryResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesPickDirectoryResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesPickDirectoryResultTag::Ok` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &CanceledOrChosenType240 {
        unsafe { &*(self.payload.as_ptr() as *const CanceledOrChosenType240) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesPickDirectoryResultTag::Ok` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &CanceledOrChosenType240 {
        unsafe { &*(&self.payload.ok as *const core::mem::ManuallyDrop<CanceledOrChosenType240> as *const CanceledOrChosenType240) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesPickDirectoryResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> CanceledOrChosenType240 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const CanceledOrChosenType240) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesPickDirectoryResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> CanceledOrChosenType240 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.ok) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<InternalFilesPickDirectoryResult>() == 48, "InternalFilesPickDirectoryResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<InternalFilesPickDirectoryResult>() == 8, "InternalFilesPickDirectoryResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(InternalFilesPickDirectoryResult, tag) == 40, "InternalFilesPickDirectoryResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<InternalFilesPickDirectoryResult>() == 24, "InternalFilesPickDirectoryResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<InternalFilesPickDirectoryResult>() == 4, "InternalFilesPickDirectoryResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(InternalFilesPickDirectoryResult, tag) == 20, "InternalFilesPickDirectoryResult tag offset mismatch");

/// Tag discriminant for ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag {
    ListDirectoryErr = 0,
    OpenAppDataErr = 1,
    OpenReadDirectoryErr = 2,
    PickDirectoryErr = 3,
    PickFileErr = 4,
    ReadFileErr = 5,
    WatchDirectoryErr = 6,
    WriteFileErr = 7,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrPayload {
    pub list_directory_err: core::mem::ManuallyDrop<AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported>,
    pub open_app_data_err: core::mem::ManuallyDrop<AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported>,
    pub open_read_directory_err: core::mem::ManuallyDrop<AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported>,
    pub pick_directory_err: core::mem::ManuallyDrop<AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported>,
    pub pick_file_err: core::mem::ManuallyDrop<AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported>,
    pub read_file_err: core::mem::ManuallyDrop<AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported>,
    pub watch_directory_err: core::mem::ManuallyDrop<AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported>,
    pub write_file_err: core::mem::ManuallyDrop<AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(1))]
#[derive(Clone, Copy)]
pub struct ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrPayloadAlignment;

/// Tag union: ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr {
    pub _payload_alignment: [ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrPayloadAlignment; 0],
    pub payload: [u8; 1],
    pub tag: ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag,
}

/// Tag union: ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr {
    pub payload: ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrPayload,
    pub tag: ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag,
}

impl ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr {
    /// Borrow the `ListDirectoryErr` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::ListDirectoryErr` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_list_directory_err_unchecked(&self) -> &AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported {
        unsafe { &*(self.payload.as_ptr() as *const AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported) }
    }

    /// Borrow the `ListDirectoryErr` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::ListDirectoryErr` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_list_directory_err_unchecked(&self) -> &AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported {
        unsafe { &*(&self.payload.list_directory_err as *const core::mem::ManuallyDrop<AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported> as *const AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported) }
    }

    /// Move the `ListDirectoryErr` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::ListDirectoryErr`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_list_directory_err_unchecked(&mut self) -> AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported) }
    }

    /// Move the `ListDirectoryErr` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::ListDirectoryErr`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_list_directory_err_unchecked(&mut self) -> AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.list_directory_err) }
    }

    /// Borrow the `OpenAppDataErr` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::OpenAppDataErr` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_open_app_data_err_unchecked(&self) -> &AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported {
        unsafe { &*(self.payload.as_ptr() as *const AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported) }
    }

    /// Borrow the `OpenAppDataErr` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::OpenAppDataErr` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_open_app_data_err_unchecked(&self) -> &AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported {
        unsafe { &*(&self.payload.open_app_data_err as *const core::mem::ManuallyDrop<AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported> as *const AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported) }
    }

    /// Move the `OpenAppDataErr` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::OpenAppDataErr`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_open_app_data_err_unchecked(&mut self) -> AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported) }
    }

    /// Move the `OpenAppDataErr` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::OpenAppDataErr`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_open_app_data_err_unchecked(&mut self) -> AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.open_app_data_err) }
    }

    /// Borrow the `OpenReadDirectoryErr` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::OpenReadDirectoryErr` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_open_read_directory_err_unchecked(&self) -> &AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported {
        unsafe { &*(self.payload.as_ptr() as *const AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported) }
    }

    /// Borrow the `OpenReadDirectoryErr` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::OpenReadDirectoryErr` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_open_read_directory_err_unchecked(&self) -> &AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported {
        unsafe { &*(&self.payload.open_read_directory_err as *const core::mem::ManuallyDrop<AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported> as *const AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported) }
    }

    /// Move the `OpenReadDirectoryErr` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::OpenReadDirectoryErr`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_open_read_directory_err_unchecked(&mut self) -> AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported) }
    }

    /// Move the `OpenReadDirectoryErr` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::OpenReadDirectoryErr`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_open_read_directory_err_unchecked(&mut self) -> AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.open_read_directory_err) }
    }

    /// Borrow the `PickDirectoryErr` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::PickDirectoryErr` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_pick_directory_err_unchecked(&self) -> &AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported {
        unsafe { &*(self.payload.as_ptr() as *const AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported) }
    }

    /// Borrow the `PickDirectoryErr` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::PickDirectoryErr` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_pick_directory_err_unchecked(&self) -> &AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported {
        unsafe { &*(&self.payload.pick_directory_err as *const core::mem::ManuallyDrop<AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported> as *const AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported) }
    }

    /// Move the `PickDirectoryErr` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::PickDirectoryErr`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_pick_directory_err_unchecked(&mut self) -> AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported) }
    }

    /// Move the `PickDirectoryErr` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::PickDirectoryErr`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_pick_directory_err_unchecked(&mut self) -> AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.pick_directory_err) }
    }

    /// Borrow the `PickFileErr` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::PickFileErr` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_pick_file_err_unchecked(&self) -> &AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported {
        unsafe { &*(self.payload.as_ptr() as *const AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported) }
    }

    /// Borrow the `PickFileErr` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::PickFileErr` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_pick_file_err_unchecked(&self) -> &AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported {
        unsafe { &*(&self.payload.pick_file_err as *const core::mem::ManuallyDrop<AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported> as *const AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported) }
    }

    /// Move the `PickFileErr` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::PickFileErr`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_pick_file_err_unchecked(&mut self) -> AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported) }
    }

    /// Move the `PickFileErr` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::PickFileErr`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_pick_file_err_unchecked(&mut self) -> AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.pick_file_err) }
    }

    /// Borrow the `ReadFileErr` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::ReadFileErr` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_read_file_err_unchecked(&self) -> &AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported {
        unsafe { &*(self.payload.as_ptr() as *const AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported) }
    }

    /// Borrow the `ReadFileErr` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::ReadFileErr` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_read_file_err_unchecked(&self) -> &AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported {
        unsafe { &*(&self.payload.read_file_err as *const core::mem::ManuallyDrop<AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported> as *const AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported) }
    }

    /// Move the `ReadFileErr` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::ReadFileErr`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_read_file_err_unchecked(&mut self) -> AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported) }
    }

    /// Move the `ReadFileErr` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::ReadFileErr`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_read_file_err_unchecked(&mut self) -> AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.read_file_err) }
    }

    /// Borrow the `WatchDirectoryErr` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::WatchDirectoryErr` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_watch_directory_err_unchecked(&self) -> &AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported {
        unsafe { &*(self.payload.as_ptr() as *const AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported) }
    }

    /// Borrow the `WatchDirectoryErr` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::WatchDirectoryErr` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_watch_directory_err_unchecked(&self) -> &AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported {
        unsafe { &*(&self.payload.watch_directory_err as *const core::mem::ManuallyDrop<AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported> as *const AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported) }
    }

    /// Move the `WatchDirectoryErr` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::WatchDirectoryErr`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_watch_directory_err_unchecked(&mut self) -> AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported) }
    }

    /// Move the `WatchDirectoryErr` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::WatchDirectoryErr`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_watch_directory_err_unchecked(&mut self) -> AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.watch_directory_err) }
    }

    /// Borrow the `WriteFileErr` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::WriteFileErr` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_write_file_err_unchecked(&self) -> &AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported {
        unsafe { &*(self.payload.as_ptr() as *const AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported) }
    }

    /// Borrow the `WriteFileErr` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::WriteFileErr` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_write_file_err_unchecked(&self) -> &AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported {
        unsafe { &*(&self.payload.write_file_err as *const core::mem::ManuallyDrop<AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported> as *const AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported) }
    }

    /// Move the `WriteFileErr` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::WriteFileErr`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_write_file_err_unchecked(&mut self) -> AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported) }
    }

    /// Move the `WriteFileErr` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::WriteFileErr`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_write_file_err_unchecked(&mut self) -> AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.write_file_err) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr>() == 2, "ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr>() == 1, "ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr, tag) == 1, "ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr>() == 2, "ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr>() == 1, "ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr, tag) == 1, "ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr tag offset mismatch");

/// Tag union: AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported {
    AccessDenied = 0,
    InvalidCapability = 1,
    InvalidName = 2,
    InvalidUtf8 = 3,
    Io = 4,
    NotDirectory = 5,
    NotFound = 6,
    ResourceLimit = 7,
    Revoked = 8,
    Unavailable = 9,
    Unsupported = 10,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported>() == 1, "AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported>() == 1, "AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported>() == 1, "AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported>() == 1, "AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported alignment mismatch");

/// Tag discriminant for CanceledOrChosen.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum CanceledOrChosenType240Tag {
    Canceled = 0,
    Chosen = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union CanceledOrChosenType240Payload {
    pub canceled: [u8; 0],
    pub chosen: core::mem::ManuallyDrop<AnonStruct4869dafad3498788>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct CanceledOrChosenType240PayloadAlignment;

/// Tag union: CanceledOrChosen
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct CanceledOrChosenType240 {
    pub _payload_alignment: [CanceledOrChosenType240PayloadAlignment; 0],
    pub payload: [u8; 16],
    pub tag: CanceledOrChosenType240Tag,
}

/// Tag union: CanceledOrChosen
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct CanceledOrChosenType240 {
    pub payload: CanceledOrChosenType240Payload,
    pub tag: CanceledOrChosenType240Tag,
}

impl CanceledOrChosenType240 {
    /// Borrow the `Chosen` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `CanceledOrChosenType240Tag::Chosen` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_chosen_unchecked(&self) -> &AnonStruct4869dafad3498788 {
        unsafe { &*(self.payload.as_ptr() as *const AnonStruct4869dafad3498788) }
    }

    /// Borrow the `Chosen` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `CanceledOrChosenType240Tag::Chosen` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_chosen_unchecked(&self) -> &AnonStruct4869dafad3498788 {
        unsafe { &*(&self.payload.chosen as *const core::mem::ManuallyDrop<AnonStruct4869dafad3498788> as *const AnonStruct4869dafad3498788) }
    }

    /// Move the `Chosen` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `CanceledOrChosenType240Tag::Chosen`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_chosen_unchecked(&mut self) -> AnonStruct4869dafad3498788 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AnonStruct4869dafad3498788) }
    }

    /// Move the `Chosen` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `CanceledOrChosenType240Tag::Chosen`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_chosen_unchecked(&mut self) -> AnonStruct4869dafad3498788 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.chosen) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<CanceledOrChosenType240>() == 40, "CanceledOrChosenType240 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<CanceledOrChosenType240>() == 8, "CanceledOrChosenType240 alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(CanceledOrChosenType240, tag) == 32, "CanceledOrChosenType240 tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<CanceledOrChosenType240>() == 20, "CanceledOrChosenType240 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<CanceledOrChosenType240>() == 4, "CanceledOrChosenType240 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(CanceledOrChosenType240, tag) == 16, "CanceledOrChosenType240 tag offset mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum InternalFilesDirListResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union InternalFilesDirListResultPayload {
    pub err: core::mem::ManuallyDrop<ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr>,
    pub ok: core::mem::ManuallyDrop<RocList<AnonStruct770b9d9b3d3d255>>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct InternalFilesDirListResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct InternalFilesDirListResult {
    pub _payload_alignment: [InternalFilesDirListResultPayloadAlignment; 0],
    pub payload: [u8; 12],
    pub tag: InternalFilesDirListResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct InternalFilesDirListResult {
    pub payload: InternalFilesDirListResultPayload,
    pub tag: InternalFilesDirListResultTag,
}

impl InternalFilesDirListResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesDirListResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr {
        unsafe { &*(self.payload.as_ptr() as *const ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesDirListResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr> as *const ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesDirListResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesDirListResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesDirListResultTag::Ok` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &RocList<AnonStruct770b9d9b3d3d255> {
        unsafe { &*(self.payload.as_ptr() as *const RocList<AnonStruct770b9d9b3d3d255>) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesDirListResultTag::Ok` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &RocList<AnonStruct770b9d9b3d3d255> {
        unsafe { &*(&self.payload.ok as *const core::mem::ManuallyDrop<RocList<AnonStruct770b9d9b3d3d255>> as *const RocList<AnonStruct770b9d9b3d3d255>) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesDirListResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> RocList<AnonStruct770b9d9b3d3d255> {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const RocList<AnonStruct770b9d9b3d3d255>) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesDirListResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> RocList<AnonStruct770b9d9b3d3d255> {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.ok) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<InternalFilesDirListResult>() == 32, "InternalFilesDirListResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<InternalFilesDirListResult>() == 8, "InternalFilesDirListResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(InternalFilesDirListResult, tag) == 24, "InternalFilesDirListResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<InternalFilesDirListResult>() == 16, "InternalFilesDirListResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<InternalFilesDirListResult>() == 4, "InternalFilesDirListResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(InternalFilesDirListResult, tag) == 12, "InternalFilesDirListResult tag offset mismatch");

/// Tag discriminant for NoneOrSome.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum NoneOrSomeTag {
    None = 0,
    Some = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union NoneOrSomePayload {
    pub none: [u8; 0],
    pub some: core::mem::ManuallyDrop<u64>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(8))]
#[derive(Clone, Copy)]
pub struct NoneOrSomePayloadAlignment;

/// Tag union: NoneOrSome
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct NoneOrSome {
    pub _payload_alignment: [NoneOrSomePayloadAlignment; 0],
    pub payload: [u8; 8],
    pub tag: NoneOrSomeTag,
}

/// Tag union: NoneOrSome
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct NoneOrSome {
    pub payload: NoneOrSomePayload,
    pub tag: NoneOrSomeTag,
}

impl NoneOrSome {
    /// Borrow the `Some` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeTag::Some` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_some_unchecked(&self) -> &u64 {
        unsafe { &*(self.payload.as_ptr() as *const u64) }
    }

    /// Borrow the `Some` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeTag::Some` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_some_unchecked(&self) -> &u64 {
        unsafe { &*(&self.payload.some as *const core::mem::ManuallyDrop<u64> as *const u64) }
    }

    /// Move the `Some` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeTag::Some`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_some_unchecked(&mut self) -> u64 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const u64) }
    }

    /// Move the `Some` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeTag::Some`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_some_unchecked(&mut self) -> u64 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.some) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<NoneOrSome>() == 16, "NoneOrSome size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<NoneOrSome>() == 8, "NoneOrSome alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(NoneOrSome, tag) == 8, "NoneOrSome tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<NoneOrSome>() == 16, "NoneOrSome size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<NoneOrSome>() == 8, "NoneOrSome alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(NoneOrSome, tag) == 8, "NoneOrSome tag offset mismatch");

/// Tag union: DirectoryOrFileOrOtherOrSymbolicLink
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum DirectoryOrFileOrOtherOrSymbolicLink {
    Directory = 0,
    File = 1,
    Other = 2,
    SymbolicLink = 3,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<DirectoryOrFileOrOtherOrSymbolicLink>() == 1, "DirectoryOrFileOrOtherOrSymbolicLink size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<DirectoryOrFileOrOtherOrSymbolicLink>() == 1, "DirectoryOrFileOrOtherOrSymbolicLink alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<DirectoryOrFileOrOtherOrSymbolicLink>() == 1, "DirectoryOrFileOrOtherOrSymbolicLink size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<DirectoryOrFileOrOtherOrSymbolicLink>() == 1, "DirectoryOrFileOrOtherOrSymbolicLink alignment mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum InternalFilesDirOpenReadResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union InternalFilesDirOpenReadResultPayload {
    pub err: core::mem::ManuallyDrop<ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr>,
    pub ok: core::mem::ManuallyDrop<*mut u64>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct InternalFilesDirOpenReadResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct InternalFilesDirOpenReadResult {
    pub _payload_alignment: [InternalFilesDirOpenReadResultPayloadAlignment; 0],
    pub payload: [u8; 4],
    pub tag: InternalFilesDirOpenReadResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct InternalFilesDirOpenReadResult {
    pub payload: InternalFilesDirOpenReadResultPayload,
    pub tag: InternalFilesDirOpenReadResultTag,
}

impl InternalFilesDirOpenReadResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesDirOpenReadResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr {
        unsafe { &*(self.payload.as_ptr() as *const ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesDirOpenReadResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr> as *const ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesDirOpenReadResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesDirOpenReadResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesDirOpenReadResultTag::Ok` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &*mut u64 {
        unsafe { &*(self.payload.as_ptr() as *const *mut u64) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesDirOpenReadResultTag::Ok` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &*mut u64 {
        unsafe { &*(&self.payload.ok as *const core::mem::ManuallyDrop<*mut u64> as *const *mut u64) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesDirOpenReadResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> *mut u64 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const *mut u64) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesDirOpenReadResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> *mut u64 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.ok) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<InternalFilesDirOpenReadResult>() == 16, "InternalFilesDirOpenReadResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<InternalFilesDirOpenReadResult>() == 8, "InternalFilesDirOpenReadResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(InternalFilesDirOpenReadResult, tag) == 8, "InternalFilesDirOpenReadResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<InternalFilesDirOpenReadResult>() == 8, "InternalFilesDirOpenReadResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<InternalFilesDirOpenReadResult>() == 4, "InternalFilesDirOpenReadResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(InternalFilesDirOpenReadResult, tag) == 4, "InternalFilesDirOpenReadResult tag offset mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum InternalFilesDirReadResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union InternalFilesDirReadResultPayload {
    pub err: core::mem::ManuallyDrop<ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr>,
    pub ok: core::mem::ManuallyDrop<RocListWith<u8, false>>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct InternalFilesDirReadResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct InternalFilesDirReadResult {
    pub _payload_alignment: [InternalFilesDirReadResultPayloadAlignment; 0],
    pub payload: [u8; 12],
    pub tag: InternalFilesDirReadResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct InternalFilesDirReadResult {
    pub payload: InternalFilesDirReadResultPayload,
    pub tag: InternalFilesDirReadResultTag,
}

impl InternalFilesDirReadResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesDirReadResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr {
        unsafe { &*(self.payload.as_ptr() as *const ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesDirReadResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr> as *const ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesDirReadResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesDirReadResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesDirReadResultTag::Ok` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &RocListWith<u8, false> {
        unsafe { &*(self.payload.as_ptr() as *const RocListWith<u8, false>) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesDirReadResultTag::Ok` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &RocListWith<u8, false> {
        unsafe { &*(&self.payload.ok as *const core::mem::ManuallyDrop<RocListWith<u8, false>> as *const RocListWith<u8, false>) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesDirReadResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> RocListWith<u8, false> {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const RocListWith<u8, false>) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesDirReadResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> RocListWith<u8, false> {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.ok) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<InternalFilesDirReadResult>() == 32, "InternalFilesDirReadResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<InternalFilesDirReadResult>() == 8, "InternalFilesDirReadResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(InternalFilesDirReadResult, tag) == 24, "InternalFilesDirReadResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<InternalFilesDirReadResult>() == 16, "InternalFilesDirReadResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<InternalFilesDirReadResult>() == 4, "InternalFilesDirReadResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(InternalFilesDirReadResult, tag) == 12, "InternalFilesDirReadResult tag offset mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum InternalFilesDirSha256ResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union InternalFilesDirSha256ResultPayload {
    pub err: core::mem::ManuallyDrop<ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr>,
    pub ok: core::mem::ManuallyDrop<RocStr>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct InternalFilesDirSha256ResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct InternalFilesDirSha256Result {
    pub _payload_alignment: [InternalFilesDirSha256ResultPayloadAlignment; 0],
    pub payload: [u8; 12],
    pub tag: InternalFilesDirSha256ResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct InternalFilesDirSha256Result {
    pub payload: InternalFilesDirSha256ResultPayload,
    pub tag: InternalFilesDirSha256ResultTag,
}

impl InternalFilesDirSha256Result {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesDirSha256ResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr {
        unsafe { &*(self.payload.as_ptr() as *const ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesDirSha256ResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr> as *const ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesDirSha256ResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesDirSha256ResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesDirSha256ResultTag::Ok` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &RocStr {
        unsafe { &*(self.payload.as_ptr() as *const RocStr) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesDirSha256ResultTag::Ok` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &RocStr {
        unsafe { &*(&self.payload.ok as *const core::mem::ManuallyDrop<RocStr> as *const RocStr) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesDirSha256ResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> RocStr {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const RocStr) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesDirSha256ResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> RocStr {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.ok) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<InternalFilesDirSha256Result>() == 32, "InternalFilesDirSha256Result size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<InternalFilesDirSha256Result>() == 8, "InternalFilesDirSha256Result alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(InternalFilesDirSha256Result, tag) == 24, "InternalFilesDirSha256Result tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<InternalFilesDirSha256Result>() == 16, "InternalFilesDirSha256Result size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<InternalFilesDirSha256Result>() == 4, "InternalFilesDirSha256Result alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(InternalFilesDirSha256Result, tag) == 12, "InternalFilesDirSha256Result tag offset mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum InternalFilesDirWatchResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union InternalFilesDirWatchResultPayload {
    pub err: core::mem::ManuallyDrop<AnonStruct4c605145331877e7>,
    pub ok: core::mem::ManuallyDrop<*mut u64>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct InternalFilesDirWatchResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct InternalFilesDirWatchResult {
    pub _payload_alignment: [InternalFilesDirWatchResultPayloadAlignment; 0],
    pub payload: [u8; 16],
    pub tag: InternalFilesDirWatchResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct InternalFilesDirWatchResult {
    pub payload: InternalFilesDirWatchResultPayload,
    pub tag: InternalFilesDirWatchResultTag,
}

impl InternalFilesDirWatchResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesDirWatchResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AnonStruct4c605145331877e7 {
        unsafe { &*(self.payload.as_ptr() as *const AnonStruct4c605145331877e7) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesDirWatchResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AnonStruct4c605145331877e7 {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<AnonStruct4c605145331877e7> as *const AnonStruct4c605145331877e7) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesDirWatchResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AnonStruct4c605145331877e7 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AnonStruct4c605145331877e7) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesDirWatchResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AnonStruct4c605145331877e7 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesDirWatchResultTag::Ok` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &*mut u64 {
        unsafe { &*(self.payload.as_ptr() as *const *mut u64) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesDirWatchResultTag::Ok` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &*mut u64 {
        unsafe { &*(&self.payload.ok as *const core::mem::ManuallyDrop<*mut u64> as *const *mut u64) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesDirWatchResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> *mut u64 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const *mut u64) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesDirWatchResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> *mut u64 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.ok) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<InternalFilesDirWatchResult>() == 40, "InternalFilesDirWatchResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<InternalFilesDirWatchResult>() == 8, "InternalFilesDirWatchResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(InternalFilesDirWatchResult, tag) == 32, "InternalFilesDirWatchResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<InternalFilesDirWatchResult>() == 20, "InternalFilesDirWatchResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<InternalFilesDirWatchResult>() == 4, "InternalFilesDirWatchResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(InternalFilesDirWatchResult, tag) == 16, "InternalFilesDirWatchResult tag offset mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum InternalFilesPickFileResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union InternalFilesPickFileResultPayload {
    pub err: core::mem::ManuallyDrop<ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr>,
    pub ok: core::mem::ManuallyDrop<CanceledOrChosenType283>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct InternalFilesPickFileResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct InternalFilesPickFileResult {
    pub _payload_alignment: [InternalFilesPickFileResultPayloadAlignment; 0],
    pub payload: [u8; 20],
    pub tag: InternalFilesPickFileResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct InternalFilesPickFileResult {
    pub payload: InternalFilesPickFileResultPayload,
    pub tag: InternalFilesPickFileResultTag,
}

impl InternalFilesPickFileResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesPickFileResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr {
        unsafe { &*(self.payload.as_ptr() as *const ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesPickFileResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr> as *const ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesPickFileResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesPickFileResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesPickFileResultTag::Ok` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &CanceledOrChosenType283 {
        unsafe { &*(self.payload.as_ptr() as *const CanceledOrChosenType283) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesPickFileResultTag::Ok` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &CanceledOrChosenType283 {
        unsafe { &*(&self.payload.ok as *const core::mem::ManuallyDrop<CanceledOrChosenType283> as *const CanceledOrChosenType283) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesPickFileResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> CanceledOrChosenType283 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const CanceledOrChosenType283) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesPickFileResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> CanceledOrChosenType283 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.ok) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<InternalFilesPickFileResult>() == 48, "InternalFilesPickFileResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<InternalFilesPickFileResult>() == 8, "InternalFilesPickFileResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(InternalFilesPickFileResult, tag) == 40, "InternalFilesPickFileResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<InternalFilesPickFileResult>() == 24, "InternalFilesPickFileResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<InternalFilesPickFileResult>() == 4, "InternalFilesPickFileResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(InternalFilesPickFileResult, tag) == 20, "InternalFilesPickFileResult tag offset mismatch");

/// Tag discriminant for CanceledOrChosen.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum CanceledOrChosenType283Tag {
    Canceled = 0,
    Chosen = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union CanceledOrChosenType283Payload {
    pub canceled: [u8; 0],
    pub chosen: core::mem::ManuallyDrop<AnonStructA295f39559baa24d>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct CanceledOrChosenType283PayloadAlignment;

/// Tag union: CanceledOrChosen
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct CanceledOrChosenType283 {
    pub _payload_alignment: [CanceledOrChosenType283PayloadAlignment; 0],
    pub payload: [u8; 16],
    pub tag: CanceledOrChosenType283Tag,
}

/// Tag union: CanceledOrChosen
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct CanceledOrChosenType283 {
    pub payload: CanceledOrChosenType283Payload,
    pub tag: CanceledOrChosenType283Tag,
}

impl CanceledOrChosenType283 {
    /// Borrow the `Chosen` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `CanceledOrChosenType283Tag::Chosen` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_chosen_unchecked(&self) -> &AnonStructA295f39559baa24d {
        unsafe { &*(self.payload.as_ptr() as *const AnonStructA295f39559baa24d) }
    }

    /// Borrow the `Chosen` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `CanceledOrChosenType283Tag::Chosen` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_chosen_unchecked(&self) -> &AnonStructA295f39559baa24d {
        unsafe { &*(&self.payload.chosen as *const core::mem::ManuallyDrop<AnonStructA295f39559baa24d> as *const AnonStructA295f39559baa24d) }
    }

    /// Move the `Chosen` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `CanceledOrChosenType283Tag::Chosen`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_chosen_unchecked(&mut self) -> AnonStructA295f39559baa24d {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AnonStructA295f39559baa24d) }
    }

    /// Move the `Chosen` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `CanceledOrChosenType283Tag::Chosen`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_chosen_unchecked(&mut self) -> AnonStructA295f39559baa24d {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.chosen) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<CanceledOrChosenType283>() == 40, "CanceledOrChosenType283 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<CanceledOrChosenType283>() == 8, "CanceledOrChosenType283 alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(CanceledOrChosenType283, tag) == 32, "CanceledOrChosenType283 tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<CanceledOrChosenType283>() == 20, "CanceledOrChosenType283 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<CanceledOrChosenType283>() == 4, "CanceledOrChosenType283 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(CanceledOrChosenType283, tag) == 16, "CanceledOrChosenType283 tag offset mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum InternalFilesAppDataResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union InternalFilesAppDataResultPayload {
    pub err: core::mem::ManuallyDrop<AnonStruct4c605145331877e7>,
    pub ok: core::mem::ManuallyDrop<*mut u64>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct InternalFilesAppDataResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct InternalFilesAppDataResult {
    pub _payload_alignment: [InternalFilesAppDataResultPayloadAlignment; 0],
    pub payload: [u8; 16],
    pub tag: InternalFilesAppDataResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct InternalFilesAppDataResult {
    pub payload: InternalFilesAppDataResultPayload,
    pub tag: InternalFilesAppDataResultTag,
}

impl InternalFilesAppDataResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesAppDataResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AnonStruct4c605145331877e7 {
        unsafe { &*(self.payload.as_ptr() as *const AnonStruct4c605145331877e7) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesAppDataResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AnonStruct4c605145331877e7 {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<AnonStruct4c605145331877e7> as *const AnonStruct4c605145331877e7) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesAppDataResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AnonStruct4c605145331877e7 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AnonStruct4c605145331877e7) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesAppDataResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AnonStruct4c605145331877e7 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesAppDataResultTag::Ok` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &*mut u64 {
        unsafe { &*(self.payload.as_ptr() as *const *mut u64) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesAppDataResultTag::Ok` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &*mut u64 {
        unsafe { &*(&self.payload.ok as *const core::mem::ManuallyDrop<*mut u64> as *const *mut u64) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesAppDataResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> *mut u64 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const *mut u64) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesAppDataResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> *mut u64 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.ok) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<InternalFilesAppDataResult>() == 40, "InternalFilesAppDataResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<InternalFilesAppDataResult>() == 8, "InternalFilesAppDataResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(InternalFilesAppDataResult, tag) == 32, "InternalFilesAppDataResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<InternalFilesAppDataResult>() == 20, "InternalFilesAppDataResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<InternalFilesAppDataResult>() == 4, "InternalFilesAppDataResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(InternalFilesAppDataResult, tag) == 16, "InternalFilesAppDataResult tag offset mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum InternalFilesReadUtf8ResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union InternalFilesReadUtf8ResultPayload {
    pub err: core::mem::ManuallyDrop<AnonStruct4c605145331877e7>,
    pub ok: core::mem::ManuallyDrop<AnonStructA2f37e1d5ed0e4cd>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct InternalFilesReadUtf8ResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct InternalFilesReadUtf8Result {
    pub _payload_alignment: [InternalFilesReadUtf8ResultPayloadAlignment; 0],
    pub payload: [u8; 16],
    pub tag: InternalFilesReadUtf8ResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct InternalFilesReadUtf8Result {
    pub payload: InternalFilesReadUtf8ResultPayload,
    pub tag: InternalFilesReadUtf8ResultTag,
}

impl InternalFilesReadUtf8Result {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesReadUtf8ResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AnonStruct4c605145331877e7 {
        unsafe { &*(self.payload.as_ptr() as *const AnonStruct4c605145331877e7) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesReadUtf8ResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AnonStruct4c605145331877e7 {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<AnonStruct4c605145331877e7> as *const AnonStruct4c605145331877e7) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesReadUtf8ResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AnonStruct4c605145331877e7 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AnonStruct4c605145331877e7) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesReadUtf8ResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AnonStruct4c605145331877e7 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesReadUtf8ResultTag::Ok` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &AnonStructA2f37e1d5ed0e4cd {
        unsafe { &*(self.payload.as_ptr() as *const AnonStructA2f37e1d5ed0e4cd) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesReadUtf8ResultTag::Ok` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &AnonStructA2f37e1d5ed0e4cd {
        unsafe { &*(&self.payload.ok as *const core::mem::ManuallyDrop<AnonStructA2f37e1d5ed0e4cd> as *const AnonStructA2f37e1d5ed0e4cd) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesReadUtf8ResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> AnonStructA2f37e1d5ed0e4cd {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AnonStructA2f37e1d5ed0e4cd) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesReadUtf8ResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> AnonStructA2f37e1d5ed0e4cd {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.ok) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<InternalFilesReadUtf8Result>() == 40, "InternalFilesReadUtf8Result size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<InternalFilesReadUtf8Result>() == 8, "InternalFilesReadUtf8Result alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(InternalFilesReadUtf8Result, tag) == 32, "InternalFilesReadUtf8Result tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<InternalFilesReadUtf8Result>() == 20, "InternalFilesReadUtf8Result size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<InternalFilesReadUtf8Result>() == 4, "InternalFilesReadUtf8Result alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(InternalFilesReadUtf8Result, tag) == 16, "InternalFilesReadUtf8Result tag offset mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum InternalFilesWriteUtf8AtomicResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union InternalFilesWriteUtf8AtomicResultPayload {
    pub err: core::mem::ManuallyDrop<AnonStruct4c605145331877e7>,
    pub ok: [u8; 0],
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct InternalFilesWriteUtf8AtomicResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct InternalFilesWriteUtf8AtomicResult {
    pub _payload_alignment: [InternalFilesWriteUtf8AtomicResultPayloadAlignment; 0],
    pub payload: [u8; 16],
    pub tag: InternalFilesWriteUtf8AtomicResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct InternalFilesWriteUtf8AtomicResult {
    pub payload: InternalFilesWriteUtf8AtomicResultPayload,
    pub tag: InternalFilesWriteUtf8AtomicResultTag,
}

impl InternalFilesWriteUtf8AtomicResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesWriteUtf8AtomicResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AnonStruct4c605145331877e7 {
        unsafe { &*(self.payload.as_ptr() as *const AnonStruct4c605145331877e7) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesWriteUtf8AtomicResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &AnonStruct4c605145331877e7 {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<AnonStruct4c605145331877e7> as *const AnonStruct4c605145331877e7) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesWriteUtf8AtomicResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AnonStruct4c605145331877e7 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AnonStruct4c605145331877e7) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `InternalFilesWriteUtf8AtomicResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> AnonStruct4c605145331877e7 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<InternalFilesWriteUtf8AtomicResult>() == 40, "InternalFilesWriteUtf8AtomicResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<InternalFilesWriteUtf8AtomicResult>() == 8, "InternalFilesWriteUtf8AtomicResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(InternalFilesWriteUtf8AtomicResult, tag) == 32, "InternalFilesWriteUtf8AtomicResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<InternalFilesWriteUtf8AtomicResult>() == 20, "InternalFilesWriteUtf8AtomicResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<InternalFilesWriteUtf8AtomicResult>() == 4, "InternalFilesWriteUtf8AtomicResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(InternalFilesWriteUtf8AtomicResult, tag) == 16, "InternalFilesWriteUtf8AtomicResult tag offset mismatch");

/// Return type record for HostGlue.component_resolve!
/// Fields ordered by compiler-emitted ABI offsets.
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueComponentResolveRetRecord {
    pub instance: u64,
    pub root: u64,
}

/// Return type record for HostGlue.component_resolve!
/// Fields ordered by compiler-emitted ABI offsets.
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueComponentResolveRetRecord {
    pub instance: u64,
    pub root: u64,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueComponentResolveRetRecord>() == 16, "HostGlueComponentResolveRetRecord size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueComponentResolveRetRecord>() == 8, "HostGlueComponentResolveRetRecord alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueComponentResolveRetRecord>() == 16, "HostGlueComponentResolveRetRecord size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueComponentResolveRetRecord>() == 8, "HostGlueComponentResolveRetRecord alignment mismatch");

/// Return type record for HostGlue.node_popover!
/// Fields ordered by compiler-emitted ABI offsets.
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueNodePopoverRetRecord {
    pub hover_enter: u64,
    pub hover_exit: u64,
    pub id: u64,
    pub shortcut: u64,
}

/// Return type record for HostGlue.node_popover!
/// Fields ordered by compiler-emitted ABI offsets.
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueNodePopoverRetRecord {
    pub hover_enter: u64,
    pub hover_exit: u64,
    pub id: u64,
    pub shortcut: u64,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueNodePopoverRetRecord>() == 32, "HostGlueNodePopoverRetRecord size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueNodePopoverRetRecord>() == 8, "HostGlueNodePopoverRetRecord alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueNodePopoverRetRecord>() == 32, "HostGlueNodePopoverRetRecord size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueNodePopoverRetRecord>() == 8, "HostGlueNodePopoverRetRecord alignment mismatch");

/// Return type record for HostGlue.node_action_button!
/// Fields ordered by compiler-emitted ABI offsets.
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueNodeActionButtonRetRecord {
    pub hover_enter: u64,
    pub hover_exit: u64,
    pub id: u64,
}

/// Return type record for HostGlue.node_action_button!
/// Fields ordered by compiler-emitted ABI offsets.
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueNodeActionButtonRetRecord {
    pub hover_enter: u64,
    pub hover_exit: u64,
    pub id: u64,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueNodeActionButtonRetRecord>() == 24, "HostGlueNodeActionButtonRetRecord size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueNodeActionButtonRetRecord>() == 8, "HostGlueNodeActionButtonRetRecord alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueNodeActionButtonRetRecord>() == 24, "HostGlueNodeActionButtonRetRecord size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueNodeActionButtonRetRecord>() == 8, "HostGlueNodeActionButtonRetRecord alignment mismatch");

/// Return type record for HostGlue.canvas_event!
/// Fields ordered by compiler-emitted ABI offsets.
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueCanvasEventRetRecord {
    pub target: u64,
    pub dx: i32,
    pub dy: i32,
    pub x: i32,
    pub y: i32,
    pub phase: u8,
}

/// Return type record for HostGlue.canvas_event!
/// Fields ordered by compiler-emitted ABI offsets.
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueCanvasEventRetRecord {
    pub target: u64,
    pub dx: i32,
    pub dy: i32,
    pub x: i32,
    pub y: i32,
    pub phase: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueCanvasEventRetRecord>() == 32, "HostGlueCanvasEventRetRecord size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueCanvasEventRetRecord>() == 8, "HostGlueCanvasEventRetRecord alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueCanvasEventRetRecord>() == 32, "HostGlueCanvasEventRetRecord size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueCanvasEventRetRecord>() == 8, "HostGlueCanvasEventRetRecord alignment mismatch");

/// Return type record for HostGlue.shortcut_event!
/// Fields ordered by compiler-emitted ABI offsets.
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueShortcutEventRetRecord {
    pub index: u64,
    pub keys: RocStr,
}

/// Return type record for HostGlue.shortcut_event!
/// Fields ordered by compiler-emitted ABI offsets.
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueShortcutEventRetRecord {
    pub index: u64,
    pub keys: RocStr,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueShortcutEventRetRecord>() == 32, "HostGlueShortcutEventRetRecord size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueShortcutEventRetRecord>() == 8, "HostGlueShortcutEventRetRecord alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueShortcutEventRetRecord>() == 24, "HostGlueShortcutEventRetRecord size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueShortcutEventRetRecord>() == 8, "HostGlueShortcutEventRetRecord alignment mismatch");

/// Return type record for HostGlue.node_split!
/// Fields ordered by compiler-emitted ABI offsets.
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueNodeSplitRetRecord {
    pub id: u64,
    pub shortcut: u64,
}

/// Return type record for HostGlue.node_split!
/// Fields ordered by compiler-emitted ABI offsets.
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueNodeSplitRetRecord {
    pub id: u64,
    pub shortcut: u64,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueNodeSplitRetRecord>() == 16, "HostGlueNodeSplitRetRecord size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueNodeSplitRetRecord>() == 8, "HostGlueNodeSplitRetRecord alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueNodeSplitRetRecord>() == 16, "HostGlueNodeSplitRetRecord size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueNodeSplitRetRecord>() == 8, "HostGlueNodeSplitRetRecord alignment mismatch");

/// Return type record for HostGlue.resize_event!
/// Fields ordered by compiler-emitted ABI offsets.
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueResizeEventRetRecord {
    pub size: u32,
    pub collapsed: bool,
}

/// Return type record for HostGlue.resize_event!
/// Fields ordered by compiler-emitted ABI offsets.
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueResizeEventRetRecord {
    pub size: u32,
    pub collapsed: bool,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueResizeEventRetRecord>() == 8, "HostGlueResizeEventRetRecord size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueResizeEventRetRecord>() == 4, "HostGlueResizeEventRetRecord alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueResizeEventRetRecord>() == 8, "HostGlueResizeEventRetRecord size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueResizeEventRetRecord>() == 4, "HostGlueResizeEventRetRecord alignment mismatch");

/// Return type record for HostGlue.virtual_window!
/// Fields ordered by compiler-emitted ABI offsets.
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueVirtualWindowRetRecord {
    pub end: u64,
    pub first: u64,
}

/// Return type record for HostGlue.virtual_window!
/// Fields ordered by compiler-emitted ABI offsets.
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueVirtualWindowRetRecord {
    pub end: u64,
    pub first: u64,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueVirtualWindowRetRecord>() == 16, "HostGlueVirtualWindowRetRecord size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueVirtualWindowRetRecord>() == 8, "HostGlueVirtualWindowRetRecord alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueVirtualWindowRetRecord>() == 16, "HostGlueVirtualWindowRetRecord size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueVirtualWindowRetRecord>() == 8, "HostGlueVirtualWindowRetRecord alignment mismatch");

/// Return type record for HostGlue.virtual_rows_event!
/// Fields ordered by compiler-emitted ABI offsets.
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueVirtualRowsEventRetRecord {
    pub end: u64,
    pub start: u64,
    pub refresh: bool,
    pub report: bool,
}

/// Return type record for HostGlue.virtual_rows_event!
/// Fields ordered by compiler-emitted ABI offsets.
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueVirtualRowsEventRetRecord {
    pub end: u64,
    pub start: u64,
    pub refresh: bool,
    pub report: bool,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueVirtualRowsEventRetRecord>() == 24, "HostGlueVirtualRowsEventRetRecord size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueVirtualRowsEventRetRecord>() == 8, "HostGlueVirtualRowsEventRetRecord alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueVirtualRowsEventRetRecord>() == 24, "HostGlueVirtualRowsEventRetRecord size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueVirtualRowsEventRetRecord>() == 8, "HostGlueVirtualRowsEventRetRecord alignment mismatch");

/// Return type record for HostGlue.node_text_input!
/// Fields ordered by compiler-emitted ABI offsets.
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueNodeTextInputRetRecord {
    pub change: u64,
    pub id: u64,
    pub submit: u64,
}

/// Return type record for HostGlue.node_text_input!
/// Fields ordered by compiler-emitted ABI offsets.
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueNodeTextInputRetRecord {
    pub change: u64,
    pub id: u64,
    pub submit: u64,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueNodeTextInputRetRecord>() == 24, "HostGlueNodeTextInputRetRecord size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueNodeTextInputRetRecord>() == 8, "HostGlueNodeTextInputRetRecord alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueNodeTextInputRetRecord>() == 24, "HostGlueNodeTextInputRetRecord size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueNodeTextInputRetRecord>() == 8, "HostGlueNodeTextInputRetRecord alignment mismatch");

/// Return type record for InternalFiles.watch_next!
/// Fields ordered by compiler-emitted ABI offsets.
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct InternalFilesWatchNextRetRecord {
    pub names: RocList<RocStr>,
    pub code: u8,
    pub overflowed: bool,
    pub replaced: bool,
}

/// Return type record for InternalFiles.watch_next!
/// Fields ordered by compiler-emitted ABI offsets.
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct InternalFilesWatchNextRetRecord {
    pub names: RocList<RocStr>,
    pub code: u8,
    pub overflowed: bool,
    pub replaced: bool,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<InternalFilesWatchNextRetRecord>() == 32, "InternalFilesWatchNextRetRecord size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<InternalFilesWatchNextRetRecord>() == 8, "InternalFilesWatchNextRetRecord alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<InternalFilesWatchNextRetRecord>() == 16, "InternalFilesWatchNextRetRecord size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<InternalFilesWatchNextRetRecord>() == 4, "InternalFilesWatchNextRetRecord alignment mismatch");

/// Arguments for HostGlue.node_text!
/// Roc signature: Str => U64
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueNodeTextArgs {
    pub arg0: RocStr,
}

/// Arguments for HostGlue.node_styled_text!
/// Roc signature: { fg : U64, font_face : U8, font_size : U32, font_weight : U32, runs : List({ bg : U64, fg : U64, font_weight : U32, len : U64, monospace : Bool, underline : Bool }), value : Str } => U64
/// Refcounted fields are owned by the hosted function.
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueNodeStyledTextArgs {
    pub fg: u64,
    pub runs: RocListWith<AnonStructFdc3cce9386720ed, false>,
    pub value: RocStr,
    pub font_size: u32,
    pub font_weight: u32,
    pub font_face: u8,
}

/// Arguments for HostGlue.node_styled_text!
/// Roc signature: { fg : U64, font_face : U8, font_size : U32, font_weight : U32, runs : List({ bg : U64, fg : U64, font_weight : U32, len : U64, monospace : Bool, underline : Bool }), value : Str } => U64
/// Refcounted fields are owned by the hosted function.
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueNodeStyledTextArgs {
    pub fg: u64,
    pub runs: RocListWith<AnonStructFdc3cce9386720ed, false>,
    pub value: RocStr,
    pub font_size: u32,
    pub font_weight: u32,
    pub font_face: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueNodeStyledTextArgs>() == 72, "HostGlueNodeStyledTextArgs size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueNodeStyledTextArgs>() == 8, "HostGlueNodeStyledTextArgs alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueNodeStyledTextArgs>() == 48, "HostGlueNodeStyledTextArgs size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueNodeStyledTextArgs>() == 8, "HostGlueNodeStyledTextArgs alignment mismatch");

impl HostGlueNodeStyledTextArgs {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.runs.decref(roc_host); }
        unsafe { value.value.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.runs.incref(amount); }
        unsafe { value.value.incref(amount); }
    }
}

pub struct HostGlueNodeStyledTextArgsRelease;

unsafe impl RocRelease<HostGlueNodeStyledTextArgs> for HostGlueNodeStyledTextArgsRelease {
    unsafe fn release(value: HostGlueNodeStyledTextArgs, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

/// Arguments for HostGlue.children_push!
/// Roc signature: U64, U64 => {}
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueChildrenPushArgs {
    pub arg0: u64,
    pub arg1: u64,
}

/// Arguments for HostGlue.keyed_seed!
/// Roc signature: { container : U64, keys : List(List(U8)), revision : U64 } => {}
/// Refcounted fields are owned by the hosted function.
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueKeyedSeedArgs {
    pub container: u64,
    pub revision: u64,
    pub keys: RocList<RocListWith<u8, false>>,
}

/// Arguments for HostGlue.keyed_seed!
/// Roc signature: { container : U64, keys : List(List(U8)), revision : U64 } => {}
/// Refcounted fields are owned by the hosted function.
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueKeyedSeedArgs {
    pub container: u64,
    pub revision: u64,
    pub keys: RocList<RocListWith<u8, false>>,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueKeyedSeedArgs>() == 40, "HostGlueKeyedSeedArgs size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueKeyedSeedArgs>() == 8, "HostGlueKeyedSeedArgs alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueKeyedSeedArgs>() == 32, "HostGlueKeyedSeedArgs size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueKeyedSeedArgs>() == 8, "HostGlueKeyedSeedArgs alignment mismatch");

impl HostGlueKeyedSeedArgs {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { decref_list_of_list_of_type3(value.keys, roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.keys.incref(amount); }
    }
}

pub struct HostGlueKeyedSeedArgsRelease;

unsafe impl RocRelease<HostGlueKeyedSeedArgs> for HostGlueKeyedSeedArgsRelease {
    unsafe fn release(value: HostGlueKeyedSeedArgs, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

/// Arguments for HostGlue.keyed_edit_begin!
/// Roc signature: { base_revision : U64, container : U64, new_revision : U64 } => {}
/// Refcounted fields are owned by the hosted function.
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueKeyedEditBeginArgs {
    pub base_revision: u64,
    pub container: u64,
    pub new_revision: u64,
}

/// Arguments for HostGlue.keyed_edit_begin!
/// Roc signature: { base_revision : U64, container : U64, new_revision : U64 } => {}
/// Refcounted fields are owned by the hosted function.
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueKeyedEditBeginArgs {
    pub base_revision: u64,
    pub container: u64,
    pub new_revision: u64,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueKeyedEditBeginArgs>() == 24, "HostGlueKeyedEditBeginArgs size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueKeyedEditBeginArgs>() == 8, "HostGlueKeyedEditBeginArgs alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueKeyedEditBeginArgs>() == 24, "HostGlueKeyedEditBeginArgs size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueKeyedEditBeginArgs>() == 8, "HostGlueKeyedEditBeginArgs alignment mismatch");

impl HostGlueKeyedEditBeginArgs {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        let _ = value;
        let _ = roc_host;
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = value;
        let _ = amount;
    }
}

pub struct HostGlueKeyedEditBeginArgsRelease;

unsafe impl RocRelease<HostGlueKeyedEditBeginArgs> for HostGlueKeyedEditBeginArgsRelease {
    unsafe fn release(value: HostGlueKeyedEditBeginArgs, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

/// Arguments for HostGlue.keyed_insert_before!
/// Roc signature: { before : List(U8), key : List(U8), root : U64 } => {}
/// Refcounted fields are owned by the hosted function.
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueKeyedInsertBeforeArgs {
    pub root: u64,
    pub before: RocListWith<u8, false>,
    pub key: RocListWith<u8, false>,
}

/// Arguments for HostGlue.keyed_insert_before!
/// Roc signature: { before : List(U8), key : List(U8), root : U64 } => {}
/// Refcounted fields are owned by the hosted function.
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueKeyedInsertBeforeArgs {
    pub root: u64,
    pub before: RocListWith<u8, false>,
    pub key: RocListWith<u8, false>,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueKeyedInsertBeforeArgs>() == 56, "HostGlueKeyedInsertBeforeArgs size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueKeyedInsertBeforeArgs>() == 8, "HostGlueKeyedInsertBeforeArgs alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueKeyedInsertBeforeArgs>() == 32, "HostGlueKeyedInsertBeforeArgs size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueKeyedInsertBeforeArgs>() == 8, "HostGlueKeyedInsertBeforeArgs alignment mismatch");

impl HostGlueKeyedInsertBeforeArgs {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.before.decref(roc_host); }
        unsafe { value.key.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.before.incref(amount); }
        unsafe { value.key.incref(amount); }
    }
}

pub struct HostGlueKeyedInsertBeforeArgsRelease;

unsafe impl RocRelease<HostGlueKeyedInsertBeforeArgs> for HostGlueKeyedInsertBeforeArgsRelease {
    unsafe fn release(value: HostGlueKeyedInsertBeforeArgs, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

/// Arguments for HostGlue.keyed_remove!
/// Roc signature: List(U8) => {}
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueKeyedRemoveArgs {
    pub arg0: RocListWith<u8, false>,
}

/// Arguments for HostGlue.keyed_move_before!
/// Roc signature: { before : List(U8), key : List(U8) } => {}
/// Refcounted fields are owned by the hosted function.
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueKeyedMoveBeforeArgs {
    pub before: RocListWith<u8, false>,
    pub key: RocListWith<u8, false>,
}

/// Arguments for HostGlue.keyed_move_before!
/// Roc signature: { before : List(U8), key : List(U8) } => {}
/// Refcounted fields are owned by the hosted function.
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueKeyedMoveBeforeArgs {
    pub before: RocListWith<u8, false>,
    pub key: RocListWith<u8, false>,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueKeyedMoveBeforeArgs>() == 48, "HostGlueKeyedMoveBeforeArgs size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueKeyedMoveBeforeArgs>() == 8, "HostGlueKeyedMoveBeforeArgs alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueKeyedMoveBeforeArgs>() == 24, "HostGlueKeyedMoveBeforeArgs size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueKeyedMoveBeforeArgs>() == 4, "HostGlueKeyedMoveBeforeArgs alignment mismatch");

impl HostGlueKeyedMoveBeforeArgs {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.before.decref(roc_host); }
        unsafe { value.key.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.before.incref(amount); }
        unsafe { value.key.incref(amount); }
    }
}

pub struct HostGlueKeyedMoveBeforeArgsRelease;

unsafe impl RocRelease<HostGlueKeyedMoveBeforeArgs> for HostGlueKeyedMoveBeforeArgsRelease {
    unsafe fn release(value: HostGlueKeyedMoveBeforeArgs, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

/// Arguments for HostGlue.keyed_set!
/// Roc signature: { key : List(U8), root : U64 } => {}
/// Refcounted fields are owned by the hosted function.
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueKeyedSetArgs {
    pub root: u64,
    pub key: RocListWith<u8, false>,
}

/// Arguments for HostGlue.keyed_set!
/// Roc signature: { key : List(U8), root : U64 } => {}
/// Refcounted fields are owned by the hosted function.
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueKeyedSetArgs {
    pub root: u64,
    pub key: RocListWith<u8, false>,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueKeyedSetArgs>() == 32, "HostGlueKeyedSetArgs size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueKeyedSetArgs>() == 8, "HostGlueKeyedSetArgs alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueKeyedSetArgs>() == 24, "HostGlueKeyedSetArgs size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueKeyedSetArgs>() == 8, "HostGlueKeyedSetArgs alignment mismatch");

impl HostGlueKeyedSetArgs {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.key.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.key.incref(amount); }
    }
}

pub struct HostGlueKeyedSetArgsRelease;

unsafe impl RocRelease<HostGlueKeyedSetArgs> for HostGlueKeyedSetArgsRelease {
    unsafe fn release(value: HostGlueKeyedSetArgs, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

/// Arguments for HostGlue.component_work!
/// Roc signature: U8, U64 => {}
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueComponentWorkArgs {
    pub arg0: u8,
    pub arg1: u64,
}

/// Arguments for HostGlue.node_boundary!
/// Roc signature: U64, U64 => U64
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueNodeBoundaryArgs {
    pub arg0: u64,
    pub arg1: u64,
}

/// Arguments for HostGlue.retain_subtree!
/// Roc signature: U64 => U64
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueRetainSubtreeArgs {
    pub arg0: u64,
}

/// Arguments for HostGlue.begin_render!
/// Roc signature: U64 => {}
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueBeginRenderArgs {
    pub arg0: u64,
}

/// Arguments for HostGlue.scope_enter!
/// Roc signature: U8, Str, U64 => {}
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueScopeEnterArgs {
    pub arg0: u8,
    pub arg1: RocStr,
    pub arg2: u64,
}

/// Arguments for HostGlue.component_resolve!
/// Roc signature: U8, List(U8) => { instance : U64, root : U64 }
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueComponentResolveArgs {
    pub arg0: u8,
    pub arg1: RocListWith<u8, false>,
}

/// Arguments for HostGlue.component_enter!
/// Roc signature: U64 => {}
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueComponentEnterArgs {
    pub arg0: u64,
}

/// Arguments for HostGlue.node_row!
/// Roc signature: { active_bg : U64, align : U8, bg : U64, border_bottom : U32, border_color : U64, border_left : U32, border_right : U32, border_top : U32, builder : U64, disabled_bg : U64, disabled_fg : U64, fg : U64, focus_color : U64, font_face : U8, font_size : U32, font_weight : U32, gap : U32, grow : Bool, height : U32, height_kind : U8, hover_bg : U64, justify : U8, label : Str, max_height : U32, max_height_kind : U8, max_width : U32, max_width_kind : U8, min_height : U32, min_height_kind : U8, min_width : U32, min_width_kind : U8, overflow_x : U8, overflow_y : U8, padding_bottom : U32, padding_left : U32, padding_right : U32, padding_top : U32, radius : U32, shadow : U32, shadow_alpha : U32, shadow_color : U64, shadow_y : U32, text_overflow : U8, width : U32, width_kind : U8 } => U64
/// Refcounted fields are owned by the hosted function.
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueNodeRowArgs {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub builder: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub shadow_color: u64,
    pub label: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

/// Arguments for HostGlue.node_row!
/// Roc signature: { active_bg : U64, align : U8, bg : U64, border_bottom : U32, border_color : U64, border_left : U32, border_right : U32, border_top : U32, builder : U64, disabled_bg : U64, disabled_fg : U64, fg : U64, focus_color : U64, font_face : U8, font_size : U32, font_weight : U32, gap : U32, grow : Bool, height : U32, height_kind : U8, hover_bg : U64, justify : U8, label : Str, max_height : U32, max_height_kind : U8, max_width : U32, max_width_kind : U8, min_height : U32, min_height_kind : U8, min_width : U32, min_width_kind : U8, overflow_x : U8, overflow_y : U8, padding_bottom : U32, padding_left : U32, padding_right : U32, padding_top : U32, radius : U32, shadow : U32, shadow_alpha : U32, shadow_color : U64, shadow_y : U32, text_overflow : U8, width : U32, width_kind : U8 } => U64
/// Refcounted fields are owned by the hosted function.
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueNodeRowArgs {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub builder: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub shadow_color: u64,
    pub label: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueNodeRowArgs>() == 208, "HostGlueNodeRowArgs size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueNodeRowArgs>() == 8, "HostGlueNodeRowArgs alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueNodeRowArgs>() == 192, "HostGlueNodeRowArgs size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueNodeRowArgs>() == 8, "HostGlueNodeRowArgs alignment mismatch");

impl HostGlueNodeRowArgs {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.label.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.label.incref(amount); }
    }
}

pub struct HostGlueNodeRowArgsRelease;

unsafe impl RocRelease<HostGlueNodeRowArgs> for HostGlueNodeRowArgsRelease {
    unsafe fn release(value: HostGlueNodeRowArgs, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

/// Arguments for HostGlue.node_column!
/// Roc signature: { active_bg : U64, align : U8, bg : U64, border_bottom : U32, border_color : U64, border_left : U32, border_right : U32, border_top : U32, builder : U64, disabled_bg : U64, disabled_fg : U64, fg : U64, focus_color : U64, font_face : U8, font_size : U32, font_weight : U32, gap : U32, grow : Bool, height : U32, height_kind : U8, hover_bg : U64, justify : U8, label : Str, max_height : U32, max_height_kind : U8, max_width : U32, max_width_kind : U8, min_height : U32, min_height_kind : U8, min_width : U32, min_width_kind : U8, overflow_x : U8, overflow_y : U8, padding_bottom : U32, padding_left : U32, padding_right : U32, padding_top : U32, radius : U32, shadow : U32, shadow_alpha : U32, shadow_color : U64, shadow_y : U32, text_overflow : U8, width : U32, width_kind : U8 } => U64
/// Refcounted fields are owned by the hosted function.
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueNodeColumnArgs {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub builder: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub shadow_color: u64,
    pub label: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

/// Arguments for HostGlue.node_column!
/// Roc signature: { active_bg : U64, align : U8, bg : U64, border_bottom : U32, border_color : U64, border_left : U32, border_right : U32, border_top : U32, builder : U64, disabled_bg : U64, disabled_fg : U64, fg : U64, focus_color : U64, font_face : U8, font_size : U32, font_weight : U32, gap : U32, grow : Bool, height : U32, height_kind : U8, hover_bg : U64, justify : U8, label : Str, max_height : U32, max_height_kind : U8, max_width : U32, max_width_kind : U8, min_height : U32, min_height_kind : U8, min_width : U32, min_width_kind : U8, overflow_x : U8, overflow_y : U8, padding_bottom : U32, padding_left : U32, padding_right : U32, padding_top : U32, radius : U32, shadow : U32, shadow_alpha : U32, shadow_color : U64, shadow_y : U32, text_overflow : U8, width : U32, width_kind : U8 } => U64
/// Refcounted fields are owned by the hosted function.
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueNodeColumnArgs {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub builder: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub shadow_color: u64,
    pub label: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueNodeColumnArgs>() == 208, "HostGlueNodeColumnArgs size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueNodeColumnArgs>() == 8, "HostGlueNodeColumnArgs alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueNodeColumnArgs>() == 192, "HostGlueNodeColumnArgs size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueNodeColumnArgs>() == 8, "HostGlueNodeColumnArgs alignment mismatch");

impl HostGlueNodeColumnArgs {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.label.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.label.incref(amount); }
    }
}

pub struct HostGlueNodeColumnArgsRelease;

unsafe impl RocRelease<HostGlueNodeColumnArgs> for HostGlueNodeColumnArgsRelease {
    unsafe fn release(value: HostGlueNodeColumnArgs, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

/// Arguments for HostGlue.node_dialog!
/// Roc signature: { active_bg : U64, align : U8, bg : U64, border_bottom : U32, border_color : U64, border_left : U32, border_right : U32, border_top : U32, builder : U64, disabled_bg : U64, disabled_fg : U64, fg : U64, focus_color : U64, font_face : U8, font_size : U32, font_weight : U32, gap : U32, grow : Bool, height : U32, height_kind : U8, hover_bg : U64, justify : U8, label : Str, max_height : U32, max_height_kind : U8, max_width : U32, max_width_kind : U8, min_height : U32, min_height_kind : U8, min_width : U32, min_width_kind : U8, overflow_x : U8, overflow_y : U8, padding_bottom : U32, padding_left : U32, padding_right : U32, padding_top : U32, radius : U32, shadow : U32, shadow_alpha : U32, shadow_color : U64, shadow_y : U32, text_overflow : U8, width : U32, width_kind : U8 } => U64
/// Refcounted fields are owned by the hosted function.
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueNodeDialogArgs {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub builder: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub shadow_color: u64,
    pub label: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

/// Arguments for HostGlue.node_dialog!
/// Roc signature: { active_bg : U64, align : U8, bg : U64, border_bottom : U32, border_color : U64, border_left : U32, border_right : U32, border_top : U32, builder : U64, disabled_bg : U64, disabled_fg : U64, fg : U64, focus_color : U64, font_face : U8, font_size : U32, font_weight : U32, gap : U32, grow : Bool, height : U32, height_kind : U8, hover_bg : U64, justify : U8, label : Str, max_height : U32, max_height_kind : U8, max_width : U32, max_width_kind : U8, min_height : U32, min_height_kind : U8, min_width : U32, min_width_kind : U8, overflow_x : U8, overflow_y : U8, padding_bottom : U32, padding_left : U32, padding_right : U32, padding_top : U32, radius : U32, shadow : U32, shadow_alpha : U32, shadow_color : U64, shadow_y : U32, text_overflow : U8, width : U32, width_kind : U8 } => U64
/// Refcounted fields are owned by the hosted function.
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueNodeDialogArgs {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub builder: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub shadow_color: u64,
    pub label: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueNodeDialogArgs>() == 208, "HostGlueNodeDialogArgs size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueNodeDialogArgs>() == 8, "HostGlueNodeDialogArgs alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueNodeDialogArgs>() == 192, "HostGlueNodeDialogArgs size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueNodeDialogArgs>() == 8, "HostGlueNodeDialogArgs alignment mismatch");

impl HostGlueNodeDialogArgs {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.label.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.label.incref(amount); }
    }
}

pub struct HostGlueNodeDialogArgsRelease;

unsafe impl RocRelease<HostGlueNodeDialogArgs> for HostGlueNodeDialogArgsRelease {
    unsafe fn release(value: HostGlueNodeDialogArgs, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

/// Arguments for HostGlue.node_popover!
/// Roc signature: { active_bg : U64, align : U8, bg : U64, border_bottom : U32, border_color : U64, border_left : U32, border_right : U32, border_top : U32, builder : U64, delay_ms : U32, disabled_bg : U64, disabled_fg : U64, fg : U64, focus_color : U64, focus_serial : U64, font_face : U8, font_size : U32, font_weight : U32, gap : U32, grow : Bool, height : U32, height_kind : U8, hover_bg : U64, hover_enter : Bool, hover_exit : Bool, justify : U8, label : Str, max_height : U32, max_height_kind : U8, max_width : U32, max_width_kind : U8, min_height : U32, min_height_kind : U8, min_width : U32, min_width_kind : U8, overflow_x : U8, overflow_y : U8, padding_bottom : U32, padding_left : U32, padding_right : U32, padding_top : U32, placement : U8, radius : U32, shadow : U32, shadow_alpha : U32, shadow_color : U64, shadow_y : U32, shortcuts : List({ focus : Bool, keys : Str }), text_overflow : U8, width : U32, width_kind : U8 } => { hover_enter : U64, hover_exit : U64, id : U64, shortcut : U64 }
/// Refcounted fields are owned by the hosted function.
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueNodePopoverArgs {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub builder: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub focus_serial: u64,
    pub hover_bg: u64,
    pub shadow_color: u64,
    pub label: RocStr,
    pub shortcuts: RocList<AnonStruct1d0ac13b7d648fa3>,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub delay_ms: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub hover_enter: bool,
    pub hover_exit: bool,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub placement: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

/// Arguments for HostGlue.node_popover!
/// Roc signature: { active_bg : U64, align : U8, bg : U64, border_bottom : U32, border_color : U64, border_left : U32, border_right : U32, border_top : U32, builder : U64, delay_ms : U32, disabled_bg : U64, disabled_fg : U64, fg : U64, focus_color : U64, focus_serial : U64, font_face : U8, font_size : U32, font_weight : U32, gap : U32, grow : Bool, height : U32, height_kind : U8, hover_bg : U64, hover_enter : Bool, hover_exit : Bool, justify : U8, label : Str, max_height : U32, max_height_kind : U8, max_width : U32, max_width_kind : U8, min_height : U32, min_height_kind : U8, min_width : U32, min_width_kind : U8, overflow_x : U8, overflow_y : U8, padding_bottom : U32, padding_left : U32, padding_right : U32, padding_top : U32, placement : U8, radius : U32, shadow : U32, shadow_alpha : U32, shadow_color : U64, shadow_y : U32, shortcuts : List({ focus : Bool, keys : Str }), text_overflow : U8, width : U32, width_kind : U8 } => { hover_enter : U64, hover_exit : U64, id : U64, shortcut : U64 }
/// Refcounted fields are owned by the hosted function.
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueNodePopoverArgs {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub builder: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub focus_serial: u64,
    pub hover_bg: u64,
    pub shadow_color: u64,
    pub label: RocStr,
    pub shortcuts: RocList<AnonStruct1d0ac13b7d648fa3>,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub delay_ms: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub hover_enter: bool,
    pub hover_exit: bool,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub placement: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueNodePopoverArgs>() == 240, "HostGlueNodePopoverArgs size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueNodePopoverArgs>() == 8, "HostGlueNodePopoverArgs alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueNodePopoverArgs>() == 216, "HostGlueNodePopoverArgs size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueNodePopoverArgs>() == 8, "HostGlueNodePopoverArgs alignment mismatch");

impl HostGlueNodePopoverArgs {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.label.decref(roc_host); }
        unsafe { decref_list_of_anon_struct1d0ac13b7d648fa3(value.shortcuts, roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.label.incref(amount); }
        unsafe { value.shortcuts.incref(amount); }
    }
}

pub struct HostGlueNodePopoverArgsRelease;

unsafe impl RocRelease<HostGlueNodePopoverArgs> for HostGlueNodePopoverArgsRelease {
    unsafe fn release(value: HostGlueNodePopoverArgs, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

/// Arguments for HostGlue.node_panel!
/// Roc signature: { active_bg : U64, align : U8, bg : U64, border_bottom : U32, border_color : U64, border_left : U32, border_right : U32, border_top : U32, builder : U64, disabled_bg : U64, disabled_fg : U64, fg : U64, focus_color : U64, font_face : U8, font_size : U32, font_weight : U32, gap : U32, grow : Bool, height : U32, height_kind : U8, hover_bg : U64, justify : U8, label : Str, max_height : U32, max_height_kind : U8, max_width : U32, max_width_kind : U8, min_height : U32, min_height_kind : U8, min_width : U32, min_width_kind : U8, overflow_x : U8, overflow_y : U8, padding_bottom : U32, padding_left : U32, padding_right : U32, padding_top : U32, radius : U32, shadow : U32, shadow_alpha : U32, shadow_color : U64, shadow_y : U32, text_overflow : U8, width : U32, width_kind : U8 } => U64
/// Refcounted fields are owned by the hosted function.
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueNodePanelArgs {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub builder: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub shadow_color: u64,
    pub label: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

/// Arguments for HostGlue.node_panel!
/// Roc signature: { active_bg : U64, align : U8, bg : U64, border_bottom : U32, border_color : U64, border_left : U32, border_right : U32, border_top : U32, builder : U64, disabled_bg : U64, disabled_fg : U64, fg : U64, focus_color : U64, font_face : U8, font_size : U32, font_weight : U32, gap : U32, grow : Bool, height : U32, height_kind : U8, hover_bg : U64, justify : U8, label : Str, max_height : U32, max_height_kind : U8, max_width : U32, max_width_kind : U8, min_height : U32, min_height_kind : U8, min_width : U32, min_width_kind : U8, overflow_x : U8, overflow_y : U8, padding_bottom : U32, padding_left : U32, padding_right : U32, padding_top : U32, radius : U32, shadow : U32, shadow_alpha : U32, shadow_color : U64, shadow_y : U32, text_overflow : U8, width : U32, width_kind : U8 } => U64
/// Refcounted fields are owned by the hosted function.
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueNodePanelArgs {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub builder: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub shadow_color: u64,
    pub label: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueNodePanelArgs>() == 208, "HostGlueNodePanelArgs size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueNodePanelArgs>() == 8, "HostGlueNodePanelArgs alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueNodePanelArgs>() == 192, "HostGlueNodePanelArgs size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueNodePanelArgs>() == 8, "HostGlueNodePanelArgs alignment mismatch");

impl HostGlueNodePanelArgs {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.label.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.label.incref(amount); }
    }
}

pub struct HostGlueNodePanelArgsRelease;

unsafe impl RocRelease<HostGlueNodePanelArgs> for HostGlueNodePanelArgsRelease {
    unsafe fn release(value: HostGlueNodePanelArgs, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

/// Arguments for HostGlue.node_scroll!
/// Roc signature: { active_bg : U64, align : U8, axis : U8, bg : U64, border_bottom : U32, border_color : U64, border_left : U32, border_right : U32, border_top : U32, child : U64, disabled_bg : U64, disabled_fg : U64, fg : U64, focus_color : U64, font_face : U8, font_size : U32, font_weight : U32, gap : U32, grow : Bool, height : U32, height_kind : U8, hover_bg : U64, justify : U8, max_height : U32, max_height_kind : U8, max_width : U32, max_width_kind : U8, min_height : U32, min_height_kind : U8, min_width : U32, min_width_kind : U8, name : Str, overflow_x : U8, overflow_y : U8, padding_bottom : U32, padding_left : U32, padding_right : U32, padding_top : U32, radius : U32, shadow : U32, shadow_alpha : U32, shadow_color : U64, shadow_y : U32, text_overflow : U8, width : U32, width_kind : U8 } => U64
/// Refcounted fields are owned by the hosted function.
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueNodeScrollArgs {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub child: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub shadow_color: u64,
    pub name: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub axis: u8,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

/// Arguments for HostGlue.node_scroll!
/// Roc signature: { active_bg : U64, align : U8, axis : U8, bg : U64, border_bottom : U32, border_color : U64, border_left : U32, border_right : U32, border_top : U32, child : U64, disabled_bg : U64, disabled_fg : U64, fg : U64, focus_color : U64, font_face : U8, font_size : U32, font_weight : U32, gap : U32, grow : Bool, height : U32, height_kind : U8, hover_bg : U64, justify : U8, max_height : U32, max_height_kind : U8, max_width : U32, max_width_kind : U8, min_height : U32, min_height_kind : U8, min_width : U32, min_width_kind : U8, name : Str, overflow_x : U8, overflow_y : U8, padding_bottom : U32, padding_left : U32, padding_right : U32, padding_top : U32, radius : U32, shadow : U32, shadow_alpha : U32, shadow_color : U64, shadow_y : U32, text_overflow : U8, width : U32, width_kind : U8 } => U64
/// Refcounted fields are owned by the hosted function.
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueNodeScrollArgs {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub child: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub shadow_color: u64,
    pub name: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub axis: u8,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueNodeScrollArgs>() == 208, "HostGlueNodeScrollArgs size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueNodeScrollArgs>() == 8, "HostGlueNodeScrollArgs alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueNodeScrollArgs>() == 192, "HostGlueNodeScrollArgs size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueNodeScrollArgs>() == 8, "HostGlueNodeScrollArgs alignment mismatch");

impl HostGlueNodeScrollArgs {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.name.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.name.incref(amount); }
    }
}

pub struct HostGlueNodeScrollArgsRelease;

unsafe impl RocRelease<HostGlueNodeScrollArgs> for HostGlueNodeScrollArgsRelease {
    unsafe fn release(value: HostGlueNodeScrollArgs, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

/// Arguments for HostGlue.node_action_button!
/// Roc signature: { active_bg : U64, align : U8, bg : U64, border_bottom : U32, border_color : U64, border_left : U32, border_right : U32, border_top : U32, caption : Str, disabled_bg : U64, disabled_fg : U64, enabled : Bool, fg : U64, focus_color : U64, font_face : U8, font_size : U32, font_weight : U32, gap : U32, grow : Bool, height : U32, height_kind : U8, hover_bg : U64, hover_enter : Bool, hover_exit : Bool, justify : U8, label : Str, max_height : U32, max_height_kind : U8, max_width : U32, max_width_kind : U8, min_height : U32, min_height_kind : U8, min_width : U32, min_width_kind : U8, overflow_x : U8, overflow_y : U8, padding_bottom : U32, padding_left : U32, padding_right : U32, padding_top : U32, radius : U32, role : U8, shadow : U32, shadow_alpha : U32, shadow_color : U64, shadow_y : U32, text_overflow : U8, width : U32, width_kind : U8 } => { hover_enter : U64, hover_exit : U64, id : U64 }
/// Refcounted fields are owned by the hosted function.
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueNodeActionButtonArgs {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub shadow_color: u64,
    pub caption: RocStr,
    pub label: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub enabled: bool,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub hover_enter: bool,
    pub hover_exit: bool,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub role: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

/// Arguments for HostGlue.node_action_button!
/// Roc signature: { active_bg : U64, align : U8, bg : U64, border_bottom : U32, border_color : U64, border_left : U32, border_right : U32, border_top : U32, caption : Str, disabled_bg : U64, disabled_fg : U64, enabled : Bool, fg : U64, focus_color : U64, font_face : U8, font_size : U32, font_weight : U32, gap : U32, grow : Bool, height : U32, height_kind : U8, hover_bg : U64, hover_enter : Bool, hover_exit : Bool, justify : U8, label : Str, max_height : U32, max_height_kind : U8, max_width : U32, max_width_kind : U8, min_height : U32, min_height_kind : U8, min_width : U32, min_width_kind : U8, overflow_x : U8, overflow_y : U8, padding_bottom : U32, padding_left : U32, padding_right : U32, padding_top : U32, radius : U32, role : U8, shadow : U32, shadow_alpha : U32, shadow_color : U64, shadow_y : U32, text_overflow : U8, width : U32, width_kind : U8 } => { hover_enter : U64, hover_exit : U64, id : U64 }
/// Refcounted fields are owned by the hosted function.
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueNodeActionButtonArgs {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub shadow_color: u64,
    pub caption: RocStr,
    pub label: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub enabled: bool,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub hover_enter: bool,
    pub hover_exit: bool,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub role: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueNodeActionButtonArgs>() == 224, "HostGlueNodeActionButtonArgs size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueNodeActionButtonArgs>() == 8, "HostGlueNodeActionButtonArgs alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueNodeActionButtonArgs>() == 200, "HostGlueNodeActionButtonArgs size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueNodeActionButtonArgs>() == 8, "HostGlueNodeActionButtonArgs alignment mismatch");

impl HostGlueNodeActionButtonArgs {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.caption.decref(roc_host); }
        unsafe { value.label.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.caption.incref(amount); }
        unsafe { value.label.incref(amount); }
    }
}

pub struct HostGlueNodeActionButtonArgsRelease;

unsafe impl RocRelease<HostGlueNodeActionButtonArgs> for HostGlueNodeActionButtonArgsRelease {
    unsafe fn release(value: HostGlueNodeActionButtonArgs, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

/// Arguments for HostGlue.node_virtual_item!
/// Roc signature: U64, U64 => U64
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueNodeVirtualItemArgs {
    pub arg0: u64,
    pub arg1: u64,
}

/// Arguments for HostGlue.node_virtual_list!
/// Roc signature: { active_bg : U64, align : U8, bg : U64, border_bottom : U32, border_color : U64, border_left : U32, border_right : U32, border_top : U32, builder : U64, count : U64, disabled_bg : U64, disabled_fg : U64, fg : U64, first : U64, focus_color : U64, font_face : U8, font_size : U32, font_weight : U32, gap : U32, grow : Bool, height : U32, height_kind : U8, hover_bg : U64, instance : U64, justify : U8, max_height : U32, max_height_kind : U8, max_width : U32, max_width_kind : U8, min_height : U32, min_height_kind : U8, min_width : U32, min_width_kind : U8, name : Str, notify : Bool, overflow_x : U8, overflow_y : U8, padding_bottom : U32, padding_left : U32, padding_right : U32, padding_top : U32, radius : U32, row_gap : U32, row_height : U32, shadow : U32, shadow_alpha : U32, shadow_color : U64, shadow_y : U32, text_overflow : U8, width : U32, width_kind : U8 } => U64
/// Refcounted fields are owned by the hosted function.
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueNodeVirtualListArgs {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub builder: u64,
    pub count: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub first: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub instance: u64,
    pub shadow_color: u64,
    pub name: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub row_gap: u32,
    pub row_height: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub notify: bool,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

/// Arguments for HostGlue.node_virtual_list!
/// Roc signature: { active_bg : U64, align : U8, bg : U64, border_bottom : U32, border_color : U64, border_left : U32, border_right : U32, border_top : U32, builder : U64, count : U64, disabled_bg : U64, disabled_fg : U64, fg : U64, first : U64, focus_color : U64, font_face : U8, font_size : U32, font_weight : U32, gap : U32, grow : Bool, height : U32, height_kind : U8, hover_bg : U64, instance : U64, justify : U8, max_height : U32, max_height_kind : U8, max_width : U32, max_width_kind : U8, min_height : U32, min_height_kind : U8, min_width : U32, min_width_kind : U8, name : Str, notify : Bool, overflow_x : U8, overflow_y : U8, padding_bottom : U32, padding_left : U32, padding_right : U32, padding_top : U32, radius : U32, row_gap : U32, row_height : U32, shadow : U32, shadow_alpha : U32, shadow_color : U64, shadow_y : U32, text_overflow : U8, width : U32, width_kind : U8 } => U64
/// Refcounted fields are owned by the hosted function.
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueNodeVirtualListArgs {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub builder: u64,
    pub count: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub first: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub instance: u64,
    pub shadow_color: u64,
    pub name: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub row_gap: u32,
    pub row_height: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub notify: bool,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueNodeVirtualListArgs>() == 240, "HostGlueNodeVirtualListArgs size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueNodeVirtualListArgs>() == 8, "HostGlueNodeVirtualListArgs alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueNodeVirtualListArgs>() == 224, "HostGlueNodeVirtualListArgs size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueNodeVirtualListArgs>() == 8, "HostGlueNodeVirtualListArgs alignment mismatch");

impl HostGlueNodeVirtualListArgs {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.name.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.name.incref(amount); }
    }
}

pub struct HostGlueNodeVirtualListArgsRelease;

unsafe impl RocRelease<HostGlueNodeVirtualListArgs> for HostGlueNodeVirtualListArgsRelease {
    unsafe fn release(value: HostGlueNodeVirtualListArgs, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

/// Arguments for HostGlue.node_checkbox!
/// Roc signature: { active_bg : U64, align : U8, bg : U64, border_bottom : U32, border_color : U64, border_left : U32, border_right : U32, border_top : U32, box_bg : U64, box_border : U64, box_checked_bg : U64, checked : Bool, disabled_bg : U64, disabled_fg : U64, enabled : Bool, fg : U64, focus_color : U64, font_face : U8, font_size : U32, font_weight : U32, gap : U32, grow : Bool, height : U32, height_kind : U8, hover_bg : U64, justify : U8, label : Str, mark_color : U64, max_height : U32, max_height_kind : U8, max_width : U32, max_width_kind : U8, min_height : U32, min_height_kind : U8, min_width : U32, min_width_kind : U8, overflow_x : U8, overflow_y : U8, padding_bottom : U32, padding_left : U32, padding_right : U32, padding_top : U32, radius : U32, shadow : U32, shadow_alpha : U32, shadow_color : U64, shadow_y : U32, text_overflow : U8, width : U32, width_kind : U8 } => U64
/// Refcounted fields are owned by the hosted function.
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueNodeCheckboxArgs {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub box_bg: u64,
    pub box_border: u64,
    pub box_checked_bg: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub mark_color: u64,
    pub shadow_color: u64,
    pub label: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub checked: bool,
    pub enabled: bool,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

/// Arguments for HostGlue.node_checkbox!
/// Roc signature: { active_bg : U64, align : U8, bg : U64, border_bottom : U32, border_color : U64, border_left : U32, border_right : U32, border_top : U32, box_bg : U64, box_border : U64, box_checked_bg : U64, checked : Bool, disabled_bg : U64, disabled_fg : U64, enabled : Bool, fg : U64, focus_color : U64, font_face : U8, font_size : U32, font_weight : U32, gap : U32, grow : Bool, height : U32, height_kind : U8, hover_bg : U64, justify : U8, label : Str, mark_color : U64, max_height : U32, max_height_kind : U8, max_width : U32, max_width_kind : U8, min_height : U32, min_height_kind : U8, min_width : U32, min_width_kind : U8, overflow_x : U8, overflow_y : U8, padding_bottom : U32, padding_left : U32, padding_right : U32, padding_top : U32, radius : U32, shadow : U32, shadow_alpha : U32, shadow_color : U64, shadow_y : U32, text_overflow : U8, width : U32, width_kind : U8 } => U64
/// Refcounted fields are owned by the hosted function.
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueNodeCheckboxArgs {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub box_bg: u64,
    pub box_border: u64,
    pub box_checked_bg: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub mark_color: u64,
    pub shadow_color: u64,
    pub label: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub checked: bool,
    pub enabled: bool,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueNodeCheckboxArgs>() == 232, "HostGlueNodeCheckboxArgs size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueNodeCheckboxArgs>() == 8, "HostGlueNodeCheckboxArgs alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueNodeCheckboxArgs>() == 216, "HostGlueNodeCheckboxArgs size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueNodeCheckboxArgs>() == 8, "HostGlueNodeCheckboxArgs alignment mismatch");

impl HostGlueNodeCheckboxArgs {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.label.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.label.incref(amount); }
    }
}

pub struct HostGlueNodeCheckboxArgsRelease;

unsafe impl RocRelease<HostGlueNodeCheckboxArgs> for HostGlueNodeCheckboxArgsRelease {
    unsafe fn release(value: HostGlueNodeCheckboxArgs, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

/// Arguments for HostGlue.node_textarea!
/// Roc signature: { active_bg : U64, align : U8, bg : U64, border_bottom : U32, border_color : U64, border_left : U32, border_right : U32, border_top : U32, disabled_bg : U64, disabled_fg : U64, enabled : Bool, fg : U64, focus_color : U64, font_face : U8, font_size : U32, font_weight : U32, gap : U32, grow : Bool, height : U32, height_kind : U8, hover_bg : U64, justify : U8, label : Str, max_height : U32, max_height_kind : U8, max_width : U32, max_width_kind : U8, min_height : U32, min_height_kind : U8, min_width : U32, min_width_kind : U8, overflow_x : U8, overflow_y : U8, padding_bottom : U32, padding_left : U32, padding_right : U32, padding_top : U32, placeholder : Str, radius : U32, read_only : Bool, shadow : U32, shadow_alpha : U32, shadow_color : U64, shadow_y : U32, text_overflow : U8, value : Str, width : U32, width_kind : U8 } => U64
/// Refcounted fields are owned by the hosted function.
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueNodeTextareaArgs {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub shadow_color: u64,
    pub label: RocStr,
    pub placeholder: RocStr,
    pub value: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub enabled: bool,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub read_only: bool,
    pub text_overflow: u8,
    pub width_kind: u8,
}

/// Arguments for HostGlue.node_textarea!
/// Roc signature: { active_bg : U64, align : U8, bg : U64, border_bottom : U32, border_color : U64, border_left : U32, border_right : U32, border_top : U32, disabled_bg : U64, disabled_fg : U64, enabled : Bool, fg : U64, focus_color : U64, font_face : U8, font_size : U32, font_weight : U32, gap : U32, grow : Bool, height : U32, height_kind : U8, hover_bg : U64, justify : U8, label : Str, max_height : U32, max_height_kind : U8, max_width : U32, max_width_kind : U8, min_height : U32, min_height_kind : U8, min_width : U32, min_width_kind : U8, overflow_x : U8, overflow_y : U8, padding_bottom : U32, padding_left : U32, padding_right : U32, padding_top : U32, placeholder : Str, radius : U32, read_only : Bool, shadow : U32, shadow_alpha : U32, shadow_color : U64, shadow_y : U32, text_overflow : U8, value : Str, width : U32, width_kind : U8 } => U64
/// Refcounted fields are owned by the hosted function.
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueNodeTextareaArgs {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub shadow_color: u64,
    pub label: RocStr,
    pub placeholder: RocStr,
    pub value: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub enabled: bool,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub read_only: bool,
    pub text_overflow: u8,
    pub width_kind: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueNodeTextareaArgs>() == 248, "HostGlueNodeTextareaArgs size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueNodeTextareaArgs>() == 8, "HostGlueNodeTextareaArgs alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueNodeTextareaArgs>() == 208, "HostGlueNodeTextareaArgs size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueNodeTextareaArgs>() == 8, "HostGlueNodeTextareaArgs alignment mismatch");

impl HostGlueNodeTextareaArgs {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.label.decref(roc_host); }
        unsafe { value.placeholder.decref(roc_host); }
        unsafe { value.value.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.label.incref(amount); }
        unsafe { value.placeholder.incref(amount); }
        unsafe { value.value.incref(amount); }
    }
}

pub struct HostGlueNodeTextareaArgsRelease;

unsafe impl RocRelease<HostGlueNodeTextareaArgs> for HostGlueNodeTextareaArgsRelease {
    unsafe fn release(value: HostGlueNodeTextareaArgs, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

/// Arguments for HostGlue.node_image!
/// Roc signature: { active_bg : U64, align : U8, bg : U64, border_bottom : U32, border_color : U64, border_left : U32, border_right : U32, border_top : U32, bytes : List(U8), disabled_bg : U64, disabled_fg : U64, fg : U64, fit : U8, focus_color : U64, font_face : U8, font_size : U32, font_weight : U32, format : U8, gap : U32, grayscale : Bool, grow : Bool, height : U32, height_kind : U8, hover_bg : U64, justify : U8, label : Str, max_height : U32, max_height_kind : U8, max_width : U32, max_width_kind : U8, min_height : U32, min_height_kind : U8, min_width : U32, min_width_kind : U8, overflow_x : U8, overflow_y : U8, padding_bottom : U32, padding_left : U32, padding_right : U32, padding_top : U32, radius : U32, shadow : U32, shadow_alpha : U32, shadow_color : U64, shadow_y : U32, text_overflow : U8, width : U32, width_kind : U8 } => U64
/// Refcounted fields are owned by the hosted function.
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueNodeImageArgs {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub shadow_color: u64,
    pub bytes: RocListWith<u8, false>,
    pub label: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub fit: u8,
    pub font_face: u8,
    pub format: u8,
    pub grayscale: bool,
    pub grow: bool,
    pub height_kind: u8,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

/// Arguments for HostGlue.node_image!
/// Roc signature: { active_bg : U64, align : U8, bg : U64, border_bottom : U32, border_color : U64, border_left : U32, border_right : U32, border_top : U32, bytes : List(U8), disabled_bg : U64, disabled_fg : U64, fg : U64, fit : U8, focus_color : U64, font_face : U8, font_size : U32, font_weight : U32, format : U8, gap : U32, grayscale : Bool, grow : Bool, height : U32, height_kind : U8, hover_bg : U64, justify : U8, label : Str, max_height : U32, max_height_kind : U8, max_width : U32, max_width_kind : U8, min_height : U32, min_height_kind : U8, min_width : U32, min_width_kind : U8, overflow_x : U8, overflow_y : U8, padding_bottom : U32, padding_left : U32, padding_right : U32, padding_top : U32, radius : U32, shadow : U32, shadow_alpha : U32, shadow_color : U64, shadow_y : U32, text_overflow : U8, width : U32, width_kind : U8 } => U64
/// Refcounted fields are owned by the hosted function.
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueNodeImageArgs {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub shadow_color: u64,
    pub bytes: RocListWith<u8, false>,
    pub label: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub fit: u8,
    pub font_face: u8,
    pub format: u8,
    pub grayscale: bool,
    pub grow: bool,
    pub height_kind: u8,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueNodeImageArgs>() == 224, "HostGlueNodeImageArgs size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueNodeImageArgs>() == 8, "HostGlueNodeImageArgs alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueNodeImageArgs>() == 200, "HostGlueNodeImageArgs size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueNodeImageArgs>() == 8, "HostGlueNodeImageArgs alignment mismatch");

impl HostGlueNodeImageArgs {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.bytes.decref(roc_host); }
        unsafe { value.label.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.bytes.incref(amount); }
        unsafe { value.label.incref(amount); }
    }
}

pub struct HostGlueNodeImageArgsRelease;

unsafe impl RocRelease<HostGlueNodeImageArgs> for HostGlueNodeImageArgsRelease {
    unsafe fn release(value: HostGlueNodeImageArgs, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

/// Arguments for HostGlue.node_canvas!
/// Roc signature: { bg : U64, border_color : U64, border_width : U32, grow : Bool, height : U32, height_kind : U8, hover : Bool, label : Str, primitives : List({ align : U8, fill : U64, height : U32, key : U64, kind : U8, label : Str, radius : U32, stroke : U64, stroke_width : U32, text : Str, text_size : U32, width : U32, x : I32, x2 : I32, y : I32, y2 : I32 }), radius : U32, wheel : Bool, width : U32, width_kind : U8 } => U64
/// Refcounted fields are owned by the hosted function.
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueNodeCanvasArgs {
    pub bg: u64,
    pub border_color: u64,
    pub label: RocStr,
    pub primitives: RocList<AnonStructF74678f34f8b3cbf>,
    pub border_width: u32,
    pub height: u32,
    pub radius: u32,
    pub width: u32,
    pub grow: bool,
    pub height_kind: u8,
    pub hover: bool,
    pub wheel: bool,
    pub width_kind: u8,
}

/// Arguments for HostGlue.node_canvas!
/// Roc signature: { bg : U64, border_color : U64, border_width : U32, grow : Bool, height : U32, height_kind : U8, hover : Bool, label : Str, primitives : List({ align : U8, fill : U64, height : U32, key : U64, kind : U8, label : Str, radius : U32, stroke : U64, stroke_width : U32, text : Str, text_size : U32, width : U32, x : I32, x2 : I32, y : I32, y2 : I32 }), radius : U32, wheel : Bool, width : U32, width_kind : U8 } => U64
/// Refcounted fields are owned by the hosted function.
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueNodeCanvasArgs {
    pub bg: u64,
    pub border_color: u64,
    pub label: RocStr,
    pub primitives: RocList<AnonStructF74678f34f8b3cbf>,
    pub border_width: u32,
    pub height: u32,
    pub radius: u32,
    pub width: u32,
    pub grow: bool,
    pub height_kind: u8,
    pub hover: bool,
    pub wheel: bool,
    pub width_kind: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueNodeCanvasArgs>() == 88, "HostGlueNodeCanvasArgs size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueNodeCanvasArgs>() == 8, "HostGlueNodeCanvasArgs alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueNodeCanvasArgs>() == 64, "HostGlueNodeCanvasArgs size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueNodeCanvasArgs>() == 8, "HostGlueNodeCanvasArgs alignment mismatch");

impl HostGlueNodeCanvasArgs {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.label.decref(roc_host); }
        unsafe { decref_list_of_anon_struct_f74678f34f8b3cbf(value.primitives, roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.label.incref(amount); }
        unsafe { value.primitives.incref(amount); }
    }
}

pub struct HostGlueNodeCanvasArgsRelease;

unsafe impl RocRelease<HostGlueNodeCanvasArgs> for HostGlueNodeCanvasArgsRelease {
    unsafe fn release(value: HostGlueNodeCanvasArgs, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

/// Arguments for HostGlue.node_split!
/// Roc signature: { active_bg : U64, align : U8, axis : U8, bg : U64, border_bottom : U32, border_color : U64, border_left : U32, border_right : U32, border_top : U32, builder : U64, collapsed : Bool, collapsible : Bool, disabled_bg : U64, disabled_fg : U64, fg : U64, focus_color : U64, font_face : U8, font_size : U32, font_weight : U32, gap : U32, grow : Bool, height : U32, height_kind : U8, hover_bg : U64, justify : U8, keys : List({ focus : Bool, keys : Str }), label : Str, max : U32, max_height : U32, max_height_kind : U8, max_width : U32, max_width_kind : U8, min : U32, min_height : U32, min_height_kind : U8, min_width : U32, min_width_kind : U8, overflow_x : U8, overflow_y : U8, padding_bottom : U32, padding_left : U32, padding_right : U32, padding_top : U32, radius : U32, shadow : U32, shadow_alpha : U32, shadow_color : U64, shadow_y : U32, side : U8, size : U32, text_overflow : U8, thickness : U32, width : U32, width_kind : U8 } => { id : U64, shortcut : U64 }
/// Refcounted fields are owned by the hosted function.
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueNodeSplitArgs {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub builder: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub shadow_color: u64,
    pub keys: RocList<AnonStruct1d0ac13b7d648fa3>,
    pub label: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub size: u32,
    pub thickness: u32,
    pub width: u32,
    pub align: u8,
    pub axis: u8,
    pub collapsed: bool,
    pub collapsible: bool,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub side: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

/// Arguments for HostGlue.node_split!
/// Roc signature: { active_bg : U64, align : U8, axis : U8, bg : U64, border_bottom : U32, border_color : U64, border_left : U32, border_right : U32, border_top : U32, builder : U64, collapsed : Bool, collapsible : Bool, disabled_bg : U64, disabled_fg : U64, fg : U64, focus_color : U64, font_face : U8, font_size : U32, font_weight : U32, gap : U32, grow : Bool, height : U32, height_kind : U8, hover_bg : U64, justify : U8, keys : List({ focus : Bool, keys : Str }), label : Str, max : U32, max_height : U32, max_height_kind : U8, max_width : U32, max_width_kind : U8, min : U32, min_height : U32, min_height_kind : U8, min_width : U32, min_width_kind : U8, overflow_x : U8, overflow_y : U8, padding_bottom : U32, padding_left : U32, padding_right : U32, padding_top : U32, radius : U32, shadow : U32, shadow_alpha : U32, shadow_color : U64, shadow_y : U32, side : U8, size : U32, text_overflow : U8, thickness : U32, width : U32, width_kind : U8 } => { id : U64, shortcut : U64 }
/// Refcounted fields are owned by the hosted function.
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueNodeSplitArgs {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub builder: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub shadow_color: u64,
    pub keys: RocList<AnonStruct1d0ac13b7d648fa3>,
    pub label: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub size: u32,
    pub thickness: u32,
    pub width: u32,
    pub align: u8,
    pub axis: u8,
    pub collapsed: bool,
    pub collapsible: bool,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub side: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueNodeSplitArgs>() == 248, "HostGlueNodeSplitArgs size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueNodeSplitArgs>() == 8, "HostGlueNodeSplitArgs alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueNodeSplitArgs>() == 224, "HostGlueNodeSplitArgs size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueNodeSplitArgs>() == 8, "HostGlueNodeSplitArgs alignment mismatch");

impl HostGlueNodeSplitArgs {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { decref_list_of_anon_struct1d0ac13b7d648fa3(value.keys, roc_host); }
        unsafe { value.label.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.keys.incref(amount); }
        unsafe { value.label.incref(amount); }
    }
}

pub struct HostGlueNodeSplitArgsRelease;

unsafe impl RocRelease<HostGlueNodeSplitArgs> for HostGlueNodeSplitArgsRelease {
    unsafe fn release(value: HostGlueNodeSplitArgs, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

/// Arguments for HostGlue.virtual_window!
/// Roc signature: { count : U64, instance : U64, row_height : U32, scroll_align : U8, scroll_row : U64, scroll_serial : U64 } => { end : U64, first : U64 }
/// Refcounted fields are owned by the hosted function.
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueVirtualWindowArgs {
    pub count: u64,
    pub instance: u64,
    pub scroll_row: u64,
    pub scroll_serial: u64,
    pub row_height: u32,
    pub scroll_align: u8,
}

/// Arguments for HostGlue.virtual_window!
/// Roc signature: { count : U64, instance : U64, row_height : U32, scroll_align : U8, scroll_row : U64, scroll_serial : U64 } => { end : U64, first : U64 }
/// Refcounted fields are owned by the hosted function.
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueVirtualWindowArgs {
    pub count: u64,
    pub instance: u64,
    pub scroll_row: u64,
    pub scroll_serial: u64,
    pub row_height: u32,
    pub scroll_align: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueVirtualWindowArgs>() == 40, "HostGlueVirtualWindowArgs size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueVirtualWindowArgs>() == 8, "HostGlueVirtualWindowArgs alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueVirtualWindowArgs>() == 40, "HostGlueVirtualWindowArgs size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueVirtualWindowArgs>() == 8, "HostGlueVirtualWindowArgs alignment mismatch");

impl HostGlueVirtualWindowArgs {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        let _ = value;
        let _ = roc_host;
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = value;
        let _ = amount;
    }
}

pub struct HostGlueVirtualWindowArgsRelease;

unsafe impl RocRelease<HostGlueVirtualWindowArgs> for HostGlueVirtualWindowArgsRelease {
    unsafe fn release(value: HostGlueVirtualWindowArgs, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

/// Arguments for HostGlue.node_text_input!
/// Roc signature: { active_bg : U64, align : U8, bg : U64, border_bottom : U32, border_color : U64, border_left : U32, border_right : U32, border_top : U32, disabled_bg : U64, disabled_fg : U64, enabled : Bool, fg : U64, focus_color : U64, font_face : U8, font_size : U32, font_weight : U32, gap : U32, grow : Bool, height : U32, height_kind : U8, hover_bg : U64, justify : U8, label : Str, max_height : U32, max_height_kind : U8, max_width : U32, max_width_kind : U8, min_height : U32, min_height_kind : U8, min_width : U32, min_width_kind : U8, overflow_x : U8, overflow_y : U8, padding_bottom : U32, padding_left : U32, padding_right : U32, padding_top : U32, placeholder : Str, radius : U32, shadow : U32, shadow_alpha : U32, shadow_color : U64, shadow_y : U32, text_overflow : U8, value : Str, width : U32, width_kind : U8 } => { change : U64, id : U64, submit : U64 }
/// Refcounted fields are owned by the hosted function.
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueNodeTextInputArgs {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub shadow_color: u64,
    pub label: RocStr,
    pub placeholder: RocStr,
    pub value: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub enabled: bool,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

/// Arguments for HostGlue.node_text_input!
/// Roc signature: { active_bg : U64, align : U8, bg : U64, border_bottom : U32, border_color : U64, border_left : U32, border_right : U32, border_top : U32, disabled_bg : U64, disabled_fg : U64, enabled : Bool, fg : U64, focus_color : U64, font_face : U8, font_size : U32, font_weight : U32, gap : U32, grow : Bool, height : U32, height_kind : U8, hover_bg : U64, justify : U8, label : Str, max_height : U32, max_height_kind : U8, max_width : U32, max_width_kind : U8, min_height : U32, min_height_kind : U8, min_width : U32, min_width_kind : U8, overflow_x : U8, overflow_y : U8, padding_bottom : U32, padding_left : U32, padding_right : U32, padding_top : U32, placeholder : Str, radius : U32, shadow : U32, shadow_alpha : U32, shadow_color : U64, shadow_y : U32, text_overflow : U8, value : Str, width : U32, width_kind : U8 } => { change : U64, id : U64, submit : U64 }
/// Refcounted fields are owned by the hosted function.
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueNodeTextInputArgs {
    pub active_bg: u64,
    pub bg: u64,
    pub border_color: u64,
    pub disabled_bg: u64,
    pub disabled_fg: u64,
    pub fg: u64,
    pub focus_color: u64,
    pub hover_bg: u64,
    pub shadow_color: u64,
    pub label: RocStr,
    pub placeholder: RocStr,
    pub value: RocStr,
    pub border_bottom: u32,
    pub border_left: u32,
    pub border_right: u32,
    pub border_top: u32,
    pub font_size: u32,
    pub font_weight: u32,
    pub gap: u32,
    pub height: u32,
    pub max_height: u32,
    pub max_width: u32,
    pub min_height: u32,
    pub min_width: u32,
    pub padding_bottom: u32,
    pub padding_left: u32,
    pub padding_right: u32,
    pub padding_top: u32,
    pub radius: u32,
    pub shadow: u32,
    pub shadow_alpha: u32,
    pub shadow_y: u32,
    pub width: u32,
    pub align: u8,
    pub enabled: bool,
    pub font_face: u8,
    pub grow: bool,
    pub height_kind: u8,
    pub justify: u8,
    pub max_height_kind: u8,
    pub max_width_kind: u8,
    pub min_height_kind: u8,
    pub min_width_kind: u8,
    pub overflow_x: u8,
    pub overflow_y: u8,
    pub text_overflow: u8,
    pub width_kind: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueNodeTextInputArgs>() == 248, "HostGlueNodeTextInputArgs size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueNodeTextInputArgs>() == 8, "HostGlueNodeTextInputArgs alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueNodeTextInputArgs>() == 208, "HostGlueNodeTextInputArgs size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueNodeTextInputArgs>() == 8, "HostGlueNodeTextInputArgs alignment mismatch");

impl HostGlueNodeTextInputArgs {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.label.decref(roc_host); }
        unsafe { value.placeholder.decref(roc_host); }
        unsafe { value.value.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.label.incref(amount); }
        unsafe { value.placeholder.incref(amount); }
        unsafe { value.value.incref(amount); }
    }
}

pub struct HostGlueNodeTextInputArgsRelease;

unsafe impl RocRelease<HostGlueNodeTextInputArgs> for HostGlueNodeTextInputArgsRelease {
    unsafe fn release(value: HostGlueNodeTextInputArgs, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

/// Arguments for HostGlue.sqlite_open_read!
/// Roc signature: Resource.Handle([DirReadResource]), Str => Try(Resource.Handle([SqliteReadResource]), { code : U8, message : Str })
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueSqliteOpenReadArgs {
    pub arg0: *mut u64,
    pub arg1: RocStr,
}

/// Arguments for HostGlue.sqlite_open_file_read!
/// Roc signature: Resource.Handle([FileReadResource]) => Try(Resource.Handle([SqliteReadResource]), { code : U8, message : Str })
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueSqliteOpenFileReadArgs {
    pub arg0: *mut u64,
}

/// Arguments for HostGlue.sqlite_watch!
/// Roc signature: Resource.Handle([SqliteReadResource]) => Try(Resource.Handle([WatchResource]), { code : U8, message : Str })
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueSqliteWatchArgs {
    pub arg0: *mut u64,
}

/// Arguments for HostGlue.sqlite_query!
/// Roc signature: Resource.Handle([SqliteReadResource]), { page_rows : U64, params : List({ bytes : List(U8), integer : I64, kind : U8, real : F64, text : Str }), sql : Str } => Try({ columns : List(Str), more : Bool, rows : List(List({ bytes : List(U8), integer : I64, kind : U8, real : F64, text : Str })) }, { code : U8, message : Str })
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueSqliteQueryArgs {
    pub arg0: *mut u64,
    pub arg1: AnonStructAd07206fc55b9842,
}

/// Arguments for InternalFiles.read_utf8!
/// Roc signature: Resource.Handle([DirReadWriteResource]), Str => Try({ found : Bool, value : Str }, { code : U8, message : Str })
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct InternalFilesReadUtf8Args {
    pub arg0: *mut u64,
    pub arg1: RocStr,
}

/// Arguments for InternalFiles.write_utf8_atomic!
/// Roc signature: Resource.Handle([DirReadWriteResource]), Str, Str => Try({}, { code : U8, message : Str })
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct InternalFilesWriteUtf8AtomicArgs {
    pub arg0: *mut u64,
    pub arg1: RocStr,
    pub arg2: RocStr,
}

/// Arguments for HostGlue.clipboard_read_text!
/// Roc signature: Resource.Handle([ClipboardResource]) => Try({ sequence : U64, text : Str }, { code : U8, message : Str })
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueClipboardReadTextArgs {
    pub arg0: *mut u64,
}

/// Arguments for HostGlue.clipboard_write_text!
/// Roc signature: Resource.Handle([ClipboardResource]), Str => Try({}, { code : U8, message : Str })
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueClipboardWriteTextArgs {
    pub arg0: *mut u64,
    pub arg1: RocStr,
}

/// Arguments for HostGlue.audio_load!
/// Roc signature: Resource.Handle([AudioOutputResource]), Resource.Handle([DirReadResource]), Str => Try({ duration_ms : U64, track : Resource.Handle([AudioTrackResource]) }, { code : U8, message : Str })
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueAudioLoadArgs {
    pub arg0: *mut u64,
    pub arg1: *mut u64,
    pub arg2: RocStr,
}

/// Arguments for HostGlue.audio_play!
/// Roc signature: Resource.Handle([AudioTrackResource]) => Try({}, { code : U8, message : Str })
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueAudioPlayArgs {
    pub arg0: *mut u64,
}

/// Arguments for HostGlue.audio_pause!
/// Roc signature: Resource.Handle([AudioTrackResource]) => Try({}, { code : U8, message : Str })
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueAudioPauseArgs {
    pub arg0: *mut u64,
}

/// Arguments for HostGlue.audio_seek!
/// Roc signature: Resource.Handle([AudioTrackResource]), U64 => Try({}, { code : U8, message : Str })
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueAudioSeekArgs {
    pub arg0: *mut u64,
    pub arg1: u64,
}

/// Arguments for HostGlue.audio_status!
/// Roc signature: Resource.Handle([AudioTrackResource]) => Try({ duration_ms : U64, position_ms : U64, state : U8 }, { code : U8, message : Str })
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueAudioStatusArgs {
    pub arg0: *mut u64,
}

/// Arguments for HostGlue.audio_stop!
/// Roc signature: Resource.Handle([AudioTrackResource]) => Try({}, { code : U8, message : Str })
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueAudioStopArgs {
    pub arg0: *mut u64,
}

/// Arguments for HostGlue.tcp_read_up_to!
/// Roc signature: Resource.Handle([TcpStreamResource]), U64 => Try(List(U8), U8)
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueTcpReadUpToArgs {
    pub arg0: *mut u64,
    pub arg1: u64,
}

/// Arguments for HostGlue.tcp_write_all!
/// Roc signature: Resource.Handle([TcpStreamResource]), List(U8) => Try({}, U8)
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueTcpWriteAllArgs {
    pub arg0: *mut u64,
    pub arg1: RocListWith<u8, false>,
}

/// Arguments for HostGlue.tcp_close!
/// Roc signature: Resource.Handle([TcpStreamResource]) => Try({}, U8)
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueTcpCloseArgs {
    pub arg0: *mut u64,
}

/// Arguments for HostGlue.process_spawn!
/// Roc signature: Resource.Handle([ProcessGrantResource]), { columns : U16, rows : U16 } => Try(Resource.Handle([PtyResource]), [AcquireProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), CancelProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), ReadProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), ResizeProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), SpawnProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), WriteProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported])])
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueProcessSpawnArgs {
    pub arg0: *mut u64,
    pub arg1: AnonStruct93136bf334c2a2fc,
}

/// Arguments for HostGlue.process_read!
/// Roc signature: Resource.Handle([PtyResource]), U32 => Try([Canceled, Data(List(U8)), EndOfFile], [AcquireProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), CancelProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), ReadProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), ResizeProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), SpawnProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), WriteProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported])])
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueProcessReadArgs {
    pub arg0: *mut u64,
    pub arg1: u32,
}

/// Arguments for HostGlue.process_write!
/// Roc signature: Resource.Handle([PtyResource]), List(U8) => Try(U32, [AcquireProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), CancelProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), ReadProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), ResizeProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), SpawnProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), WriteProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported])])
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueProcessWriteArgs {
    pub arg0: *mut u64,
    pub arg1: RocListWith<u8, false>,
}

/// Arguments for HostGlue.process_resize!
/// Roc signature: Resource.Handle([PtyResource]), { columns : U16, rows : U16 } => Try({}, [AcquireProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), CancelProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), ReadProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), ResizeProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), SpawnProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), WriteProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported])])
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueProcessResizeArgs {
    pub arg0: *mut u64,
    pub arg1: AnonStruct93136bf334c2a2fc,
}

/// Arguments for HostGlue.process_cancel!
/// Roc signature: Resource.Handle([PtyResource]) => Try([AlreadyStopped, Canceled], [AcquireProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), CancelProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), ReadProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), ResizeProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), SpawnProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), WriteProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported])])
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueProcessCancelArgs {
    pub arg0: *mut u64,
}

/// Arguments for HostGlue.device_discover!
/// Roc signature: Resource.Handle([DeviceGrantResource]) => Try(List({ manufacturer : Str, product : Str, product_id : U16, vendor_id : U16 }), [AcquireDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported]), CloseDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported]), ConnectDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported]), DiscoverDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported]), TransactDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported])])
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueDeviceDiscoverArgs {
    pub arg0: *mut u64,
}

/// Arguments for HostGlue.device_connect!
/// Roc signature: Resource.Handle([DeviceGrantResource]) => Try(Resource.Handle([DeviceConnectionResource]), [AcquireDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported]), CloseDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported]), ConnectDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported]), DiscoverDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported]), TransactDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported])])
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueDeviceConnectArgs {
    pub arg0: *mut u64,
}

/// Arguments for HostGlue.device_transact!
/// Roc signature: Resource.Handle([DeviceConnectionResource]), List(U8) => Try(List(U8), [AcquireDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported]), CloseDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported]), ConnectDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported]), DiscoverDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported]), TransactDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported])])
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueDeviceTransactArgs {
    pub arg0: *mut u64,
    pub arg1: RocListWith<u8, false>,
}

/// Arguments for HostGlue.device_close!
/// Roc signature: Resource.Handle([DeviceConnectionResource]) => Try({}, [AcquireDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported]), CloseDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported]), ConnectDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported]), DiscoverDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported]), TransactDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported])])
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueDeviceCloseArgs {
    pub arg0: *mut u64,
}

/// Arguments for HostGlue.system_sample!
/// Roc signature: Resource.Handle([SystemSamplerResource]) => Try({ cpu_available : Bool, cpu_tenths : U16, disk_available : Bool, disk_read_bytes : U64, disk_written_bytes : U64, memory_available : Bool, memory_total_bytes : U64, memory_used_bytes : U64, network_available : Bool, network_received_bytes : U64, network_transmitted_bytes : U64, processes : List({ cpu_tenths : U16, memory_bytes : U64, name : Str, pid : U64 }), processes_available : Bool, sequence : U64 }, U8)
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueSystemSampleArgs {
    pub arg0: *mut u64,
}

/// Arguments for HostGlue.system_close!
/// Roc signature: Resource.Handle([SystemSamplerResource]) => Try({}, U8)
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueSystemCloseArgs {
    pub arg0: *mut u64,
}

/// Arguments for HostGlue.image_inspect!
/// Roc signature: List(U8), U8 => Try({ height : U32, width : U32 }, U8)
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueImageInspectArgs {
    pub arg0: RocListWith<u8, false>,
    pub arg1: u8,
}

/// Arguments for HostGlue.assets_open!
/// Roc signature: { asset_set : Str, content_hash : Str, content_mode : U8, content_version : U32, manifest_required : Bool, root : Str, root_kind : U8, schema : U32 } => Try(Resource.Handle([AssetStoreResource]), U8)
/// Refcounted fields are owned by the hosted function.
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueAssetsOpenArgs {
    pub asset_set: RocStr,
    pub content_hash: RocStr,
    pub root: RocStr,
    pub content_version: u32,
    pub schema: u32,
    pub content_mode: u8,
    pub manifest_required: bool,
    pub root_kind: u8,
}

/// Arguments for HostGlue.assets_open!
/// Roc signature: { asset_set : Str, content_hash : Str, content_mode : U8, content_version : U32, manifest_required : Bool, root : Str, root_kind : U8, schema : U32 } => Try(Resource.Handle([AssetStoreResource]), U8)
/// Refcounted fields are owned by the hosted function.
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueAssetsOpenArgs {
    pub asset_set: RocStr,
    pub content_hash: RocStr,
    pub root: RocStr,
    pub content_version: u32,
    pub schema: u32,
    pub content_mode: u8,
    pub manifest_required: bool,
    pub root_kind: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueAssetsOpenArgs>() == 88, "HostGlueAssetsOpenArgs size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueAssetsOpenArgs>() == 8, "HostGlueAssetsOpenArgs alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueAssetsOpenArgs>() == 48, "HostGlueAssetsOpenArgs size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueAssetsOpenArgs>() == 4, "HostGlueAssetsOpenArgs alignment mismatch");

impl HostGlueAssetsOpenArgs {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.asset_set.decref(roc_host); }
        unsafe { value.content_hash.decref(roc_host); }
        unsafe { value.root.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.asset_set.incref(amount); }
        unsafe { value.content_hash.incref(amount); }
        unsafe { value.root.incref(amount); }
    }
}

pub struct HostGlueAssetsOpenArgsRelease;

unsafe impl RocRelease<HostGlueAssetsOpenArgs> for HostGlueAssetsOpenArgsRelease {
    unsafe fn release(value: HostGlueAssetsOpenArgs, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

/// Arguments for HostGlue.assets_read!
/// Roc signature: Resource.Handle([AssetStoreResource]), Str => Try(List(U8), U8)
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueAssetsReadArgs {
    pub arg0: *mut u64,
    pub arg1: RocStr,
}

/// Arguments for HostGlue.apply!
/// Roc signature: [Mount({ root : U64 }), NoChange, Replace({ old_root : U64, root : U64 })] => {}
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueApplyArgs {
    pub arg0: MountOrNoChangeOrReplace,
}

/// Arguments for HostGlue.set_dispatch!
/// Roc signature: Box(U64 => {}) => {}
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueSetDispatchArgs {
    pub arg0: RocErasedCallable,
}

/// Arguments for HostGlue.enqueue_task!
/// Roc signature: U64, Str, Box(U64 => {}) => {}
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueEnqueueTaskArgs {
    pub arg0: u64,
    pub arg1: RocStr,
    pub arg2: RocErasedCallable,
}

/// Arguments for HostGlue.cancel_task!
/// Roc signature: U64, Str => {}
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueCancelTaskArgs {
    pub arg0: u64,
    pub arg1: RocStr,
}

/// Arguments for HostGlue.task_complete!
/// Roc signature: Box(U64 => {}) => {}
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueTaskCompleteArgs {
    pub arg0: RocErasedCallable,
}

/// Arguments for HostGlue.timer_start!
/// Roc signature: U64 => Resource.Handle([TimerResource])
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueTimerStartArgs {
    pub arg0: u64,
}

/// Arguments for HostGlue.timer_next!
/// Roc signature: Resource.Handle([TimerResource]) => Bool
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueTimerNextArgs {
    pub arg0: *mut u64,
}

/// Arguments for HostGlue.timer_cancel!
/// Roc signature: Resource.Handle([TimerResource]) => Bool
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueTimerCancelArgs {
    pub arg0: *mut u64,
}

/// Arguments for HostGlue.http_send!
/// Roc signature: { body : List(U8), client : Resource.Handle([HttpClientResource]), headers : List((Str, Str)), max_redirects : U8, max_response_bytes : U64, method : U8, method_ext : Str, timeout_ms : U64, url : Str } => Try({ body : List(U8), headers : List((Str, Str)), status : U16 }, [AccessDenied, BodyTooLarge, ConnectFailed, InvalidCapability, InvalidHeader, InvalidRequest, InvalidUrl, RedirectLimit, Revoked, Timeout, UnsupportedScheme])
/// Refcounted fields are owned by the hosted function.
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueHttpSendArgs {
    pub max_response_bytes: u64,
    pub timeout_ms: u64,
    pub body: RocListWith<u8, false>,
    pub client: *mut u64,
    pub headers: RocList<AnonStruct77eaba63dfee299d>,
    pub method_ext: RocStr,
    pub url: RocStr,
    pub max_redirects: u8,
    pub method: u8,
}

/// Arguments for HostGlue.http_send!
/// Roc signature: { body : List(U8), client : Resource.Handle([HttpClientResource]), headers : List((Str, Str)), max_redirects : U8, max_response_bytes : U64, method : U8, method_ext : Str, timeout_ms : U64, url : Str } => Try({ body : List(U8), headers : List((Str, Str)), status : U16 }, [AccessDenied, BodyTooLarge, ConnectFailed, InvalidCapability, InvalidHeader, InvalidRequest, InvalidUrl, RedirectLimit, Revoked, Timeout, UnsupportedScheme])
/// Refcounted fields are owned by the hosted function.
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueHttpSendArgs {
    pub max_response_bytes: u64,
    pub timeout_ms: u64,
    pub body: RocListWith<u8, false>,
    pub client: *mut u64,
    pub headers: RocList<AnonStruct77eaba63dfee299d>,
    pub method_ext: RocStr,
    pub url: RocStr,
    pub max_redirects: u8,
    pub method: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostGlueHttpSendArgs>() == 128, "HostGlueHttpSendArgs size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostGlueHttpSendArgs>() == 8, "HostGlueHttpSendArgs alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostGlueHttpSendArgs>() == 72, "HostGlueHttpSendArgs size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostGlueHttpSendArgs>() == 8, "HostGlueHttpSendArgs alignment mismatch");

impl HostGlueHttpSendArgs {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.body.decref(roc_host); }
        unsafe { decref_box_with(value.client as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
        unsafe { decref_list_of_anon_struct77eaba63dfee299d(value.headers, roc_host); }
        unsafe { value.method_ext.decref(roc_host); }
        unsafe { value.url.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.body.incref(amount); }
        unsafe { incref_box(value.client as RocBox, amount); }
        unsafe { value.headers.incref(amount); }
        unsafe { value.method_ext.incref(amount); }
        unsafe { value.url.incref(amount); }
    }
}

pub struct HostGlueHttpSendArgsRelease;

unsafe impl RocRelease<HostGlueHttpSendArgs> for HostGlueHttpSendArgsRelease {
    unsafe fn release(value: HostGlueHttpSendArgs, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

/// Arguments for HostGlue.work_start!
/// Roc signature: U8 => {}
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueWorkStartArgs {
    pub arg0: u8,
}

/// Arguments for HostGlue.work_end!
/// Roc signature: U8 => {}
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueWorkEndArgs {
    pub arg0: u8,
}

/// Arguments for HostGlue.window_config!
/// Roc signature: Str, U32, U32, U64, U64 => {}
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueWindowConfigArgs {
    pub arg0: RocStr,
    pub arg1: u32,
    pub arg2: u32,
    pub arg3: u64,
    pub arg4: u64,
}

/// Arguments for HostGlue.appearance_next_change!
/// Roc signature: U8 => U8
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueAppearanceNextChangeArgs {
    pub arg0: u8,
}

/// Arguments for HostGlue.appearance_prefer!
/// Roc signature: U8 => {}
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostGlueAppearancePreferArgs {
    pub arg0: u8,
}

/// Arguments for InternalFiles.dir_list!
/// Roc signature: Resource.Handle([DirReadResource]) => Try(List({ bytes : [None, Some(U64)], kind : [Directory, File, Other, SymbolicLink], name : Str }), [ListDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), OpenAppDataErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), OpenReadDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), PickDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), PickFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), ReadFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), WatchDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), WriteFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported])])
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct InternalFilesDirListArgs {
    pub arg0: *mut u64,
}

/// Arguments for InternalFiles.dir_open_read!
/// Roc signature: Resource.Handle([DirReadResource]), Str => Try(Resource.Handle([DirReadResource]), [ListDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), OpenAppDataErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), OpenReadDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), PickDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), PickFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), ReadFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), WatchDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), WriteFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported])])
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct InternalFilesDirOpenReadArgs {
    pub arg0: *mut u64,
    pub arg1: RocStr,
}

/// Arguments for InternalFiles.dir_read!
/// Roc signature: Resource.Handle([DirReadResource]), Str => Try(List(U8), [ListDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), OpenAppDataErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), OpenReadDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), PickDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), PickFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), ReadFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), WatchDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), WriteFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported])])
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct InternalFilesDirReadArgs {
    pub arg0: *mut u64,
    pub arg1: RocStr,
}

/// Arguments for InternalFiles.pick_file!
/// Roc signature: List({ extensions : List(Str), label : Str, mime_types : List(Str) }) => Try([Canceled, Chosen({ file : Resource.Handle([FileReadResource]), name : Str })], [ListDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), OpenAppDataErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), OpenReadDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), PickDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), PickFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), ReadFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), WatchDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), WriteFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported])])
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct InternalFilesPickFileArgs {
    pub arg0: RocList<AnonStructDa3665c9cce9a3c>,
}

/// Arguments for InternalFiles.file_read!
/// Roc signature: Resource.Handle([FileReadResource]) => Try(List(U8), [ListDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), OpenAppDataErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), OpenReadDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), PickDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), PickFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), ReadFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), WatchDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), WriteFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported])])
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct InternalFilesFileReadArgs {
    pub arg0: *mut u64,
}

/// Arguments for InternalFiles.dir_sha256!
/// Roc signature: Resource.Handle([DirReadResource]), Str => Try(Str, [ListDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), OpenAppDataErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), OpenReadDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), PickDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), PickFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), ReadFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), WatchDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), WriteFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported])])
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct InternalFilesDirSha256Args {
    pub arg0: *mut u64,
    pub arg1: RocStr,
}

/// Arguments for InternalFiles.file_sha256!
/// Roc signature: Resource.Handle([FileReadResource]) => Try(Str, [ListDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), OpenAppDataErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), OpenReadDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), PickDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), PickFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), ReadFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), WatchDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), WriteFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported])])
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct InternalFilesFileSha256Args {
    pub arg0: *mut u64,
}

/// Arguments for InternalFiles.dir_watch!
/// Roc signature: Resource.Handle([DirReadResource]) => Try(Resource.Handle([WatchResource]), { code : U8, message : Str })
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct InternalFilesDirWatchArgs {
    pub arg0: *mut u64,
}

/// Arguments for InternalFiles.watch_next!
/// Roc signature: Resource.Handle([WatchResource]) => { code : U8, names : List(Str), overflowed : Bool, replaced : Bool }
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct InternalFilesWatchNextArgs {
    pub arg0: *mut u64,
}

/// Arguments for InternalFiles.watch_cancel!
/// Roc signature: Resource.Handle([WatchResource]) => Bool
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct InternalFilesWatchCancelArgs {
    pub arg0: *mut u64,
}

// Platform Type Aliases

pub type HostGlueNodeStyledTextArg0 = AnonStructA47a6e0f4ab34c8a;
pub type HostGlueNodeStyledTextArg0Runs = AnonStructFdc3cce9386720ed;
pub type HostGlueKeyedSeedArg0 = AnonStructEc521db91a8efd1f;
pub type HostGlueKeyedEditBeginArg0 = AnonStruct805ea43d90e5fb6e;
pub type HostGlueKeyedInsertBeforeArg0 = AnonStruct1f2ff78011a2f7cd;
pub type HostGlueKeyedMoveBeforeArg0 = AnonStruct8e6ae4c4e7aeda0d;
pub type HostGlueKeyedSetArg0 = AnonStruct73b63742301b46aa;
pub type HostGlueComponentResolve = AnonStruct2c3712e02fef0cd;
pub type HostGlueNodeRowArg0 = AnonStructB61da4ff1a34a156;
pub type HostGlueNodeColumnArg0 = AnonStructB61da4ff1a34a156;
pub type HostGlueNodeDialogArg0 = AnonStructB61da4ff1a34a156;
pub type HostGlueNodePopoverArg0 = AnonStruct91244b23e2207007;
pub type HostGlueNodePopoverArg0Shortcuts = AnonStruct1d0ac13b7d648fa3;
pub type HostGlueNodePopover = AnonStruct10af2f3a422106e8;
pub type HostGlueNodePanelArg0 = AnonStructB61da4ff1a34a156;
pub type HostGlueNodeScrollArg0 = AnonStructDf60cd23530654b2;
pub type HostGlueNodeActionButtonArg0 = AnonStruct3188ec42c5b5e0d3;
pub type HostGlueNodeActionButton = AnonStructBecf162f14687660;
pub type HostGlueNodeVirtualListArg0 = AnonStructFad101fcce1ec1a3;
pub type HostGlueNodeCheckboxArg0 = AnonStruct751f7db6342b42f6;
pub type HostGlueNodeTextareaArg0 = AnonStructA0ad260b0f5e9c52;
pub type HostGlueNodeImageArg0 = AnonStruct71144019e2d669fc;
pub type HostGlueNodeCanvasArg0 = AnonStruct97820d55023100c7;
pub type HostGlueNodeCanvasArg0Primitives = AnonStructF74678f34f8b3cbf;
pub type HostGlueCanvasEvent = AnonStructE7c8b7462a78f14b;
pub type HostGlueShortcutEvent = AnonStructBdcbe7a50e42954d;
pub type HostGlueNodeSplitArg0 = AnonStructE5ac0e79f166cfe4;
pub type HostGlueNodeSplitArg0Keys = AnonStruct1d0ac13b7d648fa3;
pub type HostGlueNodeSplit = AnonStruct1da781635f2f9fce;
pub type HostGlueResizeEvent = AnonStruct7dea1af4373e83a;
pub type HostGlueVirtualWindowArg0 = AnonStruct521b63caff3553f9;
pub type HostGlueVirtualWindow = AnonStructBe98516b6662a57b;
pub type HostGlueVirtualRowsEvent = AnonStructD56b0eabf62517b9;
pub type HostGlueNodeTextInputArg0 = AnonStruct2ffca3ac98d0c52c;
pub type HostGlueNodeTextInput = AnonStructDc4c26f806dfc5bf;
pub type HostGlueSqliteOpenReadErr = AnonStruct4c605145331877e7;
pub type HostGlueSqliteOpenFileReadResult = HostGlueSqliteOpenReadResult;
pub type HostGlueSqliteOpenFileReadResultPayload = HostGlueSqliteOpenReadResultPayload;
pub type HostGlueSqliteOpenFileReadResultTag = HostGlueSqliteOpenReadResultTag;
pub type HostGlueSqliteOpenFileReadErr = AnonStruct4c605145331877e7;
pub type HostGlueSqliteWatchErr = AnonStruct4c605145331877e7;
pub type HostGlueSqliteQueryArg1 = AnonStructAd07206fc55b9842;
pub type HostGlueSqliteQueryArg1Params = AnonStructBff7bc5a4faa26ae;
pub type HostGlueSqliteQueryErr = AnonStruct4c605145331877e7;
pub type HostGlueSqliteQueryOk = AnonStruct89852815e920c968;
pub type HostGlueSqliteQueryOkRows = AnonStructBff7bc5a4faa26ae;
pub type InternalFilesAppDataErr = AnonStruct4c605145331877e7;
pub type InternalFilesReadUtf8Err = AnonStruct4c605145331877e7;
pub type InternalFilesReadUtf8Ok = AnonStructA2f37e1d5ed0e4cd;
pub type InternalFilesWriteUtf8AtomicErr = AnonStruct4c605145331877e7;
pub type HostGlueClipboardAcquireErr = AnonStruct4c605145331877e7;
pub type HostGlueClipboardReadTextErr = AnonStruct4c605145331877e7;
pub type HostGlueClipboardReadTextOk = AnonStruct2a44a28a63f85620;
pub type HostGlueClipboardWriteTextErr = AnonStruct4c605145331877e7;
pub type HostGlueAudioAcquireErr = AnonStruct4c605145331877e7;
pub type HostGlueAudioLoadErr = AnonStruct4c605145331877e7;
pub type HostGlueAudioLoadOk = AnonStruct8e993eac1f1cd0c;
pub type HostGlueAudioPlayResult = HostGlueClipboardWriteTextResult;
pub type HostGlueAudioPlayResultPayload = HostGlueClipboardWriteTextResultPayload;
pub type HostGlueAudioPlayResultTag = HostGlueClipboardWriteTextResultTag;
pub type HostGlueAudioPlayErr = AnonStruct4c605145331877e7;
pub type HostGlueAudioPauseResult = HostGlueClipboardWriteTextResult;
pub type HostGlueAudioPauseResultPayload = HostGlueClipboardWriteTextResultPayload;
pub type HostGlueAudioPauseResultTag = HostGlueClipboardWriteTextResultTag;
pub type HostGlueAudioPauseErr = AnonStruct4c605145331877e7;
pub type HostGlueAudioSeekResult = HostGlueClipboardWriteTextResult;
pub type HostGlueAudioSeekResultPayload = HostGlueClipboardWriteTextResultPayload;
pub type HostGlueAudioSeekResultTag = HostGlueClipboardWriteTextResultTag;
pub type HostGlueAudioSeekErr = AnonStruct4c605145331877e7;
pub type HostGlueAudioStatusErr = AnonStruct4c605145331877e7;
pub type HostGlueAudioStatusOk = AnonStruct18a809e7fe3ae87a;
pub type HostGlueAudioStopResult = HostGlueClipboardWriteTextResult;
pub type HostGlueAudioStopResultPayload = HostGlueClipboardWriteTextResultPayload;
pub type HostGlueAudioStopResultTag = HostGlueClipboardWriteTextResultTag;
pub type HostGlueAudioStopErr = AnonStruct4c605145331877e7;
pub type HostGlueTcpCloseResult = HostGlueTcpWriteAllResult;
pub type HostGlueTcpCloseResultPayload = HostGlueTcpWriteAllResultPayload;
pub type HostGlueTcpCloseResultTag = HostGlueTcpWriteAllResultTag;
pub type HostGlueProcessAcquireErr = AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr;
pub type HostGlueProcessAcquireErrPayload = AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrPayload;
pub type HostGlueProcessAcquireErrTag = AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag;
pub type HostGlueProcessAcquireErrAcquireProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type HostGlueProcessAcquireErrCancelProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type HostGlueProcessAcquireErrReadProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type HostGlueProcessAcquireErrResizeProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type HostGlueProcessAcquireErrSpawnProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type HostGlueProcessAcquireErrWriteProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrAcquireProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrCancelProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrReadProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrResizeProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrSpawnProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrWriteProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type HostGlueProcessSpawnArg1 = AnonStruct93136bf334c2a2fc;
pub type HostGlueProcessSpawnErr = AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr;
pub type HostGlueProcessSpawnErrPayload = AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrPayload;
pub type HostGlueProcessSpawnErrTag = AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag;
pub type HostGlueProcessSpawnErrAcquireProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type HostGlueProcessSpawnErrCancelProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type HostGlueProcessSpawnErrReadProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type HostGlueProcessSpawnErrResizeProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type HostGlueProcessSpawnErrSpawnProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type HostGlueProcessSpawnErrWriteProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type HostGlueProcessReadErr = AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr;
pub type HostGlueProcessReadErrPayload = AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrPayload;
pub type HostGlueProcessReadErrTag = AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag;
pub type HostGlueProcessReadErrAcquireProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type HostGlueProcessReadErrCancelProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type HostGlueProcessReadErrReadProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type HostGlueProcessReadErrResizeProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type HostGlueProcessReadErrSpawnProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type HostGlueProcessReadErrWriteProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type HostGlueProcessReadOk = CanceledOrDataOrEndOfFile;
pub type HostGlueProcessReadOkPayload = CanceledOrDataOrEndOfFilePayload;
pub type HostGlueProcessReadOkTag = CanceledOrDataOrEndOfFileTag;
pub type HostGlueProcessWriteErr = AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr;
pub type HostGlueProcessWriteErrPayload = AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrPayload;
pub type HostGlueProcessWriteErrTag = AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag;
pub type HostGlueProcessWriteErrAcquireProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type HostGlueProcessWriteErrCancelProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type HostGlueProcessWriteErrReadProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type HostGlueProcessWriteErrResizeProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type HostGlueProcessWriteErrSpawnProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type HostGlueProcessWriteErrWriteProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type HostGlueProcessResizeArg1 = AnonStruct93136bf334c2a2fc;
pub type HostGlueProcessResizeErr = AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr;
pub type HostGlueProcessResizeErrPayload = AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrPayload;
pub type HostGlueProcessResizeErrTag = AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag;
pub type HostGlueProcessResizeErrAcquireProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type HostGlueProcessResizeErrCancelProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type HostGlueProcessResizeErrReadProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type HostGlueProcessResizeErrResizeProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type HostGlueProcessResizeErrSpawnProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type HostGlueProcessResizeErrWriteProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type HostGlueProcessCancelErr = AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr;
pub type HostGlueProcessCancelErrPayload = AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrPayload;
pub type HostGlueProcessCancelErrTag = AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag;
pub type HostGlueProcessCancelErrAcquireProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type HostGlueProcessCancelErrCancelProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type HostGlueProcessCancelErrReadProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type HostGlueProcessCancelErrResizeProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type HostGlueProcessCancelErrSpawnProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type HostGlueProcessCancelErrWriteProcessErr = AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported;
pub type HostGlueProcessCancelOk = AlreadyStoppedOrCanceled;
pub type HostGlueDeviceAcquireErr = AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr;
pub type HostGlueDeviceAcquireErrPayload = AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrPayload;
pub type HostGlueDeviceAcquireErrTag = AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrTag;
pub type HostGlueDeviceAcquireErrAcquireDeviceErr = AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported;
pub type HostGlueDeviceAcquireErrCloseDeviceErr = AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported;
pub type HostGlueDeviceAcquireErrConnectDeviceErr = AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported;
pub type HostGlueDeviceAcquireErrDiscoverDeviceErr = AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported;
pub type HostGlueDeviceAcquireErrTransactDeviceErr = AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported;
pub type AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrAcquireDeviceErr = AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported;
pub type AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrCloseDeviceErr = AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported;
pub type AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrConnectDeviceErr = AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported;
pub type AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrDiscoverDeviceErr = AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported;
pub type AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrTransactDeviceErr = AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported;
pub type HostGlueDeviceDiscoverErr = AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr;
pub type HostGlueDeviceDiscoverErrPayload = AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrPayload;
pub type HostGlueDeviceDiscoverErrTag = AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrTag;
pub type HostGlueDeviceDiscoverErrAcquireDeviceErr = AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported;
pub type HostGlueDeviceDiscoverErrCloseDeviceErr = AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported;
pub type HostGlueDeviceDiscoverErrConnectDeviceErr = AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported;
pub type HostGlueDeviceDiscoverErrDiscoverDeviceErr = AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported;
pub type HostGlueDeviceDiscoverErrTransactDeviceErr = AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported;
pub type HostGlueDeviceDiscoverOk = AnonStruct27556b2f7cb4f65f;
pub type HostGlueDeviceConnectErr = AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr;
pub type HostGlueDeviceConnectErrPayload = AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrPayload;
pub type HostGlueDeviceConnectErrTag = AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrTag;
pub type HostGlueDeviceConnectErrAcquireDeviceErr = AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported;
pub type HostGlueDeviceConnectErrCloseDeviceErr = AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported;
pub type HostGlueDeviceConnectErrConnectDeviceErr = AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported;
pub type HostGlueDeviceConnectErrDiscoverDeviceErr = AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported;
pub type HostGlueDeviceConnectErrTransactDeviceErr = AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported;
pub type HostGlueDeviceTransactErr = AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr;
pub type HostGlueDeviceTransactErrPayload = AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrPayload;
pub type HostGlueDeviceTransactErrTag = AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrTag;
pub type HostGlueDeviceTransactErrAcquireDeviceErr = AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported;
pub type HostGlueDeviceTransactErrCloseDeviceErr = AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported;
pub type HostGlueDeviceTransactErrConnectDeviceErr = AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported;
pub type HostGlueDeviceTransactErrDiscoverDeviceErr = AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported;
pub type HostGlueDeviceTransactErrTransactDeviceErr = AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported;
pub type HostGlueDeviceCloseErr = AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr;
pub type HostGlueDeviceCloseErrPayload = AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrPayload;
pub type HostGlueDeviceCloseErrTag = AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrTag;
pub type HostGlueDeviceCloseErrAcquireDeviceErr = AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported;
pub type HostGlueDeviceCloseErrCloseDeviceErr = AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported;
pub type HostGlueDeviceCloseErrConnectDeviceErr = AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported;
pub type HostGlueDeviceCloseErrDiscoverDeviceErr = AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported;
pub type HostGlueDeviceCloseErrTransactDeviceErr = AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported;
pub type HostGlueSystemSampleOk = AnonStruct98efc21233c140ff;
pub type HostGlueSystemSampleOkProcesses = AnonStruct916a0c1ad2ed4712;
pub type HostGlueSystemCloseResult = HostGlueTcpWriteAllResult;
pub type HostGlueSystemCloseResultPayload = HostGlueTcpWriteAllResultPayload;
pub type HostGlueSystemCloseResultTag = HostGlueTcpWriteAllResultTag;
pub type HostGlueImageInspectOk = AnonStructB3b29ac2cb34a461;
pub type HostGlueAssetsOpenArg0 = AnonStruct513f8b80afa01b67;
pub type HostGlueAssetsReadResult = HostGlueTcpReadUpToResult;
pub type HostGlueAssetsReadResultPayload = HostGlueTcpReadUpToResultPayload;
pub type HostGlueAssetsReadResultTag = HostGlueTcpReadUpToResultTag;
pub type MountOrNoChangeOrReplaceMount = AnonStruct8ae3ad5a5c0e22dc;
pub type MountOrNoChangeOrReplaceReplace = AnonStruct8d3c9b3ac95ef074;
pub type HostGlueHttpSendArg0 = AnonStructE6f574a7975f5cda;
pub type HostGlueHttpSendArg0Headers = AnonStruct77eaba63dfee299d;
pub type HostGlueHttpSendErr = AccessDeniedOrBodyTooLargeOrConnectFailedOrInvalidCapabilityOrInvalidHeaderOrInvalidRequestOrInvalidUrlOrRedirectLimitOrRevokedOrTimeoutOrUnsupportedScheme;
pub type HostGlueHttpSendOk = AnonStructBe6bcbc15f8a1360;
pub type HostGlueHttpSendOkHeaders = AnonStruct77eaba63dfee299d;
pub type HostGlueHttpAcquireErr = AccessDeniedOrBodyTooLargeOrConnectFailedOrInvalidCapabilityOrInvalidHeaderOrInvalidRequestOrInvalidUrlOrRedirectLimitOrRevokedOrTimeoutOrUnsupportedScheme;
pub type InternalFilesPickDirectoryErr = ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr;
pub type InternalFilesPickDirectoryErrPayload = ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrPayload;
pub type InternalFilesPickDirectoryErrTag = ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag;
pub type InternalFilesPickDirectoryErrListDirectoryErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesPickDirectoryErrOpenAppDataErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesPickDirectoryErrOpenReadDirectoryErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesPickDirectoryErrPickDirectoryErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesPickDirectoryErrPickFileErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesPickDirectoryErrReadFileErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesPickDirectoryErrWatchDirectoryErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesPickDirectoryErrWriteFileErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesPickDirectoryOk = CanceledOrChosenType240;
pub type InternalFilesPickDirectoryOkPayload = CanceledOrChosenType240Payload;
pub type InternalFilesPickDirectoryOkTag = CanceledOrChosenType240Tag;
pub type InternalFilesPickDirectoryOkChosen = AnonStruct4869dafad3498788;
pub type ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrListDirectoryErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrOpenAppDataErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrOpenReadDirectoryErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrPickDirectoryErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrPickFileErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrReadFileErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrWatchDirectoryErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrWriteFileErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type CanceledOrChosen = CanceledOrChosenType240;
pub type CanceledOrChosenPayload = CanceledOrChosenType240Payload;
pub type CanceledOrChosenTag = CanceledOrChosenType240Tag;
pub type CanceledOrChosenChosen = AnonStruct4869dafad3498788;
pub type InternalFilesDirListErr = ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr;
pub type InternalFilesDirListErrPayload = ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrPayload;
pub type InternalFilesDirListErrTag = ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag;
pub type InternalFilesDirListErrListDirectoryErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesDirListErrOpenAppDataErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesDirListErrOpenReadDirectoryErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesDirListErrPickDirectoryErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesDirListErrPickFileErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesDirListErrReadFileErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesDirListErrWatchDirectoryErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesDirListErrWriteFileErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesDirListOk = AnonStruct770b9d9b3d3d255;
pub type InternalFilesDirOpenReadErr = ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr;
pub type InternalFilesDirOpenReadErrPayload = ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrPayload;
pub type InternalFilesDirOpenReadErrTag = ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag;
pub type InternalFilesDirOpenReadErrListDirectoryErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesDirOpenReadErrOpenAppDataErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesDirOpenReadErrOpenReadDirectoryErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesDirOpenReadErrPickDirectoryErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesDirOpenReadErrPickFileErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesDirOpenReadErrReadFileErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesDirOpenReadErrWatchDirectoryErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesDirOpenReadErrWriteFileErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesDirReadErr = ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr;
pub type InternalFilesDirReadErrPayload = ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrPayload;
pub type InternalFilesDirReadErrTag = ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag;
pub type InternalFilesDirReadErrListDirectoryErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesDirReadErrOpenAppDataErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesDirReadErrOpenReadDirectoryErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesDirReadErrPickDirectoryErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesDirReadErrPickFileErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesDirReadErrReadFileErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesDirReadErrWatchDirectoryErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesDirReadErrWriteFileErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesPickFileArg0 = AnonStructDa3665c9cce9a3c;
pub type InternalFilesPickFileErr = ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr;
pub type InternalFilesPickFileErrPayload = ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrPayload;
pub type InternalFilesPickFileErrTag = ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag;
pub type InternalFilesPickFileErrListDirectoryErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesPickFileErrOpenAppDataErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesPickFileErrOpenReadDirectoryErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesPickFileErrPickDirectoryErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesPickFileErrPickFileErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesPickFileErrReadFileErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesPickFileErrWatchDirectoryErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesPickFileErrWriteFileErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesPickFileOk = CanceledOrChosenType283;
pub type InternalFilesPickFileOkPayload = CanceledOrChosenType283Payload;
pub type InternalFilesPickFileOkTag = CanceledOrChosenType283Tag;
pub type InternalFilesPickFileOkChosen = AnonStructA295f39559baa24d;
pub type InternalFilesFileReadResult = InternalFilesDirReadResult;
pub type InternalFilesFileReadResultPayload = InternalFilesDirReadResultPayload;
pub type InternalFilesFileReadResultTag = InternalFilesDirReadResultTag;
pub type InternalFilesFileReadErr = ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr;
pub type InternalFilesFileReadErrPayload = ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrPayload;
pub type InternalFilesFileReadErrTag = ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag;
pub type InternalFilesFileReadErrListDirectoryErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesFileReadErrOpenAppDataErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesFileReadErrOpenReadDirectoryErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesFileReadErrPickDirectoryErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesFileReadErrPickFileErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesFileReadErrReadFileErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesFileReadErrWatchDirectoryErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesFileReadErrWriteFileErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesDirSha256Err = ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr;
pub type InternalFilesDirSha256ErrPayload = ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrPayload;
pub type InternalFilesDirSha256ErrTag = ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag;
pub type InternalFilesDirSha256ErrListDirectoryErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesDirSha256ErrOpenAppDataErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesDirSha256ErrOpenReadDirectoryErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesDirSha256ErrPickDirectoryErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesDirSha256ErrPickFileErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesDirSha256ErrReadFileErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesDirSha256ErrWatchDirectoryErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesDirSha256ErrWriteFileErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesFileSha256Result = InternalFilesDirSha256Result;
pub type InternalFilesFileSha256ResultPayload = InternalFilesDirSha256ResultPayload;
pub type InternalFilesFileSha256ResultTag = InternalFilesDirSha256ResultTag;
pub type InternalFilesFileSha256Err = ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr;
pub type InternalFilesFileSha256ErrPayload = ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrPayload;
pub type InternalFilesFileSha256ErrTag = ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag;
pub type InternalFilesFileSha256ErrListDirectoryErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesFileSha256ErrOpenAppDataErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesFileSha256ErrOpenReadDirectoryErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesFileSha256ErrPickDirectoryErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesFileSha256ErrPickFileErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesFileSha256ErrReadFileErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesFileSha256ErrWatchDirectoryErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesFileSha256ErrWriteFileErr = AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported;
pub type InternalFilesDirWatchErr = AnonStruct4c605145331877e7;
pub type InternalFilesWatchNext = AnonStruct5d4fced1f96c13bb;

// Generated Refcount Helpers

impl AnonStructA47a6e0f4ab34c8a {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.runs.decref(roc_host); }
        unsafe { value.value.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.runs.incref(amount); }
        unsafe { value.value.incref(amount); }
    }
}

pub struct AnonStructA47a6e0f4ab34c8aRelease;

unsafe impl RocRelease<AnonStructA47a6e0f4ab34c8a> for AnonStructA47a6e0f4ab34c8aRelease {
    unsafe fn release(value: AnonStructA47a6e0f4ab34c8a, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStructFdc3cce9386720ed {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        let _ = value;
        let _ = roc_host;
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = value;
        let _ = amount;
    }
}

pub struct AnonStructFdc3cce9386720edRelease;

unsafe impl RocRelease<AnonStructFdc3cce9386720ed> for AnonStructFdc3cce9386720edRelease {
    unsafe fn release(value: AnonStructFdc3cce9386720ed, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStructEc521db91a8efd1f {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { decref_list_of_list_of_type3(value.keys, roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.keys.incref(amount); }
    }
}

pub struct AnonStructEc521db91a8efd1fRelease;

unsafe impl RocRelease<AnonStructEc521db91a8efd1f> for AnonStructEc521db91a8efd1fRelease {
    unsafe fn release(value: AnonStructEc521db91a8efd1f, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStruct805ea43d90e5fb6e {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        let _ = value;
        let _ = roc_host;
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = value;
        let _ = amount;
    }
}

pub struct AnonStruct805ea43d90e5fb6eRelease;

unsafe impl RocRelease<AnonStruct805ea43d90e5fb6e> for AnonStruct805ea43d90e5fb6eRelease {
    unsafe fn release(value: AnonStruct805ea43d90e5fb6e, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStruct1f2ff78011a2f7cd {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.before.decref(roc_host); }
        unsafe { value.key.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.before.incref(amount); }
        unsafe { value.key.incref(amount); }
    }
}

pub struct AnonStruct1f2ff78011a2f7cdRelease;

unsafe impl RocRelease<AnonStruct1f2ff78011a2f7cd> for AnonStruct1f2ff78011a2f7cdRelease {
    unsafe fn release(value: AnonStruct1f2ff78011a2f7cd, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStruct8e6ae4c4e7aeda0d {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.before.decref(roc_host); }
        unsafe { value.key.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.before.incref(amount); }
        unsafe { value.key.incref(amount); }
    }
}

pub struct AnonStruct8e6ae4c4e7aeda0dRelease;

unsafe impl RocRelease<AnonStruct8e6ae4c4e7aeda0d> for AnonStruct8e6ae4c4e7aeda0dRelease {
    unsafe fn release(value: AnonStruct8e6ae4c4e7aeda0d, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStruct73b63742301b46aa {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.key.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.key.incref(amount); }
    }
}

pub struct AnonStruct73b63742301b46aaRelease;

unsafe impl RocRelease<AnonStruct73b63742301b46aa> for AnonStruct73b63742301b46aaRelease {
    unsafe fn release(value: AnonStruct73b63742301b46aa, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStruct2c3712e02fef0cd {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        let _ = value;
        let _ = roc_host;
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = value;
        let _ = amount;
    }
}

pub struct AnonStruct2c3712e02fef0cdRelease;

unsafe impl RocRelease<AnonStruct2c3712e02fef0cd> for AnonStruct2c3712e02fef0cdRelease {
    unsafe fn release(value: AnonStruct2c3712e02fef0cd, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStructB61da4ff1a34a156 {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.label.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.label.incref(amount); }
    }
}

pub struct AnonStructB61da4ff1a34a156Release;

unsafe impl RocRelease<AnonStructB61da4ff1a34a156> for AnonStructB61da4ff1a34a156Release {
    unsafe fn release(value: AnonStructB61da4ff1a34a156, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStruct10af2f3a422106e8 {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        let _ = value;
        let _ = roc_host;
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = value;
        let _ = amount;
    }
}

pub struct AnonStruct10af2f3a422106e8Release;

unsafe impl RocRelease<AnonStruct10af2f3a422106e8> for AnonStruct10af2f3a422106e8Release {
    unsafe fn release(value: AnonStruct10af2f3a422106e8, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStruct91244b23e2207007 {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.label.decref(roc_host); }
        unsafe { decref_list_of_anon_struct1d0ac13b7d648fa3(value.shortcuts, roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.label.incref(amount); }
        unsafe { value.shortcuts.incref(amount); }
    }
}

pub struct AnonStruct91244b23e2207007Release;

unsafe impl RocRelease<AnonStruct91244b23e2207007> for AnonStruct91244b23e2207007Release {
    unsafe fn release(value: AnonStruct91244b23e2207007, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStruct1d0ac13b7d648fa3 {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.keys.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.keys.incref(amount); }
    }
}

pub struct AnonStruct1d0ac13b7d648fa3Release;

unsafe impl RocRelease<AnonStruct1d0ac13b7d648fa3> for AnonStruct1d0ac13b7d648fa3Release {
    unsafe fn release(value: AnonStruct1d0ac13b7d648fa3, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStructDf60cd23530654b2 {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.name.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.name.incref(amount); }
    }
}

pub struct AnonStructDf60cd23530654b2Release;

unsafe impl RocRelease<AnonStructDf60cd23530654b2> for AnonStructDf60cd23530654b2Release {
    unsafe fn release(value: AnonStructDf60cd23530654b2, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStructBecf162f14687660 {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        let _ = value;
        let _ = roc_host;
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = value;
        let _ = amount;
    }
}

pub struct AnonStructBecf162f14687660Release;

unsafe impl RocRelease<AnonStructBecf162f14687660> for AnonStructBecf162f14687660Release {
    unsafe fn release(value: AnonStructBecf162f14687660, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStruct3188ec42c5b5e0d3 {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.caption.decref(roc_host); }
        unsafe { value.label.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.caption.incref(amount); }
        unsafe { value.label.incref(amount); }
    }
}

pub struct AnonStruct3188ec42c5b5e0d3Release;

unsafe impl RocRelease<AnonStruct3188ec42c5b5e0d3> for AnonStruct3188ec42c5b5e0d3Release {
    unsafe fn release(value: AnonStruct3188ec42c5b5e0d3, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStructFad101fcce1ec1a3 {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.name.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.name.incref(amount); }
    }
}

pub struct AnonStructFad101fcce1ec1a3Release;

unsafe impl RocRelease<AnonStructFad101fcce1ec1a3> for AnonStructFad101fcce1ec1a3Release {
    unsafe fn release(value: AnonStructFad101fcce1ec1a3, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStruct751f7db6342b42f6 {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.label.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.label.incref(amount); }
    }
}

pub struct AnonStruct751f7db6342b42f6Release;

unsafe impl RocRelease<AnonStruct751f7db6342b42f6> for AnonStruct751f7db6342b42f6Release {
    unsafe fn release(value: AnonStruct751f7db6342b42f6, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStructA0ad260b0f5e9c52 {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.label.decref(roc_host); }
        unsafe { value.placeholder.decref(roc_host); }
        unsafe { value.value.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.label.incref(amount); }
        unsafe { value.placeholder.incref(amount); }
        unsafe { value.value.incref(amount); }
    }
}

pub struct AnonStructA0ad260b0f5e9c52Release;

unsafe impl RocRelease<AnonStructA0ad260b0f5e9c52> for AnonStructA0ad260b0f5e9c52Release {
    unsafe fn release(value: AnonStructA0ad260b0f5e9c52, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStruct71144019e2d669fc {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.bytes.decref(roc_host); }
        unsafe { value.label.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.bytes.incref(amount); }
        unsafe { value.label.incref(amount); }
    }
}

pub struct AnonStruct71144019e2d669fcRelease;

unsafe impl RocRelease<AnonStruct71144019e2d669fc> for AnonStruct71144019e2d669fcRelease {
    unsafe fn release(value: AnonStruct71144019e2d669fc, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStruct97820d55023100c7 {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.label.decref(roc_host); }
        unsafe { decref_list_of_anon_struct_f74678f34f8b3cbf(value.primitives, roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.label.incref(amount); }
        unsafe { value.primitives.incref(amount); }
    }
}

pub struct AnonStruct97820d55023100c7Release;

unsafe impl RocRelease<AnonStruct97820d55023100c7> for AnonStruct97820d55023100c7Release {
    unsafe fn release(value: AnonStruct97820d55023100c7, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStructF74678f34f8b3cbf {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.label.decref(roc_host); }
        unsafe { value.text.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.label.incref(amount); }
        unsafe { value.text.incref(amount); }
    }
}

pub struct AnonStructF74678f34f8b3cbfRelease;

unsafe impl RocRelease<AnonStructF74678f34f8b3cbf> for AnonStructF74678f34f8b3cbfRelease {
    unsafe fn release(value: AnonStructF74678f34f8b3cbf, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStructBe98516b6662a57b {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        let _ = value;
        let _ = roc_host;
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = value;
        let _ = amount;
    }
}

pub struct AnonStructBe98516b6662a57bRelease;

unsafe impl RocRelease<AnonStructBe98516b6662a57b> for AnonStructBe98516b6662a57bRelease {
    unsafe fn release(value: AnonStructBe98516b6662a57b, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStruct521b63caff3553f9 {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        let _ = value;
        let _ = roc_host;
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = value;
        let _ = amount;
    }
}

pub struct AnonStruct521b63caff3553f9Release;

unsafe impl RocRelease<AnonStruct521b63caff3553f9> for AnonStruct521b63caff3553f9Release {
    unsafe fn release(value: AnonStruct521b63caff3553f9, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStructD56b0eabf62517b9 {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        let _ = value;
        let _ = roc_host;
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = value;
        let _ = amount;
    }
}

pub struct AnonStructD56b0eabf62517b9Release;

unsafe impl RocRelease<AnonStructD56b0eabf62517b9> for AnonStructD56b0eabf62517b9Release {
    unsafe fn release(value: AnonStructD56b0eabf62517b9, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStructE7c8b7462a78f14b {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        let _ = value;
        let _ = roc_host;
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = value;
        let _ = amount;
    }
}

pub struct AnonStructE7c8b7462a78f14bRelease;

unsafe impl RocRelease<AnonStructE7c8b7462a78f14b> for AnonStructE7c8b7462a78f14bRelease {
    unsafe fn release(value: AnonStructE7c8b7462a78f14b, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStructBdcbe7a50e42954d {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.keys.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.keys.incref(amount); }
    }
}

pub struct AnonStructBdcbe7a50e42954dRelease;

unsafe impl RocRelease<AnonStructBdcbe7a50e42954d> for AnonStructBdcbe7a50e42954dRelease {
    unsafe fn release(value: AnonStructBdcbe7a50e42954d, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStruct1da781635f2f9fce {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        let _ = value;
        let _ = roc_host;
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = value;
        let _ = amount;
    }
}

pub struct AnonStruct1da781635f2f9fceRelease;

unsafe impl RocRelease<AnonStruct1da781635f2f9fce> for AnonStruct1da781635f2f9fceRelease {
    unsafe fn release(value: AnonStruct1da781635f2f9fce, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStructE5ac0e79f166cfe4 {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { decref_list_of_anon_struct1d0ac13b7d648fa3(value.keys, roc_host); }
        unsafe { value.label.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.keys.incref(amount); }
        unsafe { value.label.incref(amount); }
    }
}

pub struct AnonStructE5ac0e79f166cfe4Release;

unsafe impl RocRelease<AnonStructE5ac0e79f166cfe4> for AnonStructE5ac0e79f166cfe4Release {
    unsafe fn release(value: AnonStructE5ac0e79f166cfe4, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStruct7dea1af4373e83a {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        let _ = value;
        let _ = roc_host;
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = value;
        let _ = amount;
    }
}

pub struct AnonStruct7dea1af4373e83aRelease;

unsafe impl RocRelease<AnonStruct7dea1af4373e83a> for AnonStruct7dea1af4373e83aRelease {
    unsafe fn release(value: AnonStruct7dea1af4373e83a, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStructDc4c26f806dfc5bf {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        let _ = value;
        let _ = roc_host;
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = value;
        let _ = amount;
    }
}

pub struct AnonStructDc4c26f806dfc5bfRelease;

unsafe impl RocRelease<AnonStructDc4c26f806dfc5bf> for AnonStructDc4c26f806dfc5bfRelease {
    unsafe fn release(value: AnonStructDc4c26f806dfc5bf, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStruct2ffca3ac98d0c52c {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.label.decref(roc_host); }
        unsafe { value.placeholder.decref(roc_host); }
        unsafe { value.value.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.label.incref(amount); }
        unsafe { value.placeholder.incref(amount); }
        unsafe { value.value.incref(amount); }
    }
}

pub struct AnonStruct2ffca3ac98d0c52cRelease;

unsafe impl RocRelease<AnonStruct2ffca3ac98d0c52c> for AnonStruct2ffca3ac98d0c52cRelease {
    unsafe fn release(value: AnonStruct2ffca3ac98d0c52c, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl HostGlueSqliteOpenReadResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            HostGlueSqliteOpenReadResultTag::Err => {
                let payload = unsafe { value.take_payload_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            HostGlueSqliteOpenReadResultTag::Ok => {
                let payload = unsafe { value.take_payload_ok_unchecked() };
                unsafe { decref_box_with(payload as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            HostGlueSqliteOpenReadResultTag::Err => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            HostGlueSqliteOpenReadResultTag::Ok => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_ok_unchecked()) };
                unsafe { incref_box(payload as RocBox, amount); }
            },
        }
    }
}

pub struct HostGlueSqliteOpenReadResultRelease;

unsafe impl RocRelease<HostGlueSqliteOpenReadResult> for HostGlueSqliteOpenReadResultRelease {
    unsafe fn release(value: HostGlueSqliteOpenReadResult, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStruct4c605145331877e7 {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.message.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.message.incref(amount); }
    }
}

pub struct AnonStruct4c605145331877e7Release;

unsafe impl RocRelease<AnonStruct4c605145331877e7> for AnonStruct4c605145331877e7Release {
    unsafe fn release(value: AnonStruct4c605145331877e7, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl HostGlueSqliteWatchResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            HostGlueSqliteWatchResultTag::Err => {
                let payload = unsafe { value.take_payload_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            HostGlueSqliteWatchResultTag::Ok => {
                let payload = unsafe { value.take_payload_ok_unchecked() };
                unsafe { decref_box_with(payload as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            HostGlueSqliteWatchResultTag::Err => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            HostGlueSqliteWatchResultTag::Ok => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_ok_unchecked()) };
                unsafe { incref_box(payload as RocBox, amount); }
            },
        }
    }
}

pub struct HostGlueSqliteWatchResultRelease;

unsafe impl RocRelease<HostGlueSqliteWatchResult> for HostGlueSqliteWatchResultRelease {
    unsafe fn release(value: HostGlueSqliteWatchResult, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl HostGlueSqliteQueryResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            HostGlueSqliteQueryResultTag::Err => {
                let payload = unsafe { value.take_payload_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            HostGlueSqliteQueryResultTag::Ok => {
                let payload = unsafe { value.take_payload_ok_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            HostGlueSqliteQueryResultTag::Err => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            HostGlueSqliteQueryResultTag::Ok => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_ok_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

pub struct HostGlueSqliteQueryResultRelease;

unsafe impl RocRelease<HostGlueSqliteQueryResult> for HostGlueSqliteQueryResultRelease {
    unsafe fn release(value: HostGlueSqliteQueryResult, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStruct89852815e920c968 {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { decref_list_of_str(value.columns, roc_host); }
        unsafe { decref_list_of_list_of_anon_struct_bff7bc5a4faa26ae(value.rows, roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.columns.incref(amount); }
        unsafe { value.rows.incref(amount); }
    }
}

pub struct AnonStruct89852815e920c968Release;

unsafe impl RocRelease<AnonStruct89852815e920c968> for AnonStruct89852815e920c968Release {
    unsafe fn release(value: AnonStruct89852815e920c968, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStructBff7bc5a4faa26ae {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.bytes.decref(roc_host); }
        unsafe { value.text.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.bytes.incref(amount); }
        unsafe { value.text.incref(amount); }
    }
}

pub struct AnonStructBff7bc5a4faa26aeRelease;

unsafe impl RocRelease<AnonStructBff7bc5a4faa26ae> for AnonStructBff7bc5a4faa26aeRelease {
    unsafe fn release(value: AnonStructBff7bc5a4faa26ae, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStructAd07206fc55b9842 {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { decref_list_of_anon_struct_bff7bc5a4faa26ae(value.params, roc_host); }
        unsafe { value.sql.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.params.incref(amount); }
        unsafe { value.sql.incref(amount); }
    }
}

pub struct AnonStructAd07206fc55b9842Release;

unsafe impl RocRelease<AnonStructAd07206fc55b9842> for AnonStructAd07206fc55b9842Release {
    unsafe fn release(value: AnonStructAd07206fc55b9842, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl HostGlueClipboardAcquireResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            HostGlueClipboardAcquireResultTag::Err => {
                let payload = unsafe { value.take_payload_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            HostGlueClipboardAcquireResultTag::Ok => {
                let payload = unsafe { value.take_payload_ok_unchecked() };
                unsafe { decref_box_with(payload as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            HostGlueClipboardAcquireResultTag::Err => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            HostGlueClipboardAcquireResultTag::Ok => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_ok_unchecked()) };
                unsafe { incref_box(payload as RocBox, amount); }
            },
        }
    }
}

pub struct HostGlueClipboardAcquireResultRelease;

unsafe impl RocRelease<HostGlueClipboardAcquireResult> for HostGlueClipboardAcquireResultRelease {
    unsafe fn release(value: HostGlueClipboardAcquireResult, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl HostGlueClipboardReadTextResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            HostGlueClipboardReadTextResultTag::Err => {
                let payload = unsafe { value.take_payload_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            HostGlueClipboardReadTextResultTag::Ok => {
                let payload = unsafe { value.take_payload_ok_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            HostGlueClipboardReadTextResultTag::Err => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            HostGlueClipboardReadTextResultTag::Ok => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_ok_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

pub struct HostGlueClipboardReadTextResultRelease;

unsafe impl RocRelease<HostGlueClipboardReadTextResult> for HostGlueClipboardReadTextResultRelease {
    unsafe fn release(value: HostGlueClipboardReadTextResult, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStruct2a44a28a63f85620 {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.text.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.text.incref(amount); }
    }
}

pub struct AnonStruct2a44a28a63f85620Release;

unsafe impl RocRelease<AnonStruct2a44a28a63f85620> for AnonStruct2a44a28a63f85620Release {
    unsafe fn release(value: AnonStruct2a44a28a63f85620, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl HostGlueClipboardWriteTextResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            HostGlueClipboardWriteTextResultTag::Err => {
                let payload = unsafe { value.take_payload_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            HostGlueClipboardWriteTextResultTag::Ok => {},
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            HostGlueClipboardWriteTextResultTag::Err => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            HostGlueClipboardWriteTextResultTag::Ok => {},
        }
    }
}

pub struct HostGlueClipboardWriteTextResultRelease;

unsafe impl RocRelease<HostGlueClipboardWriteTextResult> for HostGlueClipboardWriteTextResultRelease {
    unsafe fn release(value: HostGlueClipboardWriteTextResult, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl HostGlueAudioAcquireResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            HostGlueAudioAcquireResultTag::Err => {
                let payload = unsafe { value.take_payload_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            HostGlueAudioAcquireResultTag::Ok => {
                let payload = unsafe { value.take_payload_ok_unchecked() };
                unsafe { decref_box_with(payload as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            HostGlueAudioAcquireResultTag::Err => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            HostGlueAudioAcquireResultTag::Ok => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_ok_unchecked()) };
                unsafe { incref_box(payload as RocBox, amount); }
            },
        }
    }
}

pub struct HostGlueAudioAcquireResultRelease;

unsafe impl RocRelease<HostGlueAudioAcquireResult> for HostGlueAudioAcquireResultRelease {
    unsafe fn release(value: HostGlueAudioAcquireResult, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl HostGlueAudioLoadResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            HostGlueAudioLoadResultTag::Err => {
                let payload = unsafe { value.take_payload_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            HostGlueAudioLoadResultTag::Ok => {
                let payload = unsafe { value.take_payload_ok_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            HostGlueAudioLoadResultTag::Err => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            HostGlueAudioLoadResultTag::Ok => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_ok_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

pub struct HostGlueAudioLoadResultRelease;

unsafe impl RocRelease<HostGlueAudioLoadResult> for HostGlueAudioLoadResultRelease {
    unsafe fn release(value: HostGlueAudioLoadResult, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStruct8e993eac1f1cd0c {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { decref_box_with(value.track as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { incref_box(value.track as RocBox, amount); }
    }
}

pub struct AnonStruct8e993eac1f1cd0cRelease;

unsafe impl RocRelease<AnonStruct8e993eac1f1cd0c> for AnonStruct8e993eac1f1cd0cRelease {
    unsafe fn release(value: AnonStruct8e993eac1f1cd0c, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl HostGlueAudioStatusResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            HostGlueAudioStatusResultTag::Err => {
                let payload = unsafe { value.take_payload_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            HostGlueAudioStatusResultTag::Ok => {
                let payload = unsafe { value.take_payload_ok_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            HostGlueAudioStatusResultTag::Err => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            HostGlueAudioStatusResultTag::Ok => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_ok_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

pub struct HostGlueAudioStatusResultRelease;

unsafe impl RocRelease<HostGlueAudioStatusResult> for HostGlueAudioStatusResultRelease {
    unsafe fn release(value: HostGlueAudioStatusResult, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStruct18a809e7fe3ae87a {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        let _ = value;
        let _ = roc_host;
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = value;
        let _ = amount;
    }
}

pub struct AnonStruct18a809e7fe3ae87aRelease;

unsafe impl RocRelease<AnonStruct18a809e7fe3ae87a> for AnonStruct18a809e7fe3ae87aRelease {
    unsafe fn release(value: AnonStruct18a809e7fe3ae87a, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl HostGlueTcpConnectResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            HostGlueTcpConnectResultTag::Err => {},
            HostGlueTcpConnectResultTag::Ok => {
                let payload = unsafe { value.take_payload_ok_unchecked() };
                unsafe { decref_box_with(payload as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            HostGlueTcpConnectResultTag::Err => {},
            HostGlueTcpConnectResultTag::Ok => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_ok_unchecked()) };
                unsafe { incref_box(payload as RocBox, amount); }
            },
        }
    }
}

pub struct HostGlueTcpConnectResultRelease;

unsafe impl RocRelease<HostGlueTcpConnectResult> for HostGlueTcpConnectResultRelease {
    unsafe fn release(value: HostGlueTcpConnectResult, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl HostGlueTcpReadUpToResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            HostGlueTcpReadUpToResultTag::Err => {},
            HostGlueTcpReadUpToResultTag::Ok => {
                let payload = unsafe { value.take_payload_ok_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            HostGlueTcpReadUpToResultTag::Err => {},
            HostGlueTcpReadUpToResultTag::Ok => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_ok_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

pub struct HostGlueTcpReadUpToResultRelease;

unsafe impl RocRelease<HostGlueTcpReadUpToResult> for HostGlueTcpReadUpToResultRelease {
    unsafe fn release(value: HostGlueTcpReadUpToResult, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl HostGlueTcpWriteAllResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        let _ = roc_host;
        match value.tag {
            HostGlueTcpWriteAllResultTag::Err => {},
            HostGlueTcpWriteAllResultTag::Ok => {},
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            HostGlueTcpWriteAllResultTag::Err => {},
            HostGlueTcpWriteAllResultTag::Ok => {},
        }
    }
}

pub struct HostGlueTcpWriteAllResultRelease;

unsafe impl RocRelease<HostGlueTcpWriteAllResult> for HostGlueTcpWriteAllResultRelease {
    unsafe fn release(value: HostGlueTcpWriteAllResult, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl HostGlueProcessAcquireResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            HostGlueProcessAcquireResultTag::Err => {
                let payload = unsafe { value.take_payload_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            HostGlueProcessAcquireResultTag::Ok => {
                let payload = unsafe { value.take_payload_ok_unchecked() };
                unsafe { decref_box_with(payload as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            HostGlueProcessAcquireResultTag::Err => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            HostGlueProcessAcquireResultTag::Ok => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_ok_unchecked()) };
                unsafe { incref_box(payload as RocBox, amount); }
            },
        }
    }
}

pub struct HostGlueProcessAcquireResultRelease;

unsafe impl RocRelease<HostGlueProcessAcquireResult> for HostGlueProcessAcquireResultRelease {
    unsafe fn release(value: HostGlueProcessAcquireResult, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag::AcquireProcessErr => {
                let payload = unsafe { value.take_payload_acquire_process_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag::CancelProcessErr => {
                let payload = unsafe { value.take_payload_cancel_process_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag::ReadProcessErr => {
                let payload = unsafe { value.take_payload_read_process_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag::ResizeProcessErr => {
                let payload = unsafe { value.take_payload_resize_process_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag::SpawnProcessErr => {
                let payload = unsafe { value.take_payload_spawn_process_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag::WriteProcessErr => {
                let payload = unsafe { value.take_payload_write_process_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag::AcquireProcessErr => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_acquire_process_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag::CancelProcessErr => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_cancel_process_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag::ReadProcessErr => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_read_process_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag::ResizeProcessErr => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_resize_process_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag::SpawnProcessErr => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_spawn_process_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrTag::WriteProcessErr => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_write_process_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

pub struct AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrRelease;

unsafe impl RocRelease<AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr> for AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErrRelease {
    unsafe fn release(value: AcquireProcessErrOrCancelProcessErrOrReadProcessErrOrResizeProcessErrOrSpawnProcessErrOrWriteProcessErr, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let _ = self;
        let _ = roc_host;
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let _ = self;
        let _ = amount;
    }
}

pub struct AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupportedRelease;

unsafe impl RocRelease<AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported> for AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupportedRelease {
    unsafe fn release(value: AccessDeniedOrBusyOrExitedOrInvalidCapabilityOrInvalidSizeOrIoOrResourceLimitOrRevokedOrUnsupported, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl HostGlueProcessSpawnResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            HostGlueProcessSpawnResultTag::Err => {
                let payload = unsafe { value.take_payload_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            HostGlueProcessSpawnResultTag::Ok => {
                let payload = unsafe { value.take_payload_ok_unchecked() };
                unsafe { decref_box_with(payload as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            HostGlueProcessSpawnResultTag::Err => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            HostGlueProcessSpawnResultTag::Ok => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_ok_unchecked()) };
                unsafe { incref_box(payload as RocBox, amount); }
            },
        }
    }
}

pub struct HostGlueProcessSpawnResultRelease;

unsafe impl RocRelease<HostGlueProcessSpawnResult> for HostGlueProcessSpawnResultRelease {
    unsafe fn release(value: HostGlueProcessSpawnResult, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStruct93136bf334c2a2fc {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        let _ = value;
        let _ = roc_host;
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = value;
        let _ = amount;
    }
}

pub struct AnonStruct93136bf334c2a2fcRelease;

unsafe impl RocRelease<AnonStruct93136bf334c2a2fc> for AnonStruct93136bf334c2a2fcRelease {
    unsafe fn release(value: AnonStruct93136bf334c2a2fc, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl HostGlueProcessReadResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            HostGlueProcessReadResultTag::Err => {
                let payload = unsafe { value.take_payload_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            HostGlueProcessReadResultTag::Ok => {
                let payload = unsafe { value.take_payload_ok_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            HostGlueProcessReadResultTag::Err => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            HostGlueProcessReadResultTag::Ok => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_ok_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

pub struct HostGlueProcessReadResultRelease;

unsafe impl RocRelease<HostGlueProcessReadResult> for HostGlueProcessReadResultRelease {
    unsafe fn release(value: HostGlueProcessReadResult, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl CanceledOrDataOrEndOfFile {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            CanceledOrDataOrEndOfFileTag::Canceled => {},
            CanceledOrDataOrEndOfFileTag::Data => {
                let payload = unsafe { value.take_payload_data_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            CanceledOrDataOrEndOfFileTag::EndOfFile => {},
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            CanceledOrDataOrEndOfFileTag::Canceled => {},
            CanceledOrDataOrEndOfFileTag::Data => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_data_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            CanceledOrDataOrEndOfFileTag::EndOfFile => {},
        }
    }
}

pub struct CanceledOrDataOrEndOfFileRelease;

unsafe impl RocRelease<CanceledOrDataOrEndOfFile> for CanceledOrDataOrEndOfFileRelease {
    unsafe fn release(value: CanceledOrDataOrEndOfFile, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl HostGlueProcessWriteResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            HostGlueProcessWriteResultTag::Err => {
                let payload = unsafe { value.take_payload_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            HostGlueProcessWriteResultTag::Ok => {},
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            HostGlueProcessWriteResultTag::Err => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            HostGlueProcessWriteResultTag::Ok => {},
        }
    }
}

pub struct HostGlueProcessWriteResultRelease;

unsafe impl RocRelease<HostGlueProcessWriteResult> for HostGlueProcessWriteResultRelease {
    unsafe fn release(value: HostGlueProcessWriteResult, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl HostGlueProcessResizeResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            HostGlueProcessResizeResultTag::Err => {
                let payload = unsafe { value.take_payload_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            HostGlueProcessResizeResultTag::Ok => {},
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            HostGlueProcessResizeResultTag::Err => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            HostGlueProcessResizeResultTag::Ok => {},
        }
    }
}

pub struct HostGlueProcessResizeResultRelease;

unsafe impl RocRelease<HostGlueProcessResizeResult> for HostGlueProcessResizeResultRelease {
    unsafe fn release(value: HostGlueProcessResizeResult, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl HostGlueProcessCancelResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            HostGlueProcessCancelResultTag::Err => {
                let payload = unsafe { value.take_payload_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            HostGlueProcessCancelResultTag::Ok => {
                let payload = unsafe { value.take_payload_ok_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            HostGlueProcessCancelResultTag::Err => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            HostGlueProcessCancelResultTag::Ok => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_ok_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

pub struct HostGlueProcessCancelResultRelease;

unsafe impl RocRelease<HostGlueProcessCancelResult> for HostGlueProcessCancelResultRelease {
    unsafe fn release(value: HostGlueProcessCancelResult, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AlreadyStoppedOrCanceled {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let _ = self;
        let _ = roc_host;
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let _ = self;
        let _ = amount;
    }
}

pub struct AlreadyStoppedOrCanceledRelease;

unsafe impl RocRelease<AlreadyStoppedOrCanceled> for AlreadyStoppedOrCanceledRelease {
    unsafe fn release(value: AlreadyStoppedOrCanceled, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl HostGlueDeviceAcquireResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            HostGlueDeviceAcquireResultTag::Err => {
                let payload = unsafe { value.take_payload_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            HostGlueDeviceAcquireResultTag::Ok => {
                let payload = unsafe { value.take_payload_ok_unchecked() };
                unsafe { decref_box_with(payload as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            HostGlueDeviceAcquireResultTag::Err => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            HostGlueDeviceAcquireResultTag::Ok => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_ok_unchecked()) };
                unsafe { incref_box(payload as RocBox, amount); }
            },
        }
    }
}

pub struct HostGlueDeviceAcquireResultRelease;

unsafe impl RocRelease<HostGlueDeviceAcquireResult> for HostGlueDeviceAcquireResultRelease {
    unsafe fn release(value: HostGlueDeviceAcquireResult, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrTag::AcquireDeviceErr => {
                let payload = unsafe { value.take_payload_acquire_device_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrTag::CloseDeviceErr => {
                let payload = unsafe { value.take_payload_close_device_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrTag::ConnectDeviceErr => {
                let payload = unsafe { value.take_payload_connect_device_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrTag::DiscoverDeviceErr => {
                let payload = unsafe { value.take_payload_discover_device_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrTag::TransactDeviceErr => {
                let payload = unsafe { value.take_payload_transact_device_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrTag::AcquireDeviceErr => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_acquire_device_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrTag::CloseDeviceErr => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_close_device_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrTag::ConnectDeviceErr => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_connect_device_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrTag::DiscoverDeviceErr => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_discover_device_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrTag::TransactDeviceErr => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_transact_device_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

pub struct AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrRelease;

unsafe impl RocRelease<AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr> for AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErrRelease {
    unsafe fn release(value: AcquireDeviceErrOrCloseDeviceErrOrConnectDeviceErrOrDiscoverDeviceErrOrTransactDeviceErr, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let _ = self;
        let _ = roc_host;
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let _ = self;
        let _ = amount;
    }
}

pub struct AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupportedRelease;

unsafe impl RocRelease<AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported> for AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupportedRelease {
    unsafe fn release(value: AccessDeniedOrBusyOrClosedOrDisconnectedOrInvalidCapabilityOrInvalidRequestOrIoOrNotFoundOrProtocolOrResourceLimitOrRevokedOrTimeoutOrUnsupported, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl HostGlueDeviceDiscoverResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            HostGlueDeviceDiscoverResultTag::Err => {
                let payload = unsafe { value.take_payload_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            HostGlueDeviceDiscoverResultTag::Ok => {
                let payload = unsafe { value.take_payload_ok_unchecked() };
                unsafe { decref_list_of_anon_struct27556b2f7cb4f65f(payload, roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            HostGlueDeviceDiscoverResultTag::Err => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            HostGlueDeviceDiscoverResultTag::Ok => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_ok_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

pub struct HostGlueDeviceDiscoverResultRelease;

unsafe impl RocRelease<HostGlueDeviceDiscoverResult> for HostGlueDeviceDiscoverResultRelease {
    unsafe fn release(value: HostGlueDeviceDiscoverResult, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStruct27556b2f7cb4f65f {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.manufacturer.decref(roc_host); }
        unsafe { value.product.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.manufacturer.incref(amount); }
        unsafe { value.product.incref(amount); }
    }
}

pub struct AnonStruct27556b2f7cb4f65fRelease;

unsafe impl RocRelease<AnonStruct27556b2f7cb4f65f> for AnonStruct27556b2f7cb4f65fRelease {
    unsafe fn release(value: AnonStruct27556b2f7cb4f65f, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl HostGlueDeviceConnectResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            HostGlueDeviceConnectResultTag::Err => {
                let payload = unsafe { value.take_payload_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            HostGlueDeviceConnectResultTag::Ok => {
                let payload = unsafe { value.take_payload_ok_unchecked() };
                unsafe { decref_box_with(payload as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            HostGlueDeviceConnectResultTag::Err => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            HostGlueDeviceConnectResultTag::Ok => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_ok_unchecked()) };
                unsafe { incref_box(payload as RocBox, amount); }
            },
        }
    }
}

pub struct HostGlueDeviceConnectResultRelease;

unsafe impl RocRelease<HostGlueDeviceConnectResult> for HostGlueDeviceConnectResultRelease {
    unsafe fn release(value: HostGlueDeviceConnectResult, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl HostGlueDeviceTransactResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            HostGlueDeviceTransactResultTag::Err => {
                let payload = unsafe { value.take_payload_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            HostGlueDeviceTransactResultTag::Ok => {
                let payload = unsafe { value.take_payload_ok_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            HostGlueDeviceTransactResultTag::Err => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            HostGlueDeviceTransactResultTag::Ok => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_ok_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

pub struct HostGlueDeviceTransactResultRelease;

unsafe impl RocRelease<HostGlueDeviceTransactResult> for HostGlueDeviceTransactResultRelease {
    unsafe fn release(value: HostGlueDeviceTransactResult, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl HostGlueDeviceCloseResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            HostGlueDeviceCloseResultTag::Err => {
                let payload = unsafe { value.take_payload_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            HostGlueDeviceCloseResultTag::Ok => {},
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            HostGlueDeviceCloseResultTag::Err => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            HostGlueDeviceCloseResultTag::Ok => {},
        }
    }
}

pub struct HostGlueDeviceCloseResultRelease;

unsafe impl RocRelease<HostGlueDeviceCloseResult> for HostGlueDeviceCloseResultRelease {
    unsafe fn release(value: HostGlueDeviceCloseResult, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl HostGlueSystemAcquireResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            HostGlueSystemAcquireResultTag::Err => {},
            HostGlueSystemAcquireResultTag::Ok => {
                let payload = unsafe { value.take_payload_ok_unchecked() };
                unsafe { decref_box_with(payload as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            HostGlueSystemAcquireResultTag::Err => {},
            HostGlueSystemAcquireResultTag::Ok => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_ok_unchecked()) };
                unsafe { incref_box(payload as RocBox, amount); }
            },
        }
    }
}

pub struct HostGlueSystemAcquireResultRelease;

unsafe impl RocRelease<HostGlueSystemAcquireResult> for HostGlueSystemAcquireResultRelease {
    unsafe fn release(value: HostGlueSystemAcquireResult, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl HostGlueSystemSampleResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            HostGlueSystemSampleResultTag::Err => {},
            HostGlueSystemSampleResultTag::Ok => {
                let payload = unsafe { value.take_payload_ok_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            HostGlueSystemSampleResultTag::Err => {},
            HostGlueSystemSampleResultTag::Ok => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_ok_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

pub struct HostGlueSystemSampleResultRelease;

unsafe impl RocRelease<HostGlueSystemSampleResult> for HostGlueSystemSampleResultRelease {
    unsafe fn release(value: HostGlueSystemSampleResult, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStruct98efc21233c140ff {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { decref_list_of_anon_struct916a0c1ad2ed4712(value.processes, roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.processes.incref(amount); }
    }
}

pub struct AnonStruct98efc21233c140ffRelease;

unsafe impl RocRelease<AnonStruct98efc21233c140ff> for AnonStruct98efc21233c140ffRelease {
    unsafe fn release(value: AnonStruct98efc21233c140ff, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStruct916a0c1ad2ed4712 {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.name.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.name.incref(amount); }
    }
}

pub struct AnonStruct916a0c1ad2ed4712Release;

unsafe impl RocRelease<AnonStruct916a0c1ad2ed4712> for AnonStruct916a0c1ad2ed4712Release {
    unsafe fn release(value: AnonStruct916a0c1ad2ed4712, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl HostGlueImageInspectResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            HostGlueImageInspectResultTag::Err => {},
            HostGlueImageInspectResultTag::Ok => {
                let payload = unsafe { value.take_payload_ok_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            HostGlueImageInspectResultTag::Err => {},
            HostGlueImageInspectResultTag::Ok => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_ok_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

pub struct HostGlueImageInspectResultRelease;

unsafe impl RocRelease<HostGlueImageInspectResult> for HostGlueImageInspectResultRelease {
    unsafe fn release(value: HostGlueImageInspectResult, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStructB3b29ac2cb34a461 {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        let _ = value;
        let _ = roc_host;
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = value;
        let _ = amount;
    }
}

pub struct AnonStructB3b29ac2cb34a461Release;

unsafe impl RocRelease<AnonStructB3b29ac2cb34a461> for AnonStructB3b29ac2cb34a461Release {
    unsafe fn release(value: AnonStructB3b29ac2cb34a461, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl HostGlueAssetsOpenResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            HostGlueAssetsOpenResultTag::Err => {},
            HostGlueAssetsOpenResultTag::Ok => {
                let payload = unsafe { value.take_payload_ok_unchecked() };
                unsafe { decref_box_with(payload as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            HostGlueAssetsOpenResultTag::Err => {},
            HostGlueAssetsOpenResultTag::Ok => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_ok_unchecked()) };
                unsafe { incref_box(payload as RocBox, amount); }
            },
        }
    }
}

pub struct HostGlueAssetsOpenResultRelease;

unsafe impl RocRelease<HostGlueAssetsOpenResult> for HostGlueAssetsOpenResultRelease {
    unsafe fn release(value: HostGlueAssetsOpenResult, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStruct513f8b80afa01b67 {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.asset_set.decref(roc_host); }
        unsafe { value.content_hash.decref(roc_host); }
        unsafe { value.root.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.asset_set.incref(amount); }
        unsafe { value.content_hash.incref(amount); }
        unsafe { value.root.incref(amount); }
    }
}

pub struct AnonStruct513f8b80afa01b67Release;

unsafe impl RocRelease<AnonStruct513f8b80afa01b67> for AnonStruct513f8b80afa01b67Release {
    unsafe fn release(value: AnonStruct513f8b80afa01b67, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl MountOrNoChangeOrReplace {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            MountOrNoChangeOrReplaceTag::Mount => {
                let payload = unsafe { value.take_payload_mount_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            MountOrNoChangeOrReplaceTag::NoChange => {},
            MountOrNoChangeOrReplaceTag::Replace => {
                let payload = unsafe { value.take_payload_replace_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            MountOrNoChangeOrReplaceTag::Mount => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_mount_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            MountOrNoChangeOrReplaceTag::NoChange => {},
            MountOrNoChangeOrReplaceTag::Replace => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_replace_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

pub struct MountOrNoChangeOrReplaceRelease;

unsafe impl RocRelease<MountOrNoChangeOrReplace> for MountOrNoChangeOrReplaceRelease {
    unsafe fn release(value: MountOrNoChangeOrReplace, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStruct8ae3ad5a5c0e22dc {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        let _ = value;
        let _ = roc_host;
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = value;
        let _ = amount;
    }
}

pub struct AnonStruct8ae3ad5a5c0e22dcRelease;

unsafe impl RocRelease<AnonStruct8ae3ad5a5c0e22dc> for AnonStruct8ae3ad5a5c0e22dcRelease {
    unsafe fn release(value: AnonStruct8ae3ad5a5c0e22dc, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStruct8d3c9b3ac95ef074 {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        let _ = value;
        let _ = roc_host;
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = value;
        let _ = amount;
    }
}

pub struct AnonStruct8d3c9b3ac95ef074Release;

unsafe impl RocRelease<AnonStruct8d3c9b3ac95ef074> for AnonStruct8d3c9b3ac95ef074Release {
    unsafe fn release(value: AnonStruct8d3c9b3ac95ef074, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl HostGlueHttpAcquireResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            HostGlueHttpAcquireResultTag::Err => {
                let payload = unsafe { value.take_payload_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            HostGlueHttpAcquireResultTag::Ok => {
                let payload = unsafe { value.take_payload_ok_unchecked() };
                unsafe { decref_box_with(payload as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            HostGlueHttpAcquireResultTag::Err => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            HostGlueHttpAcquireResultTag::Ok => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_ok_unchecked()) };
                unsafe { incref_box(payload as RocBox, amount); }
            },
        }
    }
}

pub struct HostGlueHttpAcquireResultRelease;

unsafe impl RocRelease<HostGlueHttpAcquireResult> for HostGlueHttpAcquireResultRelease {
    unsafe fn release(value: HostGlueHttpAcquireResult, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AccessDeniedOrBodyTooLargeOrConnectFailedOrInvalidCapabilityOrInvalidHeaderOrInvalidRequestOrInvalidUrlOrRedirectLimitOrRevokedOrTimeoutOrUnsupportedScheme {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let _ = self;
        let _ = roc_host;
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let _ = self;
        let _ = amount;
    }
}

pub struct AccessDeniedOrBodyTooLargeOrConnectFailedOrInvalidCapabilityOrInvalidHeaderOrInvalidRequestOrInvalidUrlOrRedirectLimitOrRevokedOrTimeoutOrUnsupportedSchemeRelease;

unsafe impl RocRelease<AccessDeniedOrBodyTooLargeOrConnectFailedOrInvalidCapabilityOrInvalidHeaderOrInvalidRequestOrInvalidUrlOrRedirectLimitOrRevokedOrTimeoutOrUnsupportedScheme> for AccessDeniedOrBodyTooLargeOrConnectFailedOrInvalidCapabilityOrInvalidHeaderOrInvalidRequestOrInvalidUrlOrRedirectLimitOrRevokedOrTimeoutOrUnsupportedSchemeRelease {
    unsafe fn release(value: AccessDeniedOrBodyTooLargeOrConnectFailedOrInvalidCapabilityOrInvalidHeaderOrInvalidRequestOrInvalidUrlOrRedirectLimitOrRevokedOrTimeoutOrUnsupportedScheme, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl HostGlueHttpSendResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            HostGlueHttpSendResultTag::Err => {
                let payload = unsafe { value.take_payload_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            HostGlueHttpSendResultTag::Ok => {
                let payload = unsafe { value.take_payload_ok_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            HostGlueHttpSendResultTag::Err => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            HostGlueHttpSendResultTag::Ok => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_ok_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

pub struct HostGlueHttpSendResultRelease;

unsafe impl RocRelease<HostGlueHttpSendResult> for HostGlueHttpSendResultRelease {
    unsafe fn release(value: HostGlueHttpSendResult, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStructBe6bcbc15f8a1360 {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.body.decref(roc_host); }
        unsafe { decref_list_of_anon_struct77eaba63dfee299d(value.headers, roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.body.incref(amount); }
        unsafe { value.headers.incref(amount); }
    }
}

pub struct AnonStructBe6bcbc15f8a1360Release;

unsafe impl RocRelease<AnonStructBe6bcbc15f8a1360> for AnonStructBe6bcbc15f8a1360Release {
    unsafe fn release(value: AnonStructBe6bcbc15f8a1360, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStruct77eaba63dfee299d {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value._0.decref(roc_host); }
        unsafe { value._1.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value._0.incref(amount); }
        unsafe { value._1.incref(amount); }
    }
}

pub struct AnonStruct77eaba63dfee299dRelease;

unsafe impl RocRelease<AnonStruct77eaba63dfee299d> for AnonStruct77eaba63dfee299dRelease {
    unsafe fn release(value: AnonStruct77eaba63dfee299d, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStructE6f574a7975f5cda {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.body.decref(roc_host); }
        unsafe { decref_box_with(value.client as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
        unsafe { decref_list_of_anon_struct77eaba63dfee299d(value.headers, roc_host); }
        unsafe { value.method_ext.decref(roc_host); }
        unsafe { value.url.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.body.incref(amount); }
        unsafe { incref_box(value.client as RocBox, amount); }
        unsafe { value.headers.incref(amount); }
        unsafe { value.method_ext.incref(amount); }
        unsafe { value.url.incref(amount); }
    }
}

pub struct AnonStructE6f574a7975f5cdaRelease;

unsafe impl RocRelease<AnonStructE6f574a7975f5cda> for AnonStructE6f574a7975f5cdaRelease {
    unsafe fn release(value: AnonStructE6f574a7975f5cda, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl InternalFilesPickDirectoryResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            InternalFilesPickDirectoryResultTag::Err => {
                let payload = unsafe { value.take_payload_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            InternalFilesPickDirectoryResultTag::Ok => {
                let payload = unsafe { value.take_payload_ok_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            InternalFilesPickDirectoryResultTag::Err => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            InternalFilesPickDirectoryResultTag::Ok => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_ok_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

pub struct InternalFilesPickDirectoryResultRelease;

unsafe impl RocRelease<InternalFilesPickDirectoryResult> for InternalFilesPickDirectoryResultRelease {
    unsafe fn release(value: InternalFilesPickDirectoryResult, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::ListDirectoryErr => {
                let payload = unsafe { value.take_payload_list_directory_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::OpenAppDataErr => {
                let payload = unsafe { value.take_payload_open_app_data_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::OpenReadDirectoryErr => {
                let payload = unsafe { value.take_payload_open_read_directory_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::PickDirectoryErr => {
                let payload = unsafe { value.take_payload_pick_directory_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::PickFileErr => {
                let payload = unsafe { value.take_payload_pick_file_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::ReadFileErr => {
                let payload = unsafe { value.take_payload_read_file_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::WatchDirectoryErr => {
                let payload = unsafe { value.take_payload_watch_directory_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::WriteFileErr => {
                let payload = unsafe { value.take_payload_write_file_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::ListDirectoryErr => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_list_directory_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::OpenAppDataErr => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_open_app_data_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::OpenReadDirectoryErr => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_open_read_directory_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::PickDirectoryErr => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_pick_directory_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::PickFileErr => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_pick_file_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::ReadFileErr => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_read_file_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::WatchDirectoryErr => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_watch_directory_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrTag::WriteFileErr => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_write_file_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

pub struct ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrRelease;

unsafe impl RocRelease<ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr> for ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErrRelease {
    unsafe fn release(value: ListDirectoryErrOrOpenAppDataErrOrOpenReadDirectoryErrOrPickDirectoryErrOrPickFileErrOrReadFileErrOrWatchDirectoryErrOrWriteFileErr, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let _ = self;
        let _ = roc_host;
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let _ = self;
        let _ = amount;
    }
}

pub struct AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupportedRelease;

unsafe impl RocRelease<AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported> for AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupportedRelease {
    unsafe fn release(value: AccessDeniedOrInvalidCapabilityOrInvalidNameOrInvalidUtf8OrIoOrNotDirectoryOrNotFoundOrResourceLimitOrRevokedOrUnavailableOrUnsupported, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl CanceledOrChosenType240 {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            CanceledOrChosenType240Tag::Canceled => {},
            CanceledOrChosenType240Tag::Chosen => {
                let payload = unsafe { value.take_payload_chosen_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            CanceledOrChosenType240Tag::Canceled => {},
            CanceledOrChosenType240Tag::Chosen => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_chosen_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

pub struct CanceledOrChosenType240Release;

unsafe impl RocRelease<CanceledOrChosenType240> for CanceledOrChosenType240Release {
    unsafe fn release(value: CanceledOrChosenType240, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStruct4869dafad3498788 {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { decref_box_with(value.directory as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
        unsafe { value.name.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { incref_box(value.directory as RocBox, amount); }
        unsafe { value.name.incref(amount); }
    }
}

pub struct AnonStruct4869dafad3498788Release;

unsafe impl RocRelease<AnonStruct4869dafad3498788> for AnonStruct4869dafad3498788Release {
    unsafe fn release(value: AnonStruct4869dafad3498788, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl InternalFilesDirListResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            InternalFilesDirListResultTag::Err => {
                let payload = unsafe { value.take_payload_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            InternalFilesDirListResultTag::Ok => {
                let payload = unsafe { value.take_payload_ok_unchecked() };
                unsafe { decref_list_of_anon_struct770b9d9b3d3d255(payload, roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            InternalFilesDirListResultTag::Err => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            InternalFilesDirListResultTag::Ok => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_ok_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

pub struct InternalFilesDirListResultRelease;

unsafe impl RocRelease<InternalFilesDirListResult> for InternalFilesDirListResultRelease {
    unsafe fn release(value: InternalFilesDirListResult, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStruct770b9d9b3d3d255 {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.bytes.decref(roc_host); }
        unsafe { value.name.decref(roc_host); }
        unsafe { value.kind.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.bytes.incref(amount); }
        unsafe { value.name.incref(amount); }
        unsafe { value.kind.incref(amount); }
    }
}

pub struct AnonStruct770b9d9b3d3d255Release;

unsafe impl RocRelease<AnonStruct770b9d9b3d3d255> for AnonStruct770b9d9b3d3d255Release {
    unsafe fn release(value: AnonStruct770b9d9b3d3d255, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl NoneOrSome {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        let _ = roc_host;
        match value.tag {
            NoneOrSomeTag::None => {},
            NoneOrSomeTag::Some => {},
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            NoneOrSomeTag::None => {},
            NoneOrSomeTag::Some => {},
        }
    }
}

pub struct NoneOrSomeRelease;

unsafe impl RocRelease<NoneOrSome> for NoneOrSomeRelease {
    unsafe fn release(value: NoneOrSome, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl DirectoryOrFileOrOtherOrSymbolicLink {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let _ = self;
        let _ = roc_host;
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let _ = self;
        let _ = amount;
    }
}

pub struct DirectoryOrFileOrOtherOrSymbolicLinkRelease;

unsafe impl RocRelease<DirectoryOrFileOrOtherOrSymbolicLink> for DirectoryOrFileOrOtherOrSymbolicLinkRelease {
    unsafe fn release(value: DirectoryOrFileOrOtherOrSymbolicLink, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl InternalFilesDirOpenReadResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            InternalFilesDirOpenReadResultTag::Err => {
                let payload = unsafe { value.take_payload_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            InternalFilesDirOpenReadResultTag::Ok => {
                let payload = unsafe { value.take_payload_ok_unchecked() };
                unsafe { decref_box_with(payload as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            InternalFilesDirOpenReadResultTag::Err => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            InternalFilesDirOpenReadResultTag::Ok => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_ok_unchecked()) };
                unsafe { incref_box(payload as RocBox, amount); }
            },
        }
    }
}

pub struct InternalFilesDirOpenReadResultRelease;

unsafe impl RocRelease<InternalFilesDirOpenReadResult> for InternalFilesDirOpenReadResultRelease {
    unsafe fn release(value: InternalFilesDirOpenReadResult, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl InternalFilesDirReadResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            InternalFilesDirReadResultTag::Err => {
                let payload = unsafe { value.take_payload_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            InternalFilesDirReadResultTag::Ok => {
                let payload = unsafe { value.take_payload_ok_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            InternalFilesDirReadResultTag::Err => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            InternalFilesDirReadResultTag::Ok => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_ok_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

pub struct InternalFilesDirReadResultRelease;

unsafe impl RocRelease<InternalFilesDirReadResult> for InternalFilesDirReadResultRelease {
    unsafe fn release(value: InternalFilesDirReadResult, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl InternalFilesDirSha256Result {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            InternalFilesDirSha256ResultTag::Err => {
                let payload = unsafe { value.take_payload_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            InternalFilesDirSha256ResultTag::Ok => {
                let payload = unsafe { value.take_payload_ok_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            InternalFilesDirSha256ResultTag::Err => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            InternalFilesDirSha256ResultTag::Ok => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_ok_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

pub struct InternalFilesDirSha256ResultRelease;

unsafe impl RocRelease<InternalFilesDirSha256Result> for InternalFilesDirSha256ResultRelease {
    unsafe fn release(value: InternalFilesDirSha256Result, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl InternalFilesDirWatchResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            InternalFilesDirWatchResultTag::Err => {
                let payload = unsafe { value.take_payload_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            InternalFilesDirWatchResultTag::Ok => {
                let payload = unsafe { value.take_payload_ok_unchecked() };
                unsafe { decref_box_with(payload as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            InternalFilesDirWatchResultTag::Err => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            InternalFilesDirWatchResultTag::Ok => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_ok_unchecked()) };
                unsafe { incref_box(payload as RocBox, amount); }
            },
        }
    }
}

pub struct InternalFilesDirWatchResultRelease;

unsafe impl RocRelease<InternalFilesDirWatchResult> for InternalFilesDirWatchResultRelease {
    unsafe fn release(value: InternalFilesDirWatchResult, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStruct5d4fced1f96c13bb {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { decref_list_of_str(value.names, roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.names.incref(amount); }
    }
}

pub struct AnonStruct5d4fced1f96c13bbRelease;

unsafe impl RocRelease<AnonStruct5d4fced1f96c13bb> for AnonStruct5d4fced1f96c13bbRelease {
    unsafe fn release(value: AnonStruct5d4fced1f96c13bb, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl InternalFilesPickFileResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            InternalFilesPickFileResultTag::Err => {
                let payload = unsafe { value.take_payload_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            InternalFilesPickFileResultTag::Ok => {
                let payload = unsafe { value.take_payload_ok_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            InternalFilesPickFileResultTag::Err => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            InternalFilesPickFileResultTag::Ok => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_ok_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

pub struct InternalFilesPickFileResultRelease;

unsafe impl RocRelease<InternalFilesPickFileResult> for InternalFilesPickFileResultRelease {
    unsafe fn release(value: InternalFilesPickFileResult, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl CanceledOrChosenType283 {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            CanceledOrChosenType283Tag::Canceled => {},
            CanceledOrChosenType283Tag::Chosen => {
                let payload = unsafe { value.take_payload_chosen_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            CanceledOrChosenType283Tag::Canceled => {},
            CanceledOrChosenType283Tag::Chosen => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_chosen_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

pub struct CanceledOrChosenType283Release;

unsafe impl RocRelease<CanceledOrChosenType283> for CanceledOrChosenType283Release {
    unsafe fn release(value: CanceledOrChosenType283, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStructA295f39559baa24d {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { decref_box_with(value.file as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
        unsafe { value.name.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { incref_box(value.file as RocBox, amount); }
        unsafe { value.name.incref(amount); }
    }
}

pub struct AnonStructA295f39559baa24dRelease;

unsafe impl RocRelease<AnonStructA295f39559baa24d> for AnonStructA295f39559baa24dRelease {
    unsafe fn release(value: AnonStructA295f39559baa24d, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStructDa3665c9cce9a3c {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { decref_list_of_str(value.extensions, roc_host); }
        unsafe { value.label.decref(roc_host); }
        unsafe { decref_list_of_str(value.mime_types, roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.extensions.incref(amount); }
        unsafe { value.label.incref(amount); }
        unsafe { value.mime_types.incref(amount); }
    }
}

pub struct AnonStructDa3665c9cce9a3cRelease;

unsafe impl RocRelease<AnonStructDa3665c9cce9a3c> for AnonStructDa3665c9cce9a3cRelease {
    unsafe fn release(value: AnonStructDa3665c9cce9a3c, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl InternalFilesAppDataResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            InternalFilesAppDataResultTag::Err => {
                let payload = unsafe { value.take_payload_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            InternalFilesAppDataResultTag::Ok => {
                let payload = unsafe { value.take_payload_ok_unchecked() };
                unsafe { decref_box_with(payload as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            InternalFilesAppDataResultTag::Err => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            InternalFilesAppDataResultTag::Ok => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_ok_unchecked()) };
                unsafe { incref_box(payload as RocBox, amount); }
            },
        }
    }
}

pub struct InternalFilesAppDataResultRelease;

unsafe impl RocRelease<InternalFilesAppDataResult> for InternalFilesAppDataResultRelease {
    unsafe fn release(value: InternalFilesAppDataResult, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl InternalFilesReadUtf8Result {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            InternalFilesReadUtf8ResultTag::Err => {
                let payload = unsafe { value.take_payload_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            InternalFilesReadUtf8ResultTag::Ok => {
                let payload = unsafe { value.take_payload_ok_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            InternalFilesReadUtf8ResultTag::Err => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            InternalFilesReadUtf8ResultTag::Ok => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_ok_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

pub struct InternalFilesReadUtf8ResultRelease;

unsafe impl RocRelease<InternalFilesReadUtf8Result> for InternalFilesReadUtf8ResultRelease {
    unsafe fn release(value: InternalFilesReadUtf8Result, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl AnonStructA2f37e1d5ed0e4cd {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.value.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.value.incref(amount); }
    }
}

pub struct AnonStructA2f37e1d5ed0e4cdRelease;

unsafe impl RocRelease<AnonStructA2f37e1d5ed0e4cd> for AnonStructA2f37e1d5ed0e4cdRelease {
    unsafe fn release(value: AnonStructA2f37e1d5ed0e4cd, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

impl InternalFilesWriteUtf8AtomicResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            InternalFilesWriteUtf8AtomicResultTag::Err => {
                let payload = unsafe { value.take_payload_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            InternalFilesWriteUtf8AtomicResultTag::Ok => {},
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            InternalFilesWriteUtf8AtomicResultTag::Err => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            InternalFilesWriteUtf8AtomicResultTag::Ok => {},
        }
    }
}

pub struct InternalFilesWriteUtf8AtomicResultRelease;

unsafe impl RocRelease<InternalFilesWriteUtf8AtomicResult> for InternalFilesWriteUtf8AtomicResultRelease {
    unsafe fn release(value: InternalFilesWriteUtf8AtomicResult, roc_host: &RocHost) {
        unsafe { value.decref(roc_host); }
    }
}

/// Release one owned reference to a `RocList<RocListWith<u8, false>>`.
///
/// The allocation's final reference is claimed atomically before any element
/// is read, so concurrent owners cannot skip or duplicate element teardown.
///
/// # Safety
/// `value` must own one live Roc list reference.
pub unsafe fn decref_list_of_list_of_type3(value: RocList<RocListWith<u8, false>>, roc_host: &RocHost) {
    unsafe { value.release_with::<RocListSpineRelease>(roc_host); }
}

/// Release one owned reference to a `RocList<AnonStruct1d0ac13b7d648fa3>`.
///
/// The allocation's final reference is claimed atomically before any element
/// is read, so concurrent owners cannot skip or duplicate element teardown.
///
/// # Safety
/// `value` must own one live Roc list reference.
pub unsafe fn decref_list_of_anon_struct1d0ac13b7d648fa3(value: RocList<AnonStruct1d0ac13b7d648fa3>, roc_host: &RocHost) {
    unsafe { value.release_with::<AnonStruct1d0ac13b7d648fa3Release>(roc_host); }
}

/// Release one owned reference to a `RocList<AnonStructF74678f34f8b3cbf>`.
///
/// The allocation's final reference is claimed atomically before any element
/// is read, so concurrent owners cannot skip or duplicate element teardown.
///
/// # Safety
/// `value` must own one live Roc list reference.
pub unsafe fn decref_list_of_anon_struct_f74678f34f8b3cbf(value: RocList<AnonStructF74678f34f8b3cbf>, roc_host: &RocHost) {
    unsafe { value.release_with::<AnonStructF74678f34f8b3cbfRelease>(roc_host); }
}

/// Release one owned reference to a `RocList<RocStr>`.
///
/// The allocation's final reference is claimed atomically before any element
/// is read, so concurrent owners cannot skip or duplicate element teardown.
///
/// # Safety
/// `value` must own one live Roc list reference.
pub unsafe fn decref_list_of_str(value: RocList<RocStr>, roc_host: &RocHost) {
    unsafe { value.release_with::<RocStrRelease>(roc_host); }
}

/// Release one owned reference to a `RocList<RocList<AnonStructBff7bc5a4faa26ae>>`.
///
/// The allocation's final reference is claimed atomically before any element
/// is read, so concurrent owners cannot skip or duplicate element teardown.
///
/// # Safety
/// `value` must own one live Roc list reference.
pub unsafe fn decref_list_of_list_of_anon_struct_bff7bc5a4faa26ae(value: RocList<RocList<AnonStructBff7bc5a4faa26ae>>, roc_host: &RocHost) {
    unsafe { value.release_with::<RocListRelease<AnonStructBff7bc5a4faa26aeRelease>>(roc_host); }
}

/// Release one owned reference to a `RocList<AnonStructBff7bc5a4faa26ae>`.
///
/// The allocation's final reference is claimed atomically before any element
/// is read, so concurrent owners cannot skip or duplicate element teardown.
///
/// # Safety
/// `value` must own one live Roc list reference.
pub unsafe fn decref_list_of_anon_struct_bff7bc5a4faa26ae(value: RocList<AnonStructBff7bc5a4faa26ae>, roc_host: &RocHost) {
    unsafe { value.release_with::<AnonStructBff7bc5a4faa26aeRelease>(roc_host); }
}

/// Release one owned reference to a `RocList<AnonStruct27556b2f7cb4f65f>`.
///
/// The allocation's final reference is claimed atomically before any element
/// is read, so concurrent owners cannot skip or duplicate element teardown.
///
/// # Safety
/// `value` must own one live Roc list reference.
pub unsafe fn decref_list_of_anon_struct27556b2f7cb4f65f(value: RocList<AnonStruct27556b2f7cb4f65f>, roc_host: &RocHost) {
    unsafe { value.release_with::<AnonStruct27556b2f7cb4f65fRelease>(roc_host); }
}

/// Release one owned reference to a `RocList<AnonStruct916a0c1ad2ed4712>`.
///
/// The allocation's final reference is claimed atomically before any element
/// is read, so concurrent owners cannot skip or duplicate element teardown.
///
/// # Safety
/// `value` must own one live Roc list reference.
pub unsafe fn decref_list_of_anon_struct916a0c1ad2ed4712(value: RocList<AnonStruct916a0c1ad2ed4712>, roc_host: &RocHost) {
    unsafe { value.release_with::<AnonStruct916a0c1ad2ed4712Release>(roc_host); }
}

/// Release one owned reference to a `RocList<AnonStruct77eaba63dfee299d>`.
///
/// The allocation's final reference is claimed atomically before any element
/// is read, so concurrent owners cannot skip or duplicate element teardown.
///
/// # Safety
/// `value` must own one live Roc list reference.
pub unsafe fn decref_list_of_anon_struct77eaba63dfee299d(value: RocList<AnonStruct77eaba63dfee299d>, roc_host: &RocHost) {
    unsafe { value.release_with::<AnonStruct77eaba63dfee299dRelease>(roc_host); }
}

/// Release one owned reference to a `RocList<AnonStruct770b9d9b3d3d255>`.
///
/// The allocation's final reference is claimed atomically before any element
/// is read, so concurrent owners cannot skip or duplicate element teardown.
///
/// # Safety
/// `value` must own one live Roc list reference.
pub unsafe fn decref_list_of_anon_struct770b9d9b3d3d255(value: RocList<AnonStruct770b9d9b3d3d255>, roc_host: &RocHost) {
    unsafe { value.release_with::<AnonStruct770b9d9b3d3d255Release>(roc_host); }
}

/// Release one owned reference to a `RocList<AnonStructDa3665c9cce9a3c>`.
///
/// The allocation's final reference is claimed atomically before any element
/// is read, so concurrent owners cannot skip or duplicate element teardown.
///
/// # Safety
/// `value` must own one live Roc list reference.
pub unsafe fn decref_list_of_anon_struct_da3665c9cce9a3c(value: RocList<AnonStructDa3665c9cce9a3c>, roc_host: &RocHost) {
    unsafe { value.release_with::<AnonStructDa3665c9cce9a3cRelease>(roc_host); }
}


// Runtime Symbols
//
// The host defines these linker symbols. Compiled Roc code calls them directly.

#[allow(improper_ctypes)]
unsafe extern "C" {
    pub fn roc_alloc(length: usize, alignment: usize) -> *mut c_void;
    pub fn roc_dealloc(ptr: *mut c_void, alignment: usize);
    pub fn roc_realloc(ptr: *mut c_void, new_length: usize, alignment: usize) -> *mut c_void;
    pub fn roc_dbg(bytes: *const u8, len: usize);
    pub fn roc_expect_failed(bytes: *const u8, len: usize);
    pub fn roc_crashed(bytes: *const u8, len: usize);
}

extern "C" fn direct_roc_alloc(_host: *mut RocHost, length: usize, alignment: usize) -> *mut c_void {
    unsafe { roc_alloc(length, alignment) }
}

extern "C" fn direct_roc_dealloc(_host: *mut RocHost, ptr: *mut c_void, alignment: usize) {
    unsafe { roc_dealloc(ptr, alignment); }
}

extern "C" fn direct_roc_realloc(_host: *mut RocHost, ptr: *mut c_void, new_length: usize, alignment: usize) -> *mut c_void {
    unsafe { roc_realloc(ptr, new_length, alignment) }
}

extern "C" fn direct_roc_dbg(_host: *mut RocHost, bytes: *const u8, len: usize) {
    unsafe { roc_dbg(bytes, len); }
}

extern "C" fn direct_roc_expect_failed(_host: *mut RocHost, bytes: *const u8, len: usize) {
    unsafe { roc_expect_failed(bytes, len); }
}

extern "C" fn direct_roc_crashed(_host: *mut RocHost, bytes: *const u8, len: usize) {
    unsafe { roc_crashed(bytes, len); }
}

fn direct_roc_host() -> RocHost {
    RocHost {
        env: core::ptr::null_mut(),
        roc_alloc: direct_roc_alloc,
        roc_dealloc: direct_roc_dealloc,
        roc_realloc: direct_roc_realloc,
        roc_dbg: direct_roc_dbg,
        roc_expect_failed: direct_roc_expect_failed,
        roc_crashed: direct_roc_crashed,
    }
}

// Hosted Symbols
//
// The platform host must export these symbols with the exact direct C ABI signatures.
// Refcounted arguments are owned by the hosted function.

#[allow(improper_ctypes)]
unsafe extern "C" {
    /// Hosted symbol for HostGlue.node_text!
    /// Roc signature: Str => U64
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { arg0.decref(roc_host); }
    pub fn roc_gui_node_text(arg0: RocStr) -> u64;

    /// Hosted symbol for HostGlue.node_styled_text!
    /// Roc signature: { fg : U64, font_face : U8, font_size : U32, font_weight : U32, runs : List({ bg : U64, fg : U64, font_weight : U32, len : U64, monospace : Bool, underline : Bool }), value : Str } => U64
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { arg0.decref(roc_host); }
    pub fn roc_gui_node_styled_text(arg0: HostGlueNodeStyledTextArgs) -> u64;

    /// Hosted symbol for HostGlue.children_begin!
    /// Roc signature: {} => U64
    pub fn roc_gui_children_begin() -> u64;

    /// Hosted symbol for HostGlue.children_push!
    /// Roc signature: U64, U64 => {}
    pub fn roc_gui_children_push(arg0: u64, arg1: u64);

    /// Hosted symbol for HostGlue.keyed_seed!
    /// Roc signature: { container : U64, keys : List(List(U8)), revision : U64 } => {}
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { arg0.decref(roc_host); }
    pub fn roc_gui_keyed_seed(arg0: HostGlueKeyedSeedArgs);

    /// Hosted symbol for HostGlue.keyed_edit_begin!
    /// Roc signature: { base_revision : U64, container : U64, new_revision : U64 } => {}
    pub fn roc_gui_keyed_edit_begin(arg0: HostGlueKeyedEditBeginArgs);

    /// Hosted symbol for HostGlue.keyed_insert_before!
    /// Roc signature: { before : List(U8), key : List(U8), root : U64 } => {}
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { arg0.decref(roc_host); }
    pub fn roc_gui_keyed_insert_before(arg0: HostGlueKeyedInsertBeforeArgs);

    /// Hosted symbol for HostGlue.keyed_remove!
    /// Roc signature: List(U8) => {}
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { arg0.decref(roc_host); }
    pub fn roc_gui_keyed_remove(arg0: RocListWith<u8, false>);

    /// Hosted symbol for HostGlue.keyed_move_before!
    /// Roc signature: { before : List(U8), key : List(U8) } => {}
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { arg0.decref(roc_host); }
    pub fn roc_gui_keyed_move_before(arg0: HostGlueKeyedMoveBeforeArgs);

    /// Hosted symbol for HostGlue.keyed_set!
    /// Roc signature: { key : List(U8), root : U64 } => {}
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { arg0.decref(roc_host); }
    pub fn roc_gui_keyed_set(arg0: HostGlueKeyedSetArgs);

    /// Hosted symbol for HostGlue.keyed_edit_commit!
    /// Roc signature: {} => {}
    pub fn roc_gui_keyed_edit_commit();

    /// Hosted symbol for HostGlue.component_work!
    /// Roc signature: U8, U64 => {}
    pub fn roc_gui_component_work(arg0: u8, arg1: u64);

    /// Hosted symbol for HostGlue.node_boundary!
    /// Roc signature: U64, U64 => U64
    pub fn roc_gui_node_boundary(arg0: u64, arg1: u64) -> u64;

    /// Hosted symbol for HostGlue.retain_subtree!
    /// Roc signature: U64 => U64
    pub fn roc_gui_retain_subtree(arg0: u64) -> u64;

    /// Hosted symbol for HostGlue.begin_render!
    /// Roc signature: U64 => {}
    pub fn roc_gui_begin_render(arg0: u64);

    /// Hosted symbol for HostGlue.scope_enter!
    /// Roc signature: U8, Str, U64 => {}
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { arg1.decref(roc_host); }
    pub fn roc_gui_scope_enter(arg0: u8, arg1: RocStr, arg2: u64);

    /// Hosted symbol for HostGlue.scope_exit!
    /// Roc signature: {} => {}
    pub fn roc_gui_scope_exit();

    /// Hosted symbol for HostGlue.component_resolve!
    /// Roc signature: U8, List(U8) => { instance : U64, root : U64 }
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { arg1.decref(roc_host); }
    pub fn roc_gui_component_resolve(arg0: u8, arg1: RocListWith<u8, false>) -> AnonStruct2c3712e02fef0cd;

    /// Hosted symbol for HostGlue.component_enter!
    /// Roc signature: U64 => {}
    pub fn roc_gui_component_enter(arg0: u64);

    /// Hosted symbol for HostGlue.component_exit!
    /// Roc signature: {} => {}
    pub fn roc_gui_component_exit();

    /// Hosted symbol for HostGlue.node_row!
    /// Roc signature: { active_bg : U64, align : U8, bg : U64, border_bottom : U32, border_color : U64, border_left : U32, border_right : U32, border_top : U32, builder : U64, disabled_bg : U64, disabled_fg : U64, fg : U64, focus_color : U64, font_face : U8, font_size : U32, font_weight : U32, gap : U32, grow : Bool, height : U32, height_kind : U8, hover_bg : U64, justify : U8, label : Str, max_height : U32, max_height_kind : U8, max_width : U32, max_width_kind : U8, min_height : U32, min_height_kind : U8, min_width : U32, min_width_kind : U8, overflow_x : U8, overflow_y : U8, padding_bottom : U32, padding_left : U32, padding_right : U32, padding_top : U32, radius : U32, shadow : U32, shadow_alpha : U32, shadow_color : U64, shadow_y : U32, text_overflow : U8, width : U32, width_kind : U8 } => U64
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { arg0.decref(roc_host); }
    pub fn roc_gui_node_row(arg0: HostGlueNodeRowArgs) -> u64;

    /// Hosted symbol for HostGlue.node_column!
    /// Roc signature: { active_bg : U64, align : U8, bg : U64, border_bottom : U32, border_color : U64, border_left : U32, border_right : U32, border_top : U32, builder : U64, disabled_bg : U64, disabled_fg : U64, fg : U64, focus_color : U64, font_face : U8, font_size : U32, font_weight : U32, gap : U32, grow : Bool, height : U32, height_kind : U8, hover_bg : U64, justify : U8, label : Str, max_height : U32, max_height_kind : U8, max_width : U32, max_width_kind : U8, min_height : U32, min_height_kind : U8, min_width : U32, min_width_kind : U8, overflow_x : U8, overflow_y : U8, padding_bottom : U32, padding_left : U32, padding_right : U32, padding_top : U32, radius : U32, shadow : U32, shadow_alpha : U32, shadow_color : U64, shadow_y : U32, text_overflow : U8, width : U32, width_kind : U8 } => U64
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { arg0.decref(roc_host); }
    pub fn roc_gui_node_column(arg0: HostGlueNodeColumnArgs) -> u64;

    /// Hosted symbol for HostGlue.node_dialog!
    /// Roc signature: { active_bg : U64, align : U8, bg : U64, border_bottom : U32, border_color : U64, border_left : U32, border_right : U32, border_top : U32, builder : U64, disabled_bg : U64, disabled_fg : U64, fg : U64, focus_color : U64, font_face : U8, font_size : U32, font_weight : U32, gap : U32, grow : Bool, height : U32, height_kind : U8, hover_bg : U64, justify : U8, label : Str, max_height : U32, max_height_kind : U8, max_width : U32, max_width_kind : U8, min_height : U32, min_height_kind : U8, min_width : U32, min_width_kind : U8, overflow_x : U8, overflow_y : U8, padding_bottom : U32, padding_left : U32, padding_right : U32, padding_top : U32, radius : U32, shadow : U32, shadow_alpha : U32, shadow_color : U64, shadow_y : U32, text_overflow : U8, width : U32, width_kind : U8 } => U64
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { arg0.decref(roc_host); }
    pub fn roc_gui_node_dialog(arg0: HostGlueNodeDialogArgs) -> u64;

    /// Hosted symbol for HostGlue.node_popover!
    /// Roc signature: { active_bg : U64, align : U8, bg : U64, border_bottom : U32, border_color : U64, border_left : U32, border_right : U32, border_top : U32, builder : U64, delay_ms : U32, disabled_bg : U64, disabled_fg : U64, fg : U64, focus_color : U64, focus_serial : U64, font_face : U8, font_size : U32, font_weight : U32, gap : U32, grow : Bool, height : U32, height_kind : U8, hover_bg : U64, hover_enter : Bool, hover_exit : Bool, justify : U8, label : Str, max_height : U32, max_height_kind : U8, max_width : U32, max_width_kind : U8, min_height : U32, min_height_kind : U8, min_width : U32, min_width_kind : U8, overflow_x : U8, overflow_y : U8, padding_bottom : U32, padding_left : U32, padding_right : U32, padding_top : U32, placement : U8, radius : U32, shadow : U32, shadow_alpha : U32, shadow_color : U64, shadow_y : U32, shortcuts : List({ focus : Bool, keys : Str }), text_overflow : U8, width : U32, width_kind : U8 } => { hover_enter : U64, hover_exit : U64, id : U64, shortcut : U64 }
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { arg0.decref(roc_host); }
    pub fn roc_gui_node_popover(arg0: HostGlueNodePopoverArgs) -> AnonStruct10af2f3a422106e8;

    /// Hosted symbol for HostGlue.node_panel!
    /// Roc signature: { active_bg : U64, align : U8, bg : U64, border_bottom : U32, border_color : U64, border_left : U32, border_right : U32, border_top : U32, builder : U64, disabled_bg : U64, disabled_fg : U64, fg : U64, focus_color : U64, font_face : U8, font_size : U32, font_weight : U32, gap : U32, grow : Bool, height : U32, height_kind : U8, hover_bg : U64, justify : U8, label : Str, max_height : U32, max_height_kind : U8, max_width : U32, max_width_kind : U8, min_height : U32, min_height_kind : U8, min_width : U32, min_width_kind : U8, overflow_x : U8, overflow_y : U8, padding_bottom : U32, padding_left : U32, padding_right : U32, padding_top : U32, radius : U32, shadow : U32, shadow_alpha : U32, shadow_color : U64, shadow_y : U32, text_overflow : U8, width : U32, width_kind : U8 } => U64
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { arg0.decref(roc_host); }
    pub fn roc_gui_node_panel(arg0: HostGlueNodePanelArgs) -> u64;

    /// Hosted symbol for HostGlue.node_scroll!
    /// Roc signature: { active_bg : U64, align : U8, axis : U8, bg : U64, border_bottom : U32, border_color : U64, border_left : U32, border_right : U32, border_top : U32, child : U64, disabled_bg : U64, disabled_fg : U64, fg : U64, focus_color : U64, font_face : U8, font_size : U32, font_weight : U32, gap : U32, grow : Bool, height : U32, height_kind : U8, hover_bg : U64, justify : U8, max_height : U32, max_height_kind : U8, max_width : U32, max_width_kind : U8, min_height : U32, min_height_kind : U8, min_width : U32, min_width_kind : U8, name : Str, overflow_x : U8, overflow_y : U8, padding_bottom : U32, padding_left : U32, padding_right : U32, padding_top : U32, radius : U32, shadow : U32, shadow_alpha : U32, shadow_color : U64, shadow_y : U32, text_overflow : U8, width : U32, width_kind : U8 } => U64
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { arg0.decref(roc_host); }
    pub fn roc_gui_node_scroll(arg0: HostGlueNodeScrollArgs) -> u64;

    /// Hosted symbol for HostGlue.node_action_button!
    /// Roc signature: { active_bg : U64, align : U8, bg : U64, border_bottom : U32, border_color : U64, border_left : U32, border_right : U32, border_top : U32, caption : Str, disabled_bg : U64, disabled_fg : U64, enabled : Bool, fg : U64, focus_color : U64, font_face : U8, font_size : U32, font_weight : U32, gap : U32, grow : Bool, height : U32, height_kind : U8, hover_bg : U64, hover_enter : Bool, hover_exit : Bool, justify : U8, label : Str, max_height : U32, max_height_kind : U8, max_width : U32, max_width_kind : U8, min_height : U32, min_height_kind : U8, min_width : U32, min_width_kind : U8, overflow_x : U8, overflow_y : U8, padding_bottom : U32, padding_left : U32, padding_right : U32, padding_top : U32, radius : U32, role : U8, shadow : U32, shadow_alpha : U32, shadow_color : U64, shadow_y : U32, text_overflow : U8, width : U32, width_kind : U8 } => { hover_enter : U64, hover_exit : U64, id : U64 }
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { arg0.decref(roc_host); }
    pub fn roc_gui_node_action_button(arg0: HostGlueNodeActionButtonArgs) -> AnonStructBecf162f14687660;

    /// Hosted symbol for HostGlue.node_virtual_item!
    /// Roc signature: U64, U64 => U64
    pub fn roc_gui_node_virtual_item(arg0: u64, arg1: u64) -> u64;

    /// Hosted symbol for HostGlue.node_virtual_list!
    /// Roc signature: { active_bg : U64, align : U8, bg : U64, border_bottom : U32, border_color : U64, border_left : U32, border_right : U32, border_top : U32, builder : U64, count : U64, disabled_bg : U64, disabled_fg : U64, fg : U64, first : U64, focus_color : U64, font_face : U8, font_size : U32, font_weight : U32, gap : U32, grow : Bool, height : U32, height_kind : U8, hover_bg : U64, instance : U64, justify : U8, max_height : U32, max_height_kind : U8, max_width : U32, max_width_kind : U8, min_height : U32, min_height_kind : U8, min_width : U32, min_width_kind : U8, name : Str, notify : Bool, overflow_x : U8, overflow_y : U8, padding_bottom : U32, padding_left : U32, padding_right : U32, padding_top : U32, radius : U32, row_gap : U32, row_height : U32, shadow : U32, shadow_alpha : U32, shadow_color : U64, shadow_y : U32, text_overflow : U8, width : U32, width_kind : U8 } => U64
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { arg0.decref(roc_host); }
    pub fn roc_gui_node_virtual_list(arg0: HostGlueNodeVirtualListArgs) -> u64;

    /// Hosted symbol for HostGlue.node_checkbox!
    /// Roc signature: { active_bg : U64, align : U8, bg : U64, border_bottom : U32, border_color : U64, border_left : U32, border_right : U32, border_top : U32, box_bg : U64, box_border : U64, box_checked_bg : U64, checked : Bool, disabled_bg : U64, disabled_fg : U64, enabled : Bool, fg : U64, focus_color : U64, font_face : U8, font_size : U32, font_weight : U32, gap : U32, grow : Bool, height : U32, height_kind : U8, hover_bg : U64, justify : U8, label : Str, mark_color : U64, max_height : U32, max_height_kind : U8, max_width : U32, max_width_kind : U8, min_height : U32, min_height_kind : U8, min_width : U32, min_width_kind : U8, overflow_x : U8, overflow_y : U8, padding_bottom : U32, padding_left : U32, padding_right : U32, padding_top : U32, radius : U32, shadow : U32, shadow_alpha : U32, shadow_color : U64, shadow_y : U32, text_overflow : U8, width : U32, width_kind : U8 } => U64
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { arg0.decref(roc_host); }
    pub fn roc_gui_node_checkbox(arg0: HostGlueNodeCheckboxArgs) -> u64;

    /// Hosted symbol for HostGlue.node_textarea!
    /// Roc signature: { active_bg : U64, align : U8, bg : U64, border_bottom : U32, border_color : U64, border_left : U32, border_right : U32, border_top : U32, disabled_bg : U64, disabled_fg : U64, enabled : Bool, fg : U64, focus_color : U64, font_face : U8, font_size : U32, font_weight : U32, gap : U32, grow : Bool, height : U32, height_kind : U8, hover_bg : U64, justify : U8, label : Str, max_height : U32, max_height_kind : U8, max_width : U32, max_width_kind : U8, min_height : U32, min_height_kind : U8, min_width : U32, min_width_kind : U8, overflow_x : U8, overflow_y : U8, padding_bottom : U32, padding_left : U32, padding_right : U32, padding_top : U32, placeholder : Str, radius : U32, read_only : Bool, shadow : U32, shadow_alpha : U32, shadow_color : U64, shadow_y : U32, text_overflow : U8, value : Str, width : U32, width_kind : U8 } => U64
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { arg0.decref(roc_host); }
    pub fn roc_gui_node_textarea(arg0: HostGlueNodeTextareaArgs) -> u64;

    /// Hosted symbol for HostGlue.node_image!
    /// Roc signature: { active_bg : U64, align : U8, bg : U64, border_bottom : U32, border_color : U64, border_left : U32, border_right : U32, border_top : U32, bytes : List(U8), disabled_bg : U64, disabled_fg : U64, fg : U64, fit : U8, focus_color : U64, font_face : U8, font_size : U32, font_weight : U32, format : U8, gap : U32, grayscale : Bool, grow : Bool, height : U32, height_kind : U8, hover_bg : U64, justify : U8, label : Str, max_height : U32, max_height_kind : U8, max_width : U32, max_width_kind : U8, min_height : U32, min_height_kind : U8, min_width : U32, min_width_kind : U8, overflow_x : U8, overflow_y : U8, padding_bottom : U32, padding_left : U32, padding_right : U32, padding_top : U32, radius : U32, shadow : U32, shadow_alpha : U32, shadow_color : U64, shadow_y : U32, text_overflow : U8, width : U32, width_kind : U8 } => U64
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { arg0.decref(roc_host); }
    pub fn roc_gui_node_image(arg0: HostGlueNodeImageArgs) -> u64;

    /// Hosted symbol for HostGlue.node_canvas!
    /// Roc signature: { bg : U64, border_color : U64, border_width : U32, grow : Bool, height : U32, height_kind : U8, hover : Bool, label : Str, primitives : List({ align : U8, fill : U64, height : U32, key : U64, kind : U8, label : Str, radius : U32, stroke : U64, stroke_width : U32, text : Str, text_size : U32, width : U32, x : I32, x2 : I32, y : I32, y2 : I32 }), radius : U32, wheel : Bool, width : U32, width_kind : U8 } => U64
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { arg0.decref(roc_host); }
    pub fn roc_gui_node_canvas(arg0: HostGlueNodeCanvasArgs) -> u64;

    /// Hosted symbol for HostGlue.canvas_event!
    /// Roc signature: {} => { dx : I32, dy : I32, phase : U8, target : U64, x : I32, y : I32 }
    pub fn roc_gui_canvas_event() -> AnonStructE7c8b7462a78f14b;

    /// Hosted symbol for HostGlue.shortcut_event!
    /// Roc signature: {} => { index : U64, keys : Str }
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_gui_shortcut_event() -> AnonStructBdcbe7a50e42954d;

    /// Hosted symbol for HostGlue.node_split!
    /// Roc signature: { active_bg : U64, align : U8, axis : U8, bg : U64, border_bottom : U32, border_color : U64, border_left : U32, border_right : U32, border_top : U32, builder : U64, collapsed : Bool, collapsible : Bool, disabled_bg : U64, disabled_fg : U64, fg : U64, focus_color : U64, font_face : U8, font_size : U32, font_weight : U32, gap : U32, grow : Bool, height : U32, height_kind : U8, hover_bg : U64, justify : U8, keys : List({ focus : Bool, keys : Str }), label : Str, max : U32, max_height : U32, max_height_kind : U8, max_width : U32, max_width_kind : U8, min : U32, min_height : U32, min_height_kind : U8, min_width : U32, min_width_kind : U8, overflow_x : U8, overflow_y : U8, padding_bottom : U32, padding_left : U32, padding_right : U32, padding_top : U32, radius : U32, shadow : U32, shadow_alpha : U32, shadow_color : U64, shadow_y : U32, side : U8, size : U32, text_overflow : U8, thickness : U32, width : U32, width_kind : U8 } => { id : U64, shortcut : U64 }
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { arg0.decref(roc_host); }
    pub fn roc_gui_node_split(arg0: HostGlueNodeSplitArgs) -> AnonStruct1da781635f2f9fce;

    /// Hosted symbol for HostGlue.resize_event!
    /// Roc signature: {} => { collapsed : Bool, size : U32 }
    pub fn roc_gui_resize_event() -> AnonStruct7dea1af4373e83a;

    /// Hosted symbol for HostGlue.virtual_window!
    /// Roc signature: { count : U64, instance : U64, row_height : U32, scroll_align : U8, scroll_row : U64, scroll_serial : U64 } => { end : U64, first : U64 }
    pub fn roc_gui_virtual_window(arg0: HostGlueVirtualWindowArgs) -> AnonStructBe98516b6662a57b;

    /// Hosted symbol for HostGlue.virtual_rows_event!
    /// Roc signature: {} => { end : U64, refresh : Bool, report : Bool, start : U64 }
    pub fn roc_gui_virtual_rows_event() -> AnonStructD56b0eabf62517b9;

    /// Hosted symbol for HostGlue.input_value!
    /// Roc signature: {} => Str
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_gui_input_value() -> RocStr;

    /// Hosted symbol for HostGlue.node_text_input!
    /// Roc signature: { active_bg : U64, align : U8, bg : U64, border_bottom : U32, border_color : U64, border_left : U32, border_right : U32, border_top : U32, disabled_bg : U64, disabled_fg : U64, enabled : Bool, fg : U64, focus_color : U64, font_face : U8, font_size : U32, font_weight : U32, gap : U32, grow : Bool, height : U32, height_kind : U8, hover_bg : U64, justify : U8, label : Str, max_height : U32, max_height_kind : U8, max_width : U32, max_width_kind : U8, min_height : U32, min_height_kind : U8, min_width : U32, min_width_kind : U8, overflow_x : U8, overflow_y : U8, padding_bottom : U32, padding_left : U32, padding_right : U32, padding_top : U32, placeholder : Str, radius : U32, shadow : U32, shadow_alpha : U32, shadow_color : U64, shadow_y : U32, text_overflow : U8, value : Str, width : U32, width_kind : U8 } => { change : U64, id : U64, submit : U64 }
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { arg0.decref(roc_host); }
    pub fn roc_gui_node_text_input(arg0: HostGlueNodeTextInputArgs) -> AnonStructDc4c26f806dfc5bf;

    /// Hosted symbol for HostGlue.sqlite_open_read!
    /// Roc signature: Resource.Handle([DirReadResource]), Str => Try(Resource.Handle([SqliteReadResource]), { code : U8, message : Str })
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_box_with(arg0 as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    ///     unsafe { arg1.decref(roc_host); }
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_sqlite_open_read(arg0: *mut u64, arg1: RocStr) -> HostGlueSqliteOpenReadResult;

    /// Hosted symbol for HostGlue.sqlite_open_file_read!
    /// Roc signature: Resource.Handle([FileReadResource]) => Try(Resource.Handle([SqliteReadResource]), { code : U8, message : Str })
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_box_with(arg0 as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_sqlite_open_file_read(arg0: *mut u64) -> HostGlueSqliteOpenReadResult;

    /// Hosted symbol for HostGlue.sqlite_watch!
    /// Roc signature: Resource.Handle([SqliteReadResource]) => Try(Resource.Handle([WatchResource]), { code : U8, message : Str })
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_box_with(arg0 as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_sqlite_watch(arg0: *mut u64) -> HostGlueSqliteWatchResult;

    /// Hosted symbol for HostGlue.sqlite_query!
    /// Roc signature: Resource.Handle([SqliteReadResource]), { page_rows : U64, params : List({ bytes : List(U8), integer : I64, kind : U8, real : F64, text : Str }), sql : Str } => Try({ columns : List(Str), more : Bool, rows : List(List({ bytes : List(U8), integer : I64, kind : U8, real : F64, text : Str })) }, { code : U8, message : Str })
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_box_with(arg0 as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    ///     unsafe { arg1.decref(roc_host); }
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_sqlite_query(arg0: *mut u64, arg1: AnonStructAd07206fc55b9842) -> HostGlueSqliteQueryResult;

    /// Hosted symbol for InternalFiles.app_data!
    /// Roc signature: {} => Try(Resource.Handle([DirReadWriteResource]), { code : U8, message : Str })
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_files_app_data() -> InternalFilesAppDataResult;

    /// Hosted symbol for InternalFiles.read_utf8!
    /// Roc signature: Resource.Handle([DirReadWriteResource]), Str => Try({ found : Bool, value : Str }, { code : U8, message : Str })
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_box_with(arg0 as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    ///     unsafe { arg1.decref(roc_host); }
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_files_dir_read_utf8(arg0: *mut u64, arg1: RocStr) -> InternalFilesReadUtf8Result;

    /// Hosted symbol for InternalFiles.write_utf8_atomic!
    /// Roc signature: Resource.Handle([DirReadWriteResource]), Str, Str => Try({}, { code : U8, message : Str })
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_box_with(arg0 as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    ///     unsafe { arg1.decref(roc_host); }
    ///     unsafe { arg2.decref(roc_host); }
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_files_dir_write_utf8_atomic(arg0: *mut u64, arg1: RocStr, arg2: RocStr) -> InternalFilesWriteUtf8AtomicResult;

    /// Hosted symbol for HostGlue.clipboard_acquire!
    /// Roc signature: {} => Try(Resource.Handle([ClipboardResource]), { code : U8, message : Str })
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_clipboard_acquire() -> HostGlueClipboardAcquireResult;

    /// Hosted symbol for HostGlue.clipboard_read_text!
    /// Roc signature: Resource.Handle([ClipboardResource]) => Try({ sequence : U64, text : Str }, { code : U8, message : Str })
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_box_with(arg0 as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_clipboard_read_text(arg0: *mut u64) -> HostGlueClipboardReadTextResult;

    /// Hosted symbol for HostGlue.clipboard_write_text!
    /// Roc signature: Resource.Handle([ClipboardResource]), Str => Try({}, { code : U8, message : Str })
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_box_with(arg0 as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    ///     unsafe { arg1.decref(roc_host); }
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_clipboard_write_text(arg0: *mut u64, arg1: RocStr) -> HostGlueClipboardWriteTextResult;

    /// Hosted symbol for HostGlue.audio_acquire!
    /// Roc signature: {} => Try(Resource.Handle([AudioOutputResource]), { code : U8, message : Str })
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_audio_acquire() -> HostGlueAudioAcquireResult;

    /// Hosted symbol for HostGlue.audio_load!
    /// Roc signature: Resource.Handle([AudioOutputResource]), Resource.Handle([DirReadResource]), Str => Try({ duration_ms : U64, track : Resource.Handle([AudioTrackResource]) }, { code : U8, message : Str })
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_box_with(arg0 as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    ///     unsafe { decref_box_with(arg1 as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    ///     unsafe { arg2.decref(roc_host); }
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_audio_load(arg0: *mut u64, arg1: *mut u64, arg2: RocStr) -> HostGlueAudioLoadResult;

    /// Hosted symbol for HostGlue.audio_play!
    /// Roc signature: Resource.Handle([AudioTrackResource]) => Try({}, { code : U8, message : Str })
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_box_with(arg0 as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_audio_play(arg0: *mut u64) -> HostGlueClipboardWriteTextResult;

    /// Hosted symbol for HostGlue.audio_pause!
    /// Roc signature: Resource.Handle([AudioTrackResource]) => Try({}, { code : U8, message : Str })
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_box_with(arg0 as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_audio_pause(arg0: *mut u64) -> HostGlueClipboardWriteTextResult;

    /// Hosted symbol for HostGlue.audio_seek!
    /// Roc signature: Resource.Handle([AudioTrackResource]), U64 => Try({}, { code : U8, message : Str })
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_box_with(arg0 as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_audio_seek(arg0: *mut u64, arg1: u64) -> HostGlueClipboardWriteTextResult;

    /// Hosted symbol for HostGlue.audio_status!
    /// Roc signature: Resource.Handle([AudioTrackResource]) => Try({ duration_ms : U64, position_ms : U64, state : U8 }, { code : U8, message : Str })
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_box_with(arg0 as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_audio_status(arg0: *mut u64) -> HostGlueAudioStatusResult;

    /// Hosted symbol for HostGlue.audio_stop!
    /// Roc signature: Resource.Handle([AudioTrackResource]) => Try({}, { code : U8, message : Str })
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_box_with(arg0 as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_audio_stop(arg0: *mut u64) -> HostGlueClipboardWriteTextResult;

    /// Hosted symbol for HostGlue.tcp_connect!
    /// Roc signature: {} => Try(Resource.Handle([TcpStreamResource]), U8)
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_tcp_connect() -> HostGlueTcpConnectResult;

    /// Hosted symbol for HostGlue.tcp_read_up_to!
    /// Roc signature: Resource.Handle([TcpStreamResource]), U64 => Try(List(U8), U8)
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_box_with(arg0 as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_tcp_read_up_to(arg0: *mut u64, arg1: u64) -> HostGlueTcpReadUpToResult;

    /// Hosted symbol for HostGlue.tcp_write_all!
    /// Roc signature: Resource.Handle([TcpStreamResource]), List(U8) => Try({}, U8)
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_box_with(arg0 as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    ///     unsafe { arg1.decref(roc_host); }
    pub fn roc_tcp_write_all(arg0: *mut u64, arg1: RocListWith<u8, false>) -> HostGlueTcpWriteAllResult;

    /// Hosted symbol for HostGlue.tcp_close!
    /// Roc signature: Resource.Handle([TcpStreamResource]) => Try({}, U8)
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_box_with(arg0 as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    pub fn roc_tcp_close(arg0: *mut u64) -> HostGlueTcpWriteAllResult;

    /// Hosted symbol for HostGlue.process_acquire!
    /// Roc signature: {} => Try(Resource.Handle([ProcessGrantResource]), [AcquireProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), CancelProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), ReadProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), ResizeProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), SpawnProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), WriteProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported])])
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_process_acquire() -> HostGlueProcessAcquireResult;

    /// Hosted symbol for HostGlue.process_spawn!
    /// Roc signature: Resource.Handle([ProcessGrantResource]), { columns : U16, rows : U16 } => Try(Resource.Handle([PtyResource]), [AcquireProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), CancelProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), ReadProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), ResizeProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), SpawnProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), WriteProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported])])
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_box_with(arg0 as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    ///     unsafe { arg1.decref(roc_host); }
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_process_spawn(arg0: *mut u64, arg1: AnonStruct93136bf334c2a2fc) -> HostGlueProcessSpawnResult;

    /// Hosted symbol for HostGlue.process_read!
    /// Roc signature: Resource.Handle([PtyResource]), U32 => Try([Canceled, Data(List(U8)), EndOfFile], [AcquireProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), CancelProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), ReadProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), ResizeProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), SpawnProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), WriteProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported])])
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_box_with(arg0 as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_process_read(arg0: *mut u64, arg1: u32) -> HostGlueProcessReadResult;

    /// Hosted symbol for HostGlue.process_write!
    /// Roc signature: Resource.Handle([PtyResource]), List(U8) => Try(U32, [AcquireProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), CancelProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), ReadProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), ResizeProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), SpawnProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), WriteProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported])])
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_box_with(arg0 as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    ///     unsafe { arg1.decref(roc_host); }
    pub fn roc_process_write(arg0: *mut u64, arg1: RocListWith<u8, false>) -> HostGlueProcessWriteResult;

    /// Hosted symbol for HostGlue.process_resize!
    /// Roc signature: Resource.Handle([PtyResource]), { columns : U16, rows : U16 } => Try({}, [AcquireProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), CancelProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), ReadProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), ResizeProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), SpawnProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), WriteProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported])])
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_box_with(arg0 as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    ///     unsafe { arg1.decref(roc_host); }
    pub fn roc_process_resize(arg0: *mut u64, arg1: AnonStruct93136bf334c2a2fc) -> HostGlueProcessResizeResult;

    /// Hosted symbol for HostGlue.process_cancel!
    /// Roc signature: Resource.Handle([PtyResource]) => Try([AlreadyStopped, Canceled], [AcquireProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), CancelProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), ReadProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), ResizeProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), SpawnProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported]), WriteProcessErr([AccessDenied, Busy, Exited, InvalidCapability, InvalidSize, Io, ResourceLimit, Revoked, Unsupported])])
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_box_with(arg0 as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    pub fn roc_process_cancel(arg0: *mut u64) -> HostGlueProcessCancelResult;

    /// Hosted symbol for HostGlue.device_acquire!
    /// Roc signature: {} => Try(Resource.Handle([DeviceGrantResource]), [AcquireDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported]), CloseDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported]), ConnectDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported]), DiscoverDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported]), TransactDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported])])
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_device_acquire() -> HostGlueDeviceAcquireResult;

    /// Hosted symbol for HostGlue.device_discover!
    /// Roc signature: Resource.Handle([DeviceGrantResource]) => Try(List({ manufacturer : Str, product : Str, product_id : U16, vendor_id : U16 }), [AcquireDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported]), CloseDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported]), ConnectDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported]), DiscoverDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported]), TransactDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported])])
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_box_with(arg0 as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_device_discover(arg0: *mut u64) -> HostGlueDeviceDiscoverResult;

    /// Hosted symbol for HostGlue.device_connect!
    /// Roc signature: Resource.Handle([DeviceGrantResource]) => Try(Resource.Handle([DeviceConnectionResource]), [AcquireDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported]), CloseDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported]), ConnectDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported]), DiscoverDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported]), TransactDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported])])
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_box_with(arg0 as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_device_connect(arg0: *mut u64) -> HostGlueDeviceConnectResult;

    /// Hosted symbol for HostGlue.device_transact!
    /// Roc signature: Resource.Handle([DeviceConnectionResource]), List(U8) => Try(List(U8), [AcquireDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported]), CloseDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported]), ConnectDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported]), DiscoverDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported]), TransactDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported])])
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_box_with(arg0 as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    ///     unsafe { arg1.decref(roc_host); }
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_device_transact(arg0: *mut u64, arg1: RocListWith<u8, false>) -> HostGlueDeviceTransactResult;

    /// Hosted symbol for HostGlue.device_close!
    /// Roc signature: Resource.Handle([DeviceConnectionResource]) => Try({}, [AcquireDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported]), CloseDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported]), ConnectDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported]), DiscoverDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported]), TransactDeviceErr([AccessDenied, Busy, Closed, Disconnected, InvalidCapability, InvalidRequest, Io, NotFound, Protocol, ResourceLimit, Revoked, Timeout, Unsupported])])
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_box_with(arg0 as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    pub fn roc_device_close(arg0: *mut u64) -> HostGlueDeviceCloseResult;

    /// Hosted symbol for HostGlue.system_acquire!
    /// Roc signature: {} => Try(Resource.Handle([SystemSamplerResource]), U8)
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_system_acquire() -> HostGlueSystemAcquireResult;

    /// Hosted symbol for HostGlue.system_sample!
    /// Roc signature: Resource.Handle([SystemSamplerResource]) => Try({ cpu_available : Bool, cpu_tenths : U16, disk_available : Bool, disk_read_bytes : U64, disk_written_bytes : U64, memory_available : Bool, memory_total_bytes : U64, memory_used_bytes : U64, network_available : Bool, network_received_bytes : U64, network_transmitted_bytes : U64, processes : List({ cpu_tenths : U16, memory_bytes : U64, name : Str, pid : U64 }), processes_available : Bool, sequence : U64 }, U8)
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_box_with(arg0 as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_system_sample(arg0: *mut u64) -> HostGlueSystemSampleResult;

    /// Hosted symbol for HostGlue.system_close!
    /// Roc signature: Resource.Handle([SystemSamplerResource]) => Try({}, U8)
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_box_with(arg0 as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    pub fn roc_system_close(arg0: *mut u64) -> HostGlueTcpWriteAllResult;

    /// Hosted symbol for HostGlue.image_inspect!
    /// Roc signature: List(U8), U8 => Try({ height : U32, width : U32 }, U8)
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { arg0.decref(roc_host); }
    pub fn roc_image_inspect(arg0: RocListWith<u8, false>, arg1: u8) -> HostGlueImageInspectResult;

    /// Hosted symbol for HostGlue.assets_open!
    /// Roc signature: { asset_set : Str, content_hash : Str, content_mode : U8, content_version : U32, manifest_required : Bool, root : Str, root_kind : U8, schema : U32 } => Try(Resource.Handle([AssetStoreResource]), U8)
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { arg0.decref(roc_host); }
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_assets_open(arg0: HostGlueAssetsOpenArgs) -> HostGlueAssetsOpenResult;

    /// Hosted symbol for HostGlue.assets_read!
    /// Roc signature: Resource.Handle([AssetStoreResource]), Str => Try(List(U8), U8)
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_box_with(arg0 as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    ///     unsafe { arg1.decref(roc_host); }
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_assets_read(arg0: *mut u64, arg1: RocStr) -> HostGlueTcpReadUpToResult;

    /// Hosted symbol for HostGlue.apply!
    /// Roc signature: [Mount({ root : U64 }), NoChange, Replace({ old_root : U64, root : U64 })] => {}
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { arg0.decref(roc_host); }
    pub fn roc_gui_apply(arg0: MountOrNoChangeOrReplace);

    /// Hosted symbol for HostGlue.set_dispatch!
    /// Roc signature: Box(U64 => {}) => {}
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_erased_callable(arg0, roc_host); }
    pub fn roc_gui_set_dispatch(arg0: RocErasedCallable);

    /// Hosted symbol for HostGlue.enqueue_task!
    /// Roc signature: U64, Str, Box(U64 => {}) => {}
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { arg1.decref(roc_host); }
    ///     unsafe { decref_erased_callable(arg2, roc_host); }
    pub fn roc_gui_enqueue_task(arg0: u64, arg1: RocStr, arg2: RocErasedCallable);

    /// Hosted symbol for HostGlue.cancel_task!
    /// Roc signature: U64, Str => {}
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { arg1.decref(roc_host); }
    pub fn roc_gui_cancel_task(arg0: u64, arg1: RocStr);

    /// Hosted symbol for HostGlue.task_complete!
    /// Roc signature: Box(U64 => {}) => {}
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_erased_callable(arg0, roc_host); }
    pub fn roc_gui_task_complete(arg0: RocErasedCallable);

    /// Hosted symbol for HostGlue.timer_start!
    /// Roc signature: U64 => Resource.Handle([TimerResource])
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_gui_timer_start(arg0: u64) -> *mut u64;

    /// Hosted symbol for HostGlue.timer_next!
    /// Roc signature: Resource.Handle([TimerResource]) => Bool
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_box_with(arg0 as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    pub fn roc_gui_timer_next(arg0: *mut u64) -> bool;

    /// Hosted symbol for HostGlue.timer_cancel!
    /// Roc signature: Resource.Handle([TimerResource]) => Bool
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_box_with(arg0 as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    pub fn roc_gui_timer_cancel(arg0: *mut u64) -> bool;

    /// Hosted symbol for HostGlue.http_send!
    /// Roc signature: { body : List(U8), client : Resource.Handle([HttpClientResource]), headers : List((Str, Str)), max_redirects : U8, max_response_bytes : U64, method : U8, method_ext : Str, timeout_ms : U64, url : Str } => Try({ body : List(U8), headers : List((Str, Str)), status : U16 }, [AccessDenied, BodyTooLarge, ConnectFailed, InvalidCapability, InvalidHeader, InvalidRequest, InvalidUrl, RedirectLimit, Revoked, Timeout, UnsupportedScheme])
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { arg0.decref(roc_host); }
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_http_send(arg0: HostGlueHttpSendArgs) -> HostGlueHttpSendResult;

    /// Hosted symbol for HostGlue.http_acquire!
    /// Roc signature: {} => Try(Resource.Handle([HttpClientResource]), [AccessDenied, BodyTooLarge, ConnectFailed, InvalidCapability, InvalidHeader, InvalidRequest, InvalidUrl, RedirectLimit, Revoked, Timeout, UnsupportedScheme])
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_http_acquire() -> HostGlueHttpAcquireResult;

    /// Hosted symbol for HostGlue.work_start!
    /// Roc signature: U8 => {}
    pub fn roc_gui_work_start(arg0: u8);

    /// Hosted symbol for HostGlue.work_end!
    /// Roc signature: U8 => {}
    pub fn roc_gui_work_end(arg0: u8);

    /// Hosted symbol for HostGlue.window_config!
    /// Roc signature: Str, U32, U32, U64, U64 => {}
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { arg0.decref(roc_host); }
    pub fn roc_gui_window_config(arg0: RocStr, arg1: u32, arg2: u32, arg3: u64, arg4: u64);

    /// Hosted symbol for HostGlue.appearance_current!
    /// Roc signature: {} => U8
    pub fn roc_gui_appearance_current() -> u8;

    /// Hosted symbol for HostGlue.appearance_next_change!
    /// Roc signature: U8 => U8
    pub fn roc_gui_appearance_next_change(arg0: u8) -> u8;

    /// Hosted symbol for HostGlue.appearance_prefer!
    /// Roc signature: U8 => {}
    pub fn roc_gui_appearance_prefer(arg0: u8);

    /// Hosted symbol for InternalFiles.pick_directory!
    /// Roc signature: {} => Try([Canceled, Chosen({ directory : Resource.Handle([DirReadResource]), name : Str })], [ListDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), OpenAppDataErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), OpenReadDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), PickDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), PickFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), ReadFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), WatchDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), WriteFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported])])
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_files_pick_directory() -> InternalFilesPickDirectoryResult;

    /// Hosted symbol for InternalFiles.dir_list!
    /// Roc signature: Resource.Handle([DirReadResource]) => Try(List({ bytes : [None, Some(U64)], kind : [Directory, File, Other, SymbolicLink], name : Str }), [ListDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), OpenAppDataErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), OpenReadDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), PickDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), PickFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), ReadFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), WatchDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), WriteFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported])])
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_box_with(arg0 as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_files_dir_list(arg0: *mut u64) -> InternalFilesDirListResult;

    /// Hosted symbol for InternalFiles.dir_open_read!
    /// Roc signature: Resource.Handle([DirReadResource]), Str => Try(Resource.Handle([DirReadResource]), [ListDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), OpenAppDataErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), OpenReadDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), PickDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), PickFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), ReadFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), WatchDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), WriteFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported])])
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_box_with(arg0 as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    ///     unsafe { arg1.decref(roc_host); }
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_files_dir_open_read(arg0: *mut u64, arg1: RocStr) -> InternalFilesDirOpenReadResult;

    /// Hosted symbol for InternalFiles.dir_read!
    /// Roc signature: Resource.Handle([DirReadResource]), Str => Try(List(U8), [ListDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), OpenAppDataErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), OpenReadDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), PickDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), PickFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), ReadFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), WatchDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), WriteFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported])])
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_box_with(arg0 as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    ///     unsafe { arg1.decref(roc_host); }
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_files_dir_read(arg0: *mut u64, arg1: RocStr) -> InternalFilesDirReadResult;

    /// Hosted symbol for InternalFiles.pick_file!
    /// Roc signature: List({ extensions : List(Str), label : Str, mime_types : List(Str) }) => Try([Canceled, Chosen({ file : Resource.Handle([FileReadResource]), name : Str })], [ListDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), OpenAppDataErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), OpenReadDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), PickDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), PickFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), ReadFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), WatchDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), WriteFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported])])
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_list_of_anon_struct_da3665c9cce9a3c(arg0, roc_host); }
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_files_pick_file(arg0: RocList<AnonStructDa3665c9cce9a3c>) -> InternalFilesPickFileResult;

    /// Hosted symbol for InternalFiles.file_read!
    /// Roc signature: Resource.Handle([FileReadResource]) => Try(List(U8), [ListDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), OpenAppDataErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), OpenReadDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), PickDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), PickFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), ReadFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), WatchDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), WriteFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported])])
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_box_with(arg0 as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_files_file_read(arg0: *mut u64) -> InternalFilesDirReadResult;

    /// Hosted symbol for InternalFiles.dir_sha256!
    /// Roc signature: Resource.Handle([DirReadResource]), Str => Try(Str, [ListDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), OpenAppDataErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), OpenReadDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), PickDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), PickFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), ReadFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), WatchDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), WriteFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported])])
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_box_with(arg0 as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    ///     unsafe { arg1.decref(roc_host); }
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_files_dir_sha256(arg0: *mut u64, arg1: RocStr) -> InternalFilesDirSha256Result;

    /// Hosted symbol for InternalFiles.file_sha256!
    /// Roc signature: Resource.Handle([FileReadResource]) => Try(Str, [ListDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), OpenAppDataErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), OpenReadDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), PickDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), PickFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), ReadFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), WatchDirectoryErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported]), WriteFileErr([AccessDenied, InvalidCapability, InvalidName, InvalidUtf8, Io, NotDirectory, NotFound, ResourceLimit, Revoked, Unavailable, Unsupported])])
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_box_with(arg0 as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_files_file_sha256(arg0: *mut u64) -> InternalFilesDirSha256Result;

    /// Hosted symbol for InternalFiles.dir_watch!
    /// Roc signature: Resource.Handle([DirReadResource]) => Try(Resource.Handle([WatchResource]), { code : U8, message : Str })
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_box_with(arg0 as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_files_dir_watch(arg0: *mut u64) -> InternalFilesDirWatchResult;

    /// Hosted symbol for InternalFiles.watch_next!
    /// Roc signature: Resource.Handle([WatchResource]) => { code : U8, names : List(Str), overflowed : Bool, replaced : Bool }
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_box_with(arg0 as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    /// The result is owned by Roc: return exactly one owned reference.
    pub fn roc_files_watch_next(arg0: *mut u64) -> AnonStruct5d4fced1f96c13bb;

    /// Hosted symbol for InternalFiles.watch_cancel!
    /// Roc signature: Resource.Handle([WatchResource]) => Bool
    /// Owned arguments. Release each exactly once before returning, unless it is
    /// moved into storage or into the result:
    ///     unsafe { decref_box_with(arg0 as RocBox, core::mem::align_of::<u64>(), false, None, roc_host); }
    pub fn roc_files_watch_cancel(arg0: *mut u64) -> bool;

}

/// Default memory management functions for Roc platform helpers.
///
/// Memory layout: each allocation prepends size metadata so that dealloc/realloc
/// can recover the original allocation size because `roc_dealloc` receives no length.
#[cfg(not(no_roc_std_helpers))]
pub struct DefaultAllocators;

#[cfg(not(no_roc_std_helpers))]
impl DefaultAllocators {
    /// Allocate memory using the Rust global allocator.
    pub extern "C" fn roc_alloc(_roc_host: *mut RocHost, length: usize, alignment: usize) -> *mut c_void {
        unsafe {
            let min_alignment = alignment.max(core::mem::align_of::<usize>());
            let size_storage_bytes = min_alignment;
            let total_size = checked_add_usize(length, size_storage_bytes, "roc_alloc allocation size overflow");

            debug_assert!(min_alignment.is_power_of_two(), "alignment must be a power of two");
            let layout = Layout::from_size_align_unchecked(total_size, min_alignment);
            let base_ptr = std::alloc::alloc(layout);
            if base_ptr.is_null() {
                eprintln!("roc_alloc: out of memory");
                std::process::exit(1);
            }

            let size_ptr = base_ptr.add(size_storage_bytes).sub(core::mem::size_of::<usize>()) as *mut usize;
            *size_ptr = total_size;

            base_ptr.add(size_storage_bytes) as *mut c_void
        }
    }

    /// Free memory previously allocated by `roc_alloc`.
    pub extern "C" fn roc_dealloc(_roc_host: *mut RocHost, ptr: *mut c_void, alignment: usize) {
        unsafe {
            let min_alignment = alignment.max(core::mem::align_of::<usize>());
            let size_storage_bytes = min_alignment;

            let size_ptr = (ptr as *const u8).sub(core::mem::size_of::<usize>()) as *const usize;
            let total_size = *size_ptr;

            let base_ptr = (ptr as *mut u8).sub(size_storage_bytes);
            debug_assert!(min_alignment.is_power_of_two(), "alignment must be a power of two");
            let layout = Layout::from_size_align_unchecked(total_size, min_alignment);
            std::alloc::dealloc(base_ptr, layout);
        }
    }

    /// Reallocate memory, preserving existing user data.
    pub extern "C" fn roc_realloc(_roc_host: *mut RocHost, ptr: *mut c_void, new_length: usize, alignment: usize) -> *mut c_void {
        unsafe {
            let min_alignment = alignment.max(core::mem::align_of::<usize>());
            let size_storage_bytes = min_alignment;

            let old_size_ptr = (ptr as *const u8).sub(core::mem::size_of::<usize>()) as *const usize;
            let old_total_size = *old_size_ptr;
            let old_base_ptr = (ptr as *mut u8).sub(size_storage_bytes);

            let new_total_size = checked_add_usize(new_length, size_storage_bytes, "roc_realloc allocation size overflow");
            debug_assert!(min_alignment.is_power_of_two(), "alignment must be a power of two");
            let old_layout = Layout::from_size_align_unchecked(old_total_size, min_alignment);
            let new_base_ptr = std::alloc::realloc(old_base_ptr, old_layout, new_total_size);
            if new_base_ptr.is_null() {
                eprintln!("roc_realloc: out of memory");
                std::process::exit(1);
            }

            let new_user_ptr = new_base_ptr.add(size_storage_bytes);
            let new_size_ptr = new_user_ptr.sub(core::mem::size_of::<usize>()) as *mut usize;
            *new_size_ptr = new_total_size;
            new_user_ptr as *mut c_void
        }
    }
}

/// Default handlers for dbg, expect-failed, and crash.
#[cfg(not(no_roc_std_helpers))]
pub struct DefaultHandlers;

#[cfg(not(no_roc_std_helpers))]
impl DefaultHandlers {
    /// Print a `dbg` expression to stderr.
    pub extern "C" fn roc_dbg(_roc_host: *mut RocHost, bytes: *const u8, len: usize) {
        unsafe {
            let msg = core::slice::from_raw_parts(bytes, len);
            let msg = core::str::from_utf8_unchecked(msg);
            eprintln!("[ROC DBG] {}", msg);
        }
    }

    /// Print a failed `expect` to stderr.
    pub extern "C" fn roc_expect_failed(_roc_host: *mut RocHost, bytes: *const u8, len: usize) {
        unsafe {
            let msg = core::slice::from_raw_parts(bytes, len);
            let msg = core::str::from_utf8_unchecked(msg);
            eprintln!("[ROC EXPECT] {}", msg);
        }
    }

    /// Print a `crash` message to stderr and exit.
    pub extern "C" fn roc_crashed(_roc_host: *mut RocHost, bytes: *const u8, len: usize) {
        unsafe {
            let msg = core::slice::from_raw_parts(bytes, len);
            let msg = core::str::from_utf8_unchecked(msg);
            eprintln!("[ROC CRASHED] {}", msg);
            std::process::exit(1);
        }
    }
}

/// Create a host-internal helper context with default memory management and handlers.
///
/// This is only for helper functions in this generated file. It is not passed to
/// compiled Roc code, which uses the direct symbol ABI declared above.
#[cfg(not(no_roc_std_helpers))]
pub fn make_roc_host(env: *mut c_void) -> RocHost {
    RocHost {
        env,
        roc_alloc: DefaultAllocators::roc_alloc,
        roc_dealloc: DefaultAllocators::roc_dealloc,
        roc_realloc: DefaultAllocators::roc_realloc,
        roc_dbg: DefaultHandlers::roc_dbg,
        roc_expect_failed: DefaultHandlers::roc_expect_failed,
        roc_crashed: DefaultHandlers::roc_crashed,
    }
}

// Provided Symbols
//
// Roc exports these symbols from the app with their natural C ABI signatures.

#[allow(improper_ctypes)]
unsafe extern "C" {
    /// Entrypoint: gui_init!
    pub fn roc_gui_init();

    /// Entrypoint: gui_dispatch!
    pub fn roc_gui_dispatch(arg0: RocErasedCallable, arg1: u64);

}

